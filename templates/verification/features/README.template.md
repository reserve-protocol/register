# <surface> User-Facing Feature Map

This index is the maintained inventory for real-surface verification. Link one file per coherent user outcome; do not mirror every component or internal route.

| Feature | User outcome | Verification file |
| --- | --- | --- |
| `<name>` | `<observable outcome>` | [`<slug>.md`](<slug>.md) |

## Coverage Notes

- Primary surface: `<surface>`
- Secondary surfaces intentionally excluded: `<none or list with reason>`
- Realistic data/test-account profile: `<profile>`
- Known unreachable prerequisites: `<none or concrete prerequisites>`
