import styled from '@emotion/styled'
import { Box } from 'theme-ui'

const Container = styled(Box)`
  height: fit-content;
`

const Summary = () => {
  return <Container variant="layout.borderBox">Preview</Container>
}

export default Summary
