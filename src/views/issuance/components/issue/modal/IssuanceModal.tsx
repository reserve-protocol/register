import { Modal } from 'components'
import { ReserveToken } from 'types'

interface Props {
  data: ReserveToken
  onClose: () => void
}

const IssuanceModal = ({ data, onClose }: Props) => {
  const handleError = () => {}

  return null
}

export default IssuanceModal
