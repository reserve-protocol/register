version: 2
updates:
  # Keep the SHA-pinned GitHub Actions current via reviewable PRs.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
