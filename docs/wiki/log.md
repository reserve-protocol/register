---
title: Log
updated: 2026-08-22
type: log
---

# Log

Append-only chronological record: lessons, corrections, friction. Newest section last. Tag kit-caused friction with `kit-friction`.

## 2026-07-02

- Wiki initialized by llm-workflow install.
- Adopted via `skills/adopt.md` on branch `cleanup/ai-release-general-ui-cleanup-stabilization`. Inventory: root `CLAUDE.md` (+`AGENTS.md` symlink), `src/views/index-dtf/issuance/async-mint/CLAUDE.md`, `.claude/commands/*` + `.claude/skills/*`, `IMPROVEMENTS_PLAN.md`, `docs/*`.
- Dropped: `IMPROVEMENTS_PLAN.md` (stale root-level plan for the merged `feature/split-discover-home-highlight-dtfs`; component split already done; remaining items moved to [[progress]] backlog). Untracked `.agents/skills/` (Codex-generated duplicates of `.claude/commands/review.md` and `.claude/skills/*`) flagged for the human — not deleted, not committed.
- Covered by kit, dropped from project docs: generic coding rules (dumb-beats-clever, rule of three, early returns, comments-explain-why, atom patterns, laziness ladder, review discipline, verification gate) — now `skills/code-standards.md`, `skills/workflow.md`, `skills/review-panel.md`. Register-specific deltas recorded in [[project]] § Overrides.
- ui-stabilization-sweep lessons: (1) the Codex-authored DataTablePagination extraction both changed shared defaults silently AND introduced a real mobile layout bug — the rules-check caught the first, only a screenshot with 12-page data caught the second; visual verification on realistic data is non-optional for shared-component refactors. (2) The designer PR removed all DTF navigation on 640–1024px viewports (old bottom nav was `<lg`, replacement bar was `<sm`); fixed by extending the floating bar to `lg:hidden` — pending design confirmation. (3) Two CSS-hidden copies of the about card meant two autoplaying videos on every overview visit; JS-gate breakpoint-exclusive heavy content instead of CSS-hiding it. (4) User rule reinforced: `cn()` for ALL className concatenation, never template literals.
- NOT fixed, flagged for engineer review: `Layout`/`AppHeader` hardcode index-dtf overview route checks (shared-container change baked into the design); `navigation-menu.tsx` shared defaults changed by the PR (trigger padding, viewport border); desktop basket table lost its 10-row cap (100+ rows render for big DTFs — confirm intent).
- kit-friction (resolved upstream, kit commit dba52ee, pulled via --update): adoption assumed a clean tree (mid-flight is the norm — adopt.md + workflow-start error now cover it); tool-generated duplicate agent dirs weren't in the adopt inventory; ui-ux verification missed realistic-data-volume and breakpoint-band checks (both bit us today); reviewers had to be manually primed with the wiki rules (review-panel now requires it). Also: this session initially forgot the kit-friction tag itself — the audit's "user corrections" line was the only reason the evidence survived; tag at write time.
- Zapper double-mount RESOLVED (user-confirmed): the container's modal ZapperWrapper is skipped on `/issuance*` routes, restoring one-Zapper-per-route. The real hazard was config divergence between the two mounts (inline: `debug`, no `hideLargeMintPrompt`; modal: the opposite) fighting over shared zapper state. MintBox and the mobile Buy/Sell only call `useZapperModal().open()` — they never mount instances, so overview is unaffected.

## 2026-07-03

- cowswap-prompt-rework: candles default in price chart; large-mint prompt now recommends CoW Swap (both tabs, external link) with new `impact` (>1% truePriceImpact) and `capacity` (Ondo per-transaction cap from `dtf/ondo`) variants; `useOndoLimits` lifted to `src/hooks/`; new domain page [[zapper-prompt]].
- Lesson: `lingui extract` had not run since several features landed — the catalogs were stale enough that a routine extract swept in ~70 unrelated msgids. Run extract at every stage closeout that adds copy, or the next stage inherits the noise (this one filled its own 14 strings + the 4 chart-toggle labels the candles default made visible, and backlogged the rest).
- Lesson (review value): the Dark/Light pair caught a real latch hole (impact signal below the $100 applicability floor → mobile dialog re-pops after dismissal) and a product regression risk (empty candles endpoint → blank default chart, now falls back to the line body). Both were invisible to the gate; both were cheap to fix in-stage.
- cowswap-prompt-rework follow-ups (user testing): overview modal zapper now shows the prompt (`hideLargeMintPrompt` removed — flag pre-dated the rework); reducer got an escalation-only rule after real-wallet testing showed the card flickering between "High price impact" and "Large Order" as racing/refetching quotes disagreed around the 1% threshold. Latched variants never downgrade until a clean reset; 4 new reducer tests pin it.
- capacity warning: dropped the off-hours suppression after user testing ($5M input during the July-4 market closure showed "No route found" instead of "Order too large"). The signal is input-derived so it now fires pre-quote regardless of market state; closed-session caps are labeled "Regular" (the API mirrors regular caps when closed). Reverses the original "not during Off-Hours" spec line — explicit user instruction.
- Ondo off-hours rule (PM): CoW-CTA variants (large/impact/error) suppressed on Ondo DTFs while `market.isOpen === false` — off-hours secondary liquidity is stale, don't push users there. Note the inversion vs the same morning's change: capacity fires off-hours (informational, no CTA), CoW push does not. `!isOndoOffHours` also gates `isApplicable` so a latched CoW card clears when the market closes.
- referral tracking: `?referral=` capture (last-touch, no expiry) + `referral_landed` event + wallet↔code POST on connect; reserve-api gets `referral_links` table + origin-guarded `POST /referrals/link` (branch feature/referral-links there). New domain page [[referral]].
- Lesson (review value): Dark Claude caught that a bare `localStorage.setItem` inside the mixpanel-init effect would crash the app for Safari Private Mode visitors — the exact audience referral links target; all storage access now goes through safeGet/safeSet. Plan-stage review also killed two subgraph-API assumptions (index/yield query asymmetry, TRANSFER-in counted as mint) before any code was written — the feature then shrank to attribution-only and none of that code was needed.
- Fastify ordering gotcha: schema validation runs BEFORE preHandler, so a "guard" registered as preHandler does not shield validation; header-only guards belong on onRequest.
- referral follow-up (user-directed): reserve-api branch re-based onto origin/staging (deploy flow), localStorage helpers kept over atomWithStorage (no reactive consumer; dynamic flag keys don't fit atoms). /code-review high (workflow, 15 agents) then found what two Dark/Light passes missed — all three real bugs shared one root cause: captureReferral lived inside React's effect tree, where child effects (AtomUpdater link, Redirects navigate) run first. Fix was structural, not choreographic: capture to storage at module load in index.tsx, mixpanel half stays post-init. Lesson: when several findings share a root cause, fix the structure once instead of patching each ordering.
- referral scope cut (user-directed): reserve-api piece removed entirely — Mixpanel is the single attribution store (`referral_wallet_linked` with explicit wallet+code is the link record; Mixpanel export API covers extraction). reserve-api PR #212 closed unmerged, branch feature/referral-links kept on the remote as the archive of the reviewed table+endpoint if a DB record is ever wanted. Register dedupe flag re-scoped from API base to Mixpanel project key.
- weighted-ondo-limits: capacity cap re-derived as weighted `min(capacityUsd / weight)` (see [[decisions]]); new `closed-impact`/`closed-error` prompt variants fill the off-hours gap where high-impact or failed quotes previously showed nothing; signals extracted to a pure `deriveMintPromptSignals` after review flagged the spec-critical booleans were untested inline component code.
- Lesson (from the Dark reviewer's one Important find): removing a gate from a latch's reset condition (`isApplicable` lost the off-hours term so closed variants could latch) silently un-protects every state the gate was covering — the latched CoW CTA survived a flip to minting-unavailable for a full refetch cycle. When touching a latch reset, re-enumerate what each removed term was protecting and encode it as an explicit rule + test.
- Lesson: client and API disagreed on missing session buckets (client treated missing as blocked; the API's `sessionCapacity` falls back to the regular cap). For optional payload fields, read the producing side (`reserve-api`) before choosing an interpretation.

## 2026-07-06

- price-chart-granularity stage: diagnosed the "choppy/crazy charts" empirically before touching code — probed `historical/dtf` for both example BSC DTFs and found (a) 1M at `1h` = 721 points with ~50% direction reversals, (b) the <30d young-DTF override forcing `1h` on YTD/1Y for backfilled AI DTFs (4.5k–8.8k points — the actual "crazy" chart), (c) the reported "3h/6h granularity" was just data holes; the API 400s on anything but `5m/1h/1d`. Lesson: measure the API before redesigning around a symptom report.
- Verified the weekly downsample live by counting recharts path commands (`.recharts-area-curve` `C` segments) — mvDEFI All went 532 daily → ~80 rendered. A first mismatched count (394 on TEST4) turned out to be the dedupe working (518 raw rows had 125 duplicate timestamps), not a bug: replicate the app's full transform pipeline in a script before assuming the UI is wrong.
- Found pre-existing (backlogged): BTC+ yield DTF's BTC data-type mode renders historical `priceBTC` ≈ 1.1 against a live now-point ≈ 0.34 — denomination mismatch upstream of the charts.
- Correction (user feedback on the granularity pass): plain supported intervals left 24H (25 pts) and 1M (31 pts) too sparse — the fix generalized to "fetch the finest supported interval, bucket client-side to the display density" (24H: 5m→15m, 1M: 1h→6h), reusing the weekly downsample as a generic `downsampleToBucket`. Lesson: point-count targets (~100 per chart) beat interval names when choosing granularity; when the API doesn't serve the density you want, bucket down from the next finer one instead of settling for the next coarser one.
- Homepage featured cards had the worst density of all: `v1/discover/featured` embeds ~2.1k hourly points per DTF over 90d, and each card renders the series twice (pattern + stroke chart). Fixed at the data layer (`normalizeFeaturedItem`) with a span→bucket rule (`downsampleForSpan`), which forced lifting the downsample util from the overview feature folder to `src/utils/chart-downsample.ts`. The discover-table endpoint already serves daily 30d series — checked before touching it, left alone.
- Dark/Light review of the granularity branch caught a real Important bug the self-review missed: the display timeseries (now bucketed) also fed `indexDTF7dChangeAtom` via `useSync7dChange`, so selecting All on a multi-year DTF would compute the trailing-7d change from weekly points (a 7–14d window). Fixed by returning the pre-bucket `fullTimeseries` from `usePriceChartData` for derived stats. Lesson: when downsampling a series, grep every consumer of that series first — display data and derived-stat data have different fidelity requirements.
- llm-context-hardening phase 1: 8 deep explorers over sibling repos → new wiki pages [[ecosystem]], [[subgraphs]], [[index-protocol]], [[yield-protocol]], [[zapper]]; [[sdk]] extended (monorepo/version facts, live yield namespace, exported config constants, version-gated write ABIs). All references are GitHub URLs so teammates' LLMs can clone siblings. Notable finds: register pins react-sdk ^0.2.0 while SDK ships 0.3.1 (caret on 0.x minor doesn't float); react-zapper state is module-level jotai singletons (the double-mount root cause, now documented); yield subgraph's ISSUE/CLAIM entry types are dead enum surface; trusted-filler fills are same-block CoW settlements invisible to bid-shaped queries.
- llm-context-hardening phase 3: [[improvements]] written from a 7-dimension audit (structure, state, data-fetching, duplication, type-safety, build/perf, testing). Headline corrections to received wisdom: routes are NOT lazy (project.md fixed — the ~10MB bundle cause), vitest never ran in CI, RSV/zapV2 are legacy-live not dead, and the installed react-sdk is 0.2.0 (config-constant imports the SDK docs suggest are gated on a bump). Two release blockers found beyond the known RESERVE_API pin: hardcoded api-staging URLs in useIndexDTFList.ts and use-featured-dtfs.ts, and a shadow prod RESERVE_API in use-dtf-status.ts (two environments in one session). CLAUDE.md load order now points at improvements.md before code work.
- pr-1008 review complete (all 11 partitions, adversarial verify; finished on Opus/xhigh after two Fable session-limit deaths — resume-from-journal + model override was the recovery path). 25 findings stand. Also confirmed: lingui t-as-parameter renders empty preset labels in ALL locales (verified against the real babel pipeline); async-mint has no price-impact gate on submit and over-blocks existing-collateral mints; isSafeMultisig misclassifies EIP-7702 EOAs. Refutation pass earned its cost: 7 candidates killed, several because behavior was byte-identical on master.
- pr-1008 triage (Luis): the vote-lock compliance-gate removal that the review flagged as a security regression is INTENDED (decision recorded — vote-lock is governance participation, not a regulated surface; don't re-flag). Staging pins acknowledged, flip all THREE sites at release cut. Async-mint gate findings → backlog, out of scope. Lingui finding re-verified on request: vite-served transform shows t`...` untransformed inside buildVotingPresets while the component passes runtime `_`; runtime tagged call returns "" — preset labels are blank in every locale including English (deploy basket-changes + other-changes steps). Fix = msg descriptors at module scope or inline presets into component scope.
- wiki-conflict fix: the recurring PR conflicts on wiki-touching branches were all in the four append/ledger files (log/decisions/progress/index) — concurrent branches append to the same region every time. Added root `.gitattributes` marking those four `merge=union` (built-in keep-both driver, works locally + on GitHub). Verified with a live two-branch merge on real log.md: 0 conflict markers, both entries kept. Scoped to append files only — domain pages are rewrite-in-place and must not get union. See [[decisions]]. Portable follow-up for the kit: ship this attribute from the wiki skill/installer.
- featured downsample moved server-side (user-directed): reserve-api now applies the span→bucket policy (`downsampleForSpan`, ported verbatim with its tests to its `src/lib/chart-downsample.ts`) inside `discoverDtfService.getPerformance`, covering `discover/featured` (~2.1k hourly pts → ~92 daily) AND `discover/dtfs?performance=true` (daily series → no-op) — applying at the shared `getListItems` path instead of per-route was an explicit user call. `priceChange` still computes from the full hourly series (downsample runs on `performance` only). Register side surgically reverted from commit 5f42832: `normalizeFeaturedItem` passes `performance` through raw again; `downsampleForSpan`/`getDisplayBucket` pruned from `src/utils/chart-downsample.ts` (only `downsampleToBucket` + overview constants remain — overview still buckets client-side because `historical/dtf` serves only 5m/1h/1d). The overview refactor from that commit (fullTimeseries for derived stats, `FetchInterval` type) was kept. Ships when reserve-api deploys to staging; until then featured cards just get the still-hourly payload (rendering is unchanged either way).
- candlestick-parity stage: the reported "range changes when toggling Line↔Candles" was measured before coding (Playwright tick readout on all 6 ranges) and turned out to be pure rendering, not data — both endpoints read the same `historical_dtf_price` table. Three compounding causes: category-axis `interval` skip ticks (last label up to N−1 candles early), epoch-floored buckets (YTD's first 7d candle labeled in December), and 'all' fetching `from=0` while the line clamps to launch−1y. Fix stayed candle-side only: `snapToBucketStart` on the request `from`, 'all' clamped to the line's window (same snapped from keeps the 'all'→'30d' discovery cache-share), ticks reuse `useXAxisTicks`. No reserve-api change — from-aligned SQL bucketing was considered and rejected (currentHour-anchored `from` would shift candle boundaries hourly).
- Launch marker on candles: a band scale can't resolve a non-bucket timestamp (`scale(launchTs)` → undefined → the line-chart marker silently nooped), so `candlestick-launch-marker.tsx` locates the containing bucket (`locateCandleBucket`, fraction × bandwidth) and feeds the precomputed x to the untouched `PriceChartLaunchMarker` through a synthetic single-value scale. Lesson: recharts `ticks` on a category axis silently NaN-drops values that aren't exact domain members, and a numeric `interval` prop decimates explicit ticks too — verified in node_modules before writing the component (plan-stage recharts source-check killed the numeric-axis alternative for Bar charts: bars center on `scale(t)` and bandSize infers from the min tick gap).

## 2026-07-08

- lock-zap-settings stage: react-zapper grew a `disabledSettings` prop (`{ deepLiquidity?, forceMint? }`, released as 2.4.0) following the package's prop→updater→atom convention (`DisabledSettingsUpdater` force-resets both atoms to false); register's `zapper-wrapper` passes it for the 5 featured BSC DTFs hardcoded in `locked-zap-settings.ts` (see [[zapper]]). Verified with Playwright against the linked package: frozen unchecked on PHOTON, untouched on CMC20, and the SPA-nav case (checked on unlocked DTF → navigate to locked) resets correctly because the updater effect runs pre-paint and no quote can fire with an empty input.
- Dark/Light pair returned zero Critical/Important on a 9-file package diff + 2-file register diff; both independently flagged the same two Minors (missing helper test, frozen checkbox keeps its benefit-selling help copy). Convergent minors from independent reviewers are cheap confidence — the test was added, the copy left matching the frozen-dust precedent.
- Local react-zapper testing recipe that worked: `pnpm build` in react-zapper, register dep → `link:../react-zapper` + `pnpm install`, no vite config changes needed (existing `resolve.dedupe` for react/react-query sufficed; wagmi context resolved fine because pnpm links share the peer graph). Revert the pin before committing.
- mcap-tabs stage (RES-2101/2102): the exposure tab's Market Cap column was showing tokenized-supply mcaps for Ondo stocks (GLWon $985K vs Corning $158B) because the backend only had CoinGecko-by-address / totalSupply×price — both are the wrapper's number. Key discovery: Ondo GM API `GET /v1/assets/all/market` carries `underlyingMarket.marketCap` (real company mcap) under the same API key reserve-api already holds — probed live before designing; no new vendor. reserve-api now enriches exposure tokens with `underlyingMarketCap` (1h-cached bulk fetch, gated on the basket holding ondo assets); register reads it on exposure stock rows with NO tokenized fallback (`—` until the api deploy + its 1h CDN cache cycle), and the collateral tab gains the tokenized-mcap column for all DTFs. See [[basket-overview]].
- Scope moved twice mid-stage (user-directed): first "gate collateral mcap by a shared ONDO_DTFS list" (built, including folding locked-zap-settings into it), then "no — all DTFs, zapper keeps its own list" (reverted cleanly via git checkout of the zapper files). Lesson: when a gate list is speculative, build the ungated version first — the gate was pure deletion.
- Fastify gotcha worth remembering: the exposure route's zod response schema emits `additionalProperties: false`, so any new service-layer field is silently stripped unless added to the schema — verified empirically by the Dark reviewer, comment now lives at the schema.
- Dark & Light reviewers independently converged on the same Important (unguarded parse of an Ondo 200 body could 500 `/dtf/exposure` for every DTF: `for...of` on a non-array escapes `ondoFetch`'s catch); Dark alone caught the hot-path coupling (15s Ondo timeout taxing crypto-only DTFs during outages — fixed by gating on `ondoAssetByAddress` over the basket). Convergent Importants from independent reviewers = fix before ship, no debate.
- Devin PR #1028 assessed and superseded: built against master's older basket-overview (pre-tabs-in-header, pre-flattened exposure rows) and keyed exposure mcaps off `native.coingeckoId → native.marketCap` — nasdaq/nyse natives have NO coingeckoId and the API serves no `native.marketCap`, so it silently fell back to the tokenized group mcap (the exact bug). Group-level mcap can't work post-flattening anyway (one NASDAQ group holds N different companies); per-token `underlyingMarketCap` is the only correct shape.
- Workflow calibrated (user-directed: "every prompt takes a long time"; second pass refined tiers into two axes). Measured first: the mechanical gate is cheap (oxlint 0.4s, tsc 22s, vitest 9.2s ≈ 32s) — latency was LLM-side ceremony firing on every diff: Dark/Light pairs (product lens fires on any `.tsx`, complexity on half of src), the gate running twice (inner loop + closeout), visual checks and wiki ingest on trivial diffs, and "when debating, it's a stage" funneling everything into the heavy path. First iteration used one ladder (touch-up/quick/stage); Luis's counter-example — a bugfix with a large radius — broke it: small work, wide blast needs heavy review but not multi-phase ceremony. Final model: radius × size, four profiles (touch-up/low/medium/high), reviewers spawn at medium+, `scope.mjs` prints per-axis signals + `gate-equivalent`. Mirrored to the agent-workflow kit (skills, scripts, templates, README, +3 core tests). See [[decisions]].
- Drift fixes (same session, self-reported): (1) progress.md rows had become essays — 6 rows compressed to pointer form (full reconciliation lives in git history), and wiki-lint now caps ledger rows (`wiki.ledgerRowMaxChars`, default 700) so it can't recur silently; (2) profile misfiling guarded — downgrading below medium while scope.mjs prints a radius signal now requires one stated line (signal + why it doesn't apply); (3) profile boundaries iterate only from misfires recorded here, not theory — process work must not displace product work during the release window.
- Featured YTD stage (cross-repo): homepage featured now matches the overview default — reserve-api `discover/featured` serves `ytd` + daily grid, priceChange first-vs-last finite daily point, no live append; equal by construction to the overview's (penultimate−first)/first because both hit the same `historical_dtf_price` generate_series grid and the overview skips its synthetic now-point. Order server-driven via `FEATURED_TOKENS` (BUILDOUT, POWER, PHOTON, NEOCLOUD, ROBOTS); marquee sorted weight-desc at the client choke point (covers both the featured payload AND the SDK exposure route feeding the photon animation) plus server-side in `groupExposure`. `/dtf/daos` re-sourced from dtf-catalog (active only, Arbitrum-guarded) — the folio-manager brand table lagged new launches; dead `getVisibleFolios` deleted. Dark verified parity end-to-end; adopted from review: cached-rejected-catalog-import reset (one transient failure used to 500 discover+daos until restart), the Arbitrum guard the test already asserted, YTD label fallback. Expected homepage YTD once deployed: BUILDOUT +68.39, POWER +36.94, PHOTON +84.04, NEOCLOUD +57.02, ROBOTS −2.44 (staging 2026-07-08).
- Overview loading-shift sweep (QA): the worst shift was NOT a skeleton gap — `indexDTFBrandAtom` is written twice (SDK payload first, WITHOUT `dtf.video`; folio-manager merge second), so the cover card unmounted and remounted. Fix: `indexDTFBrandExtrasResolvedAtom` gates the cover skeleton until the authoritative read settles, and the SDK write merge-preserves video/files. Balance card now expands once via grid-rows animation, gated on balance + USD value + PnL all settled (`useWeekAgoPnl` returns `{ pnl, isResolved }`). Cover video cross-fades with its skeleton underlay. Note: the container updater changes are shared machinery — engineer eyes on `index-dtf-container.tsx` when reviewing the push.

## 2026-07-09

- Codex audited the E2E suite against the complete non-Yield SDK migration plan. Architecture approved; trust blockers and the flow-by-flow acceptance contract are now in [[e2e]]. Highest-value findings: enforce a real outbound-network allowlist, make snapshot capture atomic/per-file fresh, run against the exact paired SDK versions, match mocks on request identity, fail unknown calls in committed flows, capture/assert every submitted transaction, and add v1/v4/v5/v6 coverage or explicit product gates. Workflow scope correctly surfaced the active debug specs/logging; no kit change justified because Phase 2 is intentionally mid-debug.
- E2E phases 1–2 shipped (Fable orchestrating a 7-area Opus swarm + shared-infra agents): offline playwright suite, 38 tests green (one zap-sell flake named). Biggest lesson repeated THREE times independently: a mock layer nobody consumes through the real SDK path is untested foundation — the captured dtf.json was SDK-processed shape, sdk.index.get threw before its select, react-query swallowed the error, and every DTF surface rendered skeletons while zero-unmocked smoke stayed green. Fix: capture the RAW GetIndexDTF response + per-DTF chain-state.json (which exposed OPEN/VTF as v4.0.0 — the global version() mock was lying), plus a permanent dtf-data canary smoke. Second lesson: hydration fixes unlock UI that makes NEW reads — the vote flow was zero-unmocked until basket data rendered, then grew 8 eth_call gaps; fail-loud accounting must be re-checked after any mock enrichment. Swarm mechanics that worked: area-owned specs + orchestrator-owned shared files + per-test overrides as the escape hatch (zero merge conflicts across 7 parallel agents); honest-red reporting with staged assertions instead of faked green. Codex's SDK-migration audit adopted: items 8/9/11 landed, 1/4/6/7/10 queued as phase 2.5, 2/12 in phase 3.
- E2E trust hardening closed every follow-up audit finding: default-deny egress, strict teardown for all committed flows, identity-aware API/subgraph/full-calldata overlays, a correlated transaction/receipt ledger, atomic 43-file snapshot capture/checking, value/source assertions for compliance/auctions/charts, Node-24 pnpm CI, and current README/wiki. Full evidence: smoke 11/11, full 27/27, helper contracts 9/9, snapshot manifest 43/43, app tests 570/570.
- Strictness paid for itself immediately: turning on flow teardown exposed 17 failures that the old suite called green. The correct fix was not more zero fallbacks; captured discover/rebalance token metadata plus exact project-known stablecoin identities made the requests truthful and kept wrong-address negatives failing.
- Frozen-clock flake lesson: advancing a large interval once is still racy if the async request has not been scheduled. Under full parallel load, governance and issuance occasionally pumped before the send/query existed. The stable protocol is: pump the enabling layer, `expect.poll` an observable boundary request or `txLog`, then pump the response/receipt timer. Three-repeat issuance and the final full run stayed green.
- Snapshot lesson: atomic directory replacement is insufficient if GraphQL returns HTTP 200 with `errors`; the capture client must treat those errors as capture failures before publishing. Targeted refreshes intentionally leave the full freshness marker unchanged.
- Self-improve audit: no kit change. The only repeated friction was the managed sandbox denying local port/tsx IPC, which required the already-supported escalated verifier path; test races were product-suite defects and were fixed in the suite itself.

## 2026-07-10

- E2E trust-hardening reconciled and closed. Codex implemented all 13 audit findings on the uncommitted tree; Fable ran three independent passes — a verification agent (full 27/27 ×3, smoke 11/11, zero unmocked, payload spot-checks decode real calldata, grep audits of every strictness claim) plus Dark/Light on the whole diff. Zero blockers/majors; four doc-level one-liners adopted, four minors backlogged, two patterns accepted-as-is (documented read-only wildcard baselines; deterministic-empty non-goal branches).
- **Co-work incident worth remembering**: a coordinator resume-instruction written before Codex's pass told a subagent to `git checkout --` a "stray" workflow edit — but by execution time that file held Codex's completed CI rewrite, and the revert destroyed it (re-authored from the handoff spec). Rule: in a shared/co-worked tree, never issue a destructive git command from a stale snapshot of authorship — `git diff` the target and read any handoff doc FIRST, and prefer instructing agents to report unexpected diffs rather than discard them.
- Two review passes independently caught comments contradicting the code they sat on (provider.ts described the pre-fix echo-any-hash receipt behavior; rpc.ts claimed all registry DTFs are v5). Hardening passes that invert behavior must sweep the comments describing the old contract — a stale comment on a trust boundary actively misleads the next auditor.
- Don't hardcode derived counts in docs: the handoff and wiki both said "43 snapshot files" while the manifest enforced 46. Docs now name `snapshot-manifest.ts` as the owner instead of carrying a number.
- Coverage pass 2 (4 parallel Opus packages, adversarial mandate): 39 new tests — failure paths for every write flow, multichain governance/auctions (incl. the only v4 path), fee-proposal calldata round-trip, zap edge states, compliance surfaces. Verdict on the "try to break the app" brief: register held. Reverted receipts can't fake success (wagmi sets query status 'error' on status-0x0 receipts and both register's `useWatchTransaction` and the zapper gate on it); v4/mainnet renders correctly with chain-correct explorer hosts; restriction doesn't over-block (overview/governance stay open).
- **The historical zap "output stuck at 0" flake finally root-caused** — not server contention: the widget's balance hydration re-renders and WIPES an amount typed too early, so the quote never fires and every downstream wait times out. The failing-run screenshot (input showing "0" with balance loaded) was the smoking gun. Fixed centrally: `fillAmountAwaitQuote` in helpers/zapper retries fill+quote as one unit (a wipe just refills); all four zap-quoting specs converted; zap specs run serial-per-file with 90s budgets because quote round-trips take ~2s isolated but >20s under 77-test load. Lesson: a "timeout flake" whose screenshot shows DEFAULT state is an interaction race, not slowness — read the screenshot before touching timeouts.
- Adversarial-agent findings for human triage (tests landed green; app decisions pending): (1) M — `shouldBypassFormValidation` disables all zod form bounds on localhost/dev, so out-of-bounds fees submit locally and e2e can't cover validation (needs schema unit tests + a narrower bypass); (2) L — possible propose double-fire on rapid double-click (likely frozen-clock artifact; 30s manual check requested); (3) follow-up — proposal descriptions carry raw markdown/script to chain; XSS safety rests entirely on the detail renderer (`proposal-md-description.tsx`), worth a hostile-description render test; (4) L — `use-asset-prices-with-snapshot.ts:72` throws if current/historical price endpoints diverge on a token.
- Fixture lesson: an edge-state capture can MASK the state under test — the 1000 ETH high-impact quote was unaffordable at the mocked 100 ETH balance, so the insufficient-balance fallback suppressed the impact-acknowledgment gate the fixture existed to drive. Solved with an opt-in `overrides.ethBalance` (shared 100 ETH default untouched); the server's `insufficientFunds` field turned out to be dead code — the widget computes affordability client-side.

## 2026-07-11

- Unattended backlog + adversarial pass (3 parallel Opus agents) added 4 specs (10 tests) — v4 write-ABI coverage (mainnet/open castVote/queue/execute: clean, v4 uses OZ selectors), basket-settings propose, proposal-description XSS render, deprecated-DTF redeem-only (clean). Two REAL app bugs surfaced and verified against source, both engineer-review governance surfaces, both regression-locked with `test.fixme`'d specs that flip green on fix:
  - Proposal-description **iframe XSS**: `<MDEditor.Markdown>` + `rehype-raw` with no `rehype-sanitize` renders a raw `<iframe>` and loads its src (proven by real outbound egress caught by the suite's deny guard). Attacker-controlled on-chain description → live external frame on the proposal page. Same renderer duplicated in index AND yield governance. `<script>`/`onerror`/`javascript:` already neutralized.
  - **Phantom governance calldata**: `propose-basket-settings/updater.tsx` seeds the threshold field as a percentage but the change-detector divides the baseline by 1e18, so the two never match — every basket-settings proposal silently appends an unintended `setProposalThreshold` calldata and the empty-change guard never fires.
- Adversarial-mandate mechanics that worked again: agents forbidden to touch app code; a suspected bug becomes a `test.fixme` + `// BUG:` repro in the agent's own spec (suite stays green) and comes up for human triage. Both bugs were then re-verified by the orchestrator against the actual source before escalation — the XSS one turned out BROADER than tested (yield governance shares the renderer), the threshold one is a clean seed-vs-detector unit mismatch.
- Yield DTF suite scoped (recon agent) + capture feasibility CONFIRMED before committing to the track: yield subgraph reachable, public no-auth RPCs (`ethereum-rpc.publicnode.com`, `1rpc.io`) execute `eth_call`. Key architecture finding: yield reads everything from RPC (not the SDK like index), so the foundation is a record/replay eth_call map at a pinned block per RToken — fundamentally different from index chain-state seeding. Blueprint in [[e2e]]; build deferred behind this commit to avoid a shared-helper race with the in-flight backlog agents.

## 2026-07-12

- Yield DTF Phase F landed (capture + record/replay foundation + overview smokes). The Phase-F agent captured both fixtures (eUSD/hyUSD, 128 real eth_calls each at a pinned block) then died mid-debug; I took over the finish directly rather than respawn. Two isolation bugs it hit and I root-caused:
  - **Index regression from a yield seam** (the subtle one): the index DTF page incidentally polls a `dtf-yield` subgraph via an app updater. Baseline answered those with `EMPTY_SHAPE`; the agent's `resolveYieldQuery` failed them loud, so the app fell back to on-chain price reads (`0xe45a5b2d` ×24) that aren't mocked → index smoke red. The diff LOOKED additive (index dispatch byte-identical), which sent the bisect in circles until I noticed `0xe45a5b2d` was a _yield_ selector (26× in the yield capture) firing on an _index_ page. Fix: the yield subgraph resolver only engages while a yield test is active (`isYieldReplayActive()`), so index tests stay byte-identical to baseline. Lesson: a "provably additive" seam can still change behavior if the OTHER product's page shares the boundary — verify the untouched path RUNS green, don't just diff it.
  - **hyUSD (base) hit the mainnet facade**: yield read atoms fire on first render with `chainIdAtom`'s mainnet default before the route's chain propagates (`useRTokenContext` sets it in an effect). eUSD passed only because mainnet IS the default. Fix: `setYieldReplay(chainId)` + a chain-aware absorb rule — wrong-chain-facade / zero-address transient reads return deterministic zeros unlogged, while a real miss on the fixture's OWN chain still fails loud.
- Recovery mechanics worth remembering: a dropped `git stash` object survives in `git checkout <stash-sha> -- <files>` — used it to recover the agent's uncommitted subgraph/api/base after a bisect `git checkout` clobbered them.
- E2E audit hardening (2 independent audits converged; goal = push B+ → S with speed + agent-workflow as the levers). Landed: Stage 1 chain-scoped identity + closed the yield fail-loud hole (uncaptured yield reads no longer fall through to index wildcards / the $1 feed); Stage 2.1 `e2e:check` yield identity + collision validation; speed — per-worker snapshot parse cache + local workers capped at 5 (single Vite server doesn't scale past ~5; 3× flake-free vs an intermittent zap flake at 7); agent-ergonomics — self-explaining unmocked failures (`helpers/selectors.ts` decodes the selector name + names the helper to model it in) + a boundary map / new-test recipe / speed tiers in `e2e/CLAUDE.md`; CI + scoped-verify now run the mock-contract unit tests.
- Speed tiers established for the agent quick-loop: unit tests <1s (mock-contract logic) → one scoped spec ~3–5s → smoke ~16s → full ~78s. The floor per browser test (~4–5s) is app boot + hydration + clock pumps; the win for agents is running the NARROWEST tier, which the domain diff→test tables already route.
- Chain-identity is NOT uniform across boundaries: RPC (host-routed) and subgraph (chain-specific Goldsky URL) genuinely encode chain, so they enforce it. The reserve-api does NOT — it keys DTF endpoints on the globally-unique folio address and treats `chainId` as advisory (the app sends `/current/dtf?address=<base DTF>&chainId=1`). Enforcing chain there fails legitimate requests — verified empirically, then reverted. Match the real boundary's semantics, don't assume a uniform rule.

## 2026-07-14 → 07-22 — Hardening × SDK-integration effort (compact record)

Play-by-play lives in git (PRs #1053/#1054/#1055/#1063, SDK PR #27). Durable outcomes:

- "Migration IS the fix" validated: every SDK-read adoption surfaced a real pre-existing SDK bug (completed-rebalance read 100% broken via an array envelope; governance tie semantics; stale-PENDING outcomes; missing optimistic veto context). One owned boundary beats parallel implementations.
- Three-tier ownership rule → [[sdk]] (sdk primitives / react-sdk hook-per-read / register product hooks; register never imports `useDtfSdk`).
- Governance display states are SDK-derived bigint. Visible change: a nonzero tie shows DEFEATED (OZ strict majority).
- Auction launch: a 0/missing price hard-blocks with a visible reason (product decision, Luis 2026-07-20). Price volatility is a separate, non-blocking signal.
- Hybrid DTF is a curated designation — requires weight control but is not implied by it (Luis 2026-07-21). The weightControl derivation was reverted before shipping; the allowlist stays the source of truth.
- Deeper auctions SDK migration deferred to protocol-vNext, riding its rebalance-testing window.
- Comment discipline hard rule → `skills/code-standards.md`: one-line WHYs only, no finding/review IDs in source, agent warnings go to area-guide traps.
- Remaining queue: `docs/plans/FOLLOWUPS.md`.

## 2026-07-28

- Production chain cycling was a multi-tab feedback loop: each mounted DTF route automatically reasserted its own Ethereum/Base/BSC chain against one wallet-global network. Transaction buttons and the zapper only exposed the changing state. Automatic switching remains, but only the focused, visible document may request it; focusing another DTF tab makes that tab the new chain owner. Index and Yield DTF route contexts share the same guard.
- Review caught two smaller bounce paths before closeout: Index initially targeted the lagging global chain atom instead of its provider identity, and cached focus state was not revalidated at the wallet-mutation boundary. Both are now regression-protected; future route-chain synchronization must use the route/provider chain directly.

## 2026-08-07

- Design-system v1 began with a lazy in-app lab: a provisional progress matrix, current foundations, the complete current Button API/state baseline, a deterministic static Index composition, visual-direction placeholders, and light/dark desktop/mobile region captures. No shared token or component defaults changed.
- Review corrected an important progress-model trap: placeholder rows start with no gates complete. A current baseline can count as inventoried only when its relevant surface is actually mapped; it never counts as defined, approved, adopted, or verified.
- Capture lesson: this app scrolls inside its own layout, so one full-page locator capture produced misleading blank output. Stable evidence captures named lab regions with a pinned server and platform-neutral filenames. Chromium under the x86 dev-container emulation is unreliable, so `DESIGN_SYSTEM_BASE_URL` supports host-browser capture without weakening the default owned-server path.

## 2026-08-11

- The lab was narrowed to its durable job: foundations, component state sheets, progress, and migration planning. Visual-direction placeholders and the simulated product composition were removed. One visual direction evolves through real local golden screens; deterministic fixtures provide repeatable data and edge states without duplicating the application inside the lab.
- The lab became a routed capability map rather than one long page. The useful distinction is “available output” versus “expected work,” not enabled versus disabled: unaudited slots stay subdued but clickable because their explanation and open decisions are themselves part of the lab. A product-shaped catalog can borrow vocabulary from shadcn/Radix without turning either library's inventory into Register's required backlog.
- Review and direct use removed the proposed hover dropdowns: category landing pages already present the catalogs more clearly, and direct links keep the navigation small-screen friendly. The same review split catalog maturity, rendered output, and per-decision completion so future slots can fill incrementally rather than switching one misleading all-purpose status.
- Source review of `codex/ui-standardization` against the current Home, Discover, and Index overview found useful intent but no mergeable system. Keep the semantic type-role model, warm surface hierarchy, and Tabs-versus-Segmented-Control distinction; re-test the exact scale, token count, variants, and dark values; leave behind wrapper sprawl, migration coverage as a design proxy, and the previous mass-migration implementation. The lab now records candidate direction separately from evidence and accepted values.
- Golden screens are provisional screen families, not one screenshot each: Home/Discover, Index overview, swap/automated mint, governance/proposals, Portfolio, and Earn. Deploy is an overlap stress route. Portfolio needs deterministic holdings states. The pinned `@reserve-protocol/react-zapper` API exposes behavior and locale props but no theme contract, so Register owns host semantic variables and local shell work; widget internals remain an upstream package boundary.
- The first focused Color audit corrected the lab's hand-picked evidence. The beige wrapper users recognize is `secondary` (110 exact product background-utility matches), while `container` has no exact product TSX match and equals the light background; names were masquerading as roles. The recent Home/Discover/Index performance palette is already shared across 10 consumers and intentionally bypasses feedback tokens, while Portfolio still maps gains/losses to success/destructive. The candidate now keeps feedback and financial movement as separate semantic roles, with visual sharing still open for human review, and renders a surface hierarchy plus role tables instead of treating rationale bullets as the candidate itself.
- Performance evidence cannot be represented by one green and one red swatch: the shared Home/Discover/Index implementation includes line-gradient endpoints, dot/fill sources, text colors, neutral, and pre-launch treatments. Its `darkSurface` variants are defined but unconsumed—the overview dark theme still uses defaults—so the lab labels availability separately from actual usage. It then proposes a smaller three-role V1 mapping—main, emphasis, foreground—with exact values, names, and feedback-ramp sharing still open for design review.

## 2026-08-13

- The missing layout layer is now explicit but small: one outer frame and three role-based templates—content plus stable support rail, balanced peer split, and focused task column. Static source inspection found real drift (1.5:1, 2:1, and fluid plus 480px) and a 408–480px workflow cluster; these remain evidence, not approved values.
- Modal width is now a real visual decision rather than an inherited number. The lab reuses the complete quote-ready Zapper and a short success state at 432/448/464px; both states always move together, and narrow screens constrain both to the same available width. The Zapper should earn the standard modal role before other dialogs inherit it.
- Accessibility was deliberately bounded to a pragmatic shared-primitive baseline: controls and overlays own mechanics once; product work supplies truthful labels, messages, and reading order. Review caught two useful contradictions before handoff—a low-contrast performance label in the reused specimen and an older plan paragraph that still called accepted foundations unresolved.
- Responsive review was justified here because layout itself was the decision. Desktop plus phone route tests and two phone region baselines now pin equal modal state widths, viewport containment, and readable stacking. The existing designer-only lab remains otherwise desktop-first.
- Route-level layout audit now precedes component-family work so legacy composition does not accidentally define component anatomy. Static source review plus local Auctions/Governance inspection produced five task-led archetypes. The clearest decisions to test are Auctions as persistent browse-and-inspect, Governance as proposals plus a contextual/selected inspection region, and automated mint as the retained compact-to-paired progressive-workflow reference. The lab keeps these schematic and records which component contracts each hypothesis blocks; no product route or shared component changed.
- Human scope decision: Auctions is the structural migration worth doing in V1—the opened auction will be recomposed for a persistent list plus wider workspace, not merely stretched. Governance stays structurally intact for the compressed release; improve it through foundations and components, while keeping the proposal-summary idea as later work. A future summary must end clearly in the full proposal route rather than masquerading as the complete view.

## 2026-08-14

- The lab now optimizes for visual review rather than project archaeology: Foundations opens on nine compact system specimens, Components renders seven registry-derived canonical implementations before one provisional data composition and a compact inventory, and representative details place output/readiness before a single evidence disclosure. Accepted foundation studies moved to their authoritative detail routes; Studies retains only open color, geometry, layout, and modal pressure tests. The typed Current Review queue is capped at three human judgments, while Project Status derives foundation/component progress and autonomous next work from the catalogs.
- The first product-facing canonical kernel now follows the strongest current Index evidence instead of manufacturing alternatives: Entity identity, Metric/MetricValue, EmptyState, and a realistic dense Index data slice all render the actual candidate components in the lab. Production consumers remain unchanged.
- Entity pressure testing exposed a composition bug rather than a design question: placing a 2px background-matching wrapper around a 16px chain mark made the badge 20px overall and broke the ratio used on strong recent screens. The background-matching edge now belongs inside the chain mark's 16px footprint.
- Dense data review found the useful reusable seam is identity plus formatted value, not a universal Row. The table/list parent still owns columns, density, dividers, navigation, and responsive behavior; prematurely extracting those would encode one composition as a shared component.
- Safe autonomous canonicalization stops before illustrated empty states, rich navigable records, Index rail refinement, or complex form groups. Those materially affect the product's visual character and should return as real visual judgments rather than quietly becoming conventional defaults. Auction metric alignment waits for the real Auctions composition rather than entering the global Metric queue.
- EmptyState review exposed a progress-model bug: its actions looked like the old system because the state sheet imported the legacy production Button even though the catalog called Button a V1 proposal. The fix is general rather than specimen-specific: the catalog now separates visual output, reusable implementation, and product adoption; Button is a real candidate; and active canonical lab compositions consume it. This prevents visual replicas from silently drifting while preserving the explicit no-migration boundary.
- Composition review gained its missing gate: a visually meaningful dependency must be canonical V1 or explicitly retained before the parent can be called canonical-review-ready. This immediately reclassifies the core board as exploration, copied modal geometry as provisional, and the dense Index slice as a Table specimen without throwing away their evidence. The real eligibility dialog now proves the narrow interaction kernel—20px binary Checkbox, compact named IconButton, 384/432px Radix-backed Dialog shell, and canonical Button—while leaving indeterminate/invalid selection, toggle/tooltips, outcomes, final elevation, constrained screens, Table/DataRow, and production migration open. Scoped verification caught one infrastructure false start (the container lab server had exited); restarting only the lab and reusing it produced a green focused interaction check.

## 2026-08-18

- Rich-record review confirmed that Governance proposals and Auction rebalance selectors can share foundations and lower-level seams without becoming one universal Row. Repeated corrections established a stricter composition contract: 24px record inset, 16px region rhythm, 8px related rows, 4px tight icon/text pairs, 16px/500 titles, equal-sized horizontal peers, and semantic layout recipes rather than locally plausible spacing.
- Auction selectors now prioritize the process loop: current auction/status, current-auction countdown, rebalance permissionless/expiry timing, historical accuracy, impact, and total traded value. Per-auction bid count and value traded stay in the selected/full view. Selector status wraps below the title at constrained widths; provenance remains one left-aligned sentence.
- Lifecycle pills proved useful across both families but remain provisional. Their semantic feedback surfaces and borders—including neutral waiting/closed roles—are opaque, parent-independent colors. Proposal progress, quorum/vote evidence, optimistic challenge states, and Fast/Contested qualifiers were reviewed in composition without promoting a universal Table/DataRow or beginning production adoption.
- The completed proposal/auction pressure test promoted Lifecycle Status—not every compact label—to a real reusable candidate under `src/components/`. A visual pass caught that metadata-only promotion would have left a contradictory “Provisional component” state sheet and lab-owned implementation; canonical status now requires both the registry and actual import boundary to agree.
- Card audit found that “Card / content region” was two jobs hidden under one inventory label. Structural regions are already consequences of accepted foundations, while the strong Home Index feature card is authoritative existing design rather than a new alternative. Its review is limited to named square-shell and 8/24/16px foundation corrections under a preservation contract for all chart/launch, ticker, transcript/video, chain, and interaction detail; Discover remains compact adaptation evidence. The rounded legacy shared Card remains untouched evidence, not a migration target.

## 2026-08-19

- The Components landing page now renders the same complete state sheets as detail routes for all 14 available components, shows readiness/authority/delivery separately, and keeps one 29-item unresolved inventory with Product Navigation explicit. Reusing full sheets exposed a Card-study intrinsic-width leak at 390px; the lab composition now owns `min-w-0`, while the protected production card remains unchanged.
- Field/TextInput is independently reviewable across six source-grounded states without inheriting SingleChoice or the blocked repeated-governance composition. ActionGroup now encodes only accepted Button width/gap relationships, but review caught a maturity error before closeout: reusable synthesis is still exploratory/provisional until a human accepts it.
- Dialog coverage was corrected from a duplicated Eligibility specimen to compact dismissible, standard non-dismissible, and long body-scroll states; interactive focus/dismissal evidence remains detail-only. The entity-mark kernel was rechecked against Index exposure, badged-card, and Earn stack sources and required no code change.
- Tooltip/Help and Select/Combobox/Menu are prepared as evidence maps, not rendered proposals. The former must resolve explanation versus action feedback and touch access; the latter must preserve the semantic split between bounded values, searchable entities, and immediate actions while sharing only proven popup presentation.
- Independent Intent and Engineering Risk review caught premature ActionGroup authority, duplicated Dialog coverage, missing overview authority badges, and two unsupported fixture claims. All confirmed findings were fixed; focused browser coverage now pins overview width/mapping/authority and Field focus plus ARIA/native states.

- The Home feature-card foundation check is accepted without redefining the source design. Review caught two misleading box-model readings: a 48px bottom-aligned title reservation made the nominal 16px logo/name gap visually much larger, and moving that reservation below the title merely displaced the defect. The corrected candidate removes hidden title whitespace, measures 16px from the full logo/chart row to the name and 8px from name to market context, and aligns both transcript and compact metric supporting rows to the 24px content axis. Discover uses the same relationships; its taller chart supplies the visually larger logo/name distance. Extraction remains deferred and production behavior is unchanged.
- Design-system work is now explicitly canonical-first: existing product evidence and accepted rules produce the strongest complete candidate before human review, while Studies is reserved for unresolved alternatives. `canonical-candidate`, designer `accepted`, and production `in-use` are separate states. Current Review contains only a nearly complete candidate at Codex's confidence boundary. Meaningful feedback is classified as local, component, pattern, foundation, or reusable heuristic and updates the most reusable justified owner immediately; unrelated consumers, docs, and baselines may synchronize in a later coherent batch.
- Applying that model exposed the contained-form review as premature rather than review-ready. Its ordinary Field candidate remains reusable, but the parent composition is blocked until its SingleChoice dependency is accepted. A follow-up fidelity audit found that Tabs had accepted visual guidance but no reusable V1 implementation or recipe, so SingleChoice had reconstructed the model locally and drifted. The corrected candidate now uses the accepted 44px peer geometry, 14px medium control label, 20px item padding, 2px track inset without an extra inter-item gap, selected elevation, full radius, neutral control chrome, native radio semantics, and an explicit implementation source. Action groups remain evidence-prepared next; no production consumer changed.
- The Tabs/SingleChoice gap was representative of a narrow maturity-model problem: rendered output and reusable code were tracked, but current design authority was still encoded indirectly through `proposal`/`accepted` output labels. The registry now records design authority independently from rendering, implementation, and adoption. Established Button, compact IconButton, binary Checkbox, Dialog, Lifecycle Status, Entity Identity, Metric, and Home-card contracts are current baselines; Input, SingleChoice, EmptyState, Tabs, and Table remain exploratory at their stated scope. The accepted contained-selection treatment is now a reusable recipe consumed by SingleChoice, while the broader Tabs family remains unresolved.
- Lab reconciliation found that removing the mixed-authority core board had also removed the only visual output for accepted Tabs and the at-a-glance cross-family check. The Components landing page now exposes all eight families in one scroll: 13 real rendered outputs plus 30 explicit unresolved records, with no invented specimens. Compact/default text-only and contained Tabs are restored from reusable visual recipes as current baselines; their complete semantic/API contract and Product navigation remain unresolved for later family work.
- Contained-selection spacing now uses the same 2px relationship between peer items as around the track. The shared recipe updates both contained Tabs sizes and SingleChoice, preventing adjacent selected and hovered pills from visually colliding without creating component-specific exceptions.
- SingleChoice is now an accepted current baseline for the evidenced one-row submitted-value job. The accepted contract includes one default 44px size, intrinsic sizing by default, equal peer growth when explicitly full width, and the shared 2px track inset/peer gap; Field/TextInput advances as the sole Current Review while richer radio compositions remain open.
- Field state review keeps read-only behavior context-neutral: the component does not add a lock icon automatically, while permission explanations remain composition-owned and summaries use information/key-value rows. The misleading empty “submission in progress” fixture is replaced by a disabled field that preserves a visible value; submission progress stays with the form/action lifecycle.
- Field/TextInput is now an accepted current baseline for ordinary short-value fields. Its stable outer label/control/help axis, accepted type hierarchy, 44px full-radius geometry, affixes, and semantic invalid/read-only/disabled states are authoritative for new work; multiline fields, repeated form composition, and adoption remain open. ActionGroup advances as the sole Current Review.
- ActionGroup is now an accepted current baseline for intrinsic horizontal and intentional equal-width vertical action sets with the accepted 8px peer gap. With Button, Field/TextInput, and SingleChoice authoritative, the existing repeated-governance-parameter composition advances as the sole Current Review; business validation and production adoption remain outside it.

## 2026-08-21

- Spinner is accepted as an unadopted current baseline with three placement roles: 14px lifecycle-status, 16px inline/control, and 24px local-region. It inherits contextual color, supports labeled standalone and context-hidden use, respects reduced motion, and now replaces the processing pill's duplicate Loader implementation. EmptyState advances as the sole Current Review item.
- EmptyState is accepted as an unadopted current baseline for quiet title-only and actionable absence. The accepted composition owns a 4px title/description pair, 384px centered description measure, optional unframed neutral icon, and canonical intrinsic compact actions; host framing, exact request channels, bespoke milestone artwork, loading/error states, and product adoption remain separate. Button pressed treatment and long-label behavior advance as the sole Current Review boundary.
- Button label-fit behavior is accepted: routine labels are concise, intrinsic, and single-line; constrained compositions shorten copy, use their own/full-width row, or stack actions before overflow. Only unavoidable localized copy may explicitly wrap to two centered lines on a default-size action. Compact labels never wrap, and Button never truncates, scrolls, or shrinks type. The overflowing lab pressure specimen is removed; only momentary pressed feedback remains in Current Review.
- Button's reusable V1 baseline is complete without claiming production adoption: filled primary and destructive actions use opaque theme-derived colors darkened 8% on hover and 16% on press, while a centered 0.98 scale supplies momentary tactile feedback without layout movement and disappears under reduced motion. Persistent toggle selection remains outside ordinary Button. Drawer advances as the sole Current Review item using the prepared task-versus-selector evidence.
- Drawer is now a reusable unadopted candidate rather than another local specimen. It preserves the existing 512px wider-screen evidence, becomes full-width and bottom-attached on phones, reuses Radix Dialog focus/dismissal behavior, and shares only the 24px shell axis plus anchored header/body/footer regions. Task lifecycle, selector composition, tabs, drag gestures, mobile navigation, and production consumers remain outside its provisional review boundary.
- Drawer body inset is composition-owned: the shell supplies the accepted 8px structural edge and scrolling, ordinary compositions add 16px to reach the 24px content axis, and dense input/output flows such as Zapper may remain edge-aligned. This avoids both forcing unsuitable padding and creating a Zapper-specific Drawer variant.
- Product direction corrected the Drawer candidate before approval: current right-side desktop drawers are generally migration evidence, not a preferred V1 pattern. Drawer is removed from Current Review and retained as exploration only. New modal tasks use the accepted centered-desktop Dialog that becomes bottom-attached on phones; Drawer returns only when a future-facing flow specifically benefits from desktop edge continuity.
- The resolved Drawer direction is stricter than conditional reuse: V1 should aim to eliminate desktop slide-outs by pressure-testing each current Drawer flow against responsive Dialog one at a time. Only a concrete flow that cannot migrate without meaningful context or usability loss can reopen Drawer as a component decision.

## 2026-08-20

- Accepted presentation recipes do not automatically authorize a synthesized compound component. The Radix-backed Tabs candidate now connects the accepted text-only and contained treatments to panels, keyboard behavior, size, and width, but its panel/list API remains the sole Current Review judgment; every newly synthesized contract in this slice has `adoptionStatus: none`.
- Human review narrowed Tabs to the contained presentation in compact/default sizes with intrinsic/full layout settings. Text-only was removed from the component API and active guidance because its strongest assumed evidence belongs to immediate Segmented Control behavior; a real quiet panel-navigation need may justify revisiting it later. Tabs is now a current baseline without production adoption, and Segmented Control advances as the sole Current Review item.
- Follow-up review caught an incomplete propagation: reclassifying the Index overview range treatment as Segmented Control semantics had removed it from Tabs without transferring the visual treatment. Segmented Control now owns the existing unframed text-only compact/default presentation alongside the contained presentation; Tabs remains contained-only. This restores accepted work under the correct owner rather than inventing or discarding a variant.
- Segmented Control is now an accepted current baseline for immediate peer-mode selection. Text-only compact/default is intrinsic in ordinary contexts, while the compact mobile DTF chart range is an evidenced full-width case. Contained compact/default retains the shared contained-selection recipe, and Textarea advances as the sole Current Review item.
- Textarea is now an accepted current baseline that extends Field with a restrained 8px radius, the shared 20px horizontal field inset, a 16px multiline vertical inset, vertical resize, and inherited field states. Switch advances as the sole Current Review item; async lifecycle and production adoption remain outside it.
- Switch is now an accepted current baseline for an immediate boolean setting: 36×20 track, 16px thumb, consistent 2px inset, and an inverse-neutral disabled pair that preserves stored state without active color, blanket opacity, borders, or elevation. The opaque `disabled-structure` role is semantic-recipe owned. Pagination advances as the sole Current Review item; async recovery and production adoption remain outside Switch.
- Pagination is now an accepted table-independent current baseline: 32px aligned peers, optional page-size selection, seven/five-item responsive windows, fixed edge actions, a non-clickable neutral current page, and bare muted unavailable quiet actions. The host owns outer inset; shared 44px mobile hit targets are intentionally deferred to the accessibility pass. DataTable defaults remain unchanged, and Copyable Value advances as the sole Current Review item.
- Segmented Control, TextArea, Switch, Pagination, Copyable Value, Skeleton, and Spinner were pressure-tested as independent provisional candidates. Copyable Value retains clipboard normalization, event isolation, and its two-second copied state without transferring feedback ownership to explanatory HelpTooltip.
- Copyable Value now reflects the product's actual address rule: shorten addresses deliberately by default, never imply CSS clipping as an accepted address treatment, and show full values only where the composition has enough room.
- Copyable Value review is explicitly limited to one inline value, its copy action, and feedback. The vague transaction specimen was replaced with a source-grounded proposal-ID case; paired explorer actions and native/bridged chain-address lists remain tracked composition work rather than implicit parts of this approval.
- Copyable Value now distinguishes prompt from outcome without changing general Tooltip styling: “Copy to clipboard” remains neutral, while the transient copied state uses the existing semantic success surface, border, foreground, and check indicator.
- Copy confirmation now transforms the already-open prompt rather than appearing as a second Tooltip: Copyable Value holds the same surface open for its two-second feedback interval, suppresses copied-state entrance replay, and transitions semantic color over the accepted 120ms duration. General Tooltip motion remains unchanged.
- Focused self-review corrected the remaining Copyable Value issues: dark-theme feedback now uses readable foreground copy with a success-colored check, success waits for the clipboard promise, a polite live status announces completion, Escape can dismiss feedback, and the redundant proposal-ID specimen was removed without discarding it as source evidence. Failure presentation remains explicitly deferred.
- Copyable Value is accepted as an unadopted current baseline, including 14px monospace machine-value typography, deliberate address shortening, the canonical micro copy action, and truthful neutral-to-success feedback. Explorer/bridged-address compositions, failure presentation, sensitive-value policy, and production adoption remain separate; Skeleton advances as the sole Current Review judgment for neutral material and pulse only.
- Skeleton is accepted as an unadopted material-and-motion baseline using the existing border neutral. Its primitive owns no geometry; refined Index overview and Home feature-card loading compositions remain authoritative, and future migration must preserve host-authored sizing, radius, repetition, and loading boundaries while auditing reliance on the legacy default radius. Spinner advances as the sole Current Review judgment.
- Popup-row transfer is content-driven rather than one fixed height: Select and Menu retain the reviewed 40px treatment, while MultiSelect needs 44px for its 20px selection mark. That evidence did not create a shared rule or a review question.
- EmptyState remains exploratory because explicit human acceptance cannot be established from prior work. Quiet and actionable absence fixtures improve the evidence without promoting the API or claiming current-baseline authority; the token picker is evidence for the actionable role, not a reusable variant name.
- EmptyState review corrected a composition-level hierarchy error rather than shrinking the global Button: actionable titles now use the existing 16px/500 structural role, the message-to-actions relationship is the accepted 16px internal-region gap, and the picker evidence uses compact text-only secondary support actions. EmptyState still permits default 44px primary recovery actions; narrow action sets stack at equal content-driven width instead of wrapping into ragged peers.
- Real EmptyState composition pressure-testing refined the shared compact Button rather than adding a local exception: text-only compact actions now use 14px horizontal inset, and icon-bearing compact actions retain the 2px optical reduction with 12px on the icon side. Compact Select/Menu, contained-selection, and icon-only geometry are unchanged.
- EmptyState action content remains caller-supplied rather than hardcoded from the token picker. A brief equal-width vertical experiment exposed the wrong pressure response and was removed: compact actions remain intrinsic, while a separate source-grounded empty-basket specimen tests one compact primary action. Its optional bare 20px icon uses primary neutral foreground in a 24px slot; circular framing remains reserved for semantic outcomes, identity, or repeated-stack alignment rather than routine neutral absence.
- EmptyState title and description are a tight text pair, so their gap is corrected from 8px to the accepted 4px relationship. The single-action pressure specimen uses the first two real basket-setup sentences—long enough to verify natural wrapping without turning the subtitle into instructional body copy or introducing invented migration copy.
- Home feature-card extraction remains Yellow preparation only and must preserve its chart, launch marker, transcript, ticker, animation, route, and chain-switching behavior behind a view/orchestration boundary.

## 2026-08-21 — Link and Accordion preparation

- Link is prepared as the sole Current Review candidate: inline, standalone, external, Router, and canonical Button-composed navigation share one unadopted contract. Light/dark review covered recognition, wrapping, external signaling, and focus; current/visited states, product navigation, tabs, breadcrumbs, analytics policy, and migration remain excluded.
- Accordion is rendered for coordinated informational FAQ/product-detail sets with retained Radix behavior, but it stays provisional because the inherited production content-height keyframe is 200ms while the accepted component-motion rule is 180ms. No production animation was changed.
- The Accordion mismatch was an unambiguous candidate implementation defect rather than a design decision: V1 now uses dedicated 180ms content-height animations while the legacy 200ms production classes remain untouched. Accordion is review-ready after Link; task accordions and independent Collapsible disclosures remain excluded.
- The nine Collapsible importers confirm a distinct one-region disclosure job rather than an Accordion variant. A source-only package classifies help adjuncts, optional auction/configuration regions, dev diagnostics, and repeated governance code disclosure without creating a candidate UI.
- The Drawer-to-Dialog source matrix covers stake, vote-lock, shared/deploy token selection, confirm deploy, package-owned Zapper, and mobile navigation. It preserves each flow's lifecycle and establishes no Drawer exception or production migration.
- Dark/Light review corrected two authority and behavior gaps before handoff: Link and Accordion are labeled prepared rather than accepted, external links merge `noopener noreferrer` into caller rel and require caller-owned accessible new-window copy, and unavailable/loading Button `asChild` navigation is untabbable and non-activatable. Button's visual defaults remain unchanged.
- Human review accepted Accordion after removing dividers, replacing filled-row hover with label/chevron feedback, correcting the shared 14px/20px typography default, and resolving its card rhythm at the composition owner. Collapsed rows stay 48px; open content uses 8px title-to-body and 20px body-to-next-title spacing, while the card host uses a 16px header-to-list relationship and 8px horizontal/bottom edge buffer.
- Accordion and Collapsible share a reusable disclosure-presentation owner instead of allowing the next candidate to reconstruct the accepted trigger, chevron, content, and motion classes. Accordion remains coordinated-set authority; Collapsible is now the sole Current Review candidate for one independently controlled region and does not inherit set semantics or hosted content anatomy.
- Collapsible live review exposed an inherited focus collision: the ordinary outer content ring crossed into disclosure body copy because the accepted 8px title/body relationship deliberately closes the trigger boundary. A reusable inset-content focus recipe now serves Accordion and Collapsible without changing their spacing, color, or other controls.
- Collapsible resting-state review separated content naming from action affordance: the trigger must clearly identify the hidden subject, while an optional caller-owned closed/open cue may clarify the reveal. The component carries no product-copy defaults after an inaccurate supported-token example demonstrated the risk of inferring capabilities from source context.
- Collapsible is accepted as the unadopted one-region disclosure baseline. Its shared Accordion presentation, truthful caller-owned subject/cue copy, muted resting affordance, primary hover/focus feedback, and mobile cue hiding are authoritative; hosted content and production adoption remain outside.
- Inline Message advances as the sole Current Review candidate after classifying eighteen Alert importers and repeated local banners. It owns persistent information/success/warning/danger anatomy, standard tone icons, 16px default and 12px compact inset, canonical compact actions, and opaque semantic feedback roles. Visual tone stays separate from announcement urgency; Toast and transaction lifecycle remain separate work.
- Human review correctly removed Inline Message from Current Review before judging it: isolated tone specimens do not establish contextual prominence, spacing, or density. The source audit, implementation, and grid remain Yellow evidence only; it returns with source-grounded content before and after the message, or waits for the owning task composition. Adjacent Toast and Progress work does not bypass this boundary.
- Typography advances as the sole Current Review foundation candidate. The review combines the exact nine-role core hierarchy and restricted 12px exception with the 300/500 weight strategy, multiline defaults, numeric and machine-value treatment, stable responsive sizing, four realistic product contexts, and explicit stress cases. Foundation targets are now first-class in the typed Current Review queue; the candidate remains exploratory and authorizes neither production migration nor routine use of Lausanne 700.
- The typography candidate now groups its recommended refinements into one visually distinct provisional section: a display-only 48px-to-40px narrow-phone exception, guidance for choosing between similar roles, Lausanne tabular financial values versus monospace identifiers, wrapping/reading-measure behavior, and a same-copy three-paragraph comparison of 16px ordinary versus 14px supporting text. The group remains unaccepted; the 14px long-copy side is a stress test, and none of this authorizes production migration or a responsive scale for ordinary application typography.
- Human review accepted Typography as the current baseline after adding the negative-performance specimen. The authority now covers the nine semantic jobs, six core sizes, restricted 12px auxiliary use, 300/500 weight split, 16px/24px ordinary and 14px/20px supporting roles, display-only phone step, Lausanne tabular financial values, monospace identifiers, natural wrapping, and reading measure. The 14px long-copy comparison remains a stress test; real product evidence may refine the baseline later, and acceptance does not authorize production migration. Current Review is empty.

## 2026-08-22

- Color closure auditing confirmed that canonical V1 owners already follow the desired theme boundary: components consume semantic aliases and do not select different color aliases with `dark:` utilities. Existing product-level `dark:` usage remains migration evidence rather than V1 authority.
- The focused contrast audit found three bounded gaps instead of a palette-wide failure: quiet feedback foregrounds, the light destructive filled action, and light supporting text on a muted surface. The review candidate preserves surfaces, borders, radii, spacing, and semantic hues while showing heavier filled and overly neutral alternatives as rejected comparisons.
- Review self-correction: deepening the destructive candidate default without owning its interaction ramp would have made hover lighter than rest. The provisional review now carries a coherent default/hover/pressed progression; no accepted Button recipe or production token changed.
- Human review accepted the restrained Color calibration as the current V1 working baseline. Feedback, supporting text, and destructive actions now inherit named semantic roles from reusable owners; components use one alias across themes, while the theme boundary owns raw light/dark values. The legacy global `muted-foreground` remains unchanged so acceptance does not silently restyle production screens; broader migration and full-screen tuning remain separate.

## 2026-08-23 — Coordinated navigation preparation

- Global application navigation and Index DTF Product navigation advance as one Current Review judgment because they appear simultaneously, while remaining separate exploratory candidates and route models. The realistic shell covers anchored global overflow, application actions, DTF identity, collapsed/expanded product rail, current/unavailable routes, and separate constrained-screen compositions.
- Current Index DTF and application-header evidence drive the candidates; legacy Yield DTF presentation is explicitly excluded as authority. Neither unreviewed candidate may become authority for the other, and no production navigation or route taxonomy changed.
- Self-review corrected the reusable boundary before handoff: navigation destinations now consume the accepted Link owner directly, including caller-owned accessible external-window copy and secure rel behavior. Popup anchoring, popup-row inset geometry, full-region row surfaces, narrow lab overflow, and stale rendered/unresolved counts were reconciled at their owners.
- Human review caught that the DTF mark used its intrinsic 24px logo box while route icons occupied a centered 40px slot, visibly shifting the desktop identity left. The Product Navigation identity owner now reserves that same 40px leading slot in collapsed and expanded states; this is a candidate correction, not production adoption or a change to the accepted ChainBadgedLogo owner.
- Follow-up composition review separated rail and menu geometry instead of transferring the 40px rail slot to mobile: Product identity and route icons stay on one fixed desktop axis through expansion, while the constrained menu uses a 24px content-led identity slot. The lab now exposes the four real building blocks before the full compositions. Global top-level routes use meaningful icon-plus-label treatment and a quiet selected surface instead of an underline; More and constrained global navigation consume the same descriptive destination-row owner without becoming one navigation system.
- Product-rail state refinement keeps the 40px interaction target and fixed icon axis but insets the visible hover/current surface by 4px. Collapsed and expanded presentations therefore share one 32px surface geometry—circle to pill—without shifting icons or shrinking the focus and click target.
- Human review rejected that inset circle-to-pill experiment because the expanded pill still produced the wrong composition. Product rail state now stays on the stable 40px icon circle in both widths; expansion reveals a current-colored label without background, avoiding both icon shift and full-row pill chrome.
- Human review requested one final layered comparison: the collapsed route remains a single 40px circle, while expansion reveals a 40px outer pill with a 36px inner icon circle. The 2px inset transfers the contained-selection relationship rather than introducing a 36px control tier, and the icon center remains fixed.
- Layered-state review exposed three implementation defects rather than new design choices: the translucent outer pill painted above the active label, a 12px structural gap became 14px from visible circle to text, and the rail component only matched content height. Foreground stacking, a 6px structural/8px optical gap, and host-height filling now belong to the Product Navigation owner.
- Global top-level navigation now keeps a 2px sibling gap between destination pills, preventing an adjacent hovered item from visually merging with the current-page surface while matching the contained-selection inset rhythm.
- The coordinated navigation review now includes a home-screen global header with no current destination, making the neutral state directly comparable to the selected route state without creating a separate navigation variant.
- Global-navigation review corrected its application-action evidence: the common desktop cluster now shows Search, theme, language, and Connect, while the conditional contact-team bell is no longer mislabeled or presented as a permanent Notifications control. The real cluster is also exposed as a reviewable building block.
- The application-control specimen now includes deterministic disconnected and connected-account states. The connected fixture shows the current chain-mark-plus-short-address model, allowing header hierarchy and width review without a live wallet connection while leaving account-menu behavior outside the candidate.
- Human review requested a grouped utility comparison: the global application controls now sit in a 40px rounded toolbar shell, use outlined 32px compact controls, and keep 4px wrapper inset on both axes plus 4px between siblings. The wrapper uses a non-layout-consuming inset ring so the visible outline does not erode the requested physical inset.
- The disconnected Connect gateway now uses the accepted primary Button tone so it stands apart from the outlined utility controls. The connected chain-and-address account control remains secondary because its role changes from acquisition CTA to identity/menu access.
- Product Navigation now converges its constrained menu with the desktop rail at the semantic style layer rather than copying rail-only geometry: both use the same selected/hover/focus/unavailable and 14px/500 label language, while mobile retains a 44px full-width row, one fully rounded state surface, and a 2px sibling gap without the collapsible rail's nested icon circle.
- Follow-up optical review kept the constrained Product menu's simpler one-surface treatment and corrected its inherited descriptive-menu spacing: 12px outer inset, a 16px icon slot, and an 8px icon-to-label gap now align the glyph and trailing chevron while matching the desktop rail's optical label relationship.
- Live axis measurement then showed that equal 12px row insets still placed route icons 4px left of the DTF identity mark. Product-menu rows now use 16px leading and 12px trailing inset, preserving the 8px label gap while aligning the icon centerline to the identity axis without adding a compensating inner circle.
- Human optical review found the resulting uncircled icon and label too close. The constrained Product row now uses the intermediate 10px optical token between them—more air than the desktop circle relationship without returning to the earlier 14px inherited menu gap.
- Foundation clarification supersedes treating that 10px gap as a token: relationship spacing remains 4/8/16/24px, while off-grid values are limited to explicit optical edge compensation and do not authorize new sibling gaps. Product Navigation spacing is now declared provisional until its mobile composition returns to the scale. A separate non-blocking follow-up will recheck existing Button icon-side and Select/Menu trigger inset exceptions for convergence without launching a broad audit now.
- Owner-level correction resolved the identity and route-axis problem without preserving the 10px experiment. Canonical `EntityIdentity` now owns an 8px mark-slot-to-copy gap. The constrained Product menu uses symmetric 12px row inset, a 24px route-icon slot, and an 8px slot-to-label gap, so its identity mark, route glyphs, identity text, and route labels align on two shared axes. The desktop rail retains its fixed 40px axis, gives identity and route rows the same 6px structural gap, and renders the lab identity at 32px inside that slot. The broader Button and Select/Menu optical-inset reconciliation remains deferred.
- Product-identity review corrected an over-transfer from asset rows: the real Index DTF rail uses a single symbol line, and Product Navigation now does the same on rail and constrained screens while retaining chain context in the badged mark and navigation label. A requested identity-triggered DTF-switcher submode is recorded as Product Navigation work; it must establish its real list source, return behavior, collapsed-rail transition, and constrained-screen equivalent before the identity becomes a clickable trigger.
- The DTF switcher is now a complete lab interaction rather than a decorative affordance. Its 40px identity trigger uses a smaller canonical badged logo, a persistent collapsed switch cue, and a far-trailing chevron when labels are visible. Activation pins the rail, replaces page routes with visually related alternative-DTF rows, and returns after selection; constrained navigation mirrors the transition. The current DTF remains in the identity trigger and is excluded from the alternatives list. The rail keeps identity fixed while its remaining height scrolls to the surface bottom; the constrained item viewport is capped at 288px. A 32px surface-colored bottom fade signals continuation, while equal end padding lets the final row clear it. A static 16-DTF set from the captured Discover snapshot proves density and overflow only. Production list sourcing, ranking, search, route preservation, analytics, and adoption remain open.
- Mobile review corrected an incomplete lab composition: global and DTF entry points now appear in separate phone specimens instead of as already-open menus. The global specimen starts from a 56px application top bar. The DTF specimen gives switching to a detached 72 × 48px badged-logo pill with an 8px inset/gap and gives page navigation to a separate 48px floating action cluster. Both open the same full-width bottom-drawer presentation at content-appropriate heights and consume the canonical Drawer header's equal 24px top/right action axis. Navigation rows and the header align content at 24px. The current review experiment uses the existing 16px/500/24px item-title role, a compact secondary close control, and a 24px trailing-chevron slot matching the leading icon wrapper. Drawer row shapes remain at an 8px outer inset, use 16px horizontal/12px vertical padding, show a visible default outline, and separate 48px rows by 4px while leaving popup rows unchanged. The active row reuses the existing `primary/30` selected-pill outline so its edge sits between the pale selected surface and full primary foreground. Drawer identity and supplementary separators are removed in favor of spacing and outlined-row grouping. The mobile review fixture temporarily presents Auctions as an ordinary enabled destination so the row system can be judged without a disabled-state interruption. Their states remain mutually exclusive. The current production logo-to-overview link remains evidence only—no production behavior changed.
- The DTF page drawer now treats its token contract as actionable identity rather than quiet footer metadata. The first attempt wrongly expressed importance by adding a separate secondary-colored, 8px-radius card; that violated the drawer's established atomic-row shape and surface rhythm. The next pass led with the chain logo/name, incorrectly implying that the copied address belonged to the chain. The corrected non-navigation row consumes the exact shared drawer-row geometry and leads with the canonical badged DTF mark plus “DTF on Chain” copy before the shortened address and accepted copy action. Snapshot-derived fixtures retain full valid contract addresses internally, while clipboard output remains complete.
- Inspecting that real row exposed drift in the canonical `ChainBadgedLogo` owner rather than a drawer-specific issue: md reused the 10px sm badge, leaving only 6px of visible chain artwork after its 2px separator, and `rounded-md` collapsed to an effectively circular mark. Human review established the balanced md geometry at 12px with a 1px separator, leaving 10px of visible artwork without competing with its 20px token mark. Floating badges use the established 4px rounded-square role; 2px separators remain reserved for overlapping logo stacks. No drawer-only logo geometry was added.
- Multi-chain review established an adaptive token-address composition. Single-chain drawers retain the self-identifying badged “DTF on Chain” row without a heading. Multi-chain drawers add one wrapping “{ticker} token addresses” label and switch to chain-led rows with muted Native/Bridged roles; repeating the ticker per row would obscure the only meaningful distinction and reduce room for addresses. The chain artwork uses the same 16px scale as page-navigation glyphs, centered inside their shared 24px leading slot rather than filling it with a 20px mark. Real CMC20 BNB Chain and Base contracts exercise the two-row state, and each copy action retains its own full address.
- The page drawer's 8px shell inset already establishes the final row-to-edge distance. Removing the supplementary region's duplicate 8px bottom padding changes the previous 16px bottom gap to the intended 8px, matching the horizontal row-shape inset at the shared Product Navigation drawer owner rather than compensating inside the address composition.
- Without dividers, the token-address group needs section spacing rather than ordinary row spacing. The shared drawer supplementary region now begins 16px after the route list, while its title-to-first-row relationship remains 8px and the final bottom inset remains 8px.
- Accumulated drawer spacing previously placed header controls 24px from the top edge but the first row only 16px below those controls. Drawer Product Navigation now uses 16px list-top padding after the header's existing 8px bottom padding, making both visible gaps 24px while popup-menu list spacing remains 8px.
- Mobile DTF-trigger review rejected whole-pill subtle-hover recoloring: it flattened the floating identity surface and made the interaction feel unrelated to the switch cue. The trigger now keeps its surface stable and pairs an unbadged 32px DTF token logo with a separate 32px ghost switch cue at the established 2px contained-control gap. The pill retains the adjacent floating action cluster's 8px outer inset without inventing a new size tier. Hover/focus/open feedback belongs to the ghost cue rather than the entire floating shell, while the single parent button retains truthful click semantics and the accepted centered press transform; chain context remains in the drawer where it disambiguates addresses and destinations.
- DTF-switcher copy now says “Switch DTF” instead of “Choose a DTF.” The user already owns a current context, so the action-led label describes the actual transition and stays consistent across the mobile drawer and desktop switcher navigation.
- Collapsed desktop DTF-switcher review removed two competing micro-signals: neither the chain badge nor the floating up/down cue appears before expansion. A plain 24px token logo now sits inside the real outlined 40px trigger, making clickability legible without layering marks over the identity. Expansion restores the badged logo, label, and far-edge chevron where that context has room.
- Clarification: the desktop identity outline belongs to its full clickable region, not the fixed logo slot. It therefore changes from a 40px circle when collapsed to the complete logo-label-chevron pill when expanded, truthfully exposing the interaction boundary in both states.
- Desktop DTF-list scrolling no longer clips 16px before the identity divider. The same resting gap now belongs to scroll-content top padding, so rows move through it and disappear at the divider while the unscrolled composition keeps its established spacing.
- Desktop rail dismissal now treats DTF-switcher mode as part of the expanded transient navigation state. Moving the pointer outside the rail—or moving keyboard focus outside it—both collapses the rail and restores the current DTF's default page routes, matching the ordinary page-navigation behavior instead of leaving alternatives pinned open.
- Closed mobile global-header review replaced the earlier schematic Account/Menu row with a reusable 56px shell and canonical compact 32px utility, account/connect, and navigation controls. Human review rejected the initial 44px pass as an over-transfer of ordinary mobile form/action geometry into dense persistent navigation chrome. Disconnected, condensed connected-account, transparent, and 360px pressure fixtures expose the actual review boundary without changing the production header or opened global navigation.
- Compacting the header exposed a second transfer error inside the grouped utility trigger: three default 16px glyphs became a cramped visual block. The trigger now uses the existing 14px micro glyph scale and the standard 4px relationship gap, while its 32px control geometry and 8px side inset remain unchanged.
- Human review then exposed the larger classification error: treating Search, Theme, and Language as homogeneous Menu rows discarded the accepted owners and made the panel worse than the live reference. The first corrected composite utility surface preserved the live section anatomy with a truthful SearchField-family launcher, contained Segmented Control, and Select. Search closes the panel before opening the command dialog; theme and language update in place. The legacy duplicate Search row and raw shadow are not transferred.
- Utility-panel scale review aligned the default contained Segmented Control and default Select to the SearchField launcher's 44px height. This does not enlarge the persistent header controls, which remain 32px. A trigger-relative Popover could satisfy viewport geometry only at a real narrow breakpoint and rendered falsely in the embedded phone specimen, so the mobile header now owns the disclosure geometry: it begins exactly at the 56px boundary and spans the header width in both product and lab contexts.
- Human review rejected the remaining popup-inside-a-popup language interaction. Because the supported set is fixed at four values, the 44px language summary now expands canonical selection rows inside the existing utility surface; the panel grows downward, selection collapses the list, and dismissing the outer surface resets the disclosure. This is local utility-panel composition, not a new Select variant or a change to the accepted popup recipe.
- The first inline-language implementation incorrectly mapped “contained choices” to the structural `secondary` surface, producing an unjustified beige block inside the white utility panel. Human feedback corrected both the component and the heuristic: inline disclosure content inherits its owning surface unless it establishes a genuinely separate structural region. Geometry or behavioral verification does not substitute for inspecting that hierarchy in the rendered composition.
- Follow-up theme review exposed a second ownership mismatch hidden by the light palette: the attached utility panel still used `popover` while its header owner used `card`; both happened to resolve to white in light mode but diverged in dark mode. The panel now shares the header's `card` surface in both themes. Language expansion is one connected outlined disclosure that lists only alternatives, avoiding both the duplicated current language and the previous loose-row composition.
- Connected-header review reversed an unnecessary information loss in the mobile candidate. The current production account pattern and the desktop candidate both preserve chain mark plus shortened address, and measured lab compositions show that account identity still fits at the 320px lower bound when the brand compresses first. The header now uses its own inline-size container: 390px retains the full Reserve wordmark with a 7px measured control gap, while 360px and 320px use the existing square mark and preserve the same canonical compact account, utility, and menu controls. Icon-only account is not a normal mobile variant.
- Connected-account optical review converged desktop and constrained headers on the existing 14px micro chain mark and 4px relationship gap. The constrained fixture shortens only its visible address two characters earlier (`0x71…2A6C`); its accessible identity and the roomier desktop display retain `0x71F9…2A6C`. No 6px gap or account-only icon size was introduced.
- Global destination review removed the inherited descriptive-menu model. Mobile global navigation now shares the outlined 48px drawer-row recipe, icon/label/trailing axes, and 4px sibling rhythm with mobile DTF page navigation; its group headings provide context without repetitive subtitles. Desktop More consumes the same outlined single-line destination owner at the established 40px popup density, and its former subtitle-driven 384px width contracts to 256px. Both fixtures now cover the real inventory—four primary destinations, DTF Explorer, Bridge, Create Yield DTF, and five external resources—instead of the earlier three-item placeholder. Internal destinations use a chevron; external destinations replace it with the external-link indicator.

## 2026-08-25 — Navigation acceptance

- Human review accepted Global and Index DTF Product navigation as separate current baselines after coordinated desktop and mobile judgment. Current Review is empty and production adoption remains `none`; route taxonomy, live inventory and event sourcing, account-menu behavior, analytics, and navigation-specific geometry promotion remain later integration work.
- Initial sequencing favored Home/Discover because its feature-card extraction was the smallest ready screen prerequisite. User priority clarified that transaction standardization should advance sooner, so Home returns to the ready backlog rather than blocking transaction work. The corrected next stage is a cross-flow transaction-system map that reuses the existing Zapper, modal, Drawer-to-Dialog, and component audits before choosing visual candidates.
- Transaction-family consistency belongs at the grammar layer rather than in one universal flow shell. The audit must classify duration separately from execution visibility: instant work, opaque asynchronous waiting such as package-owned Zapper execution, transparent named stages such as automated mint, and delayed/later-claimable settlement. Shared rhythm, state language, actions, details, recovery, and outcome hierarchy may transfer; progress detail and animation transfer only when the underlying process truth supports them.
- Index DTF Zapper and automated mint are the primary transaction-audit anchors because of current product importance and recent functional/design investment, not because their existing presentation is accepted. Each flow step must compare available source truth, current communication, user decision/recovery needs, and missing information. Success and other terminal states are reconstructed from the real outcome—including useful amounts, assets, position or permission changes, transaction identity, remaining work, and next actions—rather than copied from today's UI.
