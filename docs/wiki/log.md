---
title: Log
updated: 2026-09-16
type: log
---

# Log

Append-only chronological record: lessons, corrections, friction. Newest section last. Tag kit-caused friction with `kit-friction`.

## 2026-07-02

- Wiki initialized by llm-workflow install.
- Adopted via `skills/adopt.md` on branch `cleanup/ai-release-general-ui-cleanup-stabilization`. Inventory: root `CLAUDE.md` (+`AGENTS.md` symlink), `src/views/index-dtf/issuance/async-mint/CLAUDE.md`, `.claude/commands/*` + `.claude/skills/*`, `IMPROVEMENTS_PLAN.md`, `docs/*`.
- Dropped: `IMPROVEMENTS_PLAN.md` (stale root-level plan for the merged `feature/split-discover-home-highlight-dtfs`; component split already done; remaining items moved to [[progress]] backlog). Untracked `.agents/skills/` (Codex-generated duplicates of `.claude/commands/review.md` and `.claude/skills/*`) flagged for the human — not deleted, not committed.
- Covered by kit, dropped from project docs: generic coding rules (dumb-beats-clever, rule of three, early returns, comments-explain-why, atom patterns, laziness ladder, review discipline, verification gate) — now `skills/code-standards.md`, `skills/workflow.md`, `skills/review-panel.md`. Register-specific deltas recorded in [[project]] § Overrides.
- ui-stabilization-sweep lessons: (1) the Codex-authored DataTablePagination extraction both changed shared defaults silently AND introduced a real mobile layout bug — the rules-check caught the first, only a screenshot with 12-page data caught the second; visual verification on realistic data is non-optional for shared-component refactors. (2) The designer PR removed all DTF navigation on 640–1024px viewports (old bottom nav was `<lg`, replacement bar was `<sm`); fixed by extending the floating bar to `lg:hidden` — pending design confirmation. (3) Two CSS-hidden copies of the about card meant two autoplaying videos on every overview visit; JS-gate breakpoint-exclusive heavy content instead of CSS-hiding it. (4) User rule reinforced: `cn()` for ALL className concatenation, never template literals.
- NOT fixed, flagged for engineer review: `Layout`/`AppHeader` hardcode index-dtf overview route checks (shared-container change baked into the design); `navigation-menu.tsx` shared defaults changed by the PR (trigger padding, viewport border); desktop basket table lost its 10-row cap (100+ rows render for big DTFs — confirm intent).
- kit-friction (resolved upstream, kit commit dba52ee, pulled via --update): adoption assumed a clean tree (mid-flight is the norm — adopt.md + workflow-start error now cover it); tool-generated duplicate agent dirs weren't in the adopt inventory; ui-ux verification missed realistic-data-volume and breakpoint-band checks (both bit us today); reviewers had to be manually primed with the wiki rules (review-panel now requires it). Also: this session initially forgot the kit-friction tag itself — the audit's "user corrections" line was the only reason the evidence survived; tag at write time.
- Zapper double-mount RESOLVED (user-confirmed): the container's modal ZapperWrapper is skipped on `/issuance*` routes, restoring one-Zapper-per-route. The real hazard was config divergence between the two mounts (inline: `debug`, no `hideLargeMintPrompt`; modal: the opposite) fighting over shared zapper state. MintBox and the mobile Buy/Sell only call `useZapperModal().open()` — they never mount instances, so overview is unaffected.

## 2026-07-03

- cowswap-prompt-rework: candles default in price chart; large-mint prompt now recommends CoW Swap (both tabs, external link) with new `impact` (>1% truePriceImpact) and `capacity` (Ondo per-transaction cap from `dtf/ondo`) variants; `useOndoLimits` lifted to `src/hooks/`; new domain page [[zapper-prompt]].
- Lesson: `lingui extract` had not run since several features landed — the catalogs were stale enough that a routine extract swept in ~70 unrelated msgids. Run extract at every stage closeout that adds copy, or the next stage inherits the noise (this one filled its own 14 strings + the 4 chart-toggle labels the candles default made visible, and backlogged the rest).
- Lesson (review value): the Dark/Light pair caught a real latch hole (impact signal below the $100 applicability floor → mobile dialog re-pops after dismissal) and a product regression risk (empty candles endpoint → blank default chart, now falls back to the line body). Both were invisible to the gate; both were cheap to fix in-stage.
- cowswap-prompt-rework follow-ups (user testing): overview modal zapper now shows the prompt (`hideLargeMintPrompt` removed — flag pre-dated the rework); reducer got an escalation-only rule after real-wallet testing showed the card flickering between "High price impact" and "Large Order" as racing/refetching quotes disagreed around the 1% threshold. Latched variants never downgrade until a clean reset; 4 new reducer tests pin it.
- capacity warning: dropped the off-hours suppression after user testing ($5M input during the July-4 market closure showed "No route found" instead of "Order too large"). The signal is input-derived so it now fires pre-quote regardless of market state; closed-session caps are labeled "Regular" (the API mirrors regular caps when closed). Reverses the original "not during Off-Hours" spec line — explicit user instruction.
- Ondo off-hours rule (PM): CoW-CTA variants (large/impact/error) suppressed on Ondo DTFs while `market.isOpen === false` — off-hours secondary liquidity is stale, don't push users there. Note the inversion vs the same morning's change: capacity fires off-hours (informational, no CTA), CoW push does not. `!isOndoOffHours` also gates `isApplicable` so a latched CoW card clears when the market closes.
- referral tracking: `?referral=` capture (last-touch, no expiry) + `referral_landed` event + wallet↔code POST on connect; reserve-api gets `referral_links` table + origin-guarded `POST /referrals/link` (branch feature/referral-links there). New domain page [[referral]].
- Lesson (review value): Dark Claude caught that a bare `localStorage.setItem` inside the mixpanel-init effect would crash the app for Safari Private Mode visitors — the exact audience referral links target; all storage access now goes through safeGet/safeSet. Plan-stage review also killed two subgraph-API assumptions (index/yield query asymmetry, TRANSFER-in counted as mint) before any code was written — the feature then shrank to attribution-only and none of that code was needed.
- Fastify ordering gotcha: schema validation runs BEFORE preHandler, so a "guard" registered as preHandler does not shield validation; header-only guards belong on onRequest.
- referral follow-up (user-directed): reserve-api branch re-based onto origin/staging (deploy flow), localStorage helpers kept over atomWithStorage (no reactive consumer; dynamic flag keys don't fit atoms). /code-review high (workflow, 15 agents) then found what two Dark/Light passes missed — all three real bugs shared one root cause: captureReferral lived inside React's effect tree, where child effects (AtomUpdater link, Redirects navigate) run first. Fix was structural, not choreographic: capture to storage at module load in index.tsx, mixpanel half stays post-init. Lesson: when several findings share a root cause, fix the structure once instead of patching each ordering.
- referral scope cut (user-directed): reserve-api piece removed entirely — Mixpanel is the single attribution store (`referral_wallet_linked` with explicit wallet+code is the link record; Mixpanel export API covers extraction). reserve-api PR #212 closed unmerged, branch feature/referral-links kept on the remote as the archive of the reviewed table+endpoint if a DB record is ever wanted. Register dedupe flag re-scoped from API base to Mixpanel project key.
- weighted-ondo-limits: capacity cap re-derived as weighted `min(capacityUsd / weight)` (see [[decisions]]); new `closed-impact`/`closed-error` prompt variants fill the off-hours gap where high-impact or failed quotes previously showed nothing; signals extracted to a pure `deriveMintPromptSignals` after review flagged the spec-critical booleans were untested inline component code.
- Lesson (from the Dark reviewer's one Important find): removing a gate from a latch's reset condition (`isApplicable` lost the off-hours term so closed variants could latch) silently un-protects every state the gate was covering — the latched CoW CTA survived a flip to minting-unavailable for a full refetch cycle. When touching a latch reset, re-enumerate what each removed term was protecting and encode it as an explicit rule + test.
- Lesson: client and API disagreed on missing session buckets (client treated missing as blocked; the API's `sessionCapacity` falls back to the regular cap). For optional payload fields, read the producing side (`reserve-api`) before choosing an interpretation.

## 2026-07-06

- price-chart-granularity stage: diagnosed the "choppy/crazy charts" empirically before touching code — probed `historical/dtf` for both example BSC DTFs and found (a) 1M at `1h` = 721 points with ~50% direction reversals, (b) the <30d young-DTF override forcing `1h` on YTD/1Y for backfilled AI DTFs (4.5k–8.8k points — the actual "crazy" chart), (c) the reported "3h/6h granularity" was just data holes; the API 400s on anything but `5m/1h/1d`. Lesson: measure the API before redesigning around a symptom report.
- Verified the weekly downsample live by counting recharts path commands (`.recharts-area-curve` `C` segments) — mvDEFI All went 532 daily → ~80 rendered. A first mismatched count (394 on TEST4) turned out to be the dedupe working (518 raw rows had 125 duplicate timestamps), not a bug: replicate the app's full transform pipeline in a script before assuming the UI is wrong.
- Found pre-existing (backlogged): BTC+ yield DTF's BTC data-type mode renders historical `priceBTC` ≈ 1.1 against a live now-point ≈ 0.34 — denomination mismatch upstream of the charts.
- Correction (user feedback on the granularity pass): plain supported intervals left 24H (25 pts) and 1M (31 pts) too sparse — the fix generalized to "fetch the finest supported interval, bucket client-side to the display density" (24H: 5m→15m, 1M: 1h→6h), reusing the weekly downsample as a generic `downsampleToBucket`. Lesson: point-count targets (~100 per chart) beat interval names when choosing granularity; when the API doesn't serve the density you want, bucket down from the next finer one instead of settling for the next coarser one.
- Homepage featured cards had the worst density of all: `v1/discover/featured` embeds ~2.1k hourly points per DTF over 90d, and each card renders the series twice (pattern + stroke chart). Fixed at the data layer (`normalizeFeaturedItem`) with a span→bucket rule (`downsampleForSpan`), which forced lifting the downsample util from the overview feature folder to `src/utils/chart-downsample.ts`. The discover-table endpoint already serves daily 30d series — checked before touching it, left alone.
- Dark/Light review of the granularity branch caught a real Important bug the self-review missed: the display timeseries (now bucketed) also fed `indexDTF7dChangeAtom` via `useSync7dChange`, so selecting All on a multi-year DTF would compute the trailing-7d change from weekly points (a 7–14d window). Fixed by returning the pre-bucket `fullTimeseries` from `usePriceChartData` for derived stats. Lesson: when downsampling a series, grep every consumer of that series first — display data and derived-stat data have different fidelity requirements.
- llm-context-hardening phase 1: 8 deep explorers over sibling repos → new wiki pages [[ecosystem]], [[subgraphs]], [[index-protocol]], [[yield-protocol]], [[zapper]]; [[sdk]] extended (monorepo/version facts, live yield namespace, exported config constants, version-gated write ABIs). All references are GitHub URLs so teammates' LLMs can clone siblings. Notable finds: register pins react-sdk ^0.2.0 while SDK ships 0.3.1 (caret on 0.x minor doesn't float); react-zapper state is module-level jotai singletons (the double-mount root cause, now documented); yield subgraph's ISSUE/CLAIM entry types are dead enum surface; trusted-filler fills are same-block CoW settlements invisible to bid-shaped queries.
- llm-context-hardening phase 3: [[improvements]] written from a 7-dimension audit (structure, state, data-fetching, duplication, type-safety, build/perf, testing). Headline corrections to received wisdom: routes are NOT lazy (project.md fixed — the ~10MB bundle cause), vitest never ran in CI, RSV/zapV2 are legacy-live not dead, and the installed react-sdk is 0.2.0 (config-constant imports the SDK docs suggest are gated on a bump). Two release blockers found beyond the known RESERVE_API pin: hardcoded api-staging URLs in useIndexDTFList.ts and use-featured-dtfs.ts, and a shadow prod RESERVE_API in use-dtf-status.ts (two environments in one session). CLAUDE.md load order now points at improvements.md before code work.
- pr-1008 review complete (all 11 partitions, adversarial verify; finished on Opus/xhigh after two Fable session-limit deaths — resume-from-journal + model override was the recovery path). 25 findings stand. Also confirmed: lingui t-as-parameter renders empty preset labels in ALL locales (verified against the real babel pipeline); async-mint has no price-impact gate on submit and over-blocks existing-collateral mints; isSafeMultisig misclassifies EIP-7702 EOAs. Refutation pass earned its cost: 7 candidates killed, several because behavior was byte-identical on master.
- pr-1008 triage (Luis): the vote-lock compliance-gate removal that the review flagged as a security regression is INTENDED (decision recorded — vote-lock is governance participation, not a regulated surface; don't re-flag). Staging pins acknowledged, flip all THREE sites at release cut. Async-mint gate findings → backlog, out of scope. Lingui finding re-verified on request: vite-served transform shows t`...` untransformed inside buildVotingPresets while the component passes runtime `_`; runtime tagged call returns "" — preset labels are blank in every locale including English (deploy basket-changes + other-changes steps). Fix = msg descriptors at module scope or inline presets into component scope.
- wiki-conflict fix: the recurring PR conflicts on wiki-touching branches were all in the four append/ledger files (log/decisions/progress/index) — concurrent branches append to the same region every time. Added root `.gitattributes` marking those four `merge=union` (built-in keep-both driver, works locally + on GitHub). Verified with a live two-branch merge on real log.md: 0 conflict markers, both entries kept. Scoped to append files only — domain pages are rewrite-in-place and must not get union. See [[decisions]]. Portable follow-up for the kit: ship this attribute from the wiki skill/installer.
- featured downsample moved server-side (user-directed): reserve-api now applies the span→bucket policy (`downsampleForSpan`, ported verbatim with its tests to its `src/lib/chart-downsample.ts`) inside `discoverDtfService.getPerformance`, covering `discover/featured` (~2.1k hourly pts → ~92 daily) AND `discover/dtfs?performance=true` (daily series → no-op) — applying at the shared `getListItems` path instead of per-route was an explicit user call. `priceChange` still computes from the full hourly series (downsample runs on `performance` only). Register side surgically reverted from commit 5f42832: `normalizeFeaturedItem` passes `performance` through raw again; `downsampleForSpan`/`getDisplayBucket` pruned from `src/utils/chart-downsample.ts` (only `downsampleToBucket` + overview constants remain — overview still buckets client-side because `historical/dtf` serves only 5m/1h/1d). The overview refactor from that commit (fullTimeseries for derived stats, `FetchInterval` type) was kept. Ships when reserve-api deploys to staging; until then featured cards just get the still-hourly payload (rendering is unchanged either way).
- candlestick-parity stage: the reported "range changes when toggling Line↔Candles" was measured before coding (Playwright tick readout on all 6 ranges) and turned out to be pure rendering, not data — both endpoints read the same `historical_dtf_price` table. Three compounding causes: category-axis `interval` skip ticks (last label up to N−1 candles early), epoch-floored buckets (YTD's first 7d candle labeled in December), and 'all' fetching `from=0` while the line clamps to launch−1y. Fix stayed candle-side only: `snapToBucketStart` on the request `from`, 'all' clamped to the line's window (same snapped from keeps the 'all'→'30d' discovery cache-share), ticks reuse `useXAxisTicks`. No reserve-api change — from-aligned SQL bucketing was considered and rejected (currentHour-anchored `from` would shift candle boundaries hourly).
- Launch marker on candles: a band scale can't resolve a non-bucket timestamp (`scale(launchTs)` → undefined → the line-chart marker silently nooped), so `candlestick-launch-marker.tsx` locates the containing bucket (`locateCandleBucket`, fraction × bandwidth) and feeds the precomputed x to the untouched `PriceChartLaunchMarker` through a synthetic single-value scale. Lesson: recharts `ticks` on a category axis silently NaN-drops values that aren't exact domain members, and a numeric `interval` prop decimates explicit ticks too — verified in node_modules before writing the component (plan-stage recharts source-check killed the numeric-axis alternative for Bar charts: bars center on `scale(t)` and bandSize infers from the min tick gap).

## 2026-07-08

- lock-zap-settings stage: react-zapper grew a `disabledSettings` prop (`{ deepLiquidity?, forceMint? }`, released as 2.4.0) following the package's prop→updater→atom convention (`DisabledSettingsUpdater` force-resets both atoms to false); register's `zapper-wrapper` passes it for the 5 featured BSC DTFs hardcoded in `locked-zap-settings.ts` (see [[zapper]]). Verified with Playwright against the linked package: frozen unchecked on PHOTON, untouched on CMC20, and the SPA-nav case (checked on unlocked DTF → navigate to locked) resets correctly because the updater effect runs pre-paint and no quote can fire with an empty input.
- Dark/Light pair returned zero Critical/Important on a 9-file package diff + 2-file register diff; both independently flagged the same two Minors (missing helper test, frozen checkbox keeps its benefit-selling help copy). Convergent minors from independent reviewers are cheap confidence — the test was added, the copy left matching the frozen-dust precedent.
- Local react-zapper testing recipe that worked: `pnpm build` in react-zapper, register dep → `link:../react-zapper` + `pnpm install`, no vite config changes needed (existing `resolve.dedupe` for react/react-query sufficed; wagmi context resolved fine because pnpm links share the peer graph). Revert the pin before committing.
- mcap-tabs stage (RES-2101/2102): the exposure tab's Market Cap column was showing tokenized-supply mcaps for Ondo stocks (GLWon $985K vs Corning $158B) because the backend only had CoinGecko-by-address / totalSupply×price — both are the wrapper's number. Key discovery: Ondo GM API `GET /v1/assets/all/market` carries `underlyingMarket.marketCap` (real company mcap) under the same API key reserve-api already holds — probed live before designing; no new vendor. reserve-api now enriches exposure tokens with `underlyingMarketCap` (1h-cached bulk fetch, gated on the basket holding ondo assets); register reads it on exposure stock rows with NO tokenized fallback (`—` until the api deploy + its 1h CDN cache cycle), and the collateral tab gains the tokenized-mcap column for all DTFs. See [[basket-overview]].
- Scope moved twice mid-stage (user-directed): first "gate collateral mcap by a shared ONDO_DTFS list" (built, including folding locked-zap-settings into it), then "no — all DTFs, zapper keeps its own list" (reverted cleanly via git checkout of the zapper files). Lesson: when a gate list is speculative, build the ungated version first — the gate was pure deletion.
- Fastify gotcha worth remembering: the exposure route's zod response schema emits `additionalProperties: false`, so any new service-layer field is silently stripped unless added to the schema — verified empirically by the Dark reviewer, comment now lives at the schema.
- Dark & Light reviewers independently converged on the same Important (unguarded parse of an Ondo 200 body could 500 `/dtf/exposure` for every DTF: `for...of` on a non-array escapes `ondoFetch`'s catch); Dark alone caught the hot-path coupling (15s Ondo timeout taxing crypto-only DTFs during outages — fixed by gating on `ondoAssetByAddress` over the basket). Convergent Importants from independent reviewers = fix before ship, no debate.
- Devin PR #1028 assessed and superseded: built against master's older basket-overview (pre-tabs-in-header, pre-flattened exposure rows) and keyed exposure mcaps off `native.coingeckoId → native.marketCap` — nasdaq/nyse natives have NO coingeckoId and the API serves no `native.marketCap`, so it silently fell back to the tokenized group mcap (the exact bug). Group-level mcap can't work post-flattening anyway (one NASDAQ group holds N different companies); per-token `underlyingMarketCap` is the only correct shape.
- Workflow calibrated (user-directed: "every prompt takes a long time"; second pass refined tiers into two axes). Measured first: the mechanical gate is cheap (oxlint 0.4s, tsc 22s, vitest 9.2s ≈ 32s) — latency was LLM-side ceremony firing on every diff: Dark/Light pairs (product lens fires on any `.tsx`, complexity on half of src), the gate running twice (inner loop + closeout), visual checks and wiki ingest on trivial diffs, and "when debating, it's a stage" funneling everything into the heavy path. First iteration used one ladder (touch-up/quick/stage); Luis's counter-example — a bugfix with a large radius — broke it: small work, wide blast needs heavy review but not multi-phase ceremony. Final model: radius × size, four profiles (touch-up/low/medium/high), reviewers spawn at medium+, `scope.mjs` prints per-axis signals + `gate-equivalent`. Mirrored to the agent-workflow kit (skills, scripts, templates, README, +3 core tests). See [[decisions]].
- Drift fixes (same session, self-reported): (1) progress.md rows had become essays — 6 rows compressed to pointer form (full reconciliation lives in git history), and wiki-lint now caps ledger rows (`wiki.ledgerRowMaxChars`, default 700) so it can't recur silently; (2) profile misfiling guarded — downgrading below medium while scope.mjs prints a radius signal now requires one stated line (signal + why it doesn't apply); (3) profile boundaries iterate only from misfires recorded here, not theory — process work must not displace product work during the release window.
- Featured YTD stage (cross-repo): homepage featured now matches the overview default — reserve-api `discover/featured` serves `ytd` + daily grid, priceChange first-vs-last finite daily point, no live append; equal by construction to the overview's (penultimate−first)/first because both hit the same `historical_dtf_price` generate_series grid and the overview skips its synthetic now-point. Order server-driven via `FEATURED_TOKENS` (BUILDOUT, POWER, PHOTON, NEOCLOUD, ROBOTS); marquee sorted weight-desc at the client choke point (covers both the featured payload AND the SDK exposure route feeding the photon animation) plus server-side in `groupExposure`. `/dtf/daos` re-sourced from dtf-catalog (active only, Arbitrum-guarded) — the folio-manager brand table lagged new launches; dead `getVisibleFolios` deleted. Dark verified parity end-to-end; adopted from review: cached-rejected-catalog-import reset (one transient failure used to 500 discover+daos until restart), the Arbitrum guard the test already asserted, YTD label fallback. Expected homepage YTD once deployed: BUILDOUT +68.39, POWER +36.94, PHOTON +84.04, NEOCLOUD +57.02, ROBOTS −2.44 (staging 2026-07-08).
- Overview loading-shift sweep (QA): the worst shift was NOT a skeleton gap — `indexDTFBrandAtom` is written twice (SDK payload first, WITHOUT `dtf.video`; folio-manager merge second), so the cover card unmounted and remounted. Fix: `indexDTFBrandExtrasResolvedAtom` gates the cover skeleton until the authoritative read settles, and the SDK write merge-preserves video/files. Balance card now expands once via grid-rows animation, gated on balance + USD value + PnL all settled (`useWeekAgoPnl` returns `{ pnl, isResolved }`). Cover video cross-fades with its skeleton underlay. Note: the container updater changes are shared machinery — engineer eyes on `index-dtf-container.tsx` when reviewing the push.

## 2026-07-09

- Codex audited the E2E suite against the complete non-Yield SDK migration plan. Architecture approved; trust blockers and the flow-by-flow acceptance contract are now in [[e2e]]. Highest-value findings: enforce a real outbound-network allowlist, make snapshot capture atomic/per-file fresh, run against the exact paired SDK versions, match mocks on request identity, fail unknown calls in committed flows, capture/assert every submitted transaction, and add v1/v4/v5/v6 coverage or explicit product gates. Workflow scope correctly surfaced the active debug specs/logging; no kit change justified because Phase 2 is intentionally mid-debug.
- E2E phases 1–2 shipped (Fable orchestrating a 7-area Opus swarm + shared-infra agents): offline playwright suite, 38 tests green (one zap-sell flake named). Biggest lesson repeated THREE times independently: a mock layer nobody consumes through the real SDK path is untested foundation — the captured dtf.json was SDK-processed shape, sdk.index.get threw before its select, react-query swallowed the error, and every DTF surface rendered skeletons while zero-unmocked smoke stayed green. Fix: capture the RAW GetIndexDTF response + per-DTF chain-state.json (which exposed OPEN/VTF as v4.0.0 — the global version() mock was lying), plus a permanent dtf-data canary smoke. Second lesson: hydration fixes unlock UI that makes NEW reads — the vote flow was zero-unmocked until basket data rendered, then grew 8 eth_call gaps; fail-loud accounting must be re-checked after any mock enrichment. Swarm mechanics that worked: area-owned specs + orchestrator-owned shared files + per-test overrides as the escape hatch (zero merge conflicts across 7 parallel agents); honest-red reporting with staged assertions instead of faked green. Codex's SDK-migration audit adopted: items 8/9/11 landed, 1/4/6/7/10 queued as phase 2.5, 2/12 in phase 3.
- E2E trust hardening closed every follow-up audit finding: default-deny egress, strict teardown for all committed flows, identity-aware API/subgraph/full-calldata overlays, a correlated transaction/receipt ledger, atomic 43-file snapshot capture/checking, value/source assertions for compliance/auctions/charts, Node-24 pnpm CI, and current README/wiki. Full evidence: smoke 11/11, full 27/27, helper contracts 9/9, snapshot manifest 43/43, app tests 570/570.
- Strictness paid for itself immediately: turning on flow teardown exposed 17 failures that the old suite called green. The correct fix was not more zero fallbacks; captured discover/rebalance token metadata plus exact project-known stablecoin identities made the requests truthful and kept wrong-address negatives failing.
- Frozen-clock flake lesson: advancing a large interval once is still racy if the async request has not been scheduled. Under full parallel load, governance and issuance occasionally pumped before the send/query existed. The stable protocol is: pump the enabling layer, `expect.poll` an observable boundary request or `txLog`, then pump the response/receipt timer. Three-repeat issuance and the final full run stayed green.
- Snapshot lesson: atomic directory replacement is insufficient if GraphQL returns HTTP 200 with `errors`; the capture client must treat those errors as capture failures before publishing. Targeted refreshes intentionally leave the full freshness marker unchanged.
- Self-improve audit: no kit change. The only repeated friction was the managed sandbox denying local port/tsx IPC, which required the already-supported escalated verifier path; test races were product-suite defects and were fixed in the suite itself.

## 2026-07-10

- E2E trust-hardening reconciled and closed. Codex implemented all 13 audit findings on the uncommitted tree; Fable ran three independent passes — a verification agent (full 27/27 ×3, smoke 11/11, zero unmocked, payload spot-checks decode real calldata, grep audits of every strictness claim) plus Dark/Light on the whole diff. Zero blockers/majors; four doc-level one-liners adopted, four minors backlogged, two patterns accepted-as-is (documented read-only wildcard baselines; deterministic-empty non-goal branches).
- **Co-work incident worth remembering**: a coordinator resume-instruction written before Codex's pass told a subagent to `git checkout --` a "stray" workflow edit — but by execution time that file held Codex's completed CI rewrite, and the revert destroyed it (re-authored from the handoff spec). Rule: in a shared/co-worked tree, never issue a destructive git command from a stale snapshot of authorship — `git diff` the target and read any handoff doc FIRST, and prefer instructing agents to report unexpected diffs rather than discard them.
- Two review passes independently caught comments contradicting the code they sat on (provider.ts described the pre-fix echo-any-hash receipt behavior; rpc.ts claimed all registry DTFs are v5). Hardening passes that invert behavior must sweep the comments describing the old contract — a stale comment on a trust boundary actively misleads the next auditor.
- Don't hardcode derived counts in docs: the handoff and wiki both said "43 snapshot files" while the manifest enforced 46. Docs now name `snapshot-manifest.ts` as the owner instead of carrying a number.
- Coverage pass 2 (4 parallel Opus packages, adversarial mandate): 39 new tests — failure paths for every write flow, multichain governance/auctions (incl. the only v4 path), fee-proposal calldata round-trip, zap edge states, compliance surfaces. Verdict on the "try to break the app" brief: register held. Reverted receipts can't fake success (wagmi sets query status 'error' on status-0x0 receipts and both register's `useWatchTransaction` and the zapper gate on it); v4/mainnet renders correctly with chain-correct explorer hosts; restriction doesn't over-block (overview/governance stay open).
- **The historical zap "output stuck at 0" flake finally root-caused** — not server contention: the widget's balance hydration re-renders and WIPES an amount typed too early, so the quote never fires and every downstream wait times out. The failing-run screenshot (input showing "0" with balance loaded) was the smoking gun. Fixed centrally: `fillAmountAwaitQuote` in helpers/zapper retries fill+quote as one unit (a wipe just refills); all four zap-quoting specs converted; zap specs run serial-per-file with 90s budgets because quote round-trips take ~2s isolated but >20s under 77-test load. Lesson: a "timeout flake" whose screenshot shows DEFAULT state is an interaction race, not slowness — read the screenshot before touching timeouts.
- Adversarial-agent findings for human triage (tests landed green; app decisions pending): (1) M — `shouldBypassFormValidation` disables all zod form bounds on localhost/dev, so out-of-bounds fees submit locally and e2e can't cover validation (needs schema unit tests + a narrower bypass); (2) L — possible propose double-fire on rapid double-click (likely frozen-clock artifact; 30s manual check requested); (3) follow-up — proposal descriptions carry raw markdown/script to chain; XSS safety rests entirely on the detail renderer (`proposal-md-description.tsx`), worth a hostile-description render test; (4) L — `use-asset-prices-with-snapshot.ts:72` throws if current/historical price endpoints diverge on a token.
- Fixture lesson: an edge-state capture can MASK the state under test — the 1000 ETH high-impact quote was unaffordable at the mocked 100 ETH balance, so the insufficient-balance fallback suppressed the impact-acknowledgment gate the fixture existed to drive. Solved with an opt-in `overrides.ethBalance` (shared 100 ETH default untouched); the server's `insufficientFunds` field turned out to be dead code — the widget computes affordability client-side.

## 2026-07-11

- Unattended backlog + adversarial pass (3 parallel Opus agents) added 4 specs (10 tests) — v4 write-ABI coverage (mainnet/open castVote/queue/execute: clean, v4 uses OZ selectors), basket-settings propose, proposal-description XSS render, deprecated-DTF redeem-only (clean). Two REAL app bugs surfaced and verified against source, both engineer-review governance surfaces, both regression-locked with `test.fixme`'d specs that flip green on fix:
  - Proposal-description **iframe XSS**: `<MDEditor.Markdown>` + `rehype-raw` with no `rehype-sanitize` renders a raw `<iframe>` and loads its src (proven by real outbound egress caught by the suite's deny guard). Attacker-controlled on-chain description → live external frame on the proposal page. Same renderer duplicated in index AND yield governance. `<script>`/`onerror`/`javascript:` already neutralized.
  - **Phantom governance calldata**: `propose-basket-settings/updater.tsx` seeds the threshold field as a percentage but the change-detector divides the baseline by 1e18, so the two never match — every basket-settings proposal silently appends an unintended `setProposalThreshold` calldata and the empty-change guard never fires.
- Adversarial-mandate mechanics that worked again: agents forbidden to touch app code; a suspected bug becomes a `test.fixme` + `// BUG:` repro in the agent's own spec (suite stays green) and comes up for human triage. Both bugs were then re-verified by the orchestrator against the actual source before escalation — the XSS one turned out BROADER than tested (yield governance shares the renderer), the threshold one is a clean seed-vs-detector unit mismatch.
- Yield DTF suite scoped (recon agent) + capture feasibility CONFIRMED before committing to the track: yield subgraph reachable, public no-auth RPCs (`ethereum-rpc.publicnode.com`, `1rpc.io`) execute `eth_call`. Key architecture finding: yield reads everything from RPC (not the SDK like index), so the foundation is a record/replay eth_call map at a pinned block per RToken — fundamentally different from index chain-state seeding. Blueprint in [[e2e]]; build deferred behind this commit to avoid a shared-helper race with the in-flight backlog agents.

## 2026-07-12

- Yield DTF Phase F landed (capture + record/replay foundation + overview smokes). The Phase-F agent captured both fixtures (eUSD/hyUSD, 128 real eth_calls each at a pinned block) then died mid-debug; I took over the finish directly rather than respawn. Two isolation bugs it hit and I root-caused:
  - **Index regression from a yield seam** (the subtle one): the index DTF page incidentally polls a `dtf-yield` subgraph via an app updater. Baseline answered those with `EMPTY_SHAPE`; the agent's `resolveYieldQuery` failed them loud, so the app fell back to on-chain price reads (`0xe45a5b2d` ×24) that aren't mocked → index smoke red. The diff LOOKED additive (index dispatch byte-identical), which sent the bisect in circles until I noticed `0xe45a5b2d` was a _yield_ selector (26× in the yield capture) firing on an _index_ page. Fix: the yield subgraph resolver only engages while a yield test is active (`isYieldReplayActive()`), so index tests stay byte-identical to baseline. Lesson: a "provably additive" seam can still change behavior if the OTHER product's page shares the boundary — verify the untouched path RUNS green, don't just diff it.
  - **hyUSD (base) hit the mainnet facade**: yield read atoms fire on first render with `chainIdAtom`'s mainnet default before the route's chain propagates (`useRTokenContext` sets it in an effect). eUSD passed only because mainnet IS the default. Fix: `setYieldReplay(chainId)` + a chain-aware absorb rule — wrong-chain-facade / zero-address transient reads return deterministic zeros unlogged, while a real miss on the fixture's OWN chain still fails loud.
- Recovery mechanics worth remembering: a dropped `git stash` object survives in `git checkout <stash-sha> -- <files>` — used it to recover the agent's uncommitted subgraph/api/base after a bisect `git checkout` clobbered them.
- E2E audit hardening (2 independent audits converged; goal = push B+ → S with speed + agent-workflow as the levers). Landed: Stage 1 chain-scoped identity + closed the yield fail-loud hole (uncaptured yield reads no longer fall through to index wildcards / the $1 feed); Stage 2.1 `e2e:check` yield identity + collision validation; speed — per-worker snapshot parse cache + local workers capped at 5 (single Vite server doesn't scale past ~5; 3× flake-free vs an intermittent zap flake at 7); agent-ergonomics — self-explaining unmocked failures (`helpers/selectors.ts` decodes the selector name + names the helper to model it in) + a boundary map / new-test recipe / speed tiers in `e2e/CLAUDE.md`; CI + scoped-verify now run the mock-contract unit tests.
- Speed tiers established for the agent quick-loop: unit tests <1s (mock-contract logic) → one scoped spec ~3–5s → smoke ~16s → full ~78s. The floor per browser test (~4–5s) is app boot + hydration + clock pumps; the win for agents is running the NARROWEST tier, which the domain diff→test tables already route.
- Chain-identity is NOT uniform across boundaries: RPC (host-routed) and subgraph (chain-specific Goldsky URL) genuinely encode chain, so they enforce it. The reserve-api does NOT — it keys DTF endpoints on the globally-unique folio address and treats `chainId` as advisory (the app sends `/current/dtf?address=<base DTF>&chainId=1`). Enforcing chain there fails legitimate requests — verified empirically, then reverted. Match the real boundary's semantics, don't assume a uniform rule.

## 2026-07-14 → 07-22 — Hardening × SDK-integration effort (compact record)

Play-by-play lives in git (PRs #1053/#1054/#1055/#1063, SDK PR #27). Durable outcomes:

- "Migration IS the fix" validated: every SDK-read adoption surfaced a real pre-existing SDK bug (completed-rebalance read 100% broken via an array envelope; governance tie semantics; stale-PENDING outcomes; missing optimistic veto context). One owned boundary beats parallel implementations.
- Three-tier ownership rule → [[sdk]] (sdk primitives / react-sdk hook-per-read / register product hooks; register never imports `useDtfSdk`).
- Governance display states are SDK-derived bigint. Visible change: a nonzero tie shows DEFEATED (OZ strict majority).
- Auction launch: a 0/missing price hard-blocks with a visible reason (product decision, Luis 2026-07-20). Price volatility is a separate, non-blocking signal.
- Hybrid DTF is a curated designation — requires weight control but is not implied by it (Luis 2026-07-21). The weightControl derivation was reverted before shipping; the allowlist stays the source of truth.
- Deeper auctions SDK migration deferred to protocol-vNext, riding its rebalance-testing window.
- Comment discipline hard rule → `skills/code-standards.md`: one-line WHYs only, no finding/review IDs in source, agent warnings go to area-guide traps.
- Remaining queue: `docs/plans/FOLLOWUPS.md`.

## 2026-07-28

- Production chain cycling was a multi-tab feedback loop: each mounted DTF route automatically reasserted its own Ethereum/Base/BSC chain against one wallet-global network. Transaction buttons and the zapper only exposed the changing state. Automatic switching remains, but only the focused, visible document may request it; focusing another DTF tab makes that tab the new chain owner. Index and Yield DTF route contexts share the same guard.
- Review caught two smaller bounce paths before closeout: Index initially targeted the lagging global chain atom instead of its provider identity, and cached focus state was not revalidated at the wallet-mutation boundary. Both are now regression-protected; future route-chain synchronization must use the route/provider chain directly.

## 2026-08-07

- Design-system v1 began with a lazy in-app lab: a provisional progress matrix, current foundations, the complete current Button API/state baseline, a deterministic static Index composition, visual-direction placeholders, and light/dark desktop/mobile region captures. No shared token or component defaults changed.
- Review corrected an important progress-model trap: placeholder rows start with no gates complete. A current baseline can count as inventoried only when its relevant surface is actually mapped; it never counts as defined, approved, adopted, or verified.
- Capture lesson: this app scrolls inside its own layout, so one full-page locator capture produced misleading blank output. Stable evidence captures named lab regions with a pinned server and platform-neutral filenames. Chromium under the x86 dev-container emulation is unreliable, so `DESIGN_SYSTEM_BASE_URL` supports host-browser capture without weakening the default owned-server path.

## 2026-08-08

- DTF Settings confirmation was globally gated by deploy-time schema validity, so unchanged on-chain values with display-rounding drift blocked unrelated governance changes even though those values were omitted from proposal calldata. The gate now scopes errors to changed settings while preserving no-change and changed-invalid guards; the E2E regression models an unchanged invalid distribution plus a valid mandate update.

## 2026-08-11

- The lab was narrowed to its durable job: foundations, component state sheets, progress, and migration planning. Visual-direction placeholders and the simulated product composition were removed. One visual direction evolves through real local golden screens; deterministic fixtures provide repeatable data and edge states without duplicating the application inside the lab.
- The lab became a routed capability map rather than one long page. The useful distinction is “available output” versus “expected work,” not enabled versus disabled: unaudited slots stay subdued but clickable because their explanation and open decisions are themselves part of the lab. A product-shaped catalog can borrow vocabulary from shadcn/Radix without turning either library's inventory into Register's required backlog.
- Review and direct use removed the proposed hover dropdowns: category landing pages already present the catalogs more clearly, and direct links keep the navigation small-screen friendly. The same review split catalog maturity, rendered output, and per-decision completion so future slots can fill incrementally rather than switching one misleading all-purpose status.
- Source review of `codex/ui-standardization` against the current Home, Discover, and Index overview found useful intent but no mergeable system. Keep the semantic type-role model, warm surface hierarchy, and Tabs-versus-Segmented-Control distinction; re-test the exact scale, token count, variants, and dark values; leave behind wrapper sprawl, migration coverage as a design proxy, and the previous mass-migration implementation. The lab now records candidate direction separately from evidence and accepted values.
- Golden screens are provisional screen families, not one screenshot each: Home/Discover, Index overview, swap/automated mint, governance/proposals, Portfolio, and Earn. Deploy is an overlap stress route. Portfolio needs deterministic holdings states. The pinned `@reserve-protocol/react-zapper` API exposes behavior and locale props but no theme contract, so Register owns host semantic variables and local shell work; widget internals remain an upstream package boundary.
- The first focused Color audit corrected the lab's hand-picked evidence. The beige wrapper users recognize is `secondary` (110 exact product background-utility matches), while `container` has no exact product TSX match and equals the light background; names were masquerading as roles. The recent Home/Discover/Index performance palette is already shared across 10 consumers and intentionally bypasses feedback tokens, while Portfolio still maps gains/losses to success/destructive. The candidate now keeps feedback and financial movement as separate semantic roles, with visual sharing still open for human review, and renders a surface hierarchy plus role tables instead of treating rationale bullets as the candidate itself.
- Performance evidence cannot be represented by one green and one red swatch: the shared Home/Discover/Index implementation includes line-gradient endpoints, dot/fill sources, text colors, neutral, and pre-launch treatments. Its `darkSurface` variants are defined but unconsumed—the overview dark theme still uses defaults—so the lab labels availability separately from actual usage. It then proposes a smaller three-role V1 mapping—main, emphasis, foreground—with exact values, names, and feedback-ramp sharing still open for design review.

## 2026-08-13

- The missing layout layer is now explicit but small: one outer frame and three role-based templates—content plus stable support rail, balanced peer split, and focused task column. Static source inspection found real drift (1.5:1, 2:1, and fluid plus 480px) and a 408–480px workflow cluster; these remain evidence, not approved values.
- Modal width is now a real visual decision rather than an inherited number. The lab reuses the complete quote-ready Zapper and a short success state at 432/448/464px; both states always move together, and narrow screens constrain both to the same available width. The Zapper should earn the standard modal role before other dialogs inherit it.
- Accessibility was deliberately bounded to a pragmatic shared-primitive baseline: controls and overlays own mechanics once; product work supplies truthful labels, messages, and reading order. Review caught two useful contradictions before handoff—a low-contrast performance label in the reused specimen and an older plan paragraph that still called accepted foundations unresolved.
- Responsive review was justified here because layout itself was the decision. Desktop plus phone route tests and two phone region baselines now pin equal modal state widths, viewport containment, and readable stacking. The existing designer-only lab remains otherwise desktop-first.
- Route-level layout audit now precedes component-family work so legacy composition does not accidentally define component anatomy. Static source review plus local Auctions/Governance inspection produced five task-led archetypes. The clearest decisions to test are Auctions as persistent browse-and-inspect, Governance as proposals plus a contextual/selected inspection region, and automated mint as the retained compact-to-paired progressive-workflow reference. The lab keeps these schematic and records which component contracts each hypothesis blocks; no product route or shared component changed.
- Human scope decision: Auctions is the structural migration worth doing in V1—the opened auction will be recomposed for a persistent list plus wider workspace, not merely stretched. Governance stays structurally intact for the compressed release; improve it through foundations and components, while keeping the proposal-summary idea as later work. A future summary must end clearly in the full proposal route rather than masquerading as the complete view.

## 2026-08-14

- The lab now optimizes for visual review rather than project archaeology: Foundations opens on nine compact system specimens, Components renders seven registry-derived canonical implementations before one provisional data composition and a compact inventory, and representative details place output/readiness before a single evidence disclosure. Accepted foundation studies moved to their authoritative detail routes; Studies retains only open color, geometry, layout, and modal pressure tests. The typed Current Review queue is capped at three human judgments, while Project Status derives foundation/component progress and autonomous next work from the catalogs.
- The first product-facing canonical kernel now follows the strongest current Index evidence instead of manufacturing alternatives: Entity identity, Metric/MetricValue, EmptyState, and a realistic dense Index data slice all render the actual candidate components in the lab. Production consumers remain unchanged.
- Entity pressure testing exposed a composition bug rather than a design question: placing a 2px background-matching wrapper around a 16px chain mark made the badge 20px overall and broke the ratio used on strong recent screens. The background-matching edge now belongs inside the chain mark's 16px footprint.
- Dense data review found the useful reusable seam is identity plus formatted value, not a universal Row. The table/list parent still owns columns, density, dividers, navigation, and responsive behavior; prematurely extracting those would encode one composition as a shared component.
- Safe autonomous canonicalization stops before illustrated empty states, rich navigable records, Index rail refinement, or complex form groups. Those materially affect the product's visual character and should return as real visual judgments rather than quietly becoming conventional defaults. Auction metric alignment waits for the real Auctions composition rather than entering the global Metric queue.
- EmptyState review exposed a progress-model bug: its actions looked like the old system because the state sheet imported the legacy production Button even though the catalog called Button a V1 proposal. The fix is general rather than specimen-specific: the catalog now separates visual output, reusable implementation, and product adoption; Button is a real candidate; and active canonical lab compositions consume it. This prevents visual replicas from silently drifting while preserving the explicit no-migration boundary.
- Composition review gained its missing gate: a visually meaningful dependency must be canonical V1 or explicitly retained before the parent can be called canonical-review-ready. This immediately reclassifies the core board as exploration, copied modal geometry as provisional, and the dense Index slice as a Table specimen without throwing away their evidence. The real eligibility dialog now proves the narrow interaction kernel—20px binary Checkbox, compact named IconButton, 384/432px Radix-backed Dialog shell, and canonical Button—while leaving indeterminate/invalid selection, toggle/tooltips, outcomes, final elevation, constrained screens, Table/DataRow, and production migration open. Scoped verification caught one infrastructure false start (the container lab server had exited); restarting only the lab and reusing it produced a green focused interaction check.

## 2026-08-18

- Kit refresh via `install.mjs --update` from agent-workflow @ b0da65a (+working-tree). 18 new skills landed (stage, foundation-review, scar-mining, codebase-deep-scan, taste, topology, wayfinder, experience-design, create/maintain-verification, evaluate-workflow, release-evidence, resume-work, resolving-merge-conflicts, model-capabilities, pair trio); router routes them all. Medium/high stage machinery moved from `workflow.md` to `skills/stage.md`.
- Register's local area-guide drift detection (`scope.mjs` `area-guide:` lines) was upstreamed into the kit (now AGENTS.md/CLAUDE.md-aware, with a kit test) instead of being clobbered by the wholesale update; the kit had already absorbed the wiki-lint duplicate-key check and the one-line comment rules. Zero local rules lost.
- Kit installer gap found and fixed upstream: skills reference `templates/{design,evaluation,evidence,verification}` in-repo but the installer never shipped them; they are now kit-owned dirs and live at `templates/` here.
- All Overrides in [[project]] re-checked: none absorbed, all stand. Config schema unchanged. Chatty's capsule pilot is explicitly Chatty-only and was not ported; chatty itself is behind the current kit.
- Rich-record review confirmed that Governance proposals and Auction rebalance selectors can share foundations and lower-level seams without becoming one universal Row. Repeated corrections established a stricter composition contract: 24px record inset, 16px region rhythm, 8px related rows, 4px tight icon/text pairs, 16px/500 titles, equal-sized horizontal peers, and semantic layout recipes rather than locally plausible spacing.
- Auction selectors now prioritize the process loop: current auction/status, current-auction countdown, rebalance permissionless/expiry timing, historical accuracy, impact, and total traded value. Per-auction bid count and value traded stay in the selected/full view. Selector status wraps below the title at constrained widths; provenance remains one left-aligned sentence.
- Lifecycle pills proved useful across both families but remain provisional. Their semantic feedback surfaces and borders—including neutral waiting/closed roles—are opaque, parent-independent colors. Proposal progress, quorum/vote evidence, optimistic challenge states, and Fast/Contested qualifiers were reviewed in composition without promoting a universal Table/DataRow or beginning production adoption.
- The completed proposal/auction pressure test promoted Lifecycle Status—not every compact label—to a real reusable candidate under `src/components/`. A visual pass caught that metadata-only promotion would have left a contradictory “Provisional component” state sheet and lab-owned implementation; canonical status now requires both the registry and actual import boundary to agree.
- Card audit found that “Card / content region” was two jobs hidden under one inventory label. Structural regions are already consequences of accepted foundations, while the strong Home Index feature card is authoritative existing design rather than a new alternative. Its review is limited to named square-shell and 8/24/16px foundation corrections under a preservation contract for all chart/launch, ticker, transcript/video, chain, and interaction detail; Discover remains compact adaptation evidence. The rounded legacy shared Card remains untouched evidence, not a migration target.

## 2026-08-19

- Locale initialization now uses the first supported browser preference for visitors without a saved choice; a persisted explicit choice still wins. A clean Lingui extraction confirmed 2,543 active catalog messages with zero missing Spanish, Korean, or Simplified Chinese translations and removed obsolete catalog entries. This supersedes the earlier ~117-missing-message backlog entry. Live browser verification covered Spanish auto-selection plus a Korean selection surviving reload.
- The Components landing page now renders the same complete state sheets as detail routes for all 14 available components, shows readiness/authority/delivery separately, and keeps one 29-item unresolved inventory with Product Navigation explicit. Reusing full sheets exposed a Card-study intrinsic-width leak at 390px; the lab composition now owns `min-w-0`, while the protected production card remains unchanged.
- Field/TextInput is independently reviewable across six source-grounded states without inheriting SingleChoice or the blocked repeated-governance composition. ActionGroup now encodes only accepted Button width/gap relationships, but review caught a maturity error before closeout: reusable synthesis is still exploratory/provisional until a human accepts it.
- Dialog coverage was corrected from a duplicated Eligibility specimen to compact dismissible, standard non-dismissible, and long body-scroll states; interactive focus/dismissal evidence remains detail-only. The entity-mark kernel was rechecked against Index exposure, badged-card, and Earn stack sources and required no code change.
- Tooltip/Help and Select/Combobox/Menu are prepared as evidence maps, not rendered proposals. The former must resolve explanation versus action feedback and touch access; the latter must preserve the semantic split between bounded values, searchable entities, and immediate actions while sharing only proven popup presentation.
- Independent Intent and Engineering Risk review caught premature ActionGroup authority, duplicated Dialog coverage, missing overview authority badges, and two unsupported fixture claims. All confirmed findings were fixed; focused browser coverage now pins overview width/mapping/authority and Field focus plus ARIA/native states.

- The Home feature-card foundation check is accepted without redefining the source design. Review caught two misleading box-model readings: a 48px bottom-aligned title reservation made the nominal 16px logo/name gap visually much larger, and moving that reservation below the title merely displaced the defect. The corrected candidate removes hidden title whitespace, measures 16px from the full logo/chart row to the name and 8px from name to market context, and aligns both transcript and compact metric supporting rows to the 24px content axis. Discover uses the same relationships; its taller chart supplies the visually larger logo/name distance. Extraction remains deferred and production behavior is unchanged.
- Design-system work is now explicitly canonical-first: existing product evidence and accepted rules produce the strongest complete candidate before human review, while Studies is reserved for unresolved alternatives. `canonical-candidate`, designer `accepted`, and production `in-use` are separate states. Current Review contains only a nearly complete candidate at Codex's confidence boundary. Meaningful feedback is classified as local, component, pattern, foundation, or reusable heuristic and updates the most reusable justified owner immediately; unrelated consumers, docs, and baselines may synchronize in a later coherent batch.
- Applying that model exposed the contained-form review as premature rather than review-ready. Its ordinary Field candidate remains reusable, but the parent composition is blocked until its SingleChoice dependency is accepted. A follow-up fidelity audit found that Tabs had accepted visual guidance but no reusable V1 implementation or recipe, so SingleChoice had reconstructed the model locally and drifted. The corrected candidate now uses the accepted 44px peer geometry, 14px medium control label, 20px item padding, 2px track inset without an extra inter-item gap, selected elevation, full radius, neutral control chrome, native radio semantics, and an explicit implementation source. Action groups remain evidence-prepared next; no production consumer changed.
- The Tabs/SingleChoice gap was representative of a narrow maturity-model problem: rendered output and reusable code were tracked, but current design authority was still encoded indirectly through `proposal`/`accepted` output labels. The registry now records design authority independently from rendering, implementation, and adoption. Established Button, compact IconButton, binary Checkbox, Dialog, Lifecycle Status, Entity Identity, Metric, and Home-card contracts are current baselines; Input, SingleChoice, EmptyState, Tabs, and Table remain exploratory at their stated scope. The accepted contained-selection treatment is now a reusable recipe consumed by SingleChoice, while the broader Tabs family remains unresolved.
- Lab reconciliation found that removing the mixed-authority core board had also removed the only visual output for accepted Tabs and the at-a-glance cross-family check. The Components landing page now exposes all eight families in one scroll: 13 real rendered outputs plus 30 explicit unresolved records, with no invented specimens. Compact/default text-only and contained Tabs are restored from reusable visual recipes as current baselines; their complete semantic/API contract and Product navigation remain unresolved for later family work.
- Contained-selection spacing now uses the same 2px relationship between peer items as around the track. The shared recipe updates both contained Tabs sizes and SingleChoice, preventing adjacent selected and hovered pills from visually colliding without creating component-specific exceptions.
- SingleChoice is now an accepted current baseline for the evidenced one-row submitted-value job. The accepted contract includes one default 44px size, intrinsic sizing by default, equal peer growth when explicitly full width, and the shared 2px track inset/peer gap; Field/TextInput advances as the sole Current Review while richer radio compositions remain open.
- Field state review keeps read-only behavior context-neutral: the component does not add a lock icon automatically, while permission explanations remain composition-owned and summaries use information/key-value rows. The misleading empty “submission in progress” fixture is replaced by a disabled field that preserves a visible value; submission progress stays with the form/action lifecycle.
- Field/TextInput is now an accepted current baseline for ordinary short-value fields. Its stable outer label/control/help axis, accepted type hierarchy, 44px full-radius geometry, affixes, and semantic invalid/read-only/disabled states are authoritative for new work; multiline fields, repeated form composition, and adoption remain open. ActionGroup advances as the sole Current Review.
- ActionGroup is now an accepted current baseline for intrinsic horizontal and intentional equal-width vertical action sets with the accepted 8px peer gap. With Button, Field/TextInput, and SingleChoice authoritative, the existing repeated-governance-parameter composition advances as the sole Current Review; business validation and production adoption remain outside it.

## 2026-08-21

- Spinner is accepted as an unadopted current baseline with three placement roles: 14px lifecycle-status, 16px inline/control, and 24px local-region. It inherits contextual color, supports labeled standalone and context-hidden use, respects reduced motion, and now replaces the processing pill's duplicate Loader implementation. EmptyState advances as the sole Current Review item.
- EmptyState is accepted as an unadopted current baseline for quiet title-only and actionable absence. The accepted composition owns a 4px title/description pair, 384px centered description measure, optional unframed neutral icon, and canonical intrinsic compact actions; host framing, exact request channels, bespoke milestone artwork, loading/error states, and product adoption remain separate. Button pressed treatment and long-label behavior advance as the sole Current Review boundary.
- Button label-fit behavior is accepted: routine labels are concise, intrinsic, and single-line; constrained compositions shorten copy, use their own/full-width row, or stack actions before overflow. Only unavoidable localized copy may explicitly wrap to two centered lines on a default-size action. Compact labels never wrap, and Button never truncates, scrolls, or shrinks type. The overflowing lab pressure specimen is removed; only momentary pressed feedback remains in Current Review.
- Button's reusable V1 baseline is complete without claiming production adoption: filled primary and destructive actions use opaque theme-derived colors darkened 8% on hover and 16% on press, while a centered 0.98 scale supplies momentary tactile feedback without layout movement and disappears under reduced motion. Persistent toggle selection remains outside ordinary Button. Drawer advances as the sole Current Review item using the prepared task-versus-selector evidence.
- Drawer is now a reusable unadopted candidate rather than another local specimen. It preserves the existing 512px wider-screen evidence, becomes full-width and bottom-attached on phones, reuses Radix Dialog focus/dismissal behavior, and shares only the 24px shell axis plus anchored header/body/footer regions. Task lifecycle, selector composition, tabs, drag gestures, mobile navigation, and production consumers remain outside its provisional review boundary.
- Drawer body inset is composition-owned: the shell supplies the accepted 8px structural edge and scrolling, ordinary compositions add 16px to reach the 24px content axis, and dense input/output flows such as Zapper may remain edge-aligned. This avoids both forcing unsuitable padding and creating a Zapper-specific Drawer variant.
- Product direction corrected the Drawer candidate before approval: current right-side desktop drawers are generally migration evidence, not a preferred V1 pattern. Drawer is removed from Current Review and retained as exploration only. New modal tasks use the accepted centered-desktop Dialog that becomes bottom-attached on phones; Drawer returns only when a future-facing flow specifically benefits from desktop edge continuity.
- The resolved Drawer direction is stricter than conditional reuse: V1 should aim to eliminate desktop slide-outs by pressure-testing each current Drawer flow against responsive Dialog one at a time. Only a concrete flow that cannot migrate without meaningful context or usability loss can reopen Drawer as a component decision.

## 2026-08-20

- Accepted presentation recipes do not automatically authorize a synthesized compound component. The Radix-backed Tabs candidate now connects the accepted text-only and contained treatments to panels, keyboard behavior, size, and width, but its panel/list API remains the sole Current Review judgment; every newly synthesized contract in this slice has `adoptionStatus: none`.
- Human review narrowed Tabs to the contained presentation in compact/default sizes with intrinsic/full layout settings. Text-only was removed from the component API and active guidance because its strongest assumed evidence belongs to immediate Segmented Control behavior; a real quiet panel-navigation need may justify revisiting it later. Tabs is now a current baseline without production adoption, and Segmented Control advances as the sole Current Review item.
- Follow-up review caught an incomplete propagation: reclassifying the Index overview range treatment as Segmented Control semantics had removed it from Tabs without transferring the visual treatment. Segmented Control now owns the existing unframed text-only compact/default presentation alongside the contained presentation; Tabs remains contained-only. This restores accepted work under the correct owner rather than inventing or discarding a variant.
- Segmented Control is now an accepted current baseline for immediate peer-mode selection. Text-only compact/default is intrinsic in ordinary contexts, while the compact mobile DTF chart range is an evidenced full-width case. Contained compact/default retains the shared contained-selection recipe, and Textarea advances as the sole Current Review item.
- Textarea is now an accepted current baseline that extends Field with a restrained 8px radius, the shared 20px horizontal field inset, a 16px multiline vertical inset, vertical resize, and inherited field states. Switch advances as the sole Current Review item; async lifecycle and production adoption remain outside it.
- Switch is now an accepted current baseline for an immediate boolean setting: 36×20 track, 16px thumb, consistent 2px inset, and an inverse-neutral disabled pair that preserves stored state without active color, blanket opacity, borders, or elevation. The opaque `disabled-structure` role is semantic-recipe owned. Pagination advances as the sole Current Review item; async recovery and production adoption remain outside Switch.
- Pagination is now an accepted table-independent current baseline: 32px aligned peers, optional page-size selection, seven/five-item responsive windows, fixed edge actions, a non-clickable neutral current page, and bare muted unavailable quiet actions. The host owns outer inset; shared 44px mobile hit targets are intentionally deferred to the accessibility pass. DataTable defaults remain unchanged, and Copyable Value advances as the sole Current Review item.
- Segmented Control, TextArea, Switch, Pagination, Copyable Value, Skeleton, and Spinner were pressure-tested as independent provisional candidates. Copyable Value retains clipboard normalization, event isolation, and its two-second copied state without transferring feedback ownership to explanatory HelpTooltip.
- Copyable Value now reflects the product's actual address rule: shorten addresses deliberately by default, never imply CSS clipping as an accepted address treatment, and show full values only where the composition has enough room.
- Copyable Value review is explicitly limited to one inline value, its copy action, and feedback. The vague transaction specimen was replaced with a source-grounded proposal-ID case; paired explorer actions and native/bridged chain-address lists remain tracked composition work rather than implicit parts of this approval.
- Copyable Value now distinguishes prompt from outcome without changing general Tooltip styling: “Copy to clipboard” remains neutral, while the transient copied state uses the existing semantic success surface, border, foreground, and check indicator.
- Copy confirmation now transforms the already-open prompt rather than appearing as a second Tooltip: Copyable Value holds the same surface open for its two-second feedback interval, suppresses copied-state entrance replay, and transitions semantic color over the accepted 120ms duration. General Tooltip motion remains unchanged.
- Focused self-review corrected the remaining Copyable Value issues: dark-theme feedback now uses readable foreground copy with a success-colored check, success waits for the clipboard promise, a polite live status announces completion, Escape can dismiss feedback, and the redundant proposal-ID specimen was removed without discarding it as source evidence. Failure presentation remains explicitly deferred.
- Copyable Value is accepted as an unadopted current baseline, including 14px monospace machine-value typography, deliberate address shortening, the canonical micro copy action, and truthful neutral-to-success feedback. Explorer/bridged-address compositions, failure presentation, sensitive-value policy, and production adoption remain separate; Skeleton advances as the sole Current Review judgment for neutral material and pulse only.
- Skeleton is accepted as an unadopted material-and-motion baseline using the existing border neutral. Its primitive owns no geometry; refined Index overview and Home feature-card loading compositions remain authoritative, and future migration must preserve host-authored sizing, radius, repetition, and loading boundaries while auditing reliance on the legacy default radius. Spinner advances as the sole Current Review judgment.
- Popup-row transfer is content-driven rather than one fixed height: Select and Menu retain the reviewed 40px treatment, while MultiSelect needs 44px for its 20px selection mark. That evidence did not create a shared rule or a review question.
- EmptyState remains exploratory because explicit human acceptance cannot be established from prior work. Quiet and actionable absence fixtures improve the evidence without promoting the API or claiming current-baseline authority; the token picker is evidence for the actionable role, not a reusable variant name.
- EmptyState review corrected a composition-level hierarchy error rather than shrinking the global Button: actionable titles now use the existing 16px/500 structural role, the message-to-actions relationship is the accepted 16px internal-region gap, and the picker evidence uses compact text-only secondary support actions. EmptyState still permits default 44px primary recovery actions; narrow action sets stack at equal content-driven width instead of wrapping into ragged peers.
- Real EmptyState composition pressure-testing refined the shared compact Button rather than adding a local exception: text-only compact actions now use 14px horizontal inset, and icon-bearing compact actions retain the 2px optical reduction with 12px on the icon side. Compact Select/Menu, contained-selection, and icon-only geometry are unchanged.
- EmptyState action content remains caller-supplied rather than hardcoded from the token picker. A brief equal-width vertical experiment exposed the wrong pressure response and was removed: compact actions remain intrinsic, while a separate source-grounded empty-basket specimen tests one compact primary action. Its optional bare 20px icon uses primary neutral foreground in a 24px slot; circular framing remains reserved for semantic outcomes, identity, or repeated-stack alignment rather than routine neutral absence.
- EmptyState title and description are a tight text pair, so their gap is corrected from 8px to the accepted 4px relationship. The single-action pressure specimen uses the first two real basket-setup sentences—long enough to verify natural wrapping without turning the subtitle into instructional body copy or introducing invented migration copy.
- Home feature-card extraction remains Yellow preparation only and must preserve its chart, launch marker, transcript, ticker, animation, route, and chain-switching behavior behind a view/orchestration boundary.

## 2026-08-21 — Link and Accordion preparation

- Link is prepared as the sole Current Review candidate: inline, standalone, external, Router, and canonical Button-composed navigation share one unadopted contract. Light/dark review covered recognition, wrapping, external signaling, and focus; current/visited states, product navigation, tabs, breadcrumbs, analytics policy, and migration remain excluded.
- Accordion is rendered for coordinated informational FAQ/product-detail sets with retained Radix behavior, but it stays provisional because the inherited production content-height keyframe is 200ms while the accepted component-motion rule is 180ms. No production animation was changed.
- The Accordion mismatch was an unambiguous candidate implementation defect rather than a design decision: V1 now uses dedicated 180ms content-height animations while the legacy 200ms production classes remain untouched. Accordion is review-ready after Link; task accordions and independent Collapsible disclosures remain excluded.
- The nine Collapsible importers confirm a distinct one-region disclosure job rather than an Accordion variant. A source-only package classifies help adjuncts, optional auction/configuration regions, dev diagnostics, and repeated governance code disclosure without creating a candidate UI.
- The Drawer-to-Dialog source matrix covers stake, vote-lock, shared/deploy token selection, confirm deploy, package-owned Zapper, and mobile navigation. It preserves each flow's lifecycle and establishes no Drawer exception or production migration.
- Dark/Light review corrected two authority and behavior gaps before handoff: Link and Accordion are labeled prepared rather than accepted, external links merge `noopener noreferrer` into caller rel and require caller-owned accessible new-window copy, and unavailable/loading Button `asChild` navigation is untabbable and non-activatable. Button's visual defaults remain unchanged.
- Human review accepted Accordion after removing dividers, replacing filled-row hover with label/chevron feedback, correcting the shared 14px/20px typography default, and resolving its card rhythm at the composition owner. Collapsed rows stay 48px; open content uses 8px title-to-body and 20px body-to-next-title spacing, while the card host uses a 16px header-to-list relationship and 8px horizontal/bottom edge buffer.
- Accordion and Collapsible share a reusable disclosure-presentation owner instead of allowing the next candidate to reconstruct the accepted trigger, chevron, content, and motion classes. Accordion remains coordinated-set authority; Collapsible is now the sole Current Review candidate for one independently controlled region and does not inherit set semantics or hosted content anatomy.
- Collapsible live review exposed an inherited focus collision: the ordinary outer content ring crossed into disclosure body copy because the accepted 8px title/body relationship deliberately closes the trigger boundary. A reusable inset-content focus recipe now serves Accordion and Collapsible without changing their spacing, color, or other controls.
- Collapsible resting-state review separated content naming from action affordance: the trigger must clearly identify the hidden subject, while an optional caller-owned closed/open cue may clarify the reveal. The component carries no product-copy defaults after an inaccurate supported-token example demonstrated the risk of inferring capabilities from source context.
- Collapsible is accepted as the unadopted one-region disclosure baseline. Its shared Accordion presentation, truthful caller-owned subject/cue copy, muted resting affordance, primary hover/focus feedback, and mobile cue hiding are authoritative; hosted content and production adoption remain outside.
- Inline Message advances as the sole Current Review candidate after classifying eighteen Alert importers and repeated local banners. It owns persistent information/success/warning/danger anatomy, standard tone icons, 16px default and 12px compact inset, canonical compact actions, and opaque semantic feedback roles. Visual tone stays separate from announcement urgency; Toast and transaction lifecycle remain separate work.
- Human review correctly removed Inline Message from Current Review before judging it: isolated tone specimens do not establish contextual prominence, spacing, or density. The source audit, implementation, and grid remain Yellow evidence only; it returns with source-grounded content before and after the message, or waits for the owning task composition. Adjacent Toast and Progress work does not bypass this boundary.
- Typography advances as the sole Current Review foundation candidate. The review combines the exact nine-role core hierarchy and restricted 12px exception with the 300/500 weight strategy, multiline defaults, numeric and machine-value treatment, stable responsive sizing, four realistic product contexts, and explicit stress cases. Foundation targets are now first-class in the typed Current Review queue; the candidate remains exploratory and authorizes neither production migration nor routine use of Lausanne 700.
- The typography candidate now groups its recommended refinements into one visually distinct provisional section: a display-only 48px-to-40px narrow-phone exception, guidance for choosing between similar roles, Lausanne tabular financial values versus monospace identifiers, wrapping/reading-measure behavior, and a same-copy three-paragraph comparison of 16px ordinary versus 14px supporting text. The group remains unaccepted; the 14px long-copy side is a stress test, and none of this authorizes production migration or a responsive scale for ordinary application typography.
- Human review accepted Typography as the current baseline after adding the negative-performance specimen. The authority now covers the nine semantic jobs, six core sizes, restricted 12px auxiliary use, 300/500 weight split, 16px/24px ordinary and 14px/20px supporting roles, display-only phone step, Lausanne tabular financial values, monospace identifiers, natural wrapping, and reading measure. The 14px long-copy comparison remains a stress test; real product evidence may refine the baseline later, and acceptance does not authorize production migration. Current Review is empty.

## 2026-08-22

- Color closure auditing confirmed that canonical V1 owners already follow the desired theme boundary: components consume semantic aliases and do not select different color aliases with `dark:` utilities. Existing product-level `dark:` usage remains migration evidence rather than V1 authority.
- The focused contrast audit found three bounded gaps instead of a palette-wide failure: quiet feedback foregrounds, the light destructive filled action, and light supporting text on a muted surface. The review candidate preserves surfaces, borders, radii, spacing, and semantic hues while showing heavier filled and overly neutral alternatives as rejected comparisons.
- Review self-correction: deepening the destructive candidate default without owning its interaction ramp would have made hover lighter than rest. The provisional review now carries a coherent default/hover/pressed progression; no accepted Button recipe or production token changed.
- Human review accepted the restrained Color calibration as the current V1 working baseline. Feedback, supporting text, and destructive actions now inherit named semantic roles from reusable owners; components use one alias across themes, while the theme boundary owns raw light/dark values. The legacy global `muted-foreground` remains unchanged so acceptance does not silently restyle production screens; broader migration and full-screen tuning remain separate.

## 2026-08-23 — Coordinated navigation preparation

- Global application navigation and Index DTF Product navigation advance as one Current Review judgment because they appear simultaneously, while remaining separate exploratory candidates and route models. The realistic shell covers anchored global overflow, application actions, DTF identity, collapsed/expanded product rail, current/unavailable routes, and separate constrained-screen compositions.
- Current Index DTF and application-header evidence drive the candidates; legacy Yield DTF presentation is explicitly excluded as authority. Neither unreviewed candidate may become authority for the other, and no production navigation or route taxonomy changed.
- Self-review corrected the reusable boundary before handoff: navigation destinations now consume the accepted Link owner directly, including caller-owned accessible external-window copy and secure rel behavior. Popup anchoring, popup-row inset geometry, full-region row surfaces, narrow lab overflow, and stale rendered/unresolved counts were reconciled at their owners.
- Human review caught that the DTF mark used its intrinsic 24px logo box while route icons occupied a centered 40px slot, visibly shifting the desktop identity left. The Product Navigation identity owner now reserves that same 40px leading slot in collapsed and expanded states; this is a candidate correction, not production adoption or a change to the accepted ChainBadgedLogo owner.
- Follow-up composition review separated rail and menu geometry instead of transferring the 40px rail slot to mobile: Product identity and route icons stay on one fixed desktop axis through expansion, while the constrained menu uses a 24px content-led identity slot. The lab now exposes the four real building blocks before the full compositions. Global top-level routes use meaningful icon-plus-label treatment and a quiet selected surface instead of an underline; More and constrained global navigation consume the same descriptive destination-row owner without becoming one navigation system.
- Product-rail state refinement keeps the 40px interaction target and fixed icon axis but insets the visible hover/current surface by 4px. Collapsed and expanded presentations therefore share one 32px surface geometry—circle to pill—without shifting icons or shrinking the focus and click target.
- Human review rejected that inset circle-to-pill experiment because the expanded pill still produced the wrong composition. Product rail state now stays on the stable 40px icon circle in both widths; expansion reveals a current-colored label without background, avoiding both icon shift and full-row pill chrome.
- Human review requested one final layered comparison: the collapsed route remains a single 40px circle, while expansion reveals a 40px outer pill with a 36px inner icon circle. The 2px inset transfers the contained-selection relationship rather than introducing a 36px control tier, and the icon center remains fixed.
- Layered-state review exposed three implementation defects rather than new design choices: the translucent outer pill painted above the active label, a 12px structural gap became 14px from visible circle to text, and the rail component only matched content height. Foreground stacking, a 6px structural/8px optical gap, and host-height filling now belong to the Product Navigation owner.
- Global top-level navigation now keeps a 2px sibling gap between destination pills, preventing an adjacent hovered item from visually merging with the current-page surface while matching the contained-selection inset rhythm.
- The coordinated navigation review now includes a home-screen global header with no current destination, making the neutral state directly comparable to the selected route state without creating a separate navigation variant.
- Global-navigation review corrected its application-action evidence: the common desktop cluster now shows Search, theme, language, and Connect, while the conditional contact-team bell is no longer mislabeled or presented as a permanent Notifications control. The real cluster is also exposed as a reviewable building block.
- The application-control specimen now includes deterministic disconnected and connected-account states. The connected fixture shows the current chain-mark-plus-short-address model, allowing header hierarchy and width review without a live wallet connection while leaving account-menu behavior outside the candidate.
- Human review requested a grouped utility comparison: the global application controls now sit in a 40px rounded toolbar shell, use outlined 32px compact controls, and keep 4px wrapper inset on both axes plus 4px between siblings. The wrapper uses a non-layout-consuming inset ring so the visible outline does not erode the requested physical inset.
- The disconnected Connect gateway now uses the accepted primary Button tone so it stands apart from the outlined utility controls. The connected chain-and-address account control remains secondary because its role changes from acquisition CTA to identity/menu access.
- Product Navigation now converges its constrained menu with the desktop rail at the semantic style layer rather than copying rail-only geometry: both use the same selected/hover/focus/unavailable and 14px/500 label language, while mobile retains a 44px full-width row, one fully rounded state surface, and a 2px sibling gap without the collapsible rail's nested icon circle.
- Follow-up optical review kept the constrained Product menu's simpler one-surface treatment and corrected its inherited descriptive-menu spacing: 12px outer inset, a 16px icon slot, and an 8px icon-to-label gap now align the glyph and trailing chevron while matching the desktop rail's optical label relationship.
- Live axis measurement then showed that equal 12px row insets still placed route icons 4px left of the DTF identity mark. Product-menu rows now use 16px leading and 12px trailing inset, preserving the 8px label gap while aligning the icon centerline to the identity axis without adding a compensating inner circle.
- Human optical review found the resulting uncircled icon and label too close. The constrained Product row now uses the intermediate 10px optical token between them—more air than the desktop circle relationship without returning to the earlier 14px inherited menu gap.
- Foundation clarification supersedes treating that 10px gap as a token: relationship spacing remains 4/8/16/24px, while off-grid values are limited to explicit optical edge compensation and do not authorize new sibling gaps. Product Navigation spacing is now declared provisional until its mobile composition returns to the scale. A separate non-blocking follow-up will recheck existing Button icon-side and Select/Menu trigger inset exceptions for convergence without launching a broad audit now.
- Owner-level correction resolved the identity and route-axis problem without preserving the 10px experiment. Canonical `EntityIdentity` now owns an 8px mark-slot-to-copy gap. The constrained Product menu uses symmetric 12px row inset, a 24px route-icon slot, and an 8px slot-to-label gap, so its identity mark, route glyphs, identity text, and route labels align on two shared axes. The desktop rail retains its fixed 40px axis, gives identity and route rows the same 6px structural gap, and renders the lab identity at 32px inside that slot. The broader Button and Select/Menu optical-inset reconciliation remains deferred.
- Product-identity review corrected an over-transfer from asset rows: the real Index DTF rail uses a single symbol line, and Product Navigation now does the same on rail and constrained screens while retaining chain context in the badged mark and navigation label. A requested identity-triggered DTF-switcher submode is recorded as Product Navigation work; it must establish its real list source, return behavior, collapsed-rail transition, and constrained-screen equivalent before the identity becomes a clickable trigger.
- The DTF switcher is now a complete lab interaction rather than a decorative affordance. Its 40px identity trigger uses a smaller canonical badged logo, a persistent collapsed switch cue, and a far-trailing chevron when labels are visible. Activation pins the rail, replaces page routes with visually related alternative-DTF rows, and returns after selection; constrained navigation mirrors the transition. The current DTF remains in the identity trigger and is excluded from the alternatives list. The rail keeps identity fixed while its remaining height scrolls to the surface bottom; the constrained item viewport is capped at 288px. A 32px surface-colored bottom fade signals continuation, while equal end padding lets the final row clear it. A static 16-DTF set from the captured Discover snapshot proves density and overflow only. Production list sourcing, ranking, search, route preservation, analytics, and adoption remain open.
- Mobile review corrected an incomplete lab composition: global and DTF entry points now appear in separate phone specimens instead of as already-open menus. The global specimen starts from a 56px application top bar. The DTF specimen gives switching to a detached 72 × 48px badged-logo pill with an 8px inset/gap and gives page navigation to a separate 48px floating action cluster. Both open the same full-width bottom-drawer presentation at content-appropriate heights and consume the canonical Drawer header's equal 24px top/right action axis. Navigation rows and the header align content at 24px. The current review experiment uses the existing 16px/500/24px item-title role, a compact secondary close control, and a 24px trailing-chevron slot matching the leading icon wrapper. Drawer row shapes remain at an 8px outer inset, use 16px horizontal/12px vertical padding, show a visible default outline, and separate 48px rows by 4px while leaving popup rows unchanged. The active row reuses the existing `primary/30` selected-pill outline so its edge sits between the pale selected surface and full primary foreground. Drawer identity and supplementary separators are removed in favor of spacing and outlined-row grouping. The mobile review fixture temporarily presents Auctions as an ordinary enabled destination so the row system can be judged without a disabled-state interruption. Their states remain mutually exclusive. The current production logo-to-overview link remains evidence only—no production behavior changed.
- The DTF page drawer now treats its token contract as actionable identity rather than quiet footer metadata. The first attempt wrongly expressed importance by adding a separate secondary-colored, 8px-radius card; that violated the drawer's established atomic-row shape and surface rhythm. The next pass led with the chain logo/name, incorrectly implying that the copied address belonged to the chain. The corrected non-navigation row consumes the exact shared drawer-row geometry and leads with the canonical badged DTF mark plus “DTF on Chain” copy before the shortened address and accepted copy action. Snapshot-derived fixtures retain full valid contract addresses internally, while clipboard output remains complete.
- Inspecting that real row exposed drift in the canonical `ChainBadgedLogo` owner rather than a drawer-specific issue: md reused the 10px sm badge, leaving only 6px of visible chain artwork after its 2px separator, and `rounded-md` collapsed to an effectively circular mark. Human review established the balanced md geometry at 12px with a 1px separator, leaving 10px of visible artwork without competing with its 20px token mark. Floating badges use the established 4px rounded-square role; 2px separators remain reserved for overlapping logo stacks. No drawer-only logo geometry was added.
- Multi-chain review established an adaptive token-address composition. Single-chain drawers retain the self-identifying badged “DTF on Chain” row without a heading. Multi-chain drawers add one wrapping “{ticker} token addresses” label and switch to chain-led rows with muted Native/Bridged roles; repeating the ticker per row would obscure the only meaningful distinction and reduce room for addresses. The chain artwork uses the same 16px scale as page-navigation glyphs, centered inside their shared 24px leading slot rather than filling it with a 20px mark. Real CMC20 BNB Chain and Base contracts exercise the two-row state, and each copy action retains its own full address.
- The page drawer's 8px shell inset already establishes the final row-to-edge distance. Removing the supplementary region's duplicate 8px bottom padding changes the previous 16px bottom gap to the intended 8px, matching the horizontal row-shape inset at the shared Product Navigation drawer owner rather than compensating inside the address composition.
- Without dividers, the token-address group needs section spacing rather than ordinary row spacing. The shared drawer supplementary region now begins 16px after the route list, while its title-to-first-row relationship remains 8px and the final bottom inset remains 8px.
- Accumulated drawer spacing previously placed header controls 24px from the top edge but the first row only 16px below those controls. Drawer Product Navigation now uses 16px list-top padding after the header's existing 8px bottom padding, making both visible gaps 24px while popup-menu list spacing remains 8px.
- Mobile DTF-trigger review rejected whole-pill subtle-hover recoloring: it flattened the floating identity surface and made the interaction feel unrelated to the switch cue. The trigger now keeps its surface stable and pairs an unbadged 32px DTF token logo with a separate 32px ghost switch cue at the established 2px contained-control gap. The pill retains the adjacent floating action cluster's 8px outer inset without inventing a new size tier. Hover/focus/open feedback belongs to the ghost cue rather than the entire floating shell, while the single parent button retains truthful click semantics and the accepted centered press transform; chain context remains in the drawer where it disambiguates addresses and destinations.
- DTF-switcher copy now says “Switch DTF” instead of “Choose a DTF.” The user already owns a current context, so the action-led label describes the actual transition and stays consistent across the mobile drawer and desktop switcher navigation.
- Collapsed desktop DTF-switcher review removed two competing micro-signals: neither the chain badge nor the floating up/down cue appears before expansion. A plain 24px token logo now sits inside the real outlined 40px trigger, making clickability legible without layering marks over the identity. Expansion restores the badged logo, label, and far-edge chevron where that context has room.
- Clarification: the desktop identity outline belongs to its full clickable region, not the fixed logo slot. It therefore changes from a 40px circle when collapsed to the complete logo-label-chevron pill when expanded, truthfully exposing the interaction boundary in both states.
- Desktop DTF-list scrolling no longer clips 16px before the identity divider. The same resting gap now belongs to scroll-content top padding, so rows move through it and disappear at the divider while the unscrolled composition keeps its established spacing.
- Desktop rail dismissal now treats DTF-switcher mode as part of the expanded transient navigation state. Moving the pointer outside the rail—or moving keyboard focus outside it—both collapses the rail and restores the current DTF's default page routes, matching the ordinary page-navigation behavior instead of leaving alternatives pinned open.
- Closed mobile global-header review replaced the earlier schematic Account/Menu row with a reusable 56px shell and canonical compact 32px utility, account/connect, and navigation controls. Human review rejected the initial 44px pass as an over-transfer of ordinary mobile form/action geometry into dense persistent navigation chrome. Disconnected, condensed connected-account, transparent, and 360px pressure fixtures expose the actual review boundary without changing the production header or opened global navigation.
- Compacting the header exposed a second transfer error inside the grouped utility trigger: three default 16px glyphs became a cramped visual block. The trigger now uses the existing 14px micro glyph scale and the standard 4px relationship gap, while its 32px control geometry and 8px side inset remain unchanged.
- Human review then exposed the larger classification error: treating Search, Theme, and Language as homogeneous Menu rows discarded the accepted owners and made the panel worse than the live reference. The first corrected composite utility surface preserved the live section anatomy with a truthful SearchField-family launcher, contained Segmented Control, and Select. Search closes the panel before opening the command dialog; theme and language update in place. The legacy duplicate Search row and raw shadow are not transferred.
- Utility-panel scale review aligned the default contained Segmented Control and default Select to the SearchField launcher's 44px height. This does not enlarge the persistent header controls, which remain 32px. A trigger-relative Popover could satisfy viewport geometry only at a real narrow breakpoint and rendered falsely in the embedded phone specimen, so the mobile header now owns the disclosure geometry: it begins exactly at the 56px boundary and spans the header width in both product and lab contexts.
- Human review rejected the remaining popup-inside-a-popup language interaction. Because the supported set is fixed at four values, the 44px language summary now expands canonical selection rows inside the existing utility surface; the panel grows downward, selection collapses the list, and dismissing the outer surface resets the disclosure. This is local utility-panel composition, not a new Select variant or a change to the accepted popup recipe.
- The first inline-language implementation incorrectly mapped “contained choices” to the structural `secondary` surface, producing an unjustified beige block inside the white utility panel. Human feedback corrected both the component and the heuristic: inline disclosure content inherits its owning surface unless it establishes a genuinely separate structural region. Geometry or behavioral verification does not substitute for inspecting that hierarchy in the rendered composition.
- Follow-up theme review exposed a second ownership mismatch hidden by the light palette: the attached utility panel still used `popover` while its header owner used `card`; both happened to resolve to white in light mode but diverged in dark mode. The panel now shares the header's `card` surface in both themes. Language expansion is one connected outlined disclosure that lists only alternatives, avoiding both the duplicated current language and the previous loose-row composition.
- Connected-header review reversed an unnecessary information loss in the mobile candidate. The current production account pattern and the desktop candidate both preserve chain mark plus shortened address, and measured lab compositions show that account identity still fits at the 320px lower bound when the brand compresses first. The header now uses its own inline-size container: 390px retains the full Reserve wordmark with a 7px measured control gap, while 360px and 320px use the existing square mark and preserve the same canonical compact account, utility, and menu controls. Icon-only account is not a normal mobile variant.
- Connected-account optical review converged desktop and constrained headers on the existing 14px micro chain mark and 4px relationship gap. The constrained fixture shortens only its visible address two characters earlier (`0x71…2A6C`); its accessible identity and the roomier desktop display retain `0x71F9…2A6C`. No 6px gap or account-only icon size was introduced.
- Global destination review removed the inherited descriptive-menu model. Mobile global navigation now shares the outlined 48px drawer-row recipe, icon/label/trailing axes, and 4px sibling rhythm with mobile DTF page navigation; its group headings provide context without repetitive subtitles. Desktop More consumes the same outlined single-line destination owner at the established 40px popup density, and its former subtitle-driven 384px width contracts to 256px. Both fixtures now cover the real inventory—four primary destinations, DTF Explorer, Bridge, Create Yield DTF, and five external resources—instead of the earlier three-item placeholder. Internal destinations use a chevron; external destinations replace it with the external-link indicator.

## 2026-08-25 — Navigation acceptance

- Human review accepted Global and Index DTF Product navigation as separate current baselines after coordinated desktop and mobile judgment. Current Review is empty and production adoption remains `none`; route taxonomy, live inventory and event sourcing, account-menu behavior, analytics, and navigation-specific geometry promotion remain later integration work.
- Initial sequencing favored Home/Discover because its feature-card extraction was the smallest ready screen prerequisite. User priority clarified that transaction standardization should advance sooner, so Home returns to the ready backlog rather than blocking transaction work. The corrected next stage is a cross-flow transaction-system map that reuses the existing Zapper, modal, Drawer-to-Dialog, and component audits before choosing visual candidates.
- Transaction-family consistency belongs at the grammar layer rather than in one universal flow shell. The audit must classify duration separately from execution visibility: instant work, opaque asynchronous waiting such as package-owned Zapper execution, transparent named stages such as automated mint, and delayed/later-claimable settlement. Shared rhythm, state language, actions, details, recovery, and outcome hierarchy may transfer; progress detail and animation transfer only when the underlying process truth supports them.
- Index DTF Zapper and automated mint are the primary transaction-audit anchors because of current product importance and recent functional/design investment, not because their existing presentation is accepted. Each flow step must compare available source truth, current communication, user decision/recovery needs, and missing information. Success and other terminal states are reconstructed from the real outcome—including useful amounts, assets, position or permission changes, transaction identity, remaining work, and next actions—rather than copied from today's UI.
- The transaction audit is now expressed as one deterministic action-to-outcome truth-spectrum board across atomic, RFQ/order, transparent staged, and delayed settlement. Current Review asks for one shared presentation judgment while preserving evidence-specific words, transaction-versus-order identity, scoped recovery, and the distinction between a confirmed initiation and a complete goal. The board is exploratory and unadopted; Inline Message remains provisional, and no universal controller, flow orchestration, package restyling, exact result sourcing, or production behavior is implied.
- Human review exposed that isolated lifecycle anatomy was insufficient for visual judgment. The board now renders each in-flight, outcome, and recovery/follow-up branch as a separate coherent snapshot inside a representative dialog, package-owned Zapper composition, or durable account card, retaining the surrounding amount, asset, quote/plan, metadata, and action context. These bounded fixtures make hierarchy reviewable without turning full screens, responsive shell behavior, or package internals into authority.
- A second human review clarified that static summary rows around lifecycle specimens were still too isolated. Each family now reads as a compact flow slice beginning with a credible review/action composition—amount or position, balance, expected output, asset identity, estimate, material details, and initiating action—before execution, consequential result, and its evidenced recovery/follow-up. This exposed real coupling rather than hiding it: Amount anatomy depends on output and action context, consequential outcomes depend on reliable result sources, and delayed settlement must cross from an initiation Dialog to a durable account surface.
- Expanded scope makes the complete transaction audit the transaction-system inventory rather than context for one lifecycle board. The lab now treats Amount, local selection, approval requirements, detail hierarchy, lifecycle presentation, identity, recovery, durable state, and outcomes as intentional work inside four realistic family compositions; each visible seam declares accepted, retained, proposed, flow-owned, upstream-owned, or deferred authority. Composition-first review does not collapse ownership: package Zapper remains upstream, automated orders and queue indices remain flow-owned, and P0 Confirm Deploy truth remains deferred.
- A bounded evidence-routing correction replaces the earlier behavior-only characterization of the Zapper references. The faithful lab study and real package host are now named, directly linked strong visual-composition evidence without becoming canonical authority. Reassessment restores their paired input/output relationship, prominent financial and asset hierarchy, balance/Max context, compact task framing, and dominant action, while retaining the newer board's final-versus-estimated truth, order-versus-transaction identity, scoped recovery, and delayed-settlement semantics. Independent review then corrected contradictory approval/order counts, removed an invented manual-mint output picker, marked deterministic amount specimens read-only so they do not imply live financial calculation, and stopped the local selector from claiming an unimplemented listbox contract. A visible transfer contract requires stronger evidence or a named product constraint before a successful predecessor quality is intentionally removed; no accepted foundation, canonical component, package internal, or production behavior changed.

- Composition synthesis corrected the remaining “valid parts assembled as admin UI” failure. Review states now lead with financial intent, while execution, recovery, and outcomes lead when they become the current user job without discarding the relevant amounts, requirements, orders, or durable queue. Amount pairs restore the predecessor's input emphasis, explicit direction, and footer relationship for fiat plus balance/Max; accepted Metric owns ordinary financial facts; requirement values align for comparison; lifecycle steps use a quiet connected sequence; durable queues use open rows; and outcome values replace nested generic success cards as the visual focus. The predecessor contract now explicitly marks qualities preserved, improved, or intentionally not transferred. Transaction candidates remain provisional and unadopted; accepted foundations and components are unchanged.
- Direct implementation reconciliation corrected the audit-first board before further visual review. Manual issuance again owns an editable amount, one approval-to-mint action slot, persistent per-asset requirements, and a separate Zapper escape hatch; automated mint again progresses from a narrow configure step into its wider flow-owned order workspace and dedicated result; delayed unstake again crosses from its input page through a confirmation modal into a page-owned durable queue; and installed Zapper remains one package-owned widget rather than a locally invented RFQ shell. The lab now visibly classifies preserve, standardize, consolidate, deliberate improvement, and do-not-touch decisions. Shared transaction language is limited to evidenced semantic jobs; orchestration, package economics, order execution, queue ownership, and shell transitions remain flow-owned.
- Final source review preserved delayed unstake as stRSR input with a rate-derived RSR output, separated pre-submission wallet confirmation from submitted state, kept cancellation destructive, and made unmodeled alternate branches visibly non-interactive. These are transaction-truth corrections, not new shared component variants.
- Transaction correction review found that the remaining quality failures shared a process cause: audit requirements had become UI copy, optional Dialog slots had been filled as a template, ordinary 24px content geometry had been applied to dense amount tasks, and a generic outcome component had been extracted before independent evidence converged. The board now uses concise product-intent actions, an 8px dense-task edge, fixed automated input identity, coherent staged containment, state-first recovery, a queue-first delayed outcome, and independent family outcomes. A duplicated per-row/manual approval action and stale Max/Use fixture values were also removed. The deleted generic outcome/action-status candidates are evidence that reuse must follow convergence, not create it.

## 2026-08-27 — Design-system context routing

- The active V1 plan and design-system domain page are now concise current routers; their former corpora remain explicitly historical/on-demand. Typed component groups use real foundation IDs, representative catalog entries distinguish authority/decision/implementation/visual/product/legacy source roles, and Register's known conflicts with generic surface, numeric-type, verification, and UI-parity defaults are explicit project overrides. No design authority, transaction UI, production consumer, Skill, or workflow changed.
- The Transaction System review now includes one source-grounded Vote Lock / Unlock candidate spanning quote entry, conditional approval, deposit/redeem lifecycle, cooldown, claimability, and final Portfolio withdrawal. A regular task dialog keeps the required Lock acknowledgement and concise Unlock consequence beside their affected actions; no new mandatory introduction was justified. Delegate remains an unchanged sibling outside the focused V1, and no production or shared-default behavior changed. The typed transaction route was expanded because the cleaned-up route surfaced the drawer and audit but not the quote hook, submit implementations, or governance entry needed to reconstruct the real flow.
- Vote Lock / Unlock composition feedback corrected evidence-application mistakes rather than adding new product behavior: compact mode tabs now occupy the modal header, pre-submit amount states use the existing reversible input/output control, financial and acknowledgement regions use established separators, and simple confirmation remains action-owned. Lock completion and unlock initiation now consume the same lab-level outcome status owner as corrected Zapper while preserving Vote Lock amounts, transaction identity, unlock-delay copy, and the later Portfolio queue. The exact future countdown treatment remains a narrow outcome-composition judgment, not a new shared component decision.
- Vote Lock header correction exposed two separate issues. The real authority mistake was consuming ordinary `DialogHeader` and then fighting its 8px surface + 16px internal = 24px axis; the stronger 17:48:23 correction gave Vote Lock an explicit local compact transaction-header owner matching the evidenced Zapper 8px + 8px = 16px composition without promoting a universal recipe. The implementation was not repeatedly failing to apply the visual fix: the intended 16px total inset was visible by 17:03:59, the worker reloaded its controlled browser at 17:05:22, and that client measured Vote Lock and Zapper at the same 16px axis at 17:06:34. Some later debugging was unnecessary, including an incorrect focus-ring diagnosis. The contradictory 18:14 screenshot came from a different human review client that had missed at least 70 minutes of updates and still rendered the old generic-header geometry. The reusable verification lesson is therefore to establish that human and agent are inspecting the same current mounted client/build/state before treating visual disagreement as an implementation failure.
- Transaction-composition hardening now gives the evidenced Zapper and Vote Lock candidates one provisional relationship recipe for 432px substantial-task width, the compact 16px header axis, submitted-content boundaries, parent-owned fact dividers, and action-footer spacing. Vote Lock also restores the same no-height-change inline Max action used by the compact Zapper amount row. This is intentionally not a universal transaction shell or accepted component: the active plan records every current pressure against lower-level baselines (`TX-P01`–`TX-P06`) so later visual review can promote a foundation/component/recipe, keep a justified composition-local rule, or revert an experiment instead of silently allowing candidate drift to become authority.
- A focused four-state Zapper / Vote Lock comparison now pressure-tests editable, submitted, immediate-result, and delayed-initiation compositions at the same width. It corrected three conformance issues without expanding authority: Vote Lock mode controls now lock after submission, empty durable sections no longer contribute spacing, and completed Lock uses the same finalized `Received` role as immediate Zapper outcomes. Unlock initiation intentionally remains different; the side-by-side review exposes that its current value label still needs human-owned product language rather than allowing the lab to invent receipt semantics.
- Vote Lock review now keeps the required 14-day acknowledgement to one row by composing the existing Checkbox and HelpTooltip while preserving the full consequence copy in the helper. Immediate Vote Lock and atomic Zapper outcomes omit the raw hash/copy cluster and instead pair secondary exact-record explorer navigation with a primary Done action in the footer. The earlier `ExternalReferenceValue` experiment and `TX-P08` pressure were removed rather than allowed to become authority; CoW keeps its evidenced order text link until a real order identity supports the same exact-record treatment.

## 2026-08-28 — Transaction alignment checkpoint

- Human review considers the Zapper/Vote Lock alignment solid enough to proceed while retaining further visual feedback for both flows. The checkpoint keeps their shared substantial-task geometry, paired lifecycle comparisons, and active pressure register provisional and unadopted; it does not accept the exploratory wallet action, outcome treatments, or a universal transaction shell. The Vote Lock plan now reflects the rendered candidate instead of its superseded static pressure-test state. Delegation remains a separate unreviewed sibling: its automatic self-delegation relationship, explicit delegate action, one/two-call execution, and partial-success behavior must be audited directly before any lab proposal.
- Direct product inspection now establishes Delegation as a provisional third-mode transaction composition rather than an inferred sibling. The lab preserves the existing Normal/Fast address model, optimistic-governance distinction, vote-lock eligibility, current locked amount, automatic first-lock self-delegation boundary, normal-then-fast call ordering, exact loading/action copy, and partial success when Normal completes before Fast fails. It standardizes only the visual shell, canonical fields, stable action placement, lifecycle vocabulary, and in-context outcome; it does not force Delegation into amount-pair anatomy, change production behavior, settle the long-term third-tab information architecture, or invent a combined transaction identity for two independent calls.

## 2026-08-31 — Square transaction amount geometry

- Human review accepted 0px radius for the transaction amount input/output family after a bounded lab comparison. The shared unadopted `TransactionAmountObject` now owns that geometry across editable, read-only, submitted, and outcome use, and the Zapper quote-search animation preserves it as an output replacement rather than reintroducing rounded corners. The radius authority now classifies these as structural financial regions; ordinary Delegation Fields stay fully rounded, multiline and floating contained objects stay 8px, and nested selectors/status controls keep their own radii. No production transaction flow was migrated.

## 2026-08-31 — Delegation multi-transaction outcomes

- The lab preserves one overall `Delegation updated` result while rendering only the relationships that changed and leading with the delegated voting power. Each intended Delegation call owns one directly associated explorer link. A one-call result uses the established secondary explorer + primary Done footer; a two-call result keeps both records in the ordinary fact list and leaves Done as the sole footer action. Approval stays outside the result, and the existing Fast-failed state continues to own partial-success recovery. Production still refreshes and toasts without retaining receipt identities for a persistent result, so adoption requires that separate product integration work.

## 2026-08-31 — Intrinsic status-pill sizing

- Lifecycle Status now owns intrinsic width as part of its canonical default, so direct grid or flex placement cannot stretch a short status across the available row. The transaction audit's separate authority label received the same local correction; no new component, sizing variant, or parent-specific workaround was introduced.

## 2026-08-31 — Integrated Copyable Value treatment

- Copyable Value now exposes an opt-in integrated 20px action for dense aligned facts while preserving its existing separated micro-control default. The shared treatment keeps the 14px shortened value, 14px copy/check mark, 8px internal gap, full-value clipboard behavior, whole-action interaction feedback, and stable success geometry. Delegation outcomes consume it without making Copyable Value own the surrounding role/transaction layout; production remains unadopted.

## 2026-09-01 — Transaction inline-message qualification

- Compact transaction qualifiers now use an opt-in 44px fully rounded Inline Message summary: tone icon, title, and a trailing row-level HelpTooltip share one centered axis with 16px horizontal and 8px vertical inset. High price impact replaces the redundant warning icon with the required Checkbox labeled `I understand the 5.8% price impact`, while temporary Zapper availability and expired-order recovery retain their tone icon. Their titles communicate the immediate state and their explanations are supporting rather than consequential. Material refund, wallet, partial-success, and later-action instructions remain fully visible. The default full-message anatomy was corrected separately so only the title wraps around its icon and the description resumes the full content width below it; this avoids an empty icon column without making the compact transaction treatment the universal default.

## 2026-09-01 — Transaction hardening disposition

- The active transaction plan now distinguishes accepted canonical components and amount-region geometry, provisional transaction relationship geometry, supported lifecycle distinctions, flow-owned composition, and exploratory visual pressure. Repeated lab use no longer implies promotion. The unresolved attached-region, stepper/action, outcome-frame, address-width, compact-message, and dismiss-action choices are routed to a human resolution queue; no shared token, Dialog default, rendered transaction UI, product behavior, or copy changed. Unused speculative fact-list geometry exports were removed rather than retained as an implied future contract.

## 2026-09-01 — Approved transaction hardening implementation

- Human review resolved the hardening queue without introducing a universal transaction shell. The new opaque `substrate-subtle` role supplies shallow attached depth while beige `secondary` retains major structural separation. Progress and pre-submit advisories share only frame, surface, and depth geometry; their semantic/content owners remain separate. Ordered progress now replaces inactive action chrome, while real Buttons remain for actionable steps and recovery.
- Zapper and Vote Lock outcomes now share a 26rem no-shrink minimum. Organic brand motion remains immediate-result-only; delayed initiation retains the stable branded shell with countdown/pending truth. Inline Message advances to an accepted unadopted full/compact-summary baseline with a strict supporting-only tooltip rule. Vote Lock's 448px address width and the bare advisory dismiss remain local. No production flow, product copy, generic Dialog default, or existing `secondary` behavior changed.

## 2026-09-01 — Earn Index DTF FAQ expansion

- PR #1094 expands the FAQ from three to nine validated questions, adds token-built graphics, and routes the Index DTF rate-help action into the relevant controlled FAQ item without opening the row drawer. The branch gate was green after rebase: lint, typecheck, 895 unit tests, 58 smoke tests, and zero missing active-catalog translations; desktop/mobile, light/dark, and Spanish states were inspected.
- Review corrections included flexible localized flow chips, answer heading semantics, card-contained realized-value treatment, and delay-accurate unlock copy. Korean and Chinese retain their active-catalog Vote Lock terms. The English catalog must ship with es/ko/zh. The default-open first FAQ item does not emit its open event, so `faq_what_is_vote_locking` analytics remains an acknowledged under-count; the rate-help path emits `rate_help` and uses a one-shot request atom plus controlled open/scroll behavior.

## 2026-09-02 — Current-master transaction evidence reconciliation

- Merged current `origin/master` into the Design System V1 branch after a pre-merge checkpoint. The only textual conflicts were project knowledge: the resolution retained both upstream workflow improvements and the narrower V1 authority overrides, including transaction-specific geometry, typography, verification cadence, and explicit permission for scoped visual-composition work. Generic upstream design guidance does not replace the active V1 plan or accepted owners.
- The installed Zapper boundary advanced from 2.8.0 to 2.10.5. Current evidence now records one mounted widget across wallet state, hidden-by-default optional tabs with amount-pair direction flipping, parallel provider comparison and a stable route list, background refresh without CTA flicker, sourcing/retry rather than a terminal ordinary no-route error, and a portalled exact-result dialog with contact/call attachments. The reviewed lab tabs and availability-failure specimen remain visible only as explicit proposals/special recovery, not current-product parity claims.
- No transaction composition, production transaction behavior, accepted component default, or product copy was silently changed by this reconciliation. Vote Lock and Stake/Unstake mechanics were not materially changed upstream; later visual work must use the refreshed Zapper facts while retaining the direct-flow and semantic-specificity rules already established.
- The first full gate observed one late `TokenLogo` state update after all assertions had passed and the test DOM was already torn down. It did not reproduce in a direct full unit rerun or the final full gate, both of which passed 1,037/1,037; it remains a transient verification observation rather than evidence of a product or merge failure.
- The current Zapper 2.10.5 evidence is now reviewable as a separate additive lab group: pre-quote input, prolonged sourcing, quote-received details with parallel route comparison, and the package-owned result with update/intro attachments. Existing normal and proposal states remain intact and the reviewed `Review` state remains the default. This prevents a temporary production default from erasing still-valid transaction-system coverage while making the actual current sequence available for comparison.
- Submitted Zapper and Vote Lock specimens now pressure-test a composition-local 32px committed-mode header capsule (`TX-P17`): editable tabs become the selected operation identity when the mode can no longer change, then the same slot becomes the existing outcome status. A 16px logo identifies the asset being acted on and rotates once every 12 seconds only during active work; ready-action, partial-failure, and reduced-motion states keep it static. Action and stepper regions still own lifecycle progress. Zapper Buy/Sell uses the traded DTF, Vote-lock uses RSR, and Unlock/Delegate uses vlRSR. This remains exploratory rather than a shared component decision.
- Zapper advisory actions now distinguish transaction consequence using existing Button behavior: over-capacity and route-unavailable states block submission, market-hours degradation remains executable through a secondary `Buy anyway` / `Sell anyway` action, and high price impact retains its acknowledgement-gated primary action. The capacity specimen now uses a truthful $250,000 order against a $200,000 cap and a sufficient $280,000 balance. No warning Button tone or production behavior was introduced.
- A transaction-copy audit now pressure-tests one neutral 500-weight inline **decision anchor** inside selected longer supporting messages while preserving every word of product copy. Eligible anchors state a binding limit, recovery timing/next action, automatic safety outcome, applicability qualifier, or concrete benefit of an alternate path; causes, marketing filler, and meaning already owned by a title or CTA remain supporting 300. Brand blue and feedback colors are not reused for prose emphasis. This is recorded as `TX-P18` for lab review, not a global typography or component default.

## 2026-09-02 — Transaction transfer hardening

- The active V1 plan now gives a fresh worker one explicit transaction transfer contract: establish direct product truth first, then consume shared geometry, amount, fact, action/progress, and outcome relationships only where their semantic jobs match. The plan separately names what transfers by default, what remains conditional or flow-owned, and which Zapper, Vote Lock, Delegation, attachment, motion, and stepper treatments must not propagate by appearance. The stale retired `TX-P11` reference was removed, active stepper pressure was restored as `TX-P10`, and Stake/Unstake is now the selected third-family proof run rather than presumed confirmation.
- The shared 26rem no-shrink outcome minimum moved from a lab-local file into the existing transaction relationship recipe. A new routed browser regression then found a real 24px mobile-only Review-to-Quote-search contraction: the populated Zapper output footer wrapped to two lines at 390px while its loading state reserved one. `TransactionAmountObject` now exposes an opt-in supporting-row class hook, and the Zapper reserves that two-line footer only below the desktop breakpoint. The exact port-3005 surface is stable on both desktop and mobile; product copy, transaction mechanics, generic Dialog defaults, and production consumers remain unchanged.

## 2026-09-02 — Transaction lifecycle polish

- Vote Lock lifecycle polish removes the unlock-delay acknowledgement once submission starts, keeps an 8px relationship from its canonical 28px control target to an actionable button, and restores the submitted amount boundary for ready/wallet states that had been omitted. The two unavailable Delegation notices remain canonical compact `InlineMessage` instances but opt into 16px horizontal and 12px vertical inset so their text returns to the transaction composition's 24px axis. The shared lab-only outcome entrance now owns a bottom transform origin, making the 360ms color surface rise from the former action edge instead of expanding from its center across Zapper, Vote Lock, Unlock, and Delegation outcomes. No product copy, mechanics, shared component default, or production consumer changed.
- Zapper's lab-local track-token action now uses one stable bookmark silhouette: outlined before the action and filled after it. The explicit tooltip/accessibility label and canonical compact secondary `IconButton` remain unchanged; this follows the existing production bookmark metaphor without promoting a shared icon rule.

## 2026-09-02 — Stake and Unstake transaction proof

- The third-family proof now combines direct evidence from the legacy page-entry → confirmation-modal flow and the current Earn drawer in one complete lab lifecycle. The structured Stake/Unstake task is exported independently of its contained-modal host, so inline entry and modal continuation can reuse the same amount, fact, acknowledgement, action, progress, and outcome composition without inheriting overlay behavior. Vote Lock now consumes the same lab-only modal host; its transaction header and task anatomy remain separately owned.
- Stake preserves optional approval and the existing compact `Step 1 of 2` disclosure before revealing ordered progress. Delegation is absent from Stake review and outcome because that transaction does not change it; viewing or changing the assignment belongs to a separate third mode backed by the existing one-role, one-transaction delegate implementation. Its current and editable states use the established Vote Lock delegation anatomy at one-role scope: role title and source-backed explanation, current assignment or input, then voting-power context. Vote Lock's Normal/Fast roles and two-transaction stepper do not transfer. Production's combined `stakeAndDelegate` shortcut remains an explicit engineer-review migration decision rather than being silently removed by the lab proposal.
- Unstake remains one transaction and now describes the result as availability after a delay, uses the existing `Begin unstaking process` action language, and ends in delayed initiation with the 14-day wait, immediate reward stop, and explicit later-withdrawal action. Redundant success prose was removed from both Stake-family outcomes; the structured result facts carry the durable meaning while approval receipts and persistent cooldown/withdrawal management remain outside the outcome.
- Vote Unlock review now gives the 14-day delay and immediate reward end separate facts, matching Unstake's clearer separation of duration from consequence without otherwise merging the two product flows.
- Mounted review found that the shared 26rem result minimum was insufficient for Stake's taller approval-progress predecessor. `TX-P19` records local no-shrink floors instead of silently raising the shared default: Stake measures 568px processing → 568px result, while Unstake measures 473px → 476px. Human review still owns whether later production should measure the preceding host, accept a narrow recipe input, or retain flow-owned floors.
- Follow-up Vote Unlock review corrected the acknowledgement-to-action relationship recorded earlier in the day: 8px below the 28px checkbox control looked cramped against the 16px space above it. The row now owns symmetric 16px vertical inset while the action footer remains at zero top padding; submitted states still remove the acknowledgement entirely. This is a local composition correction, not a new global checkbox/action rule.
- Pre-review normalization applied the same established acknowledgement relationship to Stake, removed the acknowledgement from submitted states while retaining it at the actionable second-transaction checkpoint, aligned compact fact rows with Vote Lock, and corrected the Unstake input valuation to match its output at the displayed exchange rate. A routed desktop/mobile proof checks the 16px compact header axis, 24px content axes, 8px action axis, 16px acknowledgement/action relationship, actual processing-to-outcome no-shrink transitions, and horizontal containment across every Stake/Unstake state. Its selectors use structural test seams rather than product copy. The exact production success messages remain unchanged for human hierarchy review.
- Final parity hardening removed the remaining false confidence in the Stake specimens. Entered Stake/Unstake amounts now drive input value, output quantity, USD value, and outcome through one bigint-only fixture calculation; edited delegate identity reaches both current and outcome states. Vote Lock and Stake delegation now share an opt-in 14px monospace `AddressTextInput` without changing ordinary field defaults. Stake adds invalid-address, wallet-disconnected, no-balance, and transaction-failure states, removes unreachable delegate branches from the amount action owner, stacks current-assignment anatomy on phones, and uses the immediate-result label `Received`. Vote Unlock retains its delay and reward-end facts after submission. The mounted light/dark desktop/mobile review found no remaining drift in the inspected cross-flow relationships; product mechanics and production consumers remain unchanged.

## 2026-09-03 — Automated mint lifecycle lab

- The fixed-point production and async-zap SDK audit separates configuration, quote searching and cancellation, both funding branches, individual CoW submission followed by normally batched wallet authorization, independent fill states, failed-leg-only retry, the explicit collateral-ready boundary, final Folio minting, and receipt-backed completion. The review lab now exposes those boundaries without changing production behavior, SDK code, packages, or shared defaults.
- Dark/Light review found that input amount and existing-collateral choice had initially been encoded in display state, so they could disappear across Start or Retry. Both are now orthogonal lab inputs, Max drives all amount fixtures through bigint scaling, and focused interaction tests retain them across quote cancellation, execution, partial failure, and recovery.
- Fulfilled production rows still render quote-leg values even though the SDK exposes CoW execution data. The lab therefore keeps `Sell quote` and `Buy quote` labels after fill, and the seven-class reuse-gap register routes executed-value promotion, cancellation semantics, and cross-session resume to engineer review instead of inventing product truth.
- The direct async-mint test recipe now follows the repository's pnpm rule. Localization extraction added the lab's source-backed lifecycle copy to all catalogs; the newly introduced Spanish, Korean, and Chinese entries are filled while their prior 83-entry missing baseline remains unchanged.
- Independent follow-up review disproved part of the first candidate's self-review: ordinary amounts were nonfunctional, lifecycle controls bypassed funding-branch ownership, recovery forced the existing-collateral branch, CoW identities were transaction-hash shaped, and terminal actions were incomplete. The corrected lab uses one controlled transition boundary, independent lifecycle/funding/input dimensions, bigint parsing for ordinary USDC values, 56-byte CoW UIDs, truthful mixed-funding hierarchy, and a real View DTF link. Human review later removed the redundant New mint action so completion has one primary destination; future state-rich specimens must test a non-preset value, branch preservation into failure, and external identity shape before reporting review readiness.
- A final source comparison found another escaped contract: production allows positive over-balance amounts to fetch quotes, keeps the balance error visible, and disables execution later. The corrected lab now preserves that distinction and tests it directly; generic “invalid amount” handling must not erase a source-backed preview path.
- The candidate-readable “held-out” scenario was not held out. It remains useful as a visible pressure case, while the workflow now forbids claiming held-out evidence unless the candidate cannot read the scenario. Dark/Light agent review is recorded as internal review evidence, never human acceptance.

## 2026-09-03 — Automated mint behavioral-baseline hardening

- The automated-mint lab and its routing documents now treat the current Register and async-zap SDK mechanics as the baseline rather than invitations to redesign product logic. The lab may improve hierarchy, component consistency, and misleading wording, but funding selection, order execution, amount/result ownership, recovery limits, and sequencing stay unchanged without explicit product or engineering decisions.
- The quote escape action is now described as pausing and resuming search because production gates continued work but does not prove that an in-flight request is aborted. The same pass removed cross-session and Dialog-focus implications from the review guidance. No production, SDK, on-chain math, shared default, or orchestration code changed.

## 2026-09-03 — Automated mint composition fidelity

- Human review accepted the compact configuration → wide workspace dynamic but rejected the first candidate's global horizontal progress treatment. Direct production comparison showed why: the left column already owns three meaningful sections—funding, automatic collateral acquisition and its first real action, then expected output and the final Mint action—while the right-side orders are inspectable evidence inside acquisition rather than an equal fourth stage. The lab now preserves that structure in V1 language.
- Desktop keeps the adjacent collateral workspace. Narrow screens keep the high-level three-stage journey primary and reveal individual orders only through `View orders`; this is deliberate prioritization for an uncommon professional mobile use case, not an attempt to optimize dense order rows for default mobile reading.
- Production exposes the total available/applied value of existing collateral, a funding-source logo stack, and per-token collateral amounts. The lab transfers the value and detailed per-token evidence but leaves the logo stack as an optional human visual choice. Completion now uses the shared outcome hierarchy while retaining the final mint transaction and completed CoW orders as distinct reconciliation records. No production mechanics, SDK/math, product policy, or shared default changed.

## 2026-09-03 — Automated mint task-composition trial

- Human review kept the compact-to-wide transition and production sequence but reopened the three-section left layout. The new trial uses the established square input/output relationship, one stable action slot, and an in-card progress region that appears only after collateral execution starts. The task uses the same 24px content, 8px control, and 2px amount-seam relationships as the simpler transaction families without turning automated mint into a modal or hiding its adjacent order evidence.
- `Edit amount` moved from a competing footer action into the funding amount's supporting line. The first actionable CTA and final Mint CTA disclose `1/2` and `2/2`; once execution starts, the connected progress rows take over that lifecycle explanation while the action slot remains stable. This is a lab-only composition trial and does not alter automated-mint mechanics, SDK ownership, or production code.

## 2026-09-03 — Automated mint workspace geometry

- Expanded desktop states now fill the beige stage's content width and share one 656px height across quote, execution, recovery, and outcome. Flexible space sits between the left task information and its bottom action region; an in-card progress region remains below that action, while completion uses the same slack before its next actions. The right header stays fixed and the order ledger owns overflow, following the useful production equal-column/internal-scroll principle without changing production code or creating a shared shell default.
- Mounted checks exercise every expanded state at desktop width, assert that the left column never overflows, keep the workspace width on the stage's padded axis, and retain the existing opt-in stacked order treatment on narrow screens. The 656px height is a flow-owned lab review value, not a universal transaction or page-height token.
- The modal-derived 2px card ring and beige attached substrate made the left page column appear larger than its peer even though their measured geometry matched. Automated mint now keeps execution progress below the action on the same white card surface; the step indicators and connector carry sequence, while the workspace divider carries column structure. Vote Lock, Stake, and Zapper retain their attached-surface treatment because they remain contained transaction tasks rather than paired page columns.
- Existing basket-token identity now stays with the funding amount instead of occupying a second right-column section. Enabling existing collateral replaces the single USDC identity with the canonical 24px WBTC/WETH logo stack while the supporting line retains the USDC-plus-collateral funding breakdown. The right workspace begins directly with collateral swaps; funding and execution behavior remain unchanged.
- Automated-mint order rows retain the production flow's direct pay-to-receive reading: the sold token mark and two-line sell quote anchor the left, while the two-line buy quote and purchased token mark anchor the right. Pre-execution rows omit per-row readiness and provider labels; once submission begins, order identity, lifecycle status, and the explorer action share one metadata row. The right column uses one continuous recessed-content surface with an 8px edge inset; transparent rows own 16px horizontal and 8px vertical inset with no explicit sibling gap. The quote values and execution semantics remain unchanged.
- The automated-mint metadata pairing reopened only Lifecycle Status geometry: the lab now tests one shared 28px pill beside the existing 28px micro secondary order action, with 10px leading/12px trailing inset for indicator pills and 12px symmetric inset for text-only pills. Status text remains 12px and all accepted roles, tones, indicators, intrinsic sizing, and motion rules remain unchanged. The former 24px geometry stays the recorded accepted decision until human visual review resolves this global trial; no product consumer adopted it.

## 2026-09-03 — Automated mint progress hierarchy trial

- The standalone process stepper duplicated the Input → collateral → output structure and left the task-level state competing with the CTA, right-header aggregate, and per-order statuses. The latest lab trial removes only automated mint's standalone stepper: the collateral section now owns Step 1 state and action, the output section owns Step 2 state and action, and the right header no longer repeats aggregate progress. Individual orders retain their own statuses and identities. The generic stepper, shared components, SDK mechanics, and production code are unchanged.
- Human review requires the two owned stages to read as one uninterrupted high-level sequence. Flexible height now follows the Mint section and its action rather than separating collateral acquisition from Mint; this is flow-owned workspace geometry, not a new shared layout default.

## 2026-09-03 — Automated mint section hierarchy and collateral risk facts

- The workspace now uses production's equal-width desktop columns. The left task uses the production-backed 2px `secondary` seams between funding, collateral acquisition, and Mint, with each seam passing through a `secondary` ring and card-colored arrow core. The treatment clarifies the three sections without restoring the rejected detached stepper or changing shared component defaults.
- The right order workspace and its collateral/order rows use one continuous recessed-content surface. The collateral stage restores the production risk summary with a 16px vertical band: price impact plus max slippage before fills, then quoted plus actual impact once collateral is ready. Values remain deterministic lab fixtures; production pricing, SDK behavior, and execution math are unchanged.

## 2026-09-03 — Automated mint entry gates

- The lab now begins where the production wizard begins: an advanced-feature introduction recommends Swap for most users, then distinct smart-account requirement states cover no connected wallet and a connected but incompatible wallet. Product copy, supported-wallet guidance, and the ordinary compatible-wallet destination are source-backed; the lab simulates Connect/Switch without importing wallet orchestration.
- The entry treatment reuses the accepted Button, Action Group, Inline Message, Lifecycle Status, typography, and attached-region roles while leaving the production gate, SDK, routing, compliance, and shared defaults untouched. Future workers must preserve the disconnected-versus-incompatible distinction rather than replacing both with one generic connection state.

## 2026-09-04 — Restrained transaction amount radius

- Human review reversed the square transaction-amount experiment. The shared unadopted `TransactionAmountObject` now uses the restrained 8px contained-object radius across editable, read-only, submitted, and outcome states; the Zapper quote-loading replacement preserves the same boundary. Ordinary Delegation address Fields remain fully rounded, structural page sections remain square unless their owner says otherwise, and production transaction flows remain unchanged.

## 2026-09-04 — Shared automated Mint/Redeem lab

- Automated Mint and Redeem now consume one lab-only configuration, workspace, order, recovery, and outcome tree. Operation policy changes assets, order direction, action boundaries, stage count, and terminal evidence without duplicating the layout; Mint retains its separate final action, while Redeem authorizes its redeem transaction before collateral-sale orders fill.
- The Redeem pressure set includes ordinary shares, shares plus held basket collateral, and the production-supported zero-share collateral-only conversion. Removing that collateral returns to an unfunded configuration, while authorization, failure, and completion preserve the two-leg collateral-only branch.
- Independent review caught two shared amount-truth defects before closeout. Mint now caps existing collateral per required basket leg and derives the displayed applied value from the same reductions, preventing negative funding and zero-sell/nonzero-buy phantom orders for small inputs. Idle over-balance Redeem quotes now use the live-balance fixture as their order/output basis, matching production's safety cap while execution remains unavailable. Exact executed values, cancellation semantics, and cross-session reconstruction remain engineer-review boundaries.

## 2026-09-04 — Automated reconciliation and manual issuance readiness

- Source reconciliation found automated production UI branches the visual lab still needs to pressure-test: no-swap completion, per-leg quote failure, Ondo blocking and split orders, expiry/cancellation, wallet failure, missing token price, and BSC USDT identity. MetaMask batch splitting is mechanically real but has no distinct current UI, so it remains outside the lab-state requirement. The lab's clipped recovery state and duplicate-asset row identity were corrected without changing production behavior.
- Manual issuance is now mapped before visual work begins. Its direct Folio flow can reuse the amount, action, status, recovery, identity, and outcome grammar, but needs a flow-owned requirement/allowance ledger rather than CoW order anatomy. Engineer review remains required for address normalization, parallel and USDT approval sequencing, zero minimum-output redeem legs, and receipt-backed outcome truth. The current owner is [the manual issuance readiness plan](../plans/manual-mint-design-system-readiness.md).

## 2026-09-04 — Production-visible automated issuance pressure states

- The automated lab now exposes only additional branches that the current product visibly renders: BSC/18-decimal USDT configuration, Ondo trading pause, missing input price, one failed quote leg, capacity-split duplicate-asset orders, no-swaps completion, ordinary order expiry, Cancelled status, missing wallet client, transaction failure, and the pre-execution switch to manual issuance. MetaMask call splitting remains documented behavior with no invented dedicated UI state.
- Migration guidance now requires a source-backed production inventory and an explicit reviewed-replacement, preserved-behavior, or unresolved disposition for every visible state, action, copy block, relationship, and recovery path. Absence from the lab is never evidence for deletion or simplification, and a reviewed specimen transfers only to the job and state actually reviewed.

## 2026-09-04 — Automated issuance attention decisions

- Five previously ambiguous automated-issuance choices are now rendered as reviewable behavior: no-swap flows retain the Collateral checkpoint without an empty order ledger; imminent order expiry appears in the high-level task as well as order detail; zero-share Redeem identifies existing collateral as its source instead of claiming `0 CMC20` was redeemed; trade-backed outcomes label aggregate impact as `Collateral swap price impact`; and desktop keeps stable equal columns. The no-swap path omits swap-impact evidence. The displayed value remains a deterministic lab fixture until engineers identify an authoritative production result source.

## 2026-09-04 — Contained Inline Message icon

- Inline Message compact summaries may opt into a contained leading icon when balancing a trailing compact action. The shared treatment owns a 32px semantic-tone circle, 16px glyph, and 8px perimeter inset; plain icons remain the default and full messages are unchanged. The automated-issuance introduction now consumes that contract without caller-owned spacing or production adoption.

## 2026-09-04 — Transaction metric color semantics

- Transaction metric color now resolves from phase and meaning instead of numeric sign. Routine slippage and impact estimates stay neutral; critical preflight risk remains warning/danger UI; meaningful realized movement uses the separate performance palette; and an actual value visually supersedes its quote. The local recipe names realized tones explicitly so a future caller cannot mistake a favorable projection for a realized gain. Production consumers, thresholds, math, and shared component defaults remain unchanged.
- A repository-wide run exposed an intermittent TokenLogo teardown race after all assertions passed; the immediate clean rerun passed 1087/1087. The issue is recorded in the backlog rather than widening this visual-semantics stage into production image-loader behavior.

## 2026-09-04 — Transaction mobile responsiveness audit

- A mounted cross-family pass exercised every Zapper, Vote Lock, Stake and automated-issuance lab state at 320px and 390px, then pressure-tested 639px, 767px, 1023px and the 1024px two-column transition. Manual issuance stayed excluded. The pass found no document-level overflow after repairing clipped transaction amounts/assets, compact three-mode headers, truncated high-level automated-issuance progress, and the order-detail header.
- The responsive ownership is deliberately narrow: transaction values take one smaller existing type step only below 360px; compact headers reduce only item inset; automated issuance wraps high-level meaning on phones and centers a 640px-max single task column until `lg`. Narrow screens still start with orders hidden and disclose them explicitly. None of these repairs changes product mechanics, transaction ownership, or shared defaults.
- The floating Reserve AI launcher still overlaps bottom actions in some 320px captures. This confirms the existing application-shell backlog item; transaction candidates must not add per-flow clearance for a global overlay. If the shell is addressed, prefer hiding or repositioning the launcher while a bottom transaction action is present.

## 2026-09-04 — Transaction checkpoint and manual composition preflight

- The reviewed Zapper, Vote Lock, Stake, and Automated Mint/Redeem work is checkpointed before Manual Mint/Redeem begins. A reusable flow-composition transfer template now requires a production-visible state inventory, explicit task hierarchy, geometry ownership, continuity model, transaction-truth boundaries, reuse/deviation dispositions, and a deliberately small first review slice. The Manual Mint/Redeem readiness plan instantiates that preflight so a fresh task starts from source-backed behavior rather than inheriting the previous flow's layout by analogy.
- The checkpoint gate caught two stale collateral-progress assertions and a network-backed TokenLogo timeout escaping after a lab-test environment had unmounted. The assertions now pin the accepted `1 order open · expires in 1m 42s` wording, and the automated-issuance unit suite isolates logo rendering while retaining its geometry and identity assertions. This keeps the complete unit gate deterministic without changing production image loading.

## 2026-09-04 — Manual issuance two-anchor proof

- The coarse generic atomic prototype was replaced by the deliberately bounded first Manual Mint/Redeem proof. Mint now keeps a sufficient page-hosted share task beside five realistic basket requirements, including independent Approved, Approve, and USDT-like Revoke boundaries plus the production unlimited-approval choice. Redeem keeps the same geometry but presents only expected basket amounts and values; no CoW, order, expiry, approval, or stepper semantics leak across.
- The pass caught two process failures while reconciling the lab: the first Redeem total did not update with the entered amount, and the coverage map still claimed that manual execution, recovery, and outcomes were rendered after those speculative states had been removed. Bigint-backed fixtures now update every visible dependent value, and the lab explicitly marks execution, recovery, and result identity as deferred. Human review of these two anchors remains required before state multiplication.

## 2026-09-04 — Manual anchor fresh-eyes corrections

- The first proof's visual checks and bigint claims were insufficient: nested child padding produced a 40px amount-text axis, viewport breakpoints could overflow a narrow ledger column, Max exceeded basket capacity, and clearing the amount retained permissions. The bounded correction pass measures the actual 8px control/24px text axes, tests container width independently of viewport width, derives lab Max and allowances from raw fixtures, and labels transaction/navigation controls as non-executing previews. The basket is explicitly synthetic, not a production CMC20 basket or math contract.
- The existing transfer template now requires measured composite geometry and an explicit interaction boundary, not just named spacing tokens. Both original layout regressions were reproduced before their browser tests passed. Focused tests passed 51/51; mounted light/dark tests passed at nine widths plus a narrow embedded host; desktop/mobile compositions were visually inspected. This verifies the two anchors, not execution, recovery, or outcome completeness.
- Human clarification: preserve the current app's Unlimited label, default, and behavior. Approval/security explanations and policy are not design-review decisions. The readiness plan no longer demands new explanatory copy, a production address-normalization fix before lab work, or an invented dust-blocked state. These engineering boundaries remain deferred until adoption; production behavior is unchanged.

## 2026-09-05 — Manual anchor composition refinement

- Human clarification makes production functionality the floor, not a requirement to copy legacy presentation. The bounded two-anchor pass restores the token-address explorer links and exact production USDT Revoke explanation that the first inventory overlooked. Token addresses are repository-backed Ethereum fixtures; they do not imply the synthetic basket is CMC20's production basket. Unlimited and approval mechanics remain unchanged.
- Manual rows now use existing identity, 32px token logo, Link, Help Tooltip, 28px Button/status and typography primitives in one flow-owned two-tier arrangement. Required/expected amounts lead; balances/values are supporting. Inset gray separators sit on the 24px axis, and narrow rows wrap actions instead of clipping addresses. The earlier generic-row container extension is no longer needed and was removed without changing shared defaults.
- The left task now wraps its content instead of stretching to the ledger: the previous dependency added a 318px header gap at the desktop column transition. Its regression fails on that old layout and passes with the intended 24px gap. The tradeoff is more vertical space for readable asset rows and independent column heights; long-basket scrolling and execution remain the next pressure work, not completed features. Alternate Zapper navigation keeps its wording but uses the existing text action.
- Process correction: inventory secondary links and explanatory controls, not just primary actions and field values. Verify geometry in the rendered host, and distinguish flow-local layout from shared primitives. Focused tests and mounted width/theme checks support the current review; no production adoption or design acceptance is implied.
- Follow-up human review replaces the separators above with spacing-only grouping (16px symmetric row padding, 8px within Mint rows), a plain Unlimited checkbox, and simpler receive-only Redeem rows. Mint balance numbers use foreground; insufficiency replaces the Balance label. A new mounted regression reproduced the former 40px warning-induced jump at 320px and now verifies stable row heights/positions and non-overlapping labels through sufficient/insufficient transitions. Approval defaults and security policy are untouched; this remains a flow-local candidate.
- The next approved refinement moves the setting out of the ledger heading and into the Mint input/action group, labeled “Approve unlimited token amounts.” It uses the 24px text axis and 16px above/below; Redeem's direct input/action gap now matches automated configuration at 8px. The choice stays at composition scope and survives mode changes. Production defaults, allowance semantics, and non-executing preview boundaries remain unchanged. Focused tests verify label-click toggling and persistence; mounted tests verify both spacing relationships across nine widths in light/dark.

## 2026-09-05 — Zapper loading-layer correction

- The output's absolute loading overlay escaped its wrapper and competed at z-10 with the direction-control parent. Later DOM order covered the arrow's lower half. Isolating the output wrapper contains its overlay and detail rule below the shared arrow without changing shared z-index defaults or geometry. A mounted paint-order regression failed in Quote search before the fix, then passed in Review/search at 320/390/1280px in both themes. Disabled hit testing is temporarily enabled only for the probe; the test separately verifies the control remains disabled during search.

## 2026-09-05 — Manual soft-action trial

- Human requested a local soft-blue Approve / neutral quiet Revoke comparison. Canonical micro dimensions, main primary CTA, approval policy, and success status are unchanged. `TX-P21` keeps this provisional: any later shared-variant promotion must include a retrospective review of earlier transaction action choices, not only future adoption. Navigation selected-state styling is explicitly deferred.

## 2026-09-05 — Manual canonical-owner audit

- The Manual approval setting incorrectly imported the legacy 16px `ui/checkbox`; previous interaction and spacing checks did not establish canonical ownership. It now imports the catalog's `components/checkbox` (28px slot, 20px mark) and uses the shared label typography, without a local size override. Token explorer links also now use Link's external API instead of a manually drawn glyph, retaining the muted treatment while adding the standard new-tab announcement.
- The readiness brief now records exact import owners and variants for every visible dependency in both anchors. No new generic rule or skill was needed: the existing exact-owner requirement was not followed. Regression tests failed on the legacy checkbox and missing external announcement before correction; mounted checks now verify actual mark/slot dimensions and keyboard toggling as well as the existing geometry. The remaining reviewed layouts, production mechanics, shared defaults, and deferred execution boundary are preserved.
- The maximum-length Redeem stress case exposed another weak assertion: outer-width containment passed even though the primary label painted into its 24px padding. A line-box test now measures the actual text against the button's inner edge and failed before the local wrapping fix. This preserves the full label and shared default size instead of clipping meaningful numbers or changing copy.
- Human approved factual approval help without security advice. The setting now uses the standard right-aligned HelpTooltip outside the checkbox label: “When selected, approvals set the token spending allowance to its maximum. This does not change the amount you mint.” Checked/unchecked policy is unchanged, and no exact-amount claim is made for unchecked approvals. Focused tests verify opening help cannot toggle the setting; mounted checks cover right-edge/vertical alignment, narrow wrapping, and whole-card containment including the standard expanded help hit area. The two new messages were extracted in isolation, translated, and catalog compilation verified without unrelated extraction churn.

## 2026-09-05 — Alternate issuance paths and Manual row rhythm

- Human approved contextual text actions beneath the primary action instead of an abbreviated header pill. Automated configuration now uses the full manual Mint/Redeem link with context inside the active card; Manual keeps its Zapper alternative below the CTA. Existing route destinations, non-executing Manual preview boundary, and initial introduction actions remain unchanged.
- Manual asset content now has 24px between items (12px row insets), reduced from 32px. List edge padding preserves the existing 24px outer content axis and bottom inset. The task retains its natural height: stretching to a variable basket would separate the amount and CTA or create arbitrary internal whitespace. This remains a page-specific choice, not a change to modal no-shrink rules.

## 2026-09-05 — Manual identity and amount typography

- Corrected a density mismatch rather than adding a typography variant: standard EntityIdentity now gives token names the same 16px/24px rhythm as Required, Balance, and received amounts. Compact identity had demoted names to 14px, especially visibly beside Redeem quantities. Supporting labels, addresses, and USD remain 14px/20px; weights, colors, logos, and 24px row gaps are preserved. Mounted checks now verify the actual name/value typography across both modes and all tested widths.

## 2026-09-05 — Manual lifecycle expansion

- Expanded the two anchors into 29 reviewable configuration, permission, direct Mint/Redeem, recovery, and outcome states. One bigint-backed lab session drives dependent values; explicit external simulator controls exercise independent wallet responses without production writes. The task stays natural-height and asset actions remain visible on narrow screens. Existing geometry and local soft-Approve trial are preserved, not promoted.
- Independent review caught missing failure reasons, an aggregate-versus-individual USDT allowance distinction, insufficient mixed-progress simulation, stale two-anchor-only documentation, and repeated accessible names. All were reconciled. Production's parallel approvals and aggregate USDT reset mismatch remain explicit engineering boundaries rather than being repaired in a design fixture.
- Mounted testing caught an outcome route constructed for a synthetic Ethereum CMC20 identity with no live deployment. View DTF now demonstrates navigation explicitly without sending the reviewer to a different-chain asset. Persistent results reuse the existing outcome grammar, but submitted shares and expected basket quantities do not claim receipt decoding. Production currently toasts and resets; this result screen remains a visual proposal.
- Verified 169 design-system tests and six mounted Manual checks, including every state in light/dark and four widths, plus nine-width anchor geometry and a non-preset recovery journey. Visual inspection corrected floating processing actions, recovery-help alignment, and a premature loading approval count. Readiness, reference, state-contract, and coverage docs now point to the lifecycle owner. No new skill was needed: the useful process improvement was enforcing existing source, geometry, and truth owners with actual interaction/paint evidence.

## 2026-09-05 — Manual mobile continuity

- Static state containment missed a 36px layout jump when short Approve actions became longer signing/status pills at 320px. Measured approval transitions reproduced it; a local 128px-wide, 28px-minimum-height permission slot fixes inconsistent wrapping without changing shared controls. Loading placeholders now use the same wrapping anatomy. The regression compares quantity and subsequent-row positions, not just absence of horizontal overflow.
- Fresh Manual browser suite: 8/8, including light/dark lifecycle states, seven-width approval transitions, and short/landscape reachability. Focused unit tests 19/19 and app/E2E types passed. Native mobile keyboards remain outside this browser-emulated evidence.

## 2026-09-05 — Manual recovery and outcome refinements

- Applied the user-authorized small-improvement list: failed symbols in the existing summary and an alert in the existing token action, informational known signing rejection, a same-height readiness replacement for the retained Unlimited setting, and removal of duplicated completion facts. Expected basket requirements/return remain estimates; no executed amounts or approval-policy changes were introduced.
- Readiness is derived from sufficient allowances, not a count of successful requests. A mounted finite-allowance journey verifies that setting value and action position survive completion and changed requirements. Keeping visual continuity at the composition owner avoids another shared component variant. No new workflow rule was needed.

## 2026-09-05 — Transaction lab canvas cleanup

- Reproduced section navigation covered by preview descendants and incomplete Stake/Vote Lock backdrop coverage (17px vertical offset on mobile, capped nested host on desktop). Preview-level stacking isolation fixes navigation without a z-index escalation. Flush stages and full-width hosts remove the extra beige gutters; one grid cell keeps backdrop coverage while allowing tall dialog content to determine canvas height. Explicit zero-minimum grid tracks prevent narrow intrinsic-width overflow.
- Removed Zapper's obsolete 980/760px fixed preview-height rule in favor of a 680px minimum that grows for content. Intentional transaction widths, page caps, internal task spacing and production overlays are unchanged. Browser regressions cover navigation hit-testing, backdrop bounds and tall previews; existing Zapper arrow layering remains covered separately.

## 2026-09-06 — Manual alternate-flow alignment

- User-approved Manual helper copy now says “Want to buy with a single token?” because Zapper may swap or mint. The helper stays left and the existing action aligns to the right 24px content axis, sharing a line when they fit and wrapping naturally when narrow. Redeem inherits the layout with its existing wording; no navigation behavior changed.

## 2026-09-06 — Manual column surface continuity

- Distinguished surface height from content distribution: the desktop left card now fills its grid column, leaving spare space below the unchanged task group. Mobile remains natural-height, and the outcome floor still measures intrinsic task content. No shared defaults changed.
- Mounted geometry reproduced the old desktop bottom-edge mismatch, then passed with the local background wrapper; existing spacing checks retain the control/content axes across Mint/Redeem and light/dark breakpoints.

## 2026-09-06 — Stable Manual desktop review host

- User review found that content-driven workspace height still shrank between Mint, Redeem and outcomes. The lab now supplies a 720px desktop host, with both columns filling it and scrolling at their outer edges if needed. Content grouping, the 2px seam and natural-height mobile layout remain unchanged; this does not define a production page height.
- The mounted lifecycle regression failed against the prior 692px Mint height. Coverage now pins desktop height across all 29 states and checks access to the final asset when constrained column width forces overflow.

## 2026-09-06 — Manual outcome evidence and actions

- Corrected a fixture limitation leaking into design: Manual now includes the final Mint/Redeem transaction action as a standard detail row. Its lab action explains the Etherscan destination without inventing a real hash; production must retain its transaction identity through confirmation.
- Minted/Redeemed leads the result; Basket assets used/received describes the completed operation with a visible Estimated amounts qualifier for fixture quantities and total. View DTF stays primary and Done returns to configuration; internal navigation no longer carries an external arrow. No receipt sourcing, approval policy or production behavior changed.

## 2026-09-06 — Issuance outcomes aligned with Zapper

- Automated and Manual now fill desktop outcome columns with flexible brand space above the amount, not white space below the facts/actions. Automated View DTF uses a full-width default-sized Button at the 8px edge; Manual retains its default-sized Done/View DTF pair.
- Mint reuses the existing received-DTF bookmark; Redeem does not offer to track burned shares. Automated keeps hash/copy/explorer evidence with icon-only navigation; Manual retains an icon-only simulated explorer action without fabricating a hash. Wallet feedback is lab-only; production integration remains deferred.
- Focused regression tests first failed for missing wallet actions and the old 24px footer inset. Browser checks cover both operations/families and light/dark at 320, 390, 1024 and 1280px. No production behavior or shared defaults changed.

## 2026-09-06 — Outcome fiat row and blue-region inset

- Corrected Automated's local padding override: the blue region now retains 24px below the fiat row, matching Zapper's 16px amount inset plus 8px wrapper. Flexible space still stays above the amount.
- Manual Mint/Redeem restores the existing input's indicative share-value estimate below the result amount. The supporting row had been explicitly hidden; its removal was not a result-truth requirement. This is a fixture valuation, not executed cash proceeds.
- Regression checks first failed for the missing Manual estimate and Automated's 8px inset; coverage includes both themes, four viewport widths and a non-preset Manual amount. No shared defaults or production behavior changed.

## 2026-09-06 — Transaction evidence controls: rejected trial and correction

- Human review rejected the twin 28px quiet copy/explorer trial: equal button boxes did not produce good visible glyph spacing or trailing alignment. The accepted integrated Copyable Value decision already addressed this pressure; nominal geometry tests were not evidence of good composition. The trial is not retained guidance.
- Manual and Automated now pair default-sized secondary View transaction with primary View DTF in the footer. Automated keeps an inline copyable hash with no adjacent explorer action. Manual keeps its honest explorer simulation and relocates the prior reset to New mint / New redeem in the header. Multi-transaction Delegation retains labeled per-result links; individual CoW order evidence is unchanged.
- The new regressions failed first for the old detail-row explorer and absent Manual footer. Checks now target copying, footer placement, reset and visible trailing alignment rather than matching ghost-button dimensions. Detail-density proposals remain unimplemented; no shared defaults, transaction mechanics or production behavior changed.
- Final verification: 50 focused tests and six mounted-browser tests passed, including all 29 Manual states and a non-preset completion/reset journey. Inspected both families/operations in light/dark at desktop and narrow mobile widths. Footer actions wrap naturally when their full labels do not fit; typecheck, scoped lint, wiki lint and diff checks passed.

## 2026-09-06 — Manual dense-row review follow-through

- Revoke now consumes neutral InlineAction, removing the invisible trailing button inset while retaining its help and failure marker. Approve's local soft-blue trial is unchanged. Narrow rows use explicit identity/trailing columns instead of the old identity minimum plus 128px permission slot; units and insufficient-balance wording adapt without adding an error tier. Token explorer addresses retain a readable suffix and full accessible identity.
- Switch to Zapper now gives a nearby lab-only navigation preview without executing a swap or losing the manual amount. Retry labels state the action while the banner names the failure. Active approvals/mint/redeem show explanatory context instead of stale configuration instructions, with the original description geometry reserved.
- The navigation-preview and processing-context tests failed before implementation. Existing browser pressure checks caught a long fiat-value wrapping regression; it was fixed rather than weakening the test. The new browser relationship checks cover sibling placement and visible address clipping in addition to viewport containment. Lingui Plural is stubbed by the unit harness, so the recovery label is verified in the mounted journey. No approval policy, financial derivation, shared default, or production flow changed.

## 2026-09-07 — Automated introduction icon fill

- Restored the intended muted circle by wrapping the local HSL-channel token in `hsl()`. The previous raw variable produced a transparent background, leaving invisible 32px geometry. Banner tone, copy, button and shared Inline Message defaults are unchanged. A mounted regression failed for transparency in both themes before the fix and passed afterwards at desktop/mobile widths; inspected the rendered results and updated TEST_MAP.

## 2026-09-07 — Manual Mint section hierarchy

- The lab now separates desired amount, token approvals and final Mint in the left column. Full-width secondary boundaries, 24px heading insets and 8px action edges reuse the established section rhythm. Approve All and Unlimited belong to the middle section; individual ledger actions remain available without a separate mode. Redeem retains its direct action.
- The final Mint action remains mounted and gated while approvals run. Readiness replaces the approval controls while preserving their geometry, so enabling Mint does not move it. Approval recovery and final-transaction recovery belong to their respective regions. Loading does not expose fixture approval counts prematurely.
- Verification: 26 focused unit tests and 11 mounted-browser tests passed, including the 29-state lifecycle, non-preset recovery journey, both themes and 320/390/1024/1280px widths. Final screenshots were inspected; typecheck and scoped lint passed. Existing amount locking, per-token pending locks, allowance defaults and production transaction preparation are unchanged. Explicit Edit amount and approval concurrency remain separate follow-ups, not inferred requirements of this layout.

## 2026-09-07 — Manual section refinement against Automated

- The comparison exposed a tradeoff hidden by geometry tests: reserving the entire approval-control group kept Mint stationary but left a poor completed section. The authorized refinement now removes that reservation and compacts completion with 24px bottom inset. The final action stays mounted, follows the completed region, and uses a true 24px summary/action gap without hidden wrapping text. Spare height belongs after the task content.
- Token readiness now has a separate live status line, counting sufficient allowances including pre-existing approvals. The loading action no longer repeats an attempt-based fraction. Checkbox/action spacing stays 16px; individual approvals, defaults, USDT behavior and reducer rules are unchanged.
- Manual and Automated now share a 1200px maximum review width and 736px desktop height. Simply shrinking Manual to Automated's former 688px clipped normal task content; the bounded browser check caught this and the common host was sized to fit. These are lab presentation dimensions, not production layout rules.
- The new unit regression failed before implementation for the missing progress line. Final evidence: 67 focused unit tests, 14 Manual/outcome browser tests and two Automated workspace regressions passed; app/E2E types, scoped lint and wiki/diff checks passed. Light/dark and narrow-screen states were inspected. The local preview stopped during verification and was restarted on port 3005; the initial connection failures were not treated as UI test results.

## 2026-09-08 — Manual hierarchy correction after human review

- Human review rejected the paragraph-led three-section trial: correct inset measurements did not establish a clear hierarchy. The dominant introductory heading and similar-sized title/count/explanation stack failed to transfer Automated's actual composition. The prior rationale against arrows also misread the desired stage relationship; they now explicitly connect Manual's ordered stages without implying asset swaps.
- Manual now consumes TransactionAmountObject across editable shares, approval progress and final Mint confirmation. Labels share the light 16px role; amounts/counts use the same prominent financial scale and supporting status is 14px. The separate-transactions explanation remains accessible in factual help. Switch to Zapper moved into the first section. Final shares/USD confirm the requested amount and are not an executed result.
- Automated and Manual share the same local TransactionStageBoundary implementation. Visual inspection caught its circle overlapping the middle approval button at an 8px bottom edge; that boundary now has 24px button clearance, while the terminal footer retains 8px. Ready state removes controls without hidden space. Approval policy, reducer rules and individual ledger actions are unchanged.
- New regression evidence failed first for missing connected boundaries. Verification covers relative label/value hierarchy, whole-width boundaries, circle centering, button clearance, top-section navigation and tooltip disclosure, not merely token use. The 68 focused unit tests and 11 Manual browser checks passed, including light/dark and narrow screens; typecheck, scoped lint and wiki/diff checks passed. Automated divider extraction is covered by its two existing workspace regressions. Visual approval remains with the user.

## 2026-09-08 — Manual final action simplification

- Human feedback rejected both the repeated final amount/fiat and a second large signing/processing status. The final region now uses a quiet Mint CMC20 heading matching the section-label role. The exact requested amount remains in the input and action; existing button states own wallet signing and confirmation. Outstanding approvals get the approved short prerequisite helper.
- Visual review rejected reserving that helper's hidden text box: on narrow screens it left a large empty gap. The helper now leaves with its space when no longer needed. Ready, signing and confirming share a compact heading-to-action relationship of 24px; completed approval controls still compact as before. No transaction, allowance, reducer, or shared-default change.
- Verification: the new presentation assertions failed before implementation, then 34 focused unit tests and eight mounted-browser checks passed, including all 29 Manual states in light/dark at 320/390/1024/1280px and the non-preset recovery journey. Inspected populated, ready, processing and recovery renders. Current contracts and test mapping were reconciled; the change remains lab-only.

## 2026-09-08 — Loading hierarchy and issuance outcome follow-through

- Zapper's existing Sign order state now visibly waits for the wallet using the canonical loading Button. Active primary approvals retain primary emphasis in Manual, Vote Lock, Stake and Automated collateral authorization; the upcoming blocked Mint stays neutral and non-busy. The current decision and composition guidance now supersede the earlier subdued-loading instruction without changing shared Button defaults.
- Automated outcomes now offer the same quiet header New mint / New redeem action as Manual. Restart clears the input and session choices while retaining operation and chain. The redundant final-transaction hash row is removed; the default-sized footer View transaction remains the sole explorer link for that final transaction. Individual order evidence and links are unchanged.
- Regression assertions failed before implementation, then 125 focused unit tests and seven browser tests passed. Coverage includes both operations and chains for restart, exact final explorer destinations, loading semantics, header/footer geometry and both themes at mobile/desktop widths. Visually inspected Zapper, Manual, Vote Lock, Stake and Automated outcomes. App/E2E types, scoped lint, extraction, wiki lint and diff checks passed. These are lab presentation changes; approval policy, transaction preparation, production behavior and shared defaults remain unchanged.

## 2026-09-08 — Issuance current-stage emphasis and recovery clarity

- Manual and Automated now use a local label/value emphasis recipe to identify the current task, with matching `aria-current` semantics. Completed content remains readable and neutral; small success checks sit beside normal-foreground confirmation. Structural arrows and section backgrounds stay unchanged. Manual's final heading remains quiet rather than restoring a duplicate amount or processing message.
- Manual's insufficient-collateral message now belongs beside the amount. Full allowances still show truthful permission completion without implying sufficient balances or highlighting a blocked Mint. Production-supported approval availability, defaults and transaction preparation remain unchanged. Recovery has a measured 16px heading/message gap and 8px message/action gap; ordinary readiness retains 24px. Automated failure no longer falls through to an inaccurate “Orders filled” sublabel, and its stacked swap facts retain their opposite-edge alignment without a vertical divider.
- Regression assertions failed before implementation. Verification passed 137 focused unit tests and 15 browser checks, covering all 29 Manual states, recovery, both themes and narrow/desktop widths. Inspected approval, ready, insufficient, failure and fill-progress renders. Contracts and TEST_MAP now distinguish permissions, balances and current-task emphasis; no shared defaults or production behavior changed.

## 2026-09-08 — Manual final action-only region

- Human review found the remaining Mint heading redundant with the explicit amount-bearing button. The final region now contains only its action and any applicable recovery message; the separate prerequisite paragraph is also removed. Its accessible section name and current-stage semantics remain. The region owns 24px top inset to clear the structural arrow, 8px side/bottom edges, and an 8px recovery message/action gap. Automated retains its distinct output summary.
- Eight new/updated assertions failed before implementation; all 40 focused unit tests and 10 browser checks passed afterwards. Browser coverage includes the 29-state Manual lifecycle, stable ready/signing/confirming geometry, recovery, arrow clearance and mobile/light/dark rendering. Inspected ready and failure screenshots; typecheck, scoped lint, extraction and documentation checks passed. No transaction or approval behavior changed.

## 2026-09-08 — Manual readiness-led Mint summary

- The action-only region removed redundancy but failed human review for visual structure. The new local proposal restores label, prominent readiness text, CMC20 mark and explanatory supporting copy, without restoring the repeated numeric amount. It deliberately separates coarse step readiness from the button's exact wallet/chain phase. Empty, blocked and insufficient states cannot claim “Ready to mint”; approval policy and transaction gates are unchanged.
- The summary uses the existing 24px/30px light section-heading role rather than financial-number styling, the canonical 32px asset mark, and the established 24px text axis. Ordinary explanation/action spacing is 24px; recovery uses 16px to its message and 8px to its button. Current-stage emphasis stays local. Automated retains its numeric output summary.
- Eleven updated assertions failed before implementation. All 42 focused unit tests and 13 distinct browser checks passed, covering the 29-state Manual lifecycle, recovery, English ready/signing/confirming geometry, light/dark and narrow/desktop views. Added and checked the approved copy in es/ko/zh, including translated-label wrapping. Visually inspected desktop readiness, narrow dark signing, and the longer Spanish summary. App/E2E types, scoped lint, extraction and wiki/diff checks passed; current contracts and TEST_MAP were reconciled without promoting shared defaults.

## 2026-09-08 — Consolidated transaction regression

- Checked the current Zapper, Vote Lock, Stake and Automated/Manual issuance lab as one batch, without promoting entry cards, standalone selection or Portfolio rows. The new browser keyboard check reproduced a Shift+Tab escape from the initially focused Vote Lock/Stake dialog shell; the lab wrapper now contains it and preserves Escape/focus return/reopening.
- Corrected an obsolete duplicate-expiry browser assertion and isolated the composition unit test's asynchronous image loader after it produced teardown errors. Transaction behavior stays real in those tests; remote artwork and real wallet/RPC behavior remain outside the harness's proof. Reduced-motion checks explicitly cover committed logos and attachments, not the existing shared Button spinner.
- Final evidence: focused unit 243/243 without unhandled errors; desktop browser 37/37; mobile browser 9/9; types, scoped lint, catalogs, formatting, wiki and diff checks green. Independent intent/risk review and bounded fix re-review passed. Exact evidence and limits are in [the consolidated report](../plans/transaction-consolidated-regression.md). No production mechanics, approval/security policy or shared defaults changed; no commit or push.

## 2026-09-08 — Transaction checkpoint documentation closeout

- The user authorized documentation closeout and a checkpoint after the regression pass. Current Review now explicitly retains a paused transaction checkpoint; the catalog remains exploratory, specimen-only and unadopted. Existing local human decisions remain intact. Entry cards, the standalone static selector, and Portfolio balance/cooldown/withdrawal rows do not gain acceptance; review the latter later with the row/table system in page context.
- Reconciled stale square-amount, 4px seam, acknowledgement-gap, Manual deferred-coverage and Automated sole-terminal-action claims. Current contracts now route to one checkpoint disposition rather than asking a fresh worker to restart the anchor phase or an open-ended transaction review. No new skill or generic abstraction was needed.
- Fresh catalog/composition tests 75/75, types, scoped lint/format/wiki/diff checks passed; the mounted client's coverage and selector notes are current. Generated JavaScript catalogs stay local and ignored; tracked PO sources remain authoritative. No production behavior, approval policy or transaction layout changed in this closeout; no push is authorized.

## 2026-09-08 — Bounded inventory reconciliation

- Separated unprepared, deferred and dependency-blocked review work; linked Amount, Asset picker, Stepper and Popover to existing composition evidence without promoting transaction ingredients. Accepted navigation, Metric and Empty state baselines no longer remain in stale review queues. Breadcrumb stays low priority; generic Combobox stays unnecessary. Historical layout measurements are not current V1 contracts.
- Removed the unused blanket Verified milestone in favor of scoped evidence, and stopped presenting an empty schedule as blocked inventory. The active plan retains strong defaults with composition-level judgment; the outside audit should assess overall simplicity and future-feature quality as well as migration reliability.
- Low-profile, lab-only metadata/display changes: the mechanical medium size hint comes from catalog, presentation and documentation files, not a wider product radius. Six regression tests failed before implementation; final focused coverage is 40 passing tests, with types, scoped lint, formatting and wiki/diff checks green. Mounted desktop links and narrow-screen wrapping were checked on a temporary preview; no component defaults, transaction designs or production behavior changed. The outside audit is not started.

## 2026-09-08 — Post-audit consolidation planning

- Prepared one [bounded plan](../plans/design-system-foundation-consolidation.md) from the outside audit and its reconciliation. It proposes a source-bound rendered-review loop, consolidated semantic/type owners, a shorter truthful authority path, fresh-agent validation, and a separately authorized integration rehearsal. The audit remains independent evidence; the plan is pending Claude review, not implementation permission.
- Retained accepted square structure, component defaults, transaction behavior, partial acceptance and pending trials. Historical progress labels do not become approval records. Updated the active plan's stale audit-not-started claim and added an on-demand router link; no UI, verification tooling, shared tokens, or production code changed.
- Planning verification: seven contract sections and twelve local links/anchors checked; scoped formatting, lint, wiki and diff checks passed. The scope inventory also reports unmapped documentation/evidence and an empty-catch flag in the unchanged external audit capture script; neither is presented as runtime verification. Independent plan review remains pending; no commit or push.

## 2026-09-09 — Consolidation plan review reconciled

- Revised the [same plan](../plans/design-system-foundation-consolidation.md) after Claude's ready-with-adjustments review: earlier controls and guidance cleanup, narrower capture prerequisite for role work, explicit read-only baselines, source provenance, CI artifacts and failing hygiene tests. One owner may interleave slices; no extra stages or parallel agents are required.
- Retained mixed-scope historical dispositions, existing workflow status vocabulary, component defaults and accepted square structure. Added a bounded full-page context proof distinct from the one-section migration rehearsal. Removed the planning document's drift-source coupling while keeping its on-demand link. The original external reports remain unchanged; this revision authorizes no implementation, experiment, commit or push.

## 2026-09-09 — Consolidation first implementation slice

- User authorized the bounded plan. Preserved two isolated checkpoint controls before changing guidance; initial outputs, read events, hashes and limitations are retained in the [evaluation record](../plans/design-system-consolidation-evaluation.md). No comparison verdict or production rehearsal yet.
- Added owned-preview captures with source manifests, fail-closed change/attachment checks, read-only baseline mode, and retained light/dark desktop/phone images. Final helper tests 81/81 and captures 4/4 passed; all twelve images inspected. Native watching requires execution outside this host sandbox. The scoped run stopped there; no generic smoke/full-gate claim. [Evidence and remaining scope](../plans/design-system-consolidation-capture.md).
- Corrected stale ActionGroup/Field acceptance claims without promoting adoption or pending designs. The control repeated an obsolete reference claim, reinforcing the need for evidence-backed cleanup; it is not yet an independent comparative finding. No new skill or workflow ceremony was added: the concrete fixes fit existing docs and test seams. The program remains active; external reports, app code and design defaults are unchanged.

## 2026-09-09 — Bounded guidance and acceptance-link cleanup

- Moved transaction-only contracts and all 18 pressure rows unchanged into the existing transaction checkpoint; moved the dated upstream reconciliation into the audit. The required V1 plan now routes to them instead of embedding them. Its source precedence, migration-preservation rule and cross-project engineering register remain intact.
- Added scoped evidence links to the 28 previously unlinked recorded baselines. Seven explicitly use existing synthesized records without inventing dedicated decisions. Reconciled eight historical progress rows while keeping pending density/elevation trials, deferred engineer review and production adoption distinct. Catalog authority, review, implementation and adoption fields did not change. Two obsolete Textarea/Collapsible next-action prompts now consume accepted scope rather than requesting its completed review again.
- Fresh catalog/UI tests, app typecheck, scoped lint/format/wiki/diff and link/content-preservation checks passed; exact counts are in the active stage. Self-review is not independent acceptance; no fresh visual proof or full repository gate is claimed for this documentation slice. A generated metadata insertion initially failed parsing; it was corrected and checked for field parity, apart from the intended context links and two routing prompts. No shared component defaults, production behavior or kit skill changed. The saved guidance-only candidate still needs performer-safe packaging and focused Claude review before further foundation work; no treatment or comparison verdict yet.

## 2026-09-09 — Capture privacy and typography owner

- Reconciled the focused first-slice review: private fingerprints now stay in memory, historical retained records are sanitized/deduplicated, source watchers avoid the dependency tree, and captures record inner scrolling and test-environment limits. Raw credentials were not stored. A synthetic secret-file regression fails before the fix and passes afterward without weakening drift rejection.
- Narrowed uncertain approval provenance without inventing decisions or downgrading pre-existing classifications. Added dated loading/empty acceptance pointers and Metric's composition-scoped decision; reused existing ledger statuses. The guidance-only fixture is assembled from the original archive with ten allowed changes and no coordinator findings; 186 links and 20 wiki pages pass. One overlong ledger row was caught and corrected before freezing the final fixture. No treatment has run; private retention remains bounded by 2026-09-16.
- Began S2 with the ten-role shared typography owner and unchanged usage descriptions. Fresh before/after light/dark phone/desktop captures show twelve byte-identical image pairs and equal computed type/geometry. The wider suite caught a legacy exact-four-keys assertion; its compatibility scope is now explicit, and final source-bound captures were rerun. Full unit 1164/1164, helpers 82/82, mounted 4/4, app/e2e types and lint pass. Program remains active, not a completed integration gate or new design acceptance. No kit change was justified; fixes belong at the existing code, test and documentation owners.

## 2026-09-09 — Consolidation implementation verified; comparison pending

- S1–S3b are implemented: one semantic owner accounts for 61 original modules, ten type roles are shared, twelve aliases preserve existing theme values, thirteen layout relationships retain explicit owners, and all 45 catalog entries expose a lossless five-question view. No component-default or production adoption changes. The [capture record](../plans/design-system-consolidation-capture.md) owns fresh proof and exceptions.
- The final review pair's confirmed CI coverage, hygiene-bypass and engineering-register findings were corrected. Unit gate 1172/1172, helpers 84/84, read-only browser 159/159 and app smoke 58 passed/one existing fixme are green. Final browser runs had no retries. Earlier baseline/assertion drift, observer timing, watcher setup and asynchronous test teardown failures remain disclosed; assertion counts alone did not prove a green process exit. Twelve final normal-viewport captures were inspected. Remote CI remains unobserved.
- All six original fresh-agent outputs are retained, with no repairs or feedback. All three compositions pass the same mounted behavior checks; the [evaluation record](../plans/design-system-consolidation-evaluation.md) discloses invalid setup, frozen-input differences, usage and privacy limits. This does not establish better first-pass design quality. Independent comparative judgment is next; the Downloadable resources pilot is source-inspected but not approved or implemented. Raw private evidence expires by 2026-09-16 unless explicitly extended.
- Self-improvement conclusion: no kit rewrite or new ritual is justified by this slice. Concrete fixes belong in the existing owners, capture/tests and one lab area guide. The expensive full-sheet check remains checkpoint evidence, not a requirement for every small lab edit. Preserve source inspection and whole-composition judgment; do not mistake cleaner docs or passing checks for acceptance. No commit or push.

## 2026-09-09 — Comparative review reconciled

- [Reconciled the independent comparison](../plans/design-system-consolidation-reconciliation.md) against original receipts, read events and fresh unchanged-candidate checks. Unit 12/12 and supplemental browser 12/12 pass. The predicted Quartz failure did not reproduce; no shared stubs added. Cedar's limited reads did not return the relevant authority, so a whole-catalog read count was not evidence it ignored a received decision. Mobile feedback shifts are 44px, not the report's 60/56px.
- Added an exact catalog-search hint, distinguished lab chrome from the accepted scoped repeated-field pattern, clarified width ownership, and removed duplicate live reference status lists while preserving scoped acceptance evidence. The Intent review caught this pass initially trusting the specimen's stale provisional badge; the corrected guidance cites the higher-authority acceptance decision. Corrected the project router's review-reconciliation link: the similarly named kit-refresh skill is not a review tool. No kit change, component/default change, candidate repair, new experiment or table implementation.
- S4 has a mixed result, not its promised positive reliability proof. Keep the useful owner consolidation; do not promote the synthetic specimens or silently clear the production-pilot gate. New guidance has static/source support, not a new causal performance result. Original evidence and the independent report remain unchanged; private retention still ends by 2026-09-16. No commit or push.

## 2026-09-09 — First table-family candidates

- Added shared cell examples and two bounded Portfolio-derived anchors: ordinary Index/Yield positions and pending/ready withdrawals. Direct production source inspection and fresh fixtures informed the candidate; the outside report remained a discovery map. Holdings supplied the stacked-record precedent, not a universal responsive policy. Current Review now targets only these anchors; transaction and rich-record acceptance did not change.
- Reused DataTable and canonical cell/action owners. Explicit name wrapping and accessible table metadata are opt-in shared additions, recorded for project-closeout engineering review. Product sources, defaults, wallet/chain logic, financial calculations and SDK contracts remain unchanged. Source-sidebar/receipt controls are explicitly non-executing lab simulations.
- Visual inspection found overlap that ordinary overflow checks missed. The regression now checks intersecting text bounds and fails when the old layout is restored. Independent review caught lost intermediate-width sorting, missing accessible direction, coarse countdowns and an omitted Action header; fixes and affected-axis rechecks are complete. [Evidence](../plans/design-system-table-family-evidence/first-slice.md) distinguishes the 51 focused tests, 7 source/viewport checks, retained detail checks and production observations from unproved live transactions and human design acceptance.
- Process conclusion: no new skill or generic enforcement layer. Check visible behavior at every projection band, not just DOM control counts; inspect actual text relationships in addition to overflow. These checks belong in the existing transfer brief, lab guide and test seam. Density, mobile hierarchy and readiness emphasis remain human-review questions. No commit or push.

## 2026-09-09 — Shared chain-badge optical trial

- User authorized the core change after comparing visible artwork rather than outer border boxes. All four ChainBadgedLogo sizes now keep the colored badge flush below and 1px beyond the token at the right. Badge dimensions, the 1px surface separator, identity text spacing and stacked-logo geometry are unchanged. Legacy production wrappers remain outside scope.
- Focused unit 21/21 and source-bound browser 6/6 passed; rendered light/dark single-line, two-line and compact identities inspected. App/E2E types and scoped lint pass; independent Intent/Risk reviews found no blocker. Visual acceptance remains with the user; shared-default engineer review is recorded for project closeout. No commit or push.
- The subsequent user-approved border trial uses 2px for xl and 1.5px for smaller badges, keeping outer dimensions and compensating offsets. Live Chrome on the user's preview renders the intended thickness and colored-edge geometry. Headless Chromium rounds fractional borders even under scale-2 emulation; an initial exact check exposed this, and bounded half-pixel expectations now cover both densities. Six table cases and two high-density identity cases pass; no component workaround was added for emulation.

## 2026-09-09 — Table Source and column rhythm

- Added opt-in neutral contextual Link/InlineAction for Source; navigation keeps an external arrow and local details a chevron. Defaults remain unchanged. Source uses body type on desktop and supporting type in mobile identity. Desktop Withdraw is compact, mobile remains touch-sized; independent column gutters now total 24px with unchanged edge insets.
- Related tables share spacing, not a forced page-wide Value axis: the two current anchors place different jobs after Value. No empty-track grid or dense variant was introduced. Exact shared financial axes remain a future page-composition decision.
- Initial mounted checks failed before implementation. Initial browser checks and independent Light/Dark review caught processing width growth; local 128px desktop/144px mobile reservations and matching skeletons fix it without modifying Button defaults. All 36 focused mounted tests and eight browser cases pass; screenshots inspected in light/dark and across projection boundaries. App/E2E types and focused lint pass. The broad scope command attempted the accumulated 425-file tree and stopped at pnpm dependency-verification setup; no full-repository gate is claimed or required for this bounded lab iteration. Shared opt-in adoption is recorded for project-closeout engineer review; no commit or push.

- Subsequent user review rejected that width reservation: compact actions should wrap content. Removed the local desktop/mobile minimum widths and kept stable alignment in the action column. The regression now checks intrinsic width and a stable right edge/height across idle, processing and withdrawn states. Its negative control measured 128px against approximately 91px of required content/padding. The earlier test proved the wrong design invariant; width changes caused by a real label/icon change are legitimate. Shared Button defaults remain untouched.

## 2026-09-09 — 32px identity gap trial

- User requested 12px after comparing the existing 8px logo-to-copy gap. ChainBadgedLogo exposes its size; EntityIdentity applies 12px to a direct xl mark and retains 8px for smaller/custom/stacked marks. The table loading placeholder mirrors the gap. No badge geometry, typography, text-stack rhythm or legacy wrapper changes.
- Focused mounted tests pass 23/23 and the initial source-bound table/identity browser matrix passes 8/8. Independent Intent/Risk review found no component blocker; the risk review identified an over-broad gap oracle, corrected to use direct-mark classification with an explicit unclassified fallback check. The change remains a visual trial, with shared engineering review deferred to adoption.
- Corrected oracle and manual-anchor checks pass 3/3, covering light/dark manual column transitions and the shared desktop identity specimens. Rendered table, single/two-line identity and mobile manual examples inspected; E2E types, scoped lint, formatting and wiki checks pass. No commit or push.

## 2026-09-09 — Combined Withdrawal region

- User authorized replacing the separate Progress/Action columns with one Withdrawal column. Countdown replaces the disabled button; Withdraw alone conveys ready; submitted/receipt previews replace the button with processing/completion feedback. Mobile uses the same sequence. The feedback region preserves height/right alignment without stretching the ready button. Keyboard activation moves focus to that persistent region, with polite state announcements.
- Lifecycle and readiness remain separate fixture inputs, and live wallet/network/prepared-call conditions are not rewritten. This composition is appropriate for alternate phases of one job, not a universal requirement to hide independent status/actions. Shared status vocabulary and Button defaults stay unchanged.
- Mounted RED→GREEN, table/catalog 46/46, six light/dark desktop/phone browser cases, E2E types and scoped lint/format checks pass. Inspected initial, processing and withdrawn captures; updated current-review claims for the combined region and the preceding 12px identity-gap trial. No commit, push or production migration.

## 2026-09-09 — Table header spacing trial

- Removed the header-to-body divider from the two table-family anchors and their inherited header minimum height. Header bottom padding is 8px with unchanged 16px first-row top padding; mobile record separators and global DataTable defaults remain unchanged.
- Browser RED proved the old border, then all six light/dark desktop/phone cases passed with measured header height and insets, including constrained containers. Inspected both tables and mobile captures; E2E types and scoped lint pass. This remains a local visual trial, not production adoption.

## 2026-09-09 — Collapsed table record hierarchy

- User review exposed mixed financial-fact treatments and an orphaned withdrawal footer that previous containment checks had not caught. Collapsed Index/Yield positions now put consistently stacked Balance/Value facts below identity; withdrawals group availability/action with identity above those facts. Existing breakpoints, desktop tables, typography and production behavior remain unchanged. Content-driven wrapping is intentional, including left-aligned availability when it moves below identity.
- Browser RED reproduced the old hierarchy, then the corrected six-case light/dark matrix passed at phone, constrained and intermediate widths, with both position families and long values captured explicitly. The expanded test initially retained a desktop-only right-edge assumption and Index-only logo fixtures; both oracles were corrected without changing the UI to satisfy them. Mounted tests and app/E2E types pass. Inspected full compositions, loading and lifecycle feedback, not just bounds. Human visual acceptance is still separate; no commit or production adoption.

## 2026-09-09 — Identity supporting-line investigation

- Investigated the reported larger visual text gap with an external icon. Both the owned browser check and the live Chrome preview measured 20px supporting rows, identical text offsets, and no gap between the name/supporting boxes; the icon is 14px. Neither the initial height probe nor the stronger identical-text baseline comparison reproduced an icon-induced spacing defect. No component or spacing change was made from an unconfirmed cause.
- Added characterization coverage comparing actual text position with an identical plain-text probe, alongside the line-height assertion. This protects the existing relationship; it is not a RED→GREEN fix or a claim that differing glyphs and colors look optically identical.

## 2026-09-09 — Compact collapsed withdrawal actions

- User explicitly chose compact Withdraw actions for phone/constrained rows as well as desktop. The local action and feedback region now use 32px height; the button shares the logo's horizontal centerline. This is a bounded table-action choice, not a change to shared Button defaults or other mobile flows.
- Mounted RED→GREEN passes 13/13; six light/dark browser cases verify actual 32px height and logo-center alignment across phone, desktop and constrained containers. Inspected fresh 320px, 375px dark and constrained captures. E2E types, scoped lint and formatting pass; production transaction behavior is unchanged.

## 2026-09-09 — Table header alignment and collapsed insets

- User authorized title-baseline section navigation, shared standalone Link hover underlines, and equal 24px vertical/horizontal padding for divided collapsed records. Desktop row padding remains 16px; internal fact spacing, link geometry, navigation and keyboard-focus behavior stay unchanged. Shared Link hover feedback is recorded for deferred engineering review before production adoption.
- Browser RED reproduced all three previous treatments; the six-case light/dark matrix then passed with hover, baseline alignment and row-inset assertions. Inspected desktop Yield header, phone positions and constrained withdrawals. Mounted tests pass 32/32; E2E types, scoped lint, formatting and wiki/diff checks pass. No production migration or commit.

## 2026-09-09 — Constrained table sorting

- Replaced constrained position column headings with one canonical-menu Sort by control for the six existing fields and ascending/descending direction. The optional DataTable toolbar receives the same TanStack instance as desktop headers, avoiding duplicated sort state. Withdrawals have no interactive sort headers to preserve, so their constrained Token header is removed without adding sorting. Existing DataTable callers and production behavior are unchanged.
- Corrected the previous inset pass's missed divider offset: CSS row padding was 24px but the absolute separator still used 16px. Both collapsed record types now use the matching 24px offset, with a browser assertion against actual separator geometry.
- Mounted RED was missing-menu behavior; menu/desktop synchronization and existing DataTable tests pass 24/24. App/E2E types and scoped lint pass. Independent scoped Intent/Risk review found no confirmed blocker. Broad scope verification remains unavailable because its package-manager preflight attempts an install; this bounded change uses explicit targeted receipts, not a claimed whole-tree gate. Shared toolbar API is listed for deferred engineering review before adoption.
- The six-case light/dark browser matrix passes at 320/375/1400px, including constrained and intermediate containers, menu field/direction selection, return focus and popup containment. Inspected opened mobile menus, constrained Yield and header-free withdrawals. Formatting, wiki lint and diff checks pass; no commit or production adoption.

## 2026-09-09 — Desktop header breathing room trial

- User requested trying 16px rather than 8px below desktop column labels. Together with the unchanged 16px first-row top padding, this matches the 32px combined padding between desktop rows. Constrained headers remain hidden, and their Sort by control and record spacing are unchanged. This is a local table-family visual trial, not a shared DataTable default.
- Both light/dark desktop browser cases pass, including constrained/boundary projections; inspected fresh table captures. Scoped lint, formatting and wiki/diff checks pass. No commit or production migration.

## 2026-09-09 — Table hardening and Holdings preparation

- Corrected lab preview ordering: TanStack sorts the complete eligible list before the optional five-row render limit. Show all appends the remaining rows; production's older slice-before-sort behavior is explicitly an adoption difference. Existing DataTable defaults remain unchanged.
- Narrow sorting retains meaning by showing the selected supporting metric, including its loading skeleton. Local projection focus follows active sort/identity/source/withdrawal controls without pulling back deliberately blurred or outside focus. Independent review's stale-focus finding reproduced RED in Chromium, then passed after clearing visible-control blur. An immediate double menu click in the loading test raced dismissal; waiting for close/return focus and reopening by keyboard made the sequence explicit.
- Mounted table/DataTable/hygiene checks pass 22/22, types and scoped lint pass, and ten source-bound browser cases pass across themes and layout boundaries. Current evidence replaces stale review prose, while original records/hashes remain historical. Header spacing remains provisional; no production migration or commit. The broad inventory test's not-started expectation was stale and now pins provisional/non-adopted table status.
- Directly inspected Exposure/Collateral owners and prepared the next bounded brief: underlying versus held-token market cap, grouped crypto and exchange-qualified stocks, bridge information, independent loading and expansion. Sorting/tab-reset, bridge focus and expanded/exchange fixture interactions remain to inspect before new lab composition. Research is a map, not the implementation specification.
- Final sequential full gate passed: pnpm typecheck + pnpm lint + pnpm test:run, 1190/1190. Earlier sandbox watcher failure cleared with native watcher access; an unchanged transaction test timed out under concurrent browser/full-suite load but passed sequentially without timeout changes. Final source-bound browser rerun passed 10/10; retained 17 attached images, verified all 33 old/new image hashes and 183 scoped links/anchors. No workflow rule or new enforcement layer added: the concrete fixes belong in the existing implementation, tests and evidence owners.

## 2026-09-09 — Holdings table-family candidate

- Direct production interaction checks preceded implementation: crypto desktop/mobile expansion, sorting/tab reset and stock/crypto bridge dialogs. Three cases passed; original bridge return focus failed as an observation, not a production fix. Source imagery includes explicit logo fallbacks.
- Added local Exposure/Collateral cells and composition alongside Portfolio. Native versus held-token capitalization remains distinct; 27 literal rows and bridge metadata match source replay JSON. Synthetic pressure controls cover zero/unavailable, new-asset help, independent loading and long content. Ordinary type/identity/inset owners carry forward; no dense variant or universal Table API.
- Independent review found two focus gaps, both confirmed/fixed: keyed help across projections and stable return fallback after a modal's source row disappears. The late-row modal failure reproduced before correction. Active late-row links instead preserve expansion when narrowing. Existing Portfolio focus/blur/menu tests remain green.
- Final focused unit 56/56, source-bound candidate/Portfolio browser 8/8, app/E2E types and scoped lint/format pass. Retained 26 selected candidate screenshots plus six source observations; the Holdings brief owns current limits and handoff. No full repository gate was claimed for this lab-only checkpoint, per the project override. No new workflow rules; source fidelity, visual judgment and production adoption remain separate. Engineer review remains required before adopting the bridge reference/adapter seam.

## 2026-09-11 — Discover closed-trigger refinement

- Added an opt-in TokenStackTrigger rather than altering ordinary Button or logo-stack defaults. Its 24px artwork sits in a balanced 44px control; the wide Basket header aligns with the artwork, not the outer border. Ordinary tags retain one line with a local 1152px table threshold. Row-hover reveal and direct hover/keyboard focus remain distinct.
- Sizing RED reproduced 48px instead of 44px. Final browser 9/9, focused unit/catalog 46/46, types, lint/format and evidence checks pass. Independent review's keyboard-focus oracle gap was fixed by moving the pointer away and entering by Tab. See the Discover brief for retained evidence and remaining visual review.
- The vertical expanded list is explicitly pending replacement, not accepted. Preserve the original scrolling strip's speed, delay, easing and loop behavior in the next pass; accessibility improvements do not justify silently substituting a different visual interaction.

## 2026-09-11 — Discover horizontal hover strip

- Replaced the rejected expanded list with a local horizontal strip, retaining Discover's 72px/second cruise and 1.2-second acceleration. The homepage's separate 18-second loop is unchanged. Equal-width repeat padding removes the source seam discrepancy; pointer transit, pause, manual keyboard/touch and reduced-motion access are explicit additions.
- Hover RED reproduced the missing popup. Final browser 11/11, focused unit/catalog 52/52, types and lint/format pass; 40 captures and motion samples are retained in the Discover evidence index. The measured cruise was 71.993px/second. No physical-device or screen-reader proof is claimed.
- Review corrected keyboard entry and stale pin state across responsive projections. Browser checks caught Radix's hover-then-click focus suppression; explicit Escape/resize restoration excludes outside interactions so the next control retains focus. The earlier trigger geometry remains intact. Human motion/visual review and production-adoption approval remain separate.

## 2026-09-11 — Discover strip overflow refinement

- Added conditional 12px surface-colored edge fades; start/end content is not faded once fully reached. Fitting baskets render a single stationary sequence. A clearly labeled synthetic Short basket preview exercises one asset at 100% without mutating source fixtures.
- Browser checks caught manual mode surviving a quick reopen during the closing animation; local close lifecycle now resets it. Touch verification waits for native scrollend before keyboard input. Final scoped browser 7/7, focused unit 5/5, types and lint/format pass; source-bound captures are linked from the existing evidence index. Low-risk self-review only; no shared-default or production changes. Opening delay, acceleration and speed remain unchanged.

## 2026-09-11 — Basket count padding

- At the user's request, count-ended TokenStackTrigger controls use 16px trailing padding instead of 12px. Leading logo inset, 44px height and logo-only controls are unchanged. Three scoped browser cases and two unit tests pass; fresh desktop/phone images are retained in the Discover evidence index. No production adoption or ordinary Button default change.

## 2026-09-11 — Whole-row navigation styling

- Removed redundant identity hover underlines from Discover and Index/Yield position rows, plus hover-only title accents from rich governance/rebalance records. Governance gains surface hover in place of its former title-only cue; rebalance selected styling remains. Native links, keyboard focus, independent explorer/source/bridge links, section navigation and basket actions are preserved. Ordinary shared Link defaults and production remain unchanged.
- The pre-change browser case reproduced the targeted underlines/title accents. Seven final browser cases pass, including 390/1400 light/dark and basket interaction checks; 22 focused unit tests pass. Retained hover/focus screenshots are linked from the first-slice brief and evidence index. Self-review covered all lab table/record owners, distinct-action exceptions, keyboard focus and scope; this does not accept the wider rich-record design.

## 2026-09-11 — Discover mobile card lab

- Brought the Home/Discover card pattern into the narrow Discover lab, reusing the actual production chart, ticker, timing and visibility behavior. Compact/full-chart previews share the same 30-day data; the lab wrapper applies accepted card spacing, type, shape and identity owners. One DataTable instance retains sorting while mounting only the table or card list. No production consumer is migrated.
- Dark/Light review corrected interrupted name/market hierarchy, missing weights rendered as zero, chart reduced motion, and outside-click focus theft on resize. Direct screenshot review also replaced the legacy dark ticker skeleton with canonical loading material at unchanged geometry. Browser REDs and scoped rechecks cover these findings; a local 4px toolbar inset contains the sort control's expanded target.
- Final verification: 21 source-bound lab browser cases, 7 existing Home/Discover smoke/flow cases and 51 focused unit/catalog tests pass; app/E2E types, scoped lint/format and documentation checks are green. The evidence index retains 24 current captures, not duplicate intermediate records. Compact/full chart preference remains human review; shared alternative-renderer and chart-animation opt-ins require engineer review before adoption.

## 2026-09-11 — Discover connected card surfaces

- User-directed local trial: steady card-colored square cards/media, a list-owned 2px secondary surround and 2px seams. Internal spacing and production chart/ticker behavior stay unchanged. Local production ties its mobile gradient activation to transcript-enabled highlights; Discover disables that flag. The accepted Home card and production defaults are untouched.
- Eight focused browser cases pass, including surface geometry across compact/full, long, missing and loading states, sorting/focus and motion. The old surround failed the pre-change check. Types, scoped lint/format and wiki checks pass; six source-bound captures and a direct 390px live inspection support the existing mobile-card brief. Low-risk self-review; visual preference remains human review.

## 2026-09-11 — Card framing clarification and inset diagnosis

- The user clarified that the secondary surround/gaps were only contrast for inspecting the cards, not a saved container preference. Corrected the active review and mobile-card brief; page-host composition remains undecided. Historical preview receipts do not establish a product container contract.
- Direct measurement of the current constrained preview found 32px card-edge-to-title/top-row spacing (8px shell + 24px inner), versus 24px in production source (4px + 20px). The gradient had obscured ownership of the outer shell. Recommended 24px total visible inset; diagnosis only, no card layout or production change.

## 2026-09-11 — Discover card inset correction

- Applied the authorized lab correction: removed redundant 8px shell padding, leaving 24px total header/Market Cap edge insets; footer owns its 24px bottom inset. Chart/ticker edge geometry stays region-owned, with no shared Home/default change. The secondary surround remains inspection-only framing, not a saved preference.
- The pre-change geometry test failed with five 32px measurements against 24px. All eight final card browser cases pass, including narrow long content, both chart placements/themes, loading parity, sorting/focus and ticker motion. Types and scoped lint/format pass. Six source-bound captures and a live phone check were visually reviewed; receipts are in the existing mobile-card brief. Low-risk self-review; no production adoption.

## 2026-09-11 — Independent table-family review checkpoint

- User authorized a local checkpoint and isolated Claude review handoff. The snapshot includes the accumulated foundation/table work and retained evidence, not production adoption or blanket design approval. The handoff prioritizes existing-family completeness and Earn source evidence, with report-only output in a detached worktree; contrast-only card framing stays outside product preferences.
- Fresh checks: 64 selected unit/catalog tests, eight Discover card browser cases and app/E2E types pass. The initial unit run caught an assertion tied to superseded radius prose; it now checks the actual provisional authority status. The bounded receipt is linked from `docs/plans/design-system-table-family-independent-review-handoff.md`. No full repository or CI claim. Candidate-file privacy checks found no matching environment values, private-file hashes or credential-pattern hits; ignored private/runtime files are excluded.

## 2026-09-11 — Table-family baseline and Earn source preparation

- The combined existing-family browser baseline passed 38/38 without retries; focused unit/catalog checks passed 64/64. Retained eight phone captures and bounded receipts. No app source or shared default changed. TEST_MAP no longer contradicts its own existing desktop/stacked focus-transfer coverage.
- Direct Earn inspection identified governed-asset disclosure, distinct APR/APY meaning, wallet-dependent cells and denomination questions; the public DAO response supplies real relationships. The source-capture prototype incorrectly combined multichain list data with single-chain replay and hit fixture failures. After the retry guard rejected further execution, preserved the unverified prototype outside the test suite and reassessed separate list/wallet/drawer captures. No existing test disabled; no Earn browser pass or candidate implementation claimed. User approval is needed before resuming captures; see `docs/plans/design-system-table-family-earn-preparation.md`.

## 2026-09-11 — Earn source verification and lab candidate

- User approved the narrowed restart. Separate exact-boundary source checks passed 18/18 with 24 captures: populated Index/Yield lists, Index loading/filtered-empty/wallet recovery, governed disclosure/FAQ and both real drawer entry boundaries. Deprecated hyUSD was correctly excluded; active bsdETH supplies the Base Yield example. Empty yield-feed replay remains a rate caveat, not a production defect.
- Added a bounded Earn lab candidate using the existing DataTable/cell APIs, ordinary typography and explicit non-executing actions. Independent review caught Empty unmounting the sorting owner, source-label drift, missing desktop dividers and absent unavailable-amount coverage. All were corrected and rechecked. Keyboard disclosure focus and a 23px loading-height shift received failing-before/passing-after checks; recovered disclosure observation also remains functional.
- Fresh evidence: combined existing-family/Earn browser 51/51, then final Earn-only hardening 15/15 with 52 captures; focused unit/catalog 70/70; app/E2E types, scoped lint and formatting pass. Repository lint passed with existing warnings. This follows the bounded design-system checkpoint cadence, not a full repository or CI claim. [Receipt and review agenda](../plans/design-system-table-family-earn-preparation.md#completed-checkpoint-and-next-review).
- Earn remains human-review-required; source denomination/rate-period questions require engineer review before adoption. Existing Discover card and Portfolio header trials remain open. No production/shared-default edits, new commits or pushes; the preview and isolated Claude worktree were preserved. The setup lesson stays in the local transfer brief; no workflow-kit expansion was needed.

## 2026-09-11 — Independent table review reconciliation

- Fixed the two confirmed lab defects with failing-before/passing-after tests: Holdings headers now honor explicit descending-first columns without changing other families; Discover card accessibility/tooltip uses its visible 1M period. Source and lab checks assert concrete leader/laggard order, not just a changed first row.
- Fresh unit/catalog/shared-owner checks passed 72/72, affected browser checks 46/46, then final mobile direction/field/767–768px continuity checks 2/2 in both themes. App/E2E types, scoped lint/format, wiki, links, hashes and diff checks passed. A test-only menu-close race was corrected with observable popup lifecycle waits; the interrupted diagnostic run is not counted. No full repository/CI claim.
- Intent/Risk reviews found no implementation blocker and identified the continuity coverage gap now pinned. The [reconciliation](../plans/design-system-table-family-reconciliation.md) records production adapter contracts, viewed-account withdrawal ownership requiring engineer review, page loading/error differences and remaining fixture/copy work. Claude's frozen report predates the current Earn candidate; Earn's ascending-first new-field behavior actually matches production.
- Next is human visual review of Earn and the remaining mobile Discover card trials. No production/default/token changes, transactions, commits or pushes. pnpm's dependency-purge guard was left intact; direct checks used the existing runtime without installing dependencies. No workflow-kit change was justified.

## 2026-09-11 — Earn descriptive-column grouping

- User-directed local refinement puts Governs after Gov. Token and before TVL in both Index and Yield. Optional wallet values remain with numeric metrics; proportions give Governs more room without changing number alignment, phone composition, sorting or actions. Four mounted order checks failed before the change; Earn units passed 9/9 and browser checks 15/15 afterward, with 14 retained captures inspected. App/E2E types and scoped lint/format passed; low-profile self-review, no full gate/CI claim. [Scope and evidence](../plans/design-system-table-family-earn-preparation.md#september-11-desktop-column-grouping-follow-up). Production and shared defaults remain unchanged.

## 2026-09-11 — Holdings overview-width preview

- Replaced the lab's binary width toggle with Full width / DTF overview 836px / Mobile 390px. The estimate swaps the existing 88px navigation footprint for the candidate's 72px overlay-expanding rail while retaining the real 1400px shell, 480px trading/about column and 12px frame budget. Source rendering confirms the old 820px card. This does not adopt the navigation demo's placeholder grid or freeze future Overview geometry.
- Missing-control RED → Holdings units 7/7; browser 12/12, including both PHOTON tabs, long names, loading, width/sort continuity and source geometry. Twelve captures inspected; app/E2E types and scoped lint/format pass. Low-profile self-review and bounded verification only; production and shared defaults unchanged. [Basis and receipt](../plans/design-system-table-family-holdings-slice.md#september-11-overview-width-preview).

## 2026-09-11 — Non-navigable table bottom insets

- User-directed correction separates final card inset from inter-row rhythm: Holdings adds 12px below desktop rows and Withdrawals adds 8px below its desktop section, producing 24px at the bottom in each case. Mobile and clickable Position rows remain unchanged; Discover/Earn and rich records already provide 24px. No shared/default/production changes.
- Geometry REDs measured 12px and 16px rather than 24px. Existing browser suites passed 15/15 afterward; twelve captures inspected, app/E2E types, scoped lint/format and wiki/diff green. Low-profile self-review covers intent and correctness/product; the cumulative diff's size signal belongs to earlier work, not this two-class local correction. [Receipt](../plans/design-system-table-family-holdings-slice.md#bottom-edge-balance--2026-09-11).

## 2026-09-11 — Earn zero tone and Holdings name room

- Earn wallet zero values now use supporting-text color without changing typography, TVL/rate styling or row actions. The local opt-in checks the value rather than rounded text; missing/loading remain distinct. Holdings reserves 46% for names and 14% for Weight, adding about 50px at overview width while preserving the two rightmost metric axes and all padding. Inspection found no hidden name-width cap.
- RED: two Earn zero-tone unit assertions and Lumentum's 48px wrapped name versus the desired 24px line. GREEN: 20 unit and 20 browser cases; twelve inspected captures, app/E2E types, scoped lint/format and wiki/diff. Low-radius intent/product/correctness self-review; cumulative size reflects previous work. No production/default changes. [Receipt](../plans/design-system-table-family-holdings-slice.md#name-column-room-and-earn-zero-tone--2026-09-11).

## 2026-09-11 — DeFi Yield added to the Earn lab family

- The prior scope intentionally prioritized governance/staking, with DeFi only a contrast. The new third selector option retains pool, project, chain, APY/base/reward and TVL plus independent pool/DefiLlama links. Native keyboard links replace pointer-only source targets; rows do not pretend to open governance drawers. Wallet-only controls are excluded while retaining their preference on return. Values are illustrative over CMS-backed identities, not live yield facts.
- Source desktop/phone replay and the combined 22-case browser run passed. Dark/Light review reconciled touch targets, the shared menu's independent direction, a same-name tooltip test selector and intentional help-hit-area geometry. Visual inspection added a 24px loading-height regression and aligned right-hand placeholders. Full type/lint/unit gate passed 1227 tests after allowing native watcher access; no shared defaults, production changes or commits. Final scoped geometry and human-review boundary are recorded in the [slice](../plans/design-system-table-family-defi-slice.md).

## 2026-09-11 — Draggable Ask Reserve AI launcher

- The floating Ask Reserve AI launcher now supports pointer dragging inside the viewport without opening the panel; ordinary activation still opens chat, and arrow keys provide a non-drag repositioning path. The behavior stays local to Register's existing `dtf-chat` compatibility wrapper because package 0.0.7 exposes no launcher-drag API. Desktop mouse and mobile touch/CDP Playwright coverage pins movement, edge clamping, touch cancellation, keyboard movement, and click/Enter activation.

## 2026-09-12 — Compact wrapping identity-title trial

- Added a provisional opt-in 16px/20px item-title recipe with a 24px single-line floor across wrapping table identities. The reviewed type roles and shared defaults stay unchanged. Dark/Light found a symbol-only withdrawal caller behind the common identity helper; explicit ordinary leading now preserves that exclusion. The lesson is to audit helper callers against role-based exclusions, not infer uniform meaning from component reuse.
- Shared and 836px browser REDs preceded implementation; the withdrawal exclusion also failed before repair. Final focused units 123/123 and browser 40/40 pass, with app/E2E types and scoped lint/format/wiki/diff. The initial broad unit run hit the known TokenLogo teardown race and is not counted as green. Rendered overview/phone and long/loading captures were inspected; visual acceptance and engineering adoption review remain pending. [Scope and receipt](../plans/design-system-table-family-holdings-slice.md#compact-wrapping-identity-titles--september-12-trial).

## 2026-09-12 — Table-family review checkpoint

- User authorized a local checkpoint for an independent audit of the newer Earn/DeFi candidates. Accumulated Holdings/Discover/Portfolio refinements and their evidence are retained; no new Auctions work, production adoption or design acceptance. Current Review still points to Discover; the checkpoint supplies the direct Earn audit route. The old overnight plan is explicitly historical.
- Fresh affected units/helpers passed 211/211 after an unchanged native-watcher rerun resolved the sandbox failure; browser checks passed 85/85 without retries, and 16 current captures were inspected. App/E2E types, scoped lint, wiki and diff checks passed. The pre-existing base-fixture format warning, local runtime versions and visual/production limitations are explicit in the [checkpoint](../plans/design-system-table-family-checkpoint-2026-09-12.md). Intent/Risk review found no blocker. No push or user-preview restart.

## 2026-09-12 — Auctions browse records prepared for review

- Replaced the lab's invented auction identities, placeholder links and false selection/action cues with snapshot-joined CMC20 records, explicitly illustrative outcomes/activity and independent state/phase/390px controls. Accepted square record hierarchy remains; the 512px browse split and 28px lifecycle pill remain provisional. Governance composition, production and shared defaults are unchanged.
- Dark/Light review reconciled lost visible timestamp detail and a positioned provenance region intercepting whole-record clicks. Both received failing-before/passing-after checks; the proposer remains an independent native link and its “by” prefix wraps with it. Lesson: assert physical hit regions, not only correct hrefs or keyboard reachability.
- Final browser slice passed 12/12 without retries; focused units 55/55, app/E2E types and scoped lint/format pass. Ten browser cases carry matching source guards; two legacy cases ran on the same owned preview without individual guards. Retained 24 hashed captures, visually inspected eight; wiki/links/hash/diff checks pass. Runtime/bootstrap caveats and earlier harness failures are preserved in the [slice receipt](../plans/design-system-auctions-browse-slice.md#evidence--closeout).
- Ready for human record review only: vocabulary, timing, outcomes, provenance and density. Detail/actions, route-backed selection and live/error adapters remain separate engineering work. No full repository/CI claim, transactions, commits, pushes or user-preview restart. No workflow-kit expansion was justified.

## 2026-09-12 — Auction launcher-wallet viewpoint

- Added the user-requested connected-launcher fixture independently of auction phase. Restricted records use the canonical actionable Ready to start pill for the authorized viewer while retaining both countdowns; ordinary viewers, ongoing/permissionless phases, history and destinations remain intact. The control simulates a wallet and assumes readiness only in ready-to-start fixtures; real permissions, hybrid prerequisites and transactions remain outside this browse slice. Earlier heading/background suggestions were not applied without authorization.
- Projection RED failed on waiting versus actionable. Final focused units 11/11, browser 10/10, app/E2E types, scoped lint/format and wiki/links/hash/diff pass. Four source-bound phone/desktop light/dark captures inspected. A sandbox listener denial and a new test's innerText/textContent mismatch were resolved before the final run; no product workaround or user-preview restart. Low-profile self-review and [follow-up receipt](../plans/design-system-auctions-browse-slice.md#connected-launcher-wallet-follow-up--september-12); no commit or adoption claim.

## 2026-09-12 — Repeated auctions in the browse preview

- Added independent Auctions run fixture choices 0/1/2. Two ended auctions now show the third ready or ongoing, with a supporting count, without inventing a final total or treating the gap as failure. Source numbering and rebalance completion are separate; metrics-library rounds are not auction ordinals. Default geometry, history and phase/wallet behavior are preserved. Production adapters and prior visual-layout proposals remain outside this pass.
- Three projection REDs preceded the change. Final focused units and browser cases both pass 14/14, plus app/E2E types and scoped lint/format; four source-bound phone/desktop light/dark captures inspected. The new keyboard test initially confirmed before Radix's deferred End-key focus move; waiting for actual focused options fixed the harness without sleeps or product changes. Low-profile intent/correctness/product self-review; [receipt](../plans/design-system-auctions-browse-slice.md#repeated-auction-follow-up--september-12). No transactions, commits, production adoption or user-preview restart.

## 2026-09-12 — Auctions browse composition refinement

- User authorized a coherent visual recommendation. The lab now trials 640px browsing, quiet section labels, card-colored active content and continuous recessed history. Active timing pairs have small unframed icons; previous auctions and provenance remain supporting information. History uses parallel outcomes when space allows and inline pairs when constrained. Shared Metric/Lifecycle defaults and Governance stay unchanged.
- Readiness no longer changes when a simulated launcher connects: access is a separate visible line. A source-worded price warning replaces Ready before launch, but never overwrites an ongoing auction. History-only and last-content loading fixtures were added. Real readiness/price/permission/completion adapters remain engineer-owned; this is not production preflight validation.
- Final 51/51 focused units and 24/24 browser checks pass without retries, plus app/E2E types and scoped lint/format. Retained 28 source-bound captures; seven final light/dark phone/desktop states inspected. Dark/Light found no blockers. The earlier broad smoke run exposed obsolete waiting/inset expectations; their replacements assert the revised state/access and actual content axis, without altering Governance checks. Runtime and scope-command invocation errors are recorded in the [receipt](../plans/design-system-auctions-composition-refinement.md#verification-receipts-and-reproduction), not presented as gate passes. Existing 3005 preview preserved, no commit or production change. No workflow-kit expansion justified.

## 2026-09-12 — Portfolio missing-table preparation

- User redirected unattended preparation from Governance to missing Portfolio tables. The [coverage audit](../plans/design-system-portfolio-coverage-review.md) maps all ten mounted table sections: three have bounded Portfolio candidates; seven, including current stakes and vote-locks, do not. Earlier research knew these rows; the narrower first-slice brief explicitly excluded them. Earn's wallet column and the transaction lab do not count as owned-position table coverage.
- Recommended the owned governance-position pair next, then rewards; RSR is a small reuse check, while Voting Power and activity need distinct later slices. Recorded share/underlying denomination, API-to-live valuation, API sort/filter versus displayed values, one/many governed links and Modify destination/context as source-transfer requirements, not newly accepted adapters or production fixes.
- Fresh existing tests pass 79/79 in five files with one worker; current lab accessibility inventory confirms the coverage boundary. No fresh production Portfolio rendering or action execution is claimed. Documentation/coverage links are updated; prior bounded plans and evidence remain historical/active within their stated scopes. Low-profile self-review; Auctions, Governance, Current Review, application source and running preview unchanged. No commit, new server or workflow-kit change.

## 2026-09-12 — Owned Portfolio position candidates

- Added the authorized current vote-lock and staked-RSR compositions in a separate Table-lab section. Owned Balance/Value replace opportunity TVL; governed assets remain independent links, and the appreciating-vault balance retains underlying units/exchange rate. Stake Modify keeps native new-tab navigation; vote-lock Modify is a non-executing context preview. No production data/action adapter, shared default, Auction or Current Review change.
- Dark/Light caught a real containment defect: fixed-table styling targeted the DataTable wrapper, not its nested table, so outer-width checks passed while phone values disappeared. The local selector and actual nested-scroll/text/action assertions fix that blind spot. Empty recovery now unmounts/remounts the focus owner and removes stale disclosures; the preview carries chain and first-DTF context. One final APY-width correction keeps the percent sign with the rate at the desktop threshold.
- Source hold/release proves API fallback versus live redeemable underlying, successful zero and an empty-result decode failure using exact existing RPC overrides. A temporary spec-local 503 route was rejected and replaced, not retained as strict proof. The zero-active stake's pending withdrawal remains independent. Units, eligibility, live sort keys and shared-vault/account authority remain engineer-review adoption questions.
- Final 74/74 units, 18/18 browser cases without retries, app/E2E types and scoped lint/format pass; 35 captures retained and eight visually inspected. Scope inventory is not a full gate/CI claim. Both review reports reconciled; ready for human visual review. Runtime/setup deviations and commands are in the [receipt](../plans/design-system-owned-positions-evidence/index.md). No commit/push or user-preview restart. No workflow-kit expansion justified.

## 2026-09-12 — Proposal-like Auction records

- User authorized the compact hybrid: title, state/relevant countdown, access, then supporting facts and provenance. Local pairs use 14px labels and canonical 16px MetricValue; shared homepage-style Metric stays unchanged. Remaining expiry/count and historical outcomes wrap naturally, without metric icons or a fixed-total proposal progress bar. Active/history surfaces, source wording, phase truth, independent links and Governance remain intact.
- The new grouping unit failed before implementation. Final 52/52 units and 24/24 browser cases pass, with app/E2E types and scoped lint/format. Loading reserves the actual status width locally; the type checker caught an unsupported styling prop and prevented widening the shared pill API. Retained 28 source-bound captures; six final states inspected. Low-profile self-review and [receipt](../plans/design-system-auctions-browse-evidence/proposal-like-records/README.md); human visual acceptance remains pending. No production adoption, transaction, commit or user-preview restart.

## 2026-09-12 — State-led Auction list correction

- Human review rejected the prior grouping and its 14px/16px inline pairs. Tests had asserted the implementation's mistake rather than the established horizontal-peer rule. The local guide, Current Review and catalog notes now point to existing Governance/transaction 14px/20px pairs; the old receipt is explicitly rejected, not acceptance evidence. No workflow-kit or shared-component expansion was needed.
- The authorized revision places status with the wrapping title, moves Auction N into operational context, groups access/timers/counts, and makes historical results compact. The approved shorter `Ends in` label accompanies the round. Price-unavailable retains the full warning instead of a neutral closed-looking pill. History-only mode omits the empty active group; independent record/proposer links and all state controls remain.
- REDs verified the absent header status, 16px rendered values and retained empty active group. Render checks then exposed a narrow timing overflow and a 32px loading-row growth caused by placeholder typography; both are fixed. An obsolete below-title geometry oracle now checks actual aligned/wrapped header behavior. Final 53/53 units and 24/24 browser cases pass, plus app/E2E types and scoped lint/format. Thirty source-bound captures are retained, eight inspected as complete lists. [Receipt](../plans/design-system-auctions-browse-evidence/state-led-list/README.md).
- Low-profile self-review; ready for human visual review only. The owned 3047 preview was stopped; 3005 was not restarted or navigated. Reading the user's browser inventory timed out, so visual proof is from the isolated current-source browser, not an assertion about their mounted tab. Production adapters/actions remain engineer-owned. No transactions, dependency install, commit, push or full-repository/CI claim.

## 2026-09-13 — History-first rebalance table

- User chose to settle history as an ordinary full-width table before designing the current-rebalance workspace. The main review hash now shows history only; the previous current/history record study remains at a separate hash. No empty active region or detail pane is reserved. This changes the review frontier, not Governance, production routing or shared defaults.
- Titles and provenance retain real CMC20 snapshot identity; illustrative outcomes remain labeled. Status is independent from metric availability. Desktop compares identity/status/accuracy/impact/traded, with auctions-run count secondary; constrained rows reuse the same cells and native destinations. Numeric values are 16px, stacked labels 14px; inline peers stay equal-sized.
- Visual checking caught a real regression hidden by a passing overflow test: an absolutely positioned `tr::after` produced an anonymous table cell and halved phone content width. A 179-versus-358px RED assertion now pins the real cell width; moving the seam into a cell reuses the Holdings pattern. The doubled mobile outer inset was also removed. Screenshot framing must use the actual scroll container, not assume `window` owns scrolling.
- Focused units 62/62, history/retained browser checks 27/27 and broad lab integration 2/2 pass, plus app/E2E types and scoped style checks. Low-profile intent/correctness/product self-review; visual acceptance remains human-owned. The user's existing 3005 tab was refreshed and the new table confirmed on screen. [Receipt](../plans/design-system-auctions-browse-evidence/history-table/README.md). No production change, transaction, commit or push.

## 2026-09-13 — Same-view current rebalance candidate

- Claude's full-flow inventory now feeds a source-identified current working surface above the retained history table. Stable rebalance context and a bounded operation region use the full card width; phone keeps the same task and draft. Transaction work contributes matched 14px inline facts, recovery and stable action emphasis, not its modal shell or stepper.
- The lab represents restricted/community roles, hybrid eight-token units/CSV/limits, wallet/receipt/indexing states, bids, repeated auctions, expiry/results and conditional filler feedback. Unknown data is not zero. New explanations remain explicitly Lab copy; synthetic liquidity pressure adds a disclosed same-chain NVDAon identity rather than misidentifying XRP as an Ondo token.
- Final Dark/Light review caught actual reset and expiry-count bugs plus missing source explanations. Actual-hook/reducer RED tests preceded the fixes. Cumulative traded values survive repeated rounds; result facts/help remain visible before the same row moves to history. Touch proof measures mounted/hittable controls, including the actual 44px field input.
- Fresh checkpoint: units 91/91, complete browser 27/27 with no retries, detail repetition 6/6, app/E2E types and scoped lint/format/wiki/links/diff green. All 81 audited production source hashes remain unchanged. [Receipt and reconciliation](../plans/design-system-current-rebalance-workspace-evidence/index.md) retain 87 ordinary-viewport PNGs and a source-bound report. Human visual review and engineer adoption remain separate; no commit, push, wallet send or preview restart.
- Final harness corrections waited for disclosure geometry, popover focus and tooltip mount/exit, and selected the visible historical projection. No assertion was replaced with sleeps or weakened into a markup-only test. No workflow-kit change: the existing real-surface/source-freeze rules already cover the lesson.

## 2026-09-13 — Auction-owned workspace hierarchy

- Human review rejected the disconnected trade plan and nested beige operation card. One Auction N heading now owns assets, launch terms and action; live monitoring replaces that body. General references live in a quiet header disclosure, full-width liquidity inspection belongs to the working auction, and cumulative results follow separately. History geometry, production actions and shared defaults are unchanged.
- Rendered REDs caught incorrect public-logo sizing, a 46px clipped ticker in the 320px failed-liquidity row, repeated outcome count, stretched metric spacing and duplicate recovery controls after expiry during indexing. Local owner fixes preserve readable identity, 44px controls, one recovery path and the pending auction identity. Debug-only volatility is not promoted into general information.
- Final frozen checkpoint: browser 37/37 without retries, focused units 91/91, app/E2E types, scoped lint/format and wiki/links/diff pass. All 81 audited production hashes remain unchanged. Independent Intent/Risk findings and focused correction reviews are reconciled; [current receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md) supersedes the earlier 27-case visual candidate. Ordinary desktop/intermediate/phone light/dark inspection included live, pending, unavailable, hybrid, warnings, completion and history context.
- Temporary review tabs/viewport overrides and owned test previews were cleaned up; the user's 3005 preview was not restarted or navigated. No dependencies, commits, wallet writes or production adoption. Existing component-owner and mounted-geometry rules were sufficient; no workflow-kit expansion. Human visual/copy acceptance and engineering adoption remain separate gates.

## 2026-09-13 — Rebalance header disclosure correction

- The zero-padding quiet Button retained a rounded hover surface; direct reproduction opened its popover but confirmed the malformed trigger. Header expiry and a text-only Details InlineAction now share a horizontal 14px axis, with a stateful chevron and actual 44px target. This is a local composition choice, not a shared-default change.
- The new regression first failed on the old 32px vertical mismatch. Header and hierarchy checks pass 18/18 in the scoped run, with app/E2E typecheck and focused lint/format green. Light/dark desktop/middle/phone captures and the updated 3005 preview were inspected; keyboard/expiry access passes. The receipt distinguishes this follow-up from the earlier complete lifecycle baseline. Temporary tabs were closed; no user preview restart or commit.

## 2026-09-13 — Current rebalance section boundaries

- Human feedback exposed ambiguous section endings and a liquidity disclosure styled like the next section's title. Two inset separators now distinguish general header, working auction and cumulative results on one card surface. The 14px disclosure groups its count/chevron; the 16px main section headings remain peers. The generic liquidity assessment moved into its expanded trade inspection, while actionable warnings stay visible.
- Scoped source-frozen browser verification passes 27/27 across section geometry, full-width expanded rows, light/dark phone/middle/desktop, launch/recovery, liquidity and history handoff; app/E2E typecheck and lint/format pass. RED evidence measured the old 1336px trigger and missing boundaries. This is a local review candidate, not a universal divider rule or production adoption. Existing shared defaults and user preview ownership were preserved.

## 2026-09-13 — Compact rebalance spacing

- Intrinsic liquidity and live-timer pairs inherited the 16px spread-detail gap. An opt-in 8px inline layout fixes those readers without changing existing aligned detail rows. Expiry moves into normal-weight provenance, without its clock; narrow metadata has a clean left edge. No transaction or production behavior changed.
- Spacing/hierarchy/details checks passed 28/28, then final narrow-header and keyboard checks 8/8. Transaction detail consumers passed 45/45 units; app/E2E types and scoped lint/format pass. The keyboard harness now awaits Radix panel focus before Tab/Escape; it had raced visibility and then incorrectly assumed link autofocus. The [receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md) distinguishes checkpoints and records the runtime lint-launch correction. User preview preserved; owned test preview released.

## 2026-09-13 — Reproducible checkpoint before chart audit

- User-authorized local checkpoint preserves current/history Auctions, owned Portfolio rows and Discover card refinements without promoting designs or starting chart implementation. Fresh affected units pass 277/277, browser regressions/source checks 102/102, and rich-record integration 2/2; types, scoped lint/format, wiki/links and artifact checks pass. All 81 audited production Auction fingerprints remain unchanged. [Receipt](../plans/design-system-review-checkpoint-2026-09-13.md).
- The read-only pair found oversized repeated report bodies and a risk of overwriting earlier named captures. Original attachment bytes are now content-addressed with complete report round-trip verification; fresh captures use an opt-in directory with the original default preserved. Stale Current Review descriptions were corrected to match the transparent 3:2 composition. No skill/routing change was needed; all designs retain their human/engineering adoption gates. Claude's audit package and user previews are preserved.

## 2026-09-13 — Current rebalance visual-review refinements

- Applied the independent review's compatible refinements: heading-owned status/time, plan-owned disclosure placement, grouped cumulative/outcome facts, selected-row bid expansion, bottom liquidity closing/focus return, associated lab validation and truthful history handoff. Retained valid permission/expiry context and existing card/typography/control defaults. Fixed-width rail, shared dark tokens, unverified product wording and chart redesign remain excluded; [receipt and dispositions](../plans/design-system-current-rebalance-workspace-evidence/index.md).
- Dark/Light review converged on a real 16px bid overhang, repaired and verified across four widths. Owner inspection then exposed an 8px middle-width fact offset; a RED geometry assertion preceded its correction. Screenshot framing now waits for canonical disclosure motion before capturing the bottom close control. Existing workflow rules cover both lessons; no workflow-kit changes.
- Fresh current units 34/34; complete regression pass 74/74 followed by the final scoped layout/interaction pass 39/39, each separately source-bound. App/E2E types, scoped lint/format and final docs checks pass. Light/dark desktop/middle/phone captures and the existing 3005 preview were inspected. Only lab simulations, no transactions; temporary tab and owned test preview cleaned up, user preview preserved. Human visual review and engineer-owned production adoption remain open. No commit or push.

## 2026-09-13 — Fuller preparation asset summaries

- User-directed follow-up replaces the compact two-token stacks with individual 24px logos and body tickers. Each side uses two or three columns and at most three rows, reserving a final overflow count only for longer lists. Detailed amounts stay in the existing disclosure; no shared default or auction mechanic changed.
- The new rendered check failed on the old stack, then all 12 focused browser cases passed across light/dark desktop/middle/phone, including asymmetric removal, missing metadata, pending prices, resize and retained inspection. Types, scoped lint/format/wiki/diff pass. Owner inspected whole compositions; low-profile self-review, not another checkpoint. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md#preparation-asset-summaries--latest-follow-up). User preview preserved; no commit or push.

## 2026-09-13 — Bottom-aligned preparation terms

- User requested spare height above the right-hand launch group. Wide preparation terms and action now bottom-align together; live bids retain top alignment and mobile reading order is unchanged. RED measured the old 72px bottom gap; focused browser 22/22, app/E2E types, scoped lint/format/wiki/diff pass. Owner inspected desktop, the longer non-launcher message at 900px and dark phone. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md#bottom-aligned-preparation-operation). Low-profile local layout change; no shared defaults, production behavior, commit or preview restart.

## 2026-09-13 — Complete Selling/Buying stacks

- Replaced the individual-grid trial with every asset in the canonical 32px logo stack and a comma-separated ticker list 8px below it in the same order. Local adaptive overlap contains the 17-asset pressure case; labels wrap without hiding names. Shared defaults, bottom-aligned preparation, live bids and detailed inspection are unchanged.
- Missing-stack RED preceded implementation. Focused layout checks passed 22/22, then final asset checks 8/8 after test formatting, each with retained source-bound reports. Workspace mount 1/1, app/E2E types, scoped style and docs checks pass. Owner inspected desktop, dark phone and 320px removal. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md#full-stack-revision--current). Existing preview preserved; no production, chart, weight-editor or subtitle changes, no commit or push.

## 2026-09-13 — Current auction recent-feedback batch

- Auction descriptions now belong to their heading group; hybrid Basket owns Manage Weights while prerequisite-blocked Start remains in the operation column. Live chart/Bids headings and timestamp axes align; the schematic Now marker follows a pausable default-running clock. The history handoff is only in Lab simulation controls. Earlier full asset stacks, centered header and bottom-aligned preparation are preserved.
- Fresh current units 34/34, broader browser 78/78 and final focused 9/9 passed with separate source-bound reports, plus app/E2E types and scoped style/docs checks. Dark/Light review found no new blocker; the owner inspected the narrow chart and expanded bid separately in both themes. RED tests needed mounted-owner synchronization for a negative assertion and native SVG centerline measurement instead of paint bounds. Existing testing rules cover both; no workflow-kit changes.
- Updated lab metadata, area guide, test map and [candidate receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md#recent-feedback--current-candidate). Human review remains open; no new global rule, real transaction, production adoption or checkpoint. The user preview remains running; nothing committed or pushed.

## 2026-09-13 — Chart content-edge correction

- User feedback exposed a real 8px plot/time inset despite their passing mutual-alignment test. The new assertion compares them with the surrounding caption, description and note: RED at 8px, then GREEN after removing the chart-owned horizontal inset. Scoped browser 6/6, app/E2E types and style/docs checks pass; owner inspected light/dark at 1400 and 320px. Earlier broad evidence is not restated as fresh. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md#fresh-verification).
- Local low-profile presentation correction; shared defaults, plot meaning, clock and bid behavior remain unchanged. The independent hybrid-layout recommendation remains a proposal. Existing geometry-ownership rules already require whole-relationship verification; no new workflow rule. No commit, production change or user-preview restart.

## 2026-09-13 — Quieter preparation ticker lists

- User requested less emphasis on the asset inventory before launch. Changed only ticker-list typography to the existing muted 14px/20px supporting role; logos, labels, order and disclosure remain intact. RED caught the preceding 16px text; eight focused light/dark/width cases, E2E types and scoped style/docs checks pass. Owner inspected desktop, narrow removal and dark phone. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md#full-stack-revision--current). Touch-up/self-review, no new shared rule, production change or checkpoint.

## 2026-09-13 — Preparation plan width

- Removed the second width cap from Selling/Buying only: the existing grid owns width, preserving the launch column and 24px gap. Hybrid Basket remains bounded. The full Buying list including SUI changed from a 40px/two-line RED to a 20px/one-line GREEN at 1400px; narrower layouts retain natural wrapping. Asset/hybrid checks 10/10 and a final formatter-only desktop recheck 2/2 passed, plus E2E types and scoped style/docs checks. Owner inspected desktop, middle width, narrow removal and dark phone. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md#full-stack-revision--current). No significant downside found within this local scope; progress-bar removal and hybrid compaction remain proposals. No commit or production change.

## 2026-09-13 — Percentage-only cumulative summary

- Applied the accepted progress-bar removal to the local cumulative/outcome facts. Execution percentage and loading/unavailable values remain; historical tables and shared components are unchanged. Eight responsive light/dark browser checks, E2E types and scoped lint passed; owner inspected desktop and phone. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md#fresh-verification). Hybrid compaction remains proposed.

## 2026-09-13 — Auction clarity

- Applied the accepted purpose/access/target hierarchy refinements and moved record-owned simulation controls outside the product article. Execution target means estimated total rebalance progress, not guaranteed or additional progress; source inspection confirmed the library's relative-target normalization. Round names remain illustrative scenario overlays and disappear with unknown data. Control-ownership RED reproduced; 47 scoped browser checks and 12 final responsive checks passed after fixing wrapped pill alignment, title-row height and the phone clock/pill gap. App/E2E types and scoped style checks pass; owner inspected desktop/phone/light/dark. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md#fresh-verification). Low-profile local self-review, no new shared contract or production change.

## 2026-09-13 — Liquidity header divider correction

- The Selling/Buying header border survived because TableHeader's descendant selector overrode the local border-free row. Added the matching local header override, leaving shared defaults and main section separators intact. Computed-border RED caught 1px; four viewport cases now verify 0px on every header/body row in both tables. Desktop inspected; E2E types, scoped style/docs checks pass. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md#fresh-verification). No expiry or button changes in this correction.

## 2026-09-13 — Expiry emphasis

- Applied the approved local emphasis: remaining expiry time uses the canonical foreground 14px/500 label role, while its label and proposal metadata stay muted 14px/300. No position, icon, button, countdown-format or Ended changes. Eight light/dark viewport header checks, E2E types and scoped style/docs checks pass; light desktop and dark phone inspected. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/index.md#fresh-verification). Touch-up/self-review, no shared default or production change.

## 2026-09-13 — Current rebalance state composition

- Applied compact setup/initial context, visible saved units and allocations, bounded editor rows, exchange-led bid disclosure/selected chart labels, local cap/transaction feedback and execution/financial outcome groups. New status wording is held after the user asked for context; exact weights are a pre-first-auction hybrid prerequisite, not a live auction phase. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/state-composition-verified/README.md) owns source-bound proof and adoption limits.
- Dark review caught saved percentages surviving unavailable prices; rendered RED then four-width error/pending/retry checks verify guarded percentages without losing units. Owner inspection caught a split 320px token and a 9.59px loading placeholder inset violation; explicit geometry checks now pass. Final browser 25/25 plus loading/saved 4/4, focused units 27/27, app/E2E types and style/docs checks pass. The broader run's one source-freeze rejection was an area-guide edit during capture; only that guide differed, and the affected case was repeated from frozen source. No workflow-kit change was needed; existing freeze and mounted-geometry rules already covered both issues.

## 2026-09-13 — Current versus target basket clarity

- After tracing the production editor, applied the approved lab clarification: current holdings per DTF token, separate starting and editable target values, a pre-first-auction confirmation step and explicit local-save/reload meaning. Current holdings now differ from the target fixture, and the starting reference stays visible after edits. New copy is localized; no production behavior, storage, shared default or token change. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/weights-clarity/README.md).
- Comparison RED reproduced the missing current-holdings distinction. Regression browser checks passed 23/23; after grouping the save explanation with its actions, final checks passed 7/7. Owner inspected desktop/phone/light/dark and translated layouts. Current units 27/27, types and scoped style/docs checks pass. Low-profile correctness/product self-review; actual execution-unit normalization remains an engineer-owned adoption question.

## 2026-09-14 — Weight-summary continuity and familiar units

- Applied the remaining approved lab feedback: static estimate/target placeholders before confirmation, Duration last in both summary states, one prerequisite explanation, and restored Current units / New units with matching helper text. The outlined auction-size control remains unchanged. Summary RED found only Duration; 19 browser checks now pass across desktop/phone, save/reopen/reload, missing prices, translated layouts and affected risk/action states. Types and scoped style/docs checks pass; owner inspected the changed layouts. [Receipt](../plans/design-system-current-rebalance-workspace-evidence/weight-summary-followup/README.md). No production logic or parsing fallback changed; financial correctness remains an engineer-owned adoption gate.

## 2026-09-14 — Navigation-only current rebalance table

- Added a separate current table above unchanged history and made it the lab Current Review destination. Lifecycle, viewer restriction/next action, round and timing remain separate; Details navigates only. Existing workspace retained as an alternative. [Receipt](../plans/design-system-current-rebalance-table-evidence/README.md): RED missing table; final browser 14/14, retained/catalog units 44/44, types and scoped style/docs green.
- Dark review caught generic detail screenshots conflicting with selected record/round/bids. Selected context is now separate from a dated, named read-only Reference; repeat uses the actual Auction 2 capture. Ownership/image-load assertions and desktop/phone captures verify the distinction. Light found no composition blocker. Optional copy remains unapproved, and no production adapter or transaction behavior changed. No workflow-kit change warranted; the existing reference-truth and review rules covered the issue.

- All-state follow-up: default current list shows 18 labeled lifecycle, permission and data examples simultaneously; single previews remain selectable. Private preview keys preserve real proposal identities while keeping Details/Back and proposer resize focus on the exact example. RED one-versus-18 rows, GREEN browser 5/5 plus types and scoped style/docs. Low/self-review; desktop long-list and dark-phone states inspected. History and production unchanged.
- Human review removed the scenario annotations from the row identity cells to expose the intended product hierarchy. All 18 examples, states and navigation remain unchanged. Touch-up/self-review; focused desktop/mobile browser checks 2/2, app/E2E types and scoped style checks pass. Fresh captures are linked from the existing table receipt; earlier labeled captures are historical.
- Status/navigation follow-up replaces the text Details link with a 44px secondary circular arrow, preserving its accessible name and native route. Browse pills drop redundant action prompts; restrictions and data/indexing notes remain visible, bids move beside auction timing, and wallet/network prompts remain in selected detail context. RED pinned the duplicated action; a lab-control test sequence was corrected after the interrupted run, then all 17 browser checks passed freshly with types and scoped style/docs. Low/self-review; desktop/phone/light/dark inspected. Ongoing's active indicator and proposed permission-pill wording were not changed.
- Approved compact/access follow-up reduces the visible circle to the canonical 32px size while reserving and hit-testing 44px. Ready pills now read Ready · Launcher only / Ready · Anyone, independent of wallet/network; the redundant ready restriction sentence remains in detail. Weight/data exceptions and ongoing's steady indicator are retained. Exact new strings were isolated-extracted and translated in es/ko/zh. RED expected the qualified label; fresh browser 11/11, types and scoped style/docs pass, including outside-circle clicks, focus, missing-data precedence and translated phone layouts. No production permissions changed.
- At the user's request, restored the original scenario labels above 18 separate one-row tables in All, never inside product identity cells. Individual previews and the single historical table remain unchanged. RED found one table instead of 18; focused browser 8/8, types and scoped style/docs pass. Desktop and dark-phone views inspected, including history and unknown-data context. Low/self-review; existing private preview keys preserve exact Back and responsive focus without changing state or permissions.
- Approved clearer labels now read Only launcher can start / Anyone can start, with the existing HelpTooltip and the approved launcher explanation beside restricted-ready pills. Source and translations changed only in this lab consumer. RED pinned the missing control; corrected a screenshot-scroll test assumption and the old no-buttons assertion. Real-touch verification then caught Radix's auto-close overriding the explicit tap toggle, fixed through local trigger capture handlers without changing shared defaults. Final browser 11/11, types and scoped style/docs pass; open desktop/phone help and translated phone layout inspected. General HelpTooltip tap behavior is recorded for a separate owner-level review. Auction permissions, underlying states and transactions are unchanged.
- Restricted weight setup now uses one supporting Only launcher can start line and puts its exact full restriction in existing launcher help. Selected detail retains that full instruction; no vertical-alignment exception or permission change. RED missing helper; final browser 13/13 after updating the older inline-copy assertion, with types and scoped style/docs green. Desktop/phone inspected; translated help and real taps pass. [Receipt](../plans/design-system-current-rebalance-table-evidence/README.md#compact-weight-setup-restriction). Local self-review only; broader mobile cleanup and migration remain separate.

## 2026-09-14 — Auctions near-term and deferred scope

- Step one only: the [table brief](../plans/design-system-current-rebalance-table.md#near-term-and-deferred-scope) now owns the selected current/history table direction, initial retention of the existing production detail/flow, and the unfinished workspace's explicit deferral. Synced workspace/history briefs, V1 frontier and engineering register, area guide, Current Review and catalog; removed the stale instruction to review the whole workspace now. No specimen implementation, production behavior, shared default, baseline acceptance or adoption changed.
- Catalog/UI 38/38, app/E2E types and scoped lint/format/link/wiki/diff checks pass; mounted catalog wording inspected. Metadata/docs-only work used mapped checks, not a fabricated behavior RED or a repeated auction browser suite. No new commit or snapshot checkpoint. Mobile/constrained cleanup, the 4px row-bottom strip, fresh combined review and human disposition remain outstanding; production integration still needs engineer review and does not waive audit hazards.

## 2026-09-14 — Auctions table mobile cleanup

- Current status/auction facts now stay stacked below a 576px container; the former 384px split visibly wrapped a fixed-height pill and overlapped its helper at 430px and in a constrained 390px column. Historical identity/provenance take full width below 512px, and only the current table's 4px hover-bottom strip was removed. Shared defaults, desktop columns, copy, financial meanings and the deferred workspace are unchanged.
- RED pinned the layout faults; historical helpers already passed first-tap checks and were left unchanged. Final combined browser 34/34 and units 48/48, types and scoped style/wiki/diff pass. Owner inspected both themes, phones, intermediate widths and long/unknown state geometry; the live preview remains available. [Receipt](../plans/design-system-current-rebalance-table-evidence/mobile-cleanup/README.md). Low-profile self-review; bounded independent/human review and production integration gates remain open. Superseded intermediate captures were moved out of the repository rather than adding checkpoint churn.

## 2026-09-14 — Compact current-row footer

- User selected arrow beside auction number/timing, expiry below with label left/counter right. The existing compact circle and 44px hit area remain; a 12px gap replaces the standalone action footer. “Rebalance expires in” is explicit and localized in en/es/ko/zh through isolated extraction/scoped catalog merge. Desktop, history, permissions and trading behavior are unchanged.
- RED: arrow 66px below auction context. Final affected browser suite 25/25, app/E2E types and scoped style/wiki/diff pass. Light/dark phones, ongoing/unknown states and Spanish inspected. [Receipt](../plans/design-system-current-rebalance-table-evidence/mobile-footer/README.md). Low/self-review; final independent/human review and engineer migration gates remain open.

## 2026-09-14 — Compact status and navigation pairing

- Approved compact order is now auction context, status with arrow, then rebalance expiry. Below a 352px container the arrow stays beside auction context to preserve full-width status/help. One compact link, its 44px hit area and the localized expiry treatment remain. Desktop, history, state logic, permissions and trading behavior are unchanged.
- Order/alignment RED preceded implementation; the first follow-up caught three unknown-auction states needing grid-item centring. Final browser 17/17, app/E2E types and scoped style/wiki/diff pass. Light/dark phone, translated status and restricted-weight help inspected. [Receipt](../plans/design-system-current-rebalance-table-evidence/status-arrow/README.md). Low/self-review; human/independent review and engineer adoption gates remain open. Earlier compact-layout receipts are marked historical; no checkpoint commit or dependency change.

## 2026-09-14 — Design-system scope and sensitive data boundaries

- User reaffirmed that design-system construction is the primary job, not a technical product overhaul. The [V1 scope boundary](../plans/design-system-v1.md#design-system-scope-and-sensitive-behavior) separates visual requirements from changes to data sources, calculations, freshness, permissions and transactions. Chart-audit discrepancies remain reported questions until their intended meanings and inputs are verified; no such implementation is authorized or made here.
- The central engineer handoff now records the chart value/freshness questions and requires explicit evidence, unknowns, affected owners, proposed versus actual changes and verification obligations for sensitive work. Deferred engineering review is not permission to implement first. Documentation only; Claude's reviewed source tree and all product/lab code remain unchanged.

## 2026-09-14 — Overnight design-system preparation

- [Morning handoff](../plans/design-system-overnight-2026-09-14/README.md): chart visual families/first slice are separated from source-basis, freshness and SDK questions; governance has a bounded layout/coverage assessment, not a rewritten flow. All 2,437 primary reviewed files stayed identical while Claude reviewed auctions.
- Source-bound screenshots exposed two escaped compact-layout defects despite 53 baseline passes: Earn's supporting identity overlapped its rate, and Owned's fixed pair split a decimal. Two local column fixes and real text/line regressions are held in an unapplied patch. Final affected browser 42/42, units 29/29, types/style pass; Dark/Light found only corrected evidence wording/link/count. No shared defaults, financial meanings, product copy or production changes. No commit/checkpoint/install; no workflow rules added. Review the patch after Claude's source freeze; human acceptance and engineer adoption gates remain separate.

## 2026-09-14 — Auction tables independent review reconciled

- User authorized F1–F5, including the exact desktop “Rebalance expires in” wording. [Closeout receipt](../plans/design-system-current-rebalance-table-evidence/closeout/README.md): capture-owned June reference caption, local persistent historical help, neutral touch dismissal without navigation, omitted unlabelled null-round context, and approved localized desktop expiry. Selected identities, numbers, status/access derivation, shared defaults and production code remain unchanged.
- RED reproduced both tooltip flashes, accidental navigation, wrong caption and unwanted mounted round block. Combined browser 48/48; final 17/17 after non-dismissing screenshot and desktop locale checks; unit 48/48; types/style/wiki/links/diff green. The earlier absent-element race and screenshot-induced tooltip dismissal were corrected as harness issues, not hidden product fixes. Technical table closeout is complete; human visual acceptance and engineer-owned production integration remain separate. Existing detail/flow is retained for initial migration; the redesigned workspace stays deferred. No commit/checkpoint/install or overnight patch application.

## 2026-09-14 — Table and governance layout follow-up

- User authorized pieces requiring no new product decisions. The prepared Earn supporting-label containment and Owned whole-fact numeric wrapping fixes are now integrated. Governance retains all wording/statuses, qualifiers, tallies and timeline behavior; narrow titles use full width, qualifiers/evidence stack in reading order, orphan evidence dividers disappear, and lab notes use content height. [Receipt](../plans/design-system-table-governance-followup-2026-09-14/README.md).
- Fresh RED reproduced the four initial layout failures. Browser 46/46 passed; visual inspection then caught mixed status/evidence grouping just below the cutover, reproduced it with an additional failing assertion, and corrected the outer layout. Final affected 16/16 plus retained rich-record contracts 2/2; units 29/29; app/E2E types, scoped style/wiki/links/hashes/diff pass. Source comparison identifies exactly three local app owners, two specs and the lab guide; no production, shared defaults, data, financial or transaction changes.
- Overnight documents now identify the original patch as integrated historical evidence. Chart choices, queued countdown/timeline treatment, missing governance states and real proposal adapter remain separate; the engineer register records the latter boundaries. Low/self-review, no new workflow machinery or checkpoint. The user's 3005 preview and Auctions Current Review remain unchanged; the isolated runner stopped. Human visual acceptance and engineer adoption remain separate.

## 2026-09-14 — Compact Earn rate-help alignment

- User identified the unbalanced two-line identity/rate header. The 32px rate-line button displaced its text vertically and reserved 36px at the right edge. A local icon-only InlineAction now sits beside the unchanged period label; the numeric/APR line is control-free and right aligned, with a 44px help hit region outside the 20px visible layout. Existing accessible name, FAQ/focus, loading footprint, desktop IconButton and Yield remain intact. [Receipt](../plans/design-system-table-governance-followup-2026-09-14/earn-rate-help/README.md).
- Geometry RED preceded the fix. Self-review replaced an interim whole-label button to avoid a visible-label/accessibility-name mismatch. The surrounding run passed 17 cases; two new tests incorrectly measured intentional hit expansion against the inner content wrapper, corrected to the padded table cell without an app change. Final focused 2/2, units 18/18, app/E2E types and scoped style/wiki/links/hashes/diff pass. Both themes and the screenshot's Long content state inspected. No financial, copy, shared-default or production change; no checkpoint/install. The isolated runner stopped and 3005 was preserved.

## 2026-09-14 — Interactive content hover trial

- User reaffirmed that gray row fills against beige seams are undesirable. Added one opaque, theme-owned candidate role: warm ivory in light mode, subtle card lightening in dark. Only governance, Earn and current-rebalance lab rows consume it; generic control hover, static history, loading feedback and financial behavior remain unchanged. [Trial and handoff](../plans/design-system-content-hover-trial.md); human visual acceptance and engineer production review remain separate.
- Fresh browser 8/8, surrounding 17/17, final transition-settled 2/2; units 56/56/types/style pass. Dark/Light review found only stale documentation and a negative-test transition timing weakness, both resolved. Opaque interaction fill is not a reuse or redefinition of structural substrate-subtle. No commit, checkpoint or preview restart.

## 2026-09-14 — Accepted content-hover rollout

- User accepted the solid pale-beige treatment, not a gradient. One theme-owned role now covers eligible V1 content rows/cards: retained proposal/Earn/current rows, Discover, Portfolio positions, earlier rebalance records and neutral catalog content links. Non-actionable withdrawals lose inherited row hover; static/history/loading rows, individual controls, selected states and navigation keep their semantics. [Inventory and engineer handoff](../plans/design-system-content-hover-trial.md); production screens/shared defaults remain unchanged.
- RED covered missing constrained Portfolio fill and loading mask leakage. Browser 20/20 then visual inspection caught a white decorative ticker capsule inside the warm card. A new RED assertion pinned it; final 6/6 confirms strip/mask continuity and exclusions. Units 56/56, final types/scoped style/wiki/diff pass. Light/Dark and affected final cascade review found no blocker. The open 3005 preview stayed current; isolated 3047 stopped. No financial behavior, copy, checkpoint, commit or install; production adoption remains engineer-reviewed.

## 2026-09-14 — Governance presentation closeout

- Authorized lab presentation now uses one leading status pill, foreground static voting/challenge deadlines, secondary Passed, explicit Waiting period/availability help and active-only timing strips. Added separately supplied queued-ready/expired examples plus Loading/Empty previews. Native reference navigation and independent help are preserved; no live state, vote evidence, shared default or transaction changed. [Current receipt and engineer boundary](../plans/design-system-governance-presentation-closeout.md).
- Final governance 16/16 and retained desktop/mobile 2/2; 16 surrounding browser checks, 38 units, types/style/wiki/diff pass. Light/Intent and Dark/Risk had no implementation blocker; their loading-geometry and second-touch proof requests were incorporated. Trace isolated the shared tooltip's focus-scroll dismissal; visible-row keyboard proof now settles scroll first, without changing the primitive. Initial mount timing and a broad first-SVG assertion were corrected. No new workflow rule or checkpoint; 3005 remains current.

## 2026-09-14 — Table acceptance and first chart review

- Recorded the user's scoped table approval for now; catalog/decision/frontier agree without promoting production migration or the unfinished auction workspace. Current Review moved to the [first chart comparison](../plans/design-system-charts-source-reset.md), with unchanged dated price captures, explicit pressure overlays, three sizes and separate engineering questions. No return normalization, live append, sampling, freshness, SDK or shared palette change.
- Chart/lab browser 17/17 and focused units 39/39. Independent findings produced real REDs for range remount/focus loss, 32px targets and missing selected-estimate qualification; fixes and affected review pass. Curated evidence stays small. The test selector now anchors exact filenames; broad matching was interrupted, not counted. No new workflow rule: existing focus, geometry and scoped-proof rules already cover these defects. The user's 3005 preview is on charts; 3047 stopped.

## 2026-09-14 — Chart direct inspection refinement

- User prefers hover to the invented sample-navigation arrows. Removed both controls and their height reservation; retained direct touch and focused keyboard sample selection, with source-supplied date/value/estimate announcements. Mouse exit and keyboard blur return to the latest point. Range selection and all data/palette/production owners are unchanged.
- Low/self-reviewed: visible-arrow regression failed before the change; fresh exact chart/lab browser 17/17 and focused units 39/39, types and scoped lint pass. Inspected current 3005, dark phone and light desktop; dated initial evidence is explicitly historical. [Refinement receipt](../plans/design-system-charts-source-reset.md#evidence-and-boundaries). No checkpoint or commit.

## 2026-09-14 — Chart product-use context

- Added source-checked lab notes outside the chart surfaces: Discover compact trends, Home highlighted-card charts, and Overview’s line view. Each identifies what the isolated example does not reproduce; daily/hourly density is explicitly lab-only. [Source map and receipt](../plans/design-system-charts-source-reset.md#usage-and-transfer). Both-theme responsive checks 2/2, types and scoped style pass; current preview and phone notes inspected. No chart behavior, product copy or financial/source changes.

## 2026-09-14 — Source-faithful chart review reset

- User rejected generic chart/card wrappers as product replacements. The review now starts from actual Overview/Home renderers and the 90×40 Discover cell plot; source typography, axes, annotations, heights and host insets remain explicit context. Generic examples are collapsed technical pressure fixtures, not implicit design overrides. [Current plan, evidence and engineer handoff](../plans/design-system-charts-source-reset.md). The superseded generic-review plan was consolidated and its references updated.
- New header inspection is a display-only opt-in: selected event payloads, caller-owned accessible readout/focus/exit and touch compatibility. Tooltip-active effects and synthetic mouse-leave resets were unreliable on mode round trips; direct payload events plus native pointer-type-aware restoration pass repeated touch/keyboard checks without locking scroll. Existing production callers retain their tooltip, sources and calculations. Engineer review before adoption remains mandatory.
- Fresh chart/lab 20/20, source interaction repeat 6/6, existing Overview compatibility 6/6, units 43/43, types/style/wiki/diff green. Eleven chart-source attachments share the rechecked final digest; nine curated images inspected. Recharts JavaScript resize animations require actual curve/plot settling before screenshots—CSS animation disabling alone is not proof. No new workflow-kit rule was needed; area guides and test map capture the local verification seam. Isolated servers stopped; 3005 preserved and refreshed. Visual acceptance remains the user's next step.

## 2026-09-14 — Chart host V1 conformance correction

- User identified copied card rounding overriding the accepted system. Corrected square/flat outer surfaces, canonical page/panel/body text, neutral non-interactive colors and ChainBadgedLogo; Home now consumes the accepted 8px shell/media, 24px content axis and 16px/8px relationships without its legacy title-height spacer. Current Review's false radius-conformance explanation and lab/plan scope are corrected. Source preservation does not make legacy wrappers authoritative; no new design rule was invented.
- Low/self-reviewed, radius RED (20px versus accepted 0px) then fresh browser 6/6, units 43/43, types/scoped style/wiki/diff. Both themes and responsive captures inspected. [Conformance evidence](../plans/design-system-charts-source-reset.md#v1-conformance-correction--september-14) supersedes initial wrapper captures, which remain historical. Plot renderers, data and inspection behavior unchanged; no production edit, checkpoint or commit. The user's 3005 preview was refreshed; isolated 3047 stopped.

## 2026-09-14 — Compact launch annotation

- User requested a small V1 treatment preserving the callout's function. Overview's static marker now has an optional unboxed 12px auxiliary-text presentation, separate supporting estimate caption and a gap beneath the unchanged timestamp line. Existing line/candle callers keep their pill; Home's interactive token marker and all financial/segment inputs are unchanged. The lab and engineer handoff name that boundary.
- Low/self-review: 10px pill RED, then annotation/geometry/interaction/browser checks 7/7, existing Overview compatibility 6/6, units 43/43, types/scoped style/wiki/diff. Three curated renders and all seven source attachments checked against the final tree. [Current receipt](../plans/design-system-charts-source-reset.md#launch-annotation-refinement--september-14). Long translations and edge-of-domain launch positions remain pre-adoption checks. No checkpoint or commit; 3005 preserved.

## 2026-09-14 — Design-system instruction paths

- After checkpoint `49f9f22ae`, moved six intact family-guidance blocks out of the common lab guide into four explicitly routed references. Moved all 29 engineering-register rows to a linked handoff file while retaining reporting triggers, review timing, safety policy and the existing anchor in the main plan. Removed duplicated transient review status, not acceptance boundaries. The selected plan/common-guide/chart-guide reading drops from 7,335 to 3,763 words; this is not measured total context, token savings or better first-pass performance.
- [Audit, replay and proposed pilot](../plans/design-system-workflow-read-paths.md): fresh preservation/link checks, formatting, wiki and diff checks pass; Dark/Light found no blocker. A short optional handoff links existing contracts and preserves required flow preflight. Root/kit rules, model defaults, code and previews are unchanged. No pilot, commit or push; task/model approval remains required. One stale Discover receipt anchor in the touched ledger now reaches its existing evidence. The prior consolidation's mixed result remains unchanged; independent design-system consumption is still unproven.

## 2026-09-14 — Handoff policy and pilot closeout

- The user-authorized Sol-medium pilot found no mobile-axis defect. [Reconciled receipt and six archived captures](../plans/design-system-mobile-axis-pilot/README.md) preserve evidence without relying on the temporary worktree. Source comparison and capture hashes were independently checked. Worker-reported browser passes remain labeled as such; the removed temporary assertion is not replayable from the package. No chart patch, visual approval, implementation benchmark or task-cost claim follows.
- The [standing handoff rule](domains/design-system.md#implementation-handoffs) makes the coordinator proactively propose substantial implementation tasks, retain approval/integration ownership, declare tiny local exceptions and keep repairs with the implementer. Missing task discovery now has a bounded artifact/link fallback; it is not claimed fixed. [Closeout](../plans/design-system-workflow-read-paths.md#operating-rule-closeout--september-14): medium, two read-only reviewers, zero findings; fresh preservation/hash/link/format/wiki/diff checks pass, no runtime changes. No new model default, kit overhaul, commit or preview change. Chart review resumes; future agent compliance and cost remain unproven.

## 2026-09-14 — Responsive Overview page title

- User chose 24px/30px below 640px and 32px/38px from 640px. Added one shared `responsivePageTitle` opt-in; only the lab Overview title consumes it. Fixed page-title/default roles, financial row, chart behavior and production consumers stay unchanged. Current typography guidance distinguishes stable defaults from explicit variants; no 28px scale addition or app-wide audit.
- [Evidence and replay](../plans/design-system-responsive-page-title/README.md): unit and mounted-title RED, then 45/45 focused units, 5/5 browser checks, app/e2e types, scoped lint and 16 inspected normal/long title captures. Capture readiness checks fill-area draw clips as well as resize geometry; a stroke-only test assumption was corrected without chart changes. Dark/Light found no scoped blocker. Owned 3036 stopped; user previews and separate hover worktree untouched. Coordinator handoff pending; engineer review before adoption remains deferred, not waived.
- Coordinator closeout completed afterward: scoped diff and representative final images inspected, typography 2/2, wiki 20 pages and diff check independently green. The lab slice is done; production adoption and broader chart acceptance remain separate.

## 2026-09-14 — Highlighted-card framing refinement

- User accepted square outer/inner corners and a 4px surround, with the gradient immediately inside and content still 24px from the card edge. Applied to the chart lab excerpt; decision/catalog/guidance now separate highlighted-card framing from chart geometry, and the earlier full-card study is explicitly historical. Production and chart behavior unchanged.
- [Scoped evidence](../plans/design-system-charts-source-reset/highlight-frame/README.md): 4px-versus-8px RED, then 4/4 browser checks, 37/37 units, app/e2e types and scoped lint; six light/dark phone/desktop captures inspected. Low local correction, coordinator self-review; no new shared default or adoption authority.

## 2026-09-14 — Delegated hover-header integration

- Integrated the finished ticker-persistence source/test hunks, preserving the newer responsive title and highlighted-card framing rather than copying whole worktree files. No source/return/formatter or production behavior changed. The independently based headline return remains hidden during inspection and restores on exit.
- [Combined evidence](../plans/design-system-chart-hover-header/README.md): 10/10 browser, 9/9 focused units, normal app/e2e types, scoped lint and eight inspected selected-state captures. This closes the worker's missing normal-typecheck proof. Existing worker Dark/Light findings were evidence-only and already repaired; coordinator self-review covers the bounded integration. No new task, worktree removal, commit or user-preview restart; visual acceptance remains with the user.
- Follow-up: user requested even dot spacing. Separated the decorative dot from the ticker so both sides use the existing 8px gap; no fixed-width reservation. Rest/selected geometry RED→GREEN, 4/4 hover/keyboard/touch checks, e2e types and lint pass; representative phone captures inspected. [Receipt](../plans/design-system-chart-hover-header/README.md#dot-spacing-follow-up). Narrow coordinator repair because the delegated task remains unaddressable; production unchanged.

## 2026-09-14 — Genuine mobile chart preview and Home annotation

- Delegated the two approved fixes to one fresh Sol-medium worker. Mobile preview now uses a separate local chart document, with named Overview/Discover/Home contexts and real CSS/JavaScript breakpoints; Narrow desktop container remains distinct. Home opts into an unboxed auxiliary launch label with measured translations, default mobile visibility and a 4px line gap. Production callers, financial policy and security headers remain unchanged. The mobile option is loopback-only because deployed CSP prohibits framing.
- Dark/Light findings were reconciled and rechecked. [Closeout](../plans/design-system-chart-preview-closeout.md) links RED/GREEN and the exact built-preview 8/8 report; coordinator verified all eight source attachments, units 6/6, app/e2e types, scoped lint and the actual 3005 Chrome viewport/label geometry. Settled captures replace a mid-animation image; offline token-image fallback is disclosed. Engineering deployment/adoption review and human visual acceptance remain separate. No model-equivalence or measured-cost claim, commit or checkpoint.

## 2026-09-15 — Candlestick review preparation, interaction gate unresolved

- One authorized Sol-medium worker added real captured PHOTON weekly candles to the existing lab frame. Retained OHLC tooltip, source mapping, launch coordinate and production defaults; source provenance is separate from the older headline capture. Factsheet bars are excluded from the queue pending confirmation of use.
- Dark/Light review cleared the visual composition after footer-selection and conditional-note corrections. A nonfirst-candle touch test exposed a delayed reset that the original first-candle test could not detect. Three bounded interaction fixes failed; failed experiments are removed and original-renderer characterization is separate. The [receipt](../plans/design-system-candlesticks.md) records a human-required scope decision, not completed hardening or a demonstrated production defect. No commit, checkpoint or production adoption.

## 2026-09-15 — Chart family presentation completion

- Two cold-context Sol High workers completed the authorized Yield/Portfolio and candle/Home packets after the user approved Priority execution. Coordinator retained design judgment, review, integration and final verification. The [completion package](../plans/design-system-next-charts/completion/README.md) supersedes the initial next-family visual-readiness claim; chart candidates remain unaccepted and unadopted.
- Yield now follows the compact Price pilot; Portfolio has one dominant dated historical total, bottom periods and a compact category key with explicitly provisional source-aligned colors. Candle OHLC uses a local V1 tooltip through an optional default-preserving seam; Home's 20px name has a proposed lighter weight. Financial inputs, formatters, CSV/ranges, production Portfolio and production tooltip files retain their protected hashes. Sensitive adoption seams are in the engineering handoff.
- Dark/Light review caught lost persistent metric identity and a Recharts custom-axis wrapper that type checks could not detect. Fixed the identity, endpoint/interior anchors and direct axis child; installed-renderer recheck plus actual mounted timestamp-marker assertions passed. Final coordinator browser23/23, refinement6/6, units55/55, app/e2e types and scoped style/wiki/diff checks passed. Coordinator inspected desktop/phone, light/dark, rest/inspection/composition images. Unsupported touch inspection and earlier candle interaction failures were separate at this checkpoint, not false green coverage.
- Serial browser jobs were insufficient while another worker changed UI source; final captures required a source freeze across all writers. A worker-created duplicate stage-start row was reconciled into the existing chart stage. No workflow-kit or global-policy change was needed, and no user server was restarted, commit created or Claude small-component output integrated.

## 2026-09-15 — Chart review feedback refinement

- Two cold-context Sol High workers implemented the authorized morning feedback: balanced Yield review insets/header export, Portfolio ranges above the plot and key directly below, fitted axis gutters, date spacing, a supplied-total contour/inspection guide, and precise line-colored endpoints across the line family. Shared renderer seams are opt-in; data owners and production defaults remain protected. [Closeout](../plans/design-system-next-charts/feedback-pass/README.md) owns current evidence; the earlier completion package is historical.
- Coordinator caught footer target height, gradient coordinate sampling, marker anatomy and default Home launch-placement issues. Independent review found a real touch-release marker/header mismatch; its fix has exact timestamp hold proof in both readout modes. Final combined browser46/46, units23/23, app/e2e types and scoped style passed; reviewers found no remaining scoped blocker. Empty canvases and source lifecycle captures close the visual evidence gaps. Human acceptance and engineer adoption remain separate, including known candle interaction limits.
- A width-fraction hover test was coupled to the old axis gutter. The repaired test targets a known fixture timestamp through actual curve geometry instead of updating magic values to match the output. Capture-only scrolling was corrected for the app scroll container and sticky lab navigation. No workflow-kit rewrite, global token/default change, preview restart, checkpoint, commit or Claude component integration.

## 2026-09-15 — Candlestick keyboard and touch recovery

- A separately authorized G7 repair closed the candlestick lab's previously documented interaction gate without weakening its active assertions. The existing custom-tooltip seam now enables Recharts keyboard inspection and synchronizes its default index from the actual pressed candle, so keyboard navigation and a stable non-first touch retain the complete OHLC payload. Production callers omit the seam and keep their existing defaults.
- Fresh evidence passed focused keyboard/touch 2/2, protected geometry/hover 5/5, the full candlestick specification 12/12 and the integrated chart matrix 86/86. App/e2e TypeScript and scoped style/diff checks passed. The original three failed attempts remain recorded as historical recovery context; human visual acceptance and engineer review before production adoption remain separate.

## 2026-09-16 — Honest chart history boundaries

- Human review first rejected the all-partial default, then explicitly replaced the dedicated Price example with the Portfolio pre-holdings lifecycle. Normal Yield remains available-bound. The removed Price specimen and evidence are no longer authority. The simulated Portfolio fixture now owns sparse, explicit known-zero samples before its first positive holding; the generic interrupted-data fixture remains separate. [Closeout and boundaries](../plans/design-system-next-charts/partial-history/README.md).
- The initial May 31 midnight zero still let Recharts monotone interpolation rise during that day. A rendered-path RED measured the curve 152.27px above baseline one pixel before June 1. Moving the final zero to `2026-05-31T23:59:59Z` keeps that sampled path on baseline, preserves a readable UTC `31 May 2026`, and leaves all 13 later positive samples unchanged. YTD/1Y/All can select known zero; 7D/1M stay post-holdings.
- Fresh focused units pass15/15, the full next-family suite32/32 and the affected browser specification20/20. The JSON reporter artifact is `design-system-next-charts/optical-refinement/evidence/portfolio-zero-playwright-report.json`; its 20 cases have zero failures. App/E2E TypeScript, scoped lint/format, wiki lint and diff checks pass. Light/dark 320/1400 total/composition evidence was inspected. Production still hides finite periods older than its first positive value and does not synthesize or prepend zeros; unknown history must never become zero. Human visual approval and engineer review before separately authorized adoption remain required.

## 2026-09-16 — Next-family plot-edge correction

- Yield/Price and Portfolio lab plots now begin at a local 4px left drawing clearance instead of inheriting the 24px content axis. The 24px right margin, measured Y gutter, top/bottom margins, full-width focus wrapper, readouts, controls, CSV, key, heights, interaction and known-zero lifecycle remain unchanged. Overview, shared defaults and axis helpers were not changed.
- Focused browser RED measured both curve starts at 36px instead of 16px; the local margin correction passed the same 2/2 cases. The combined source-frozen replay passed32/32 with zero unexpected, flaky or skipped cases in `design-system-next-charts/optical-refinement/evidence/next-family-plot-edge-playwright-report.json`. Light/dark 320/1400 Price and Portfolio rest/known-zero total/composition captures were inspected without a scoped blocker. Human visual approval and engineer review before production adoption remain required.

## 2026-09-16 — Portfolio key mobile alignment

- The lab-only Portfolio key is now a semantic definition list. Below its existing 32rem container boundary, rows span the 24px-inset content width with grouped dot/label terms and a shared right-aligned amount edge; labels can wrap while tabular values cannot. Desktop keeps the compact wrapped entries and existing 24px/8px gaps. Category order, copy, colors, typography, 12px row rhythm, selected timestamp, live output, chart/control/data behavior and production remain unchanged.
- Browser RED measured the first 320px row ending at 212.53px instead of the required 284px content edge. Focused GREEN passed5/5 across light/dark 320/390 pressure and desktop compactness; the full affected replay passed25/25. JSON reports are `design-system-next-charts/optical-refinement/evidence/portfolio-legend-focused-playwright-report.json` and `portfolio-legend-playwright-report.json`. Long-label plus `$9,876,543,210.00` captures have no legend/document overflow or overlap. Next-family units32/32, app/E2E TypeScript, scoped style, wiki and diff checks pass.

## 2026-09-16 — Portfolio header timestamp precision

- The user's later visual decision supersedes the provisional 4px total-to-date gap with 8px while retaining the latest sample date at rest and the existing empty copy. A family-local formatter now requires explicit UTC `day` or `minute` precision instead of inferring it from cadence, range or timestamp digits. The weekly Portfolio caller declares day precision, so its 23:59:59 path sentinel renders and announces only `31 May 2026`; future Portfolio callers can opt into minutes. Yield inspection uses the same formatter at minute precision.
- RED proved all three missing behaviors: minute formatting stayed day-only, Portfolio ignored a minute precision input, Yield shifted the sentinel two hours into browser-local time, and mounted geometry measured 4px instead of 8px. GREEN passed focused component10/10, all next-family units36/36 and the affected browser specification25/25 with zero unexpected, flaky or skipped cases in `design-system-next-charts/optical-refinement/evidence/portfolio-timestamp-playwright-report.json`. The separate final-feedback browser contract also passed6/6 across both themes and 320/390/1400 Portfolio hierarchy checks in `portfolio-timestamp-final-feedback-playwright-report.json`. App/E2E TypeScript and scoped lint/format pass; light/dark captures were inspected without overlap or hierarchy regressions.
- Scope against `49f9f22ae` passed lint and typecheck. Its broad inherited-tree Vitest run passed 1389/1390 tests; the sole sandbox filesystem-watcher case then passed7/7 with its native backend. Dark review found a stale Current Review 4px claim, and closeout audit found the same stale value in the final-feedback browser contract; both were corrected. Light self-review found no further scoped blocker. Production, source data, range math, plot geometry and shared defaults remain unchanged. Human visual acceptance and engineer review before adoption remain required.

## 2026-09-16 — Yield CSV narrow label

- The lab's existing FileDown InlineAction now shows visible `CSV` below a 22rem chart container and `Download CSV` from 22rem upward. Its accessible name remains `Download CSV`; one action still owns the handler, 20px visual height, expanded target, disabled title/description and download behavior. The change is local to MetricExport and uses the parent chart section's existing container query rather than a viewport breakpoint or shared default.
- Browser RED showed `Download CSV` at the 320px narrow state. The exact light/dark 320/351/352/390 container boundary then passed2/2, and the full text-control specification passed9/9 with zero unexpected, flaky or skipped cases in `design-system-next-charts/optical-refinement/evidence/yield-csv-responsive-playwright-report.json`. The refreshed narrow captures show no visible or expanded-target overlap with the range track. MetricExport reasons pass3/3; app/E2E TypeScript, scoped style, wiki and diff checks pass. Human visual acceptance and engineer review before adoption remain required.

## 2026-09-16 — Required design-system handoff refinement

- Coherent non-trivial design-system delegation now requires the existing bounded handoff; only tiny local fixes and documentation-only changes may state an exception. The packet separates approved human decisions, coordinator interpretations and unresolved blockers; adds an explicit rendered-precedent contract; scopes starting/returned identity to owned paths and relevant evidence; maps criteria to proof; and requires a final post-edit affected-surface run with retained evidence. One topology pointer routes to the domain-owned rule without a broad skill rewrite or new enforcement tooling.
- [Receipt and predeclared pilot rubric](../plans/design-system-workflow-read-paths.md#required-handoff-refinement--september-16): Toast then Progress may test integration of existing detailed drafts but cannot prove independent composition. No suitable untouched ordinary component is queued, so the clean-composition pilot remains unassigned. Fixed model/reasoning, preserved first render and repairs, cause classification and pre-human verification capture are required before either pilot. Scoped format/wiki/diff checks pass; independent Intent/Risk review passed after its placeholder-row Minor was fixed. This stage changed no component or comment scanner and created no commit or push.

## 2026-09-16 — Design-system documentation experience planned

- The next frontier is a neutral, searchable human documentation shell with explicit Canonical, Workbench, and Internal Records destinations. It is a curated projection over existing typed authority, routing, handoffs, tests, and evidence—not a replacement. Typography, Button, and Charts are the first migration pressure set; Toast and Progress remain parked in their detached Claude package.
- Read-only upstream discovery verified `origin/master` as the remote default, a `2312d5434` fork point, and 22-ahead/17-behind divergence at fetched `715fa1906`. The dirty checkout remains untouched. A scoped checkpoint, safety ref, clean integration worktree, merge, and focused design-system impact pass are proposed but require explicit approval before any Git or shell implementation action. See the [durable plan](../plans/design-system-documentation-experience.md).
