# Bounded design-system implementation handoff — documentation complete pass

## Outcome and authority

- User-visible result and explicit approval: implement the complete scroll-first,
  share-ready documentation repair described in the linked stage contract; user
  said “plan out this work and do it all in one large pass; delegate as needed.”
- Approved human decisions: all rendered feedback in the current thread, the
  reconciled recommendations in `design-system-documentation-complete-pass/README.md`,
  and accepted authority routed from the component/foundation catalogs.
- Coordinator interpretations: “all” covers local documentation presentation and
  complex-family navigation, not production adoption or hosted publication.
- Unresolved blocking choices: none inside the owned scope. Stop if a change would
  alter tokens, shared defaults, product mechanics, or accepted design meaning.
- Preserve/change boundary: change documentation shell, projections, fixtures,
  controls, compatibility presentation, and focused tests. Preserve catalogs,
  accepted owners, product behavior, evidence, and production callers unless the
  plan explicitly names an already-approved documentation routing correction.

## Ownership and scoped identity

- Exact owned paths: `src/views/internal/design-system/**` documentation and lab
  presentation, focused `e2e/design-system/**` tests/evidence, and this pass's
  `docs/plans/**`/affected wiki records. Shared production component defaults,
  `src/app.css`, product views, SDK/data code, and unrelated dirty paths are excluded.
- Starting ref: `02434707c0614d2262b10560eebb05cde4319931`; authorized dirty input is
  the existing uncommitted documentation candidate and its receipts/evidence.
- Returned snapshot/digest: report only changed owned paths and retained evidence;
  do not create a repository-wide manifest.
- Execution: shared checkout with partitioned writable files and one coordinator.
  Agents stop on overlap. Use isolated temporary preview ports only when assigned.
- Durable result: `docs/plans/design-system-documentation-complete-pass/README.md`.
- Do not touch or stop ports 3042, 3044, or 3055. Do not commit or push.

## Read and preserve

- Read `CLAUDE.md`, `docs/plans/design-system-v1.md`, the design-system area guide,
  and the target catalog/context route before changing a family.
- Authority: `docs/wiki/decisions.md`, typed catalogs, accepted owner named by each
  entry, and family guides for Charts and Tables.
- Evidence: the two Claude audits and captures under `/private/tmp/claude-501/…`
  prove presentation/interaction defects, not new authority.
- Preserve: provider-free standalone runtime, stable ids/aliases, keyboard focus,
  accepted component behavior, real owners, honest paused/deferred boundaries,
  and result-first continuous browsing.

## Visual precedent

- Primary precedent: the current standalone Foundations continuous reference and
  the accepted real owner for each component/family.
- Why it applies: Foundations demonstrates the approved result-first hierarchy;
  family owners provide the only safe visual content.
- Preserve: neutral documentation shell, flat structural surfaces, visible specimen
  boundaries, casual scrolling, optional depth, and stable anchors.
- Do not copy: review-board radio walls, invented page bodies, fake product cards,
  tiny scaled captures, candidate-era badges, or product chrome.
- Open decisions: none within presentation scope. New shared design meaning stops.

## Acceptance and proof

- Highest seam: standalone Playwright journeys plus existing family e2e suites.
- RED proof: new scroll/history, canvas URL, and migrated-family behavior tests must
  fail for the expected missing behavior before implementation. Pure visual/copy
  repairs use preserved before renders as the independent oracle.
- Representative state: 1400 and 390 light/dark; 320 for dense controls; one deep
  state per complex family.
- Final run: affected Vitest/Playwright suites, application and e2e TypeScript,
  scope/gate output, provider/network isolation, visual evidence inspection,
  diff check, and wiki lint.
- Evidence: retain under this plan's `evidence/` directory with a concise manifest.

| Criterion                | Test seam                               | Rendered state                             | Command or artifact    | Proof owner    |
| ------------------------ | --------------------------------------- | ------------------------------------------ | ---------------------- | -------------- |
| Scroll-sync and history  | standalone Playwright                   | Foundations and Components, desktop/mobile | focused shell evidence | shell owner    |
| Ordinary reference depth | component tests + browser               | all groups, light/dark                     | group captures         | overview owner |
| Canvas state and URL     | Vitest + Playwright                     | Select + no-control pilot                  | pilot evidence         | canvas owner   |
| Complex families         | existing family e2e + new docs journeys | default + deep state                       | family evidence        | family owner   |
| Provider isolation       | standalone runtime probe                | all migrated docs routes                   | request log            | coordinator    |

## Pilot evaluation

- Fixed model and reasoning effort: inherit the coordinator settings; do not override
  between the Select/no-control first render and repairs.
- First render: preserve before repair under `evidence/pilot/first/` with source digest.
- Repair log/final: retain under `evidence/pilot/repairs.md` and `evidence/pilot/final/`.
- Evaluate authority retrieval, precedent selection, preservation of human decisions,
  substantive repair count and cause, and whether focused verification found each
  problem before human review.

## Return to the coordinator

Return changed paths, focused RED/GREEN/final evidence, inspected states, retained
artifact paths, unresolved risks, and engineer-review requirements. Do not merge
authority changes into presentation work. The coordinator owns convergence, final
verification, reconciliation, documentation housekeeping, and human-review status.

## Returned state — 2026-09-17

The complete pass is implementation-verified and remains human-review-required.
All delegated implementation packets converged in the shared tree; final Intent
and Engineering Risk re-reviews pass with no Critical or Important finding.
The standalone entry is provider/asset safe through every rendered family, and
the retained browser/unit/build evidence is indexed from the active plan. No
authority, production adoption, shared default, transaction mechanic, hosted
preview policy, commit, or remote state changed.
