# Accelerated Components documentation handoff

Authorized 2026-09-17. This packet owns a broad, reversible Components
documentation candidate after the Foundations owner releases shared paths. The
user explicitly prioritized a useful collaborator-facing experience by
2026-09-18 over serial human acceptance. The result remains local, uncommitted,
unpublished, and human-review-required.

## Outcome and authority

- User-visible result and explicit approval: turn Components into one
  continuous, anchored reference containing all 45 catalog identities, with a
  lightweight real result immediately visible wherever current authority and a
  direct reusable owner exist.
- Approved human decisions, with exact sources: U14–U17 and the accelerated
  local-candidate authorization in
  [the documentation plan](design-system-documentation-experience.md). Start
  with Actions to establish the grammar, then apply it across all eight
  families in the same candidate without waiting for intermediate acceptance.
- Coordinator interpretations, labeled as interpretations: 31 accepted entries
  may render their direct reusable owners; Card, Table, and Chart reserve honest
  pattern slots; six exploratory entries show bounded Exploring treatments; five
  undefined entries remain truthful no-result placeholders.
- Unresolved blocking choices; stop condition and owner: stop only the affected
  entry if it requires a new variant/value, normative prose without an owner,
  canonical API/default changes, promotion of exploratory work, or a heavy
  Workbench copy. Continue unrelated entries.
- Preserve/change boundary and explicit non-goals: preserve catalogs, accepted
  decisions, implementation APIs/defaults, state sheets, detail routes, product
  behavior, Toast/Progress/Slider drafts, pattern internals, and the standalone
  runtime. Do not delete legacy material, publish, commit, push, stash, reset,
  migrate production, or touch port 3042.

## Ownership and scoped identity

- Exact owned paths; excluded surfaces and shared defaults:
  - Own `canonical-components-overview.tsx`,
    `documentation-components-page.tsx`, and new documentation-owned lightweight
    overview specimen modules.
  - Own focused Components overview units/browser tests and only the necessary
    Components assertions in existing documentation/lab/standalone specs after
    inspecting their live diffs.
  - Own the receipt/evidence under
    `docs/plans/design-system-documentation-components-accelerated/`.
  - Locale files may change only for strictly necessary documentation copy and
    only after the Foundations owner releases them.
  - Exclude component catalogs, accepted decisions, existing state sheets,
    `components-pages.tsx`, routing/providers/build configuration, canonical
    component implementation files, Card/Table/Chart internals, Transaction,
    Drawer or Data-table workbenches, Toast/Progress/Slider implementation, and
    unrelated dirty paths.
- Starting commit/ref plus authorized dirty input for owned paths and relevant
  evidence only: base `02434707c0614d2262b10560eebb05cde4319931`;
  current authorized dirty overview blob
  `f69fa201255f23331240712e5c62024e410359e1`, Components page blob
  `cc04b2c04290a3ad2bfd703e05d8321f966dccee`, and broad lab-spec blob
  `0f9d7459d6f5d33ae80e2169f3dc4d81efab889f`. Recompute after Foundations
  returns and record any changed shared-file identity before editing.
- Returned snapshot/digest for changed owned paths and retained evidence only:
  return starting/ending blobs for pre-existing files, hashes for new files and
  evidence, exact verifier output, first/final renders, and repair log.
- Execution location and isolation boundary; owned preview port if applicable:
  shared checkout `/Users/lill-kire/Code/register`; isolated loopback port in the
  3058–3065 range. Never touch port 3042.
- Actual task ID when ready and durable result path: implementation task
  `/root/components_accelerated`; receipt target
  `docs/plans/design-system-documentation-components-accelerated/README.md`.
- Previews, servers and concurrent work that must not be touched: user preview
  3042, unrelated servers, stashes, safety refs, standalone runtime files,
  active worker paths, detached Toast/Progress materials, and unrelated dirt.

## Read and preserve

- Read the repository and lab routers, workflow/stage/testing/code/UI/taste
  guidance, project rules, active V1 plan, documentation plan, accelerated
  authorization, Foundations receipt, standalone receipt, this packet, all
  component catalog records, and each record's implementation/decision sources.
- Target catalog entry and canonical implementation/accepted decision: all 45
  component entries. The typed catalog and accepted decisions remain authority;
  the overview only projects them.
- Product/data sources, each labeled what it proves and does not prove:
  - Direct reusable owners prove the accepted control/component result and
    behavior within their API; they do not authorize invented host screens.
  - Focused state sheets prove supported states and visual precedent; they are
    not imported wholesale into the overview.
  - Existing heavy review boards prove Workbench coverage only.
- Existing strong qualities, copy, state/data meaning and behavior to preserve:
  accepted geometry, typography, semantic colors, focus/keyboard behavior,
  stable catalog identities, family anchors, direct detail links, concise jobs,
  and honest design/adoption status.

## Visual precedent

- Primary rendered precedent: the real canonical implementations and their
  focused state sheets, beginning with Button, Icon button, and Action group.
- Why this precedent applies to the task: the overview must show actual system
  results without copying review machinery or inventing substitute controls.
- Qualities and relationships to preserve: component geometry and states,
  neutral flat specimen boundaries, compact hierarchy, family grouping,
  continuous scroll, accessible interaction, and narrow-screen containment.
- Qualities, states or context that must not be copied: full state matrices,
  readiness headers, evidence blocks, invented product compositions, heavy
  Chart/Table/Transaction/navigation workbenches, candidate-era scaffolding, or
  provisional InlineAction/CSV material as Button authority.
- Decisions with no applicable precedent that remain open: exact overview
  canvas density and narrow overflow treatment may remain reviewable
  presentation choices; they cannot change component meaning or API.

## Acceptance and proof

- Highest stable behavior seam and independent oracle: standalone Components
  route, catalog identities, direct implementation-owned interactions, and
  existing detail routes.
- RED proof for each changed behavior, or exact limitation if RED is unavailable:
  current tests intentionally assert a row-only/no-output overview. Replace
  those assertions only after showing the continuous-result requirements fail.
- Representative state, affected breakpoint and interaction: all 45 entries;
  each of eight family anchors; Actions as the complete ordinary sample;
  accepted, pattern, exploratory, and undefined treatments; light/dark at 320,
  390, 768, and 1400px; keyboard interactions for representative controls.
- Final post-edit affected-surface run: focused units/browser matrix, all 45
  anchors and family reloads, direct detail routes, representative interactions,
  horizontal-overflow checks, standalone build/artifact/provider guard,
  application/E2E TypeScript, scoped lint/format/locales, rendered inspection,
  diff check, and wiki lint.
- Durable evidence pointer and retention owner:
  `docs/plans/design-system-documentation-components-accelerated/README.md`
  with first/final captures, hashes, artifact/runtime scans, scoped identity,
  repair log, and morning-review list; coordinator owns retention.

| Criterion                                             | Test seam                            | Rendered state                         | Proof                          |
| ----------------------------------------------------- | ------------------------------------ | -------------------------------------- | ------------------------------ |
| All catalog identities are scrollable sections        | aggregate DOM and anchors            | 45 entries, eight families             | unit/browser matrix            |
| Accepted entries show actual owners                   | direct module specimens              | 31 accepted direct entries             | import/DOM/interaction checks  |
| Non-canonical states remain truthful                  | status projection                    | 3 pattern, 6 exploratory, 5 undefined  | unit/render checks             |
| Heavy review machinery stays absent                   | source and artifact scan             | Components overview                    | forbidden-import/chunk checks  |
| Overview works casually and selectively               | anchors, TOC and keyboard            | 320/390/768/1400, light/dark           | browser + captures             |
| Standalone isolation and detail compatibility survive | production runtime and direct routes | Components plus representative details | artifact/provider/route checks |

## Pilot evaluation

- Fixed model and reasoning effort: inherited parent settings for
  `/root/components_accelerated`; do not change during repairs.
- First-render artifact and source identity: retain the first complete 45-entry
  render before subjective repair.
- Repair log and final-render artifact: classify every substantive repair and
  retain final captures.
- Correct authority retrieved: score every treatment against the catalog,
  accepted decision, and named implementation owner.
- Correct precedent chosen and applied: score Actions and one representative
  from every family against focused state sheets.
- Approved human decisions preserved: score against U14–U17 and the accelerated
  boundary.
- Substantive repair count: record exactly.
- Failure causes per repair: missing instruction, bad composition,
  implementation error, or subjective refinement.
- Focused verification caught problems before human review: score and cite.

## Return to the coordinator

Return changed paths and scoped identity, RED/GREEN proof, exact final checks,
inspected routes/interactions, first/final evidence, repair classification,
durable receipt, unresolved per-entry questions, and any engineer-review
requirement. Mark the result `implementation-verified; human visual review
required`, never accepted. Do not commit, push, publish, delete, migrate
production, alter catalogs/APIs/defaults, or expand into Patterns.
