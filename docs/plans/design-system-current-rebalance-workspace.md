# Current rebalance workspace — deferred candidate

## Disposition — September 14

This redesigned workspace is unfinished and deferred. The
[near-term/deferred split](design-system-current-rebalance-table.md#near-term-and-deferred-scope)
owns the current direction: finish the current/history tables, then retain the
existing production auction detail/flow for their initial migration. This
workspace is neither a prerequisite nor the current row's initial destination.

Preserve the implementation, user-directed refinements, simulations, source
audits and verification at `#auctions-browse-review` for a separately authorized
resumption. Do not interpret earlier completed slices or test counts below as
whole-flow design acceptance, live integration, or resolved production risks.
The [unresolved decisions](#unresolved-decisions) remain open. The active table
lab's deeper view is a dated read-only reference, not this workspace or the
working retained production flow.

## Original goal and scope

Compose the complete current-rebalance lifecycle in a full-width lab working
section above the retained historical table. The September 13 user request
authorizes thoughtful composition, not production adoption or contract writes.
Base: `404bcbc414cf54eed988a9e6c95b74fa0c6e7251`; existing dirty lab work is input.
High-size, isolated lab work: one implementation owner, one final Dark/Light
review pair, one browser worker. No concurrent browser runs or new dependencies.

## Retained implementation and evidence

### Latest weight-review follow-up

The September 14 user request authorizes the pending recent recommendations.
The unconfirmed summary retains Estimated trade value and Execution target as
static dashes above Duration, with Available after confirming target weights.
Confirmed summaries keep that order; price errors remain unavailable, not zero.
Current units and New units replace the broader basket labels beside editor
amounts, with matching helper terminology and retained starting references.
The outlined auction-size control is already implemented and remains unchanged.
This is a bounded low-radius presentation follow-up, not production work.
[Fresh receipt](design-system-current-rebalance-workspace-evidence/weight-summary-followup/README.md):
rendered-summary RED, 19/19 browser checks, app/E2E types and scoped style/docs
checks. Human review and the existing engineer-owned adoption gates remain.

### Auction-size editing affordance

The September 13 follow-up makes Max Auction Size per Token visibly editable:
canonical secondary Button, pencil/expansion cues and the existing production
USD/default explanation. It remains an inline disclosure over the same inputs,
validation and save behavior. No production or shared-default changes. The
[bounded receipt](design-system-current-rebalance-workspace-evidence/limits-affordance/README.md)
records 12/12 browser checks, type/style verification and owner-inspected
closed/open desktop and phone captures. Human review remains required.

### Weight comparison clarification

The user approved the September 13 production-flow explanation and its wording
direction. This bounded, low-radius lab follow-up keeps the existing editor and
launch mechanics. Fixed point remains `289b2af86e8245ced89d9058a09182799c65f825`;
the earlier dirty composition is input, not this follow-up's scope.

Current holdings now have a separate illustrative fixture from starting targets.
Each editor row retains the current amount, editable target, visible starting
target and current-to-target allocation. The first-auction prerequisite says
Confirm target weights; unchanged defaults can still be saved. The editor explains
current amounts per DTF token and the on-chain-target starting reference, without
calling it an immutable original proposal. Save is explicitly local preparation,
and the saved basket warns that reload discards edits.

No production sources, persistence, transaction math, shared defaults or tokens
change. Exact-unit normalization and the real target adapter remain engineer-owned
adoption questions. New approved copy is localized in en/es/ko/zh; the remaining
lab annotations retain their existing wording. No new analytics event: these are
the existing non-executing internal-lab controls.

Verification is ready for human review: [clarification receipt](design-system-current-rebalance-workspace-evidence/weights-clarity/README.md),
23/23 regression checks plus 7/7 after final footer grouping, 27 focused unit
checks, app/E2E types and scoped style/docs checks. Owner inspected desktop,
phone, dark and translated layouts. The existing state-composition receipt below
remains evidence for its earlier source, not proof of this follow-up.

### Retained state-composition refinement

Implementation is prepared for human review; the
[state-composition receipt](design-system-current-rebalance-workspace-evidence/state-composition-verified/README.md)
owns the exact verification/reconciliation status and remaining gates.

The user's September 13 approval of the deep visual analysis authorizes a
contained refinement of this lab, not production changes. Fixed point:
`289b2af86e8245ced89d9058a09182799c65f825`; the existing dirty composition is
input. Medium, one coherent lab stage, one implementation owner and a final
read-only Dark/Light pair. Focused checkpoint verification follows the V1 cadence.

Usage: a launcher configures and then verifies saved weights before starting;
a returning viewer follows actual bid exchanges and the current launch step;
a participant reviewing an expired rebalance can separate execution facts from
financial outcomes without mistaking missing values for zero. All controls remain
local simulations and drafts remain memory-only.

One candidate keeps the outer context/auction/results bands, but gives setup,
monitoring and terminal results distinct internal compositions. Reject a universal
fixed action rail, a new drawer for asset inspection, an immediately open hybrid
editor, and an invented real-price chart. Reuse canonical inputs, disclosures,
messages, buttons and typography with local layout ownership only.

Acceptance: saved non-preset values are visible after save and survive editing,
failure and resizing; bid rows identify both legs and own their details; empty
bids and launch waits explain their state; risk consequences stay visible before
launch; first-run context is compact; all terminal metrics remain available in
purposeful groups. Existing role/network/unknown/indexing guards and static
borderless history are preserved. Browser cases cover light/dark, 1400/900/390/320,
real input hit targets and asymmetric removal assets. Rubric: task clarity,
related-information grouping, restrained density, truthful data, state continuity.
Pressure cases are visible, not held-out or independent candidate evidence.

Review topology: one implementation owner, with the project-required read-only
Dark/Light pair at final source freeze. Dark inspects truth, access, copy and
regression risk; Light inspects requested grouping, canonical reuse and retained
viewport captures. Neither edits files, starts a preview or controls a browser.
Each returns one concise report in its message; the owner reconciles against
source and fresh evidence. Missing review remains pending. Reserve a repair and
focused verification after reconciliation; no separate implementation workers.

The highest stable seam is the rendered lab with its existing offline fixtures.
Add failing saved-weight/bid/compact-context assertions before implementation;
retain current reducer, CSV, access and lifecycle suites. New short state labels
are pending the user's separate copy approval. Real curve meaning, independent
progress/accuracy definitions, production cap policy and long-list mobile UX
remain unresolved rather than inferred from illustrative fixtures.

[Claude's audit](design-system-current-rebalance-flow-audit/report.md), its
[matrix](design-system-current-rebalance-flow-audit/coverage-matrix.md), and
[eight journeys](design-system-current-rebalance-flow-audit/scenarios.md) are
the production inventory. All 81 audited source fingerprints match this tree.
The report, representative harness and retained desktop/phone evidence are
inspection inputs, not fresh proof of this new composition.
[System readiness](design-system-current-rebalance-readiness/README.md) owns
DS01–DS21, exact APIs and transfer limits. Its audit-wait gate is superseded by
this intake. History remains unchanged in geometry, metrics and navigation.

## Experience and layout decision

September 13 hierarchy revision: the user authorized the six-part recommendation
after reviewing the first workspace. The earlier verification below proves its
behaviors, not acceptance of its rejected geometry. This revision is now verified
for human review with new ordinary-viewport evidence and independent review;
the [receipt](design-system-current-rebalance-workspace-evidence/index.md) owns
the hierarchy baseline and scoped follow-up results with source fingerprints.

One implementation owner; no competing candidates. A final read-only Dark/Light
pair reviews this revision while the owner checks documentation and evidence.
Neither reviewer edits files or operates the browser. Missing review stays pending.

- A visitor first sees identity, objective state, progress and the applicable
  permissionless deadline. A connection affordance does not claim permission.
- A launcher reviews the next auction's estimate, assets and visible warnings;
  any weight prerequisite happens in the same working surface. The action stays
  in a stable operation region through wallet request, receipt and indexing.
- A returning participant sees live bids or the next required auction without
  losing overall progress. Auction number is never “of N”. Expiry/completion
  preserves the result until the user moves it into history.

The overall header owns proposal provenance and expiry together beneath the
title. Expiry is a normal-weight 14px/20px sentence, without a clock or field-like
label/value spacing. Details is the sole outlined secondary action beside the title. Metadata
wraps beneath both on narrow widths without reducing its text size.
Details opens general Rebalance information, not the next auction's terms;
its standard 44px Button retains canonical outline/hover treatment and its chevron reflects open state.
The title's top edge and the button's right edge retain the card's 24px inset.
At wide widths Details is vertically centered against the complete title/metadata
block, whose text owns an 8px gap. On narrow widths the button retains its 24px
top inset beside the title, while metadata spans the full header width.
One Auction N heading, objective status and applicable event clock own the trade
plan and launch terms together. Known preparation scenarios name their purpose.
The description sits 8px below the title row rather than inside the plan column;
on phones, status and time follow the title/description pair. Before launch, a roughly 60/40 body places Selling
and Buying beside each other on the left and terms/access/action on the right,
separated by one quiet vertical rule. Narrow plans stack Selling above Buying.
The wide preparation terms and action form one bottom-aligned group, with spare
height above it; the vertical separator still spans the working area. Live bids
remain top-aligned. Narrow reading order and internal details/button spacing stay
unchanged.
Each side uses the canonical 32px overlapping logo stack with every asset, then
the complete comma-separated ticker list 8px below it in exactly the same order.
The list uses the existing 14px/20px muted supporting role; auction type, warnings
and launch readiness retain emphasis. Logos and Selling/Buying labels are unchanged.
Long stacks increase overlap to fit their available width; labels wrap naturally.
Selling/Buying fill the plan column without an additional maximum-width cap.
Preparation keeps a 3:2 split; removal allocates more of the plan to its longer
Buying list. Live monitoring uses 2:1 with an 18rem minimum for bid inspection.
Hybrid setup is a compact stack; after saving, a borderless Asset/New units/% of
Basket table exposes the saved values. Its narrow identity column protects full
symbols without reducing ordinary 16px financial values. The editor uses aligned
rows and bounded desktop inputs, with associated validation and mobile stacking.
There is no asset cap or `+N` abbreviation. This replaces the individual-grid
trial. The full asset/liquidity disclosure retains detailed amounts. Shared logo
defaults, production sources and fixture ordering are unchanged.
There is no nested colored surface or sidebar title. High-impact warnings remain
visible above the plan; the Ondo limit explanation sits beside the affected launch
terms, outside the asset disclosure. Launch-step feedback precedes the disabled
action, and indexing recovery is a smaller secondary action. Existing wording is
retained pending specific copy approval.
The compact Assets and liquidity trigger follows the wide plan independently of
the action column's height,
then expands at full width below both columns, using established
Table/identity/value anatomy and a constrained record projection. One disclosure
owns access to the complete list; token summaries no longer toggle it secretly.
A repeated trigger at the end collapses the list and returns focus to its owner.
During live monitoring, chart and bids replace preparation under the same Auction N
heading. Each bid leads with both sides of its exchange, with its number secondary;
details expand immediately beneath their owner and the selected curve marker
shows the same bid number. The redundant live subtitle is removed; its existing
wording remains only with the empty Bids count until empty-state copy is approved.
Refreshed liquidity remains explicitly estimated, not original
live terms. Rebalance so far follows the working area: progress beside deviation,
completed auctions and traded value. Above a 1024px container these are stacked
facts; below it they retain matched 14px inline pairs. Execution progress is a
percentage only, without an extra bar row. Before any auction, a compact initial
strip retains zero completed auctions and starting deviation rather than four
large zero-valued facts. The next target remains an auction term. Outcomes replace
the working band and cumulative strip with execution and financial groups under
the outcome heading: accuracy/NAV lead, five matched 14px supporting pairs retain
all other metrics. Still-open rebalance deadlines remain in context.
Phone order is heading, trade plan, terms/access/action, optional asset inspection,
then cumulative context. The editor remains in this auction's working area. History
retains its static card-color table, content axes and absence of gray row dividers.

The strongest risk is that a long expanded asset list could obscure the relationship
to launch and push cumulative context away. Keep the launch summary before expansion,
give the expanded list clear column headers/group labels, preserve its collapse
control, and inspect the full snapshot-sized list at ordinary desktop/phone heights.

Transfer from transaction work: stable action emphasis, matched 14/20 inline
facts, retained draft/operation identity and scoped recovery. Reject its dialog
width, amount objects, stepper, large outcomes and celebratory attachment.
Primary preparation and live monitoring are rendered and critiqued before the
remaining scenario variants. Visible pressure cases (not held-out): hybrid
editor at 320px, unknown live state, and two current records with independent
drafts/operations.

Rubric: (1) state/action understood without opening details; (2) restrained
hierarchy alongside history; (3) all preserved jobs reachable; (4) no fake
financial/transaction certainty; (5) same task survives narrow/wide and recovery.

September 13 section-boundary follow-up (isolated, low-profile lab UI): keep one
card surface and use two inset semantic dividers to separate general identity,
the working auction and cumulative results. Auction N uses the item-title role;
Rebalance so far now uses the muted 14px label role. Assets and liquidity becomes a compact subordinate
disclosure, keeping its count/chevron beside its label and its expanded table at
full working width. Move the detached liquidity summary inside that disclosure;
specific warnings remain visible before launch. Reject nested frames/tinted cards
and extra 8px inset layers: they add weight and reduce phone room without clarifying
ownership. Visible pressure cases are a repeated auction with its 18-token list
expanded at 320px, and live/error states. Verify section boundaries, compact trigger
geometry, full-width rows, focus and unchanged launch/recovery behavior before review.

September 13 spacing follow-up (low profile): intrinsic liquidity and live-timer
pairs opt into an 8px inline gap. Full-width launch and inspection detail rows
keep their aligned values and 16px minimum gap. Cumulative desktop facts are now
stacked by the independent-review refinement below. The reused lab detail
component gains an opt-in only; shared defaults and production consumers do not
change. Scope is compact reading distance, not compression of table columns or
the 24px section rhythm. Owner self-review and focused responsive evidence apply.

Earlier September 13 composition follow-up (isolated, low-profile lab UI), based on
`289b2af86e8245ced89d9058a09182799c65f825`: the shared Auction N heading spans the
working area with objective status at the far right. The description belongs to
the trade plan. At wide widths Selling and Buying sit beside one another; the
compact asset disclosure finishes that left-hand plan alongside the operation,
instead of creating an empty full-width footer below the launch button. One quiet
vertical separator distinguishes plan from operation within the same surface.
Opening assets still reveals full-width tables below both columns. Narrow widths
retain plan → operation → disclosure → table reading order in one mounted tree.
Live title/status remain on one row, with the timer beneath on narrow widths.
No new heading/copy, nested surface, fixed height, transaction behavior or history
change. Review ready/repeat, visitor, unavailable, hybrid and live states; prove
shared heading ownership, balanced preparation height, full-width inspection,
unclipped 320px controls and retained disclosure/bid state through resizing.
Existing horizontal facts stay matched at 14px. Owner self-review applies. The
independent-review refinement below supersedes its bottom-aligned disclosure
geometry while retaining this shared heading and plan/operation structure.

## September 13 independent visual-review refinement

Human-review-required; verified medium, domain-local stage based on
`289b2af86e8245ced89d9058a09182799c65f825`.
The inspected uncommitted composition/header changes are input, bound by the
[independent review](design-system-current-rebalance-visual-review/report.md)
source manifest. This is one candidate and one bounded verification pass, not a
checkpoint, production adoption, or a new chart design.

Usage: a disconnected launcher locates the phase deadline, connects and prepares
without losing permission context; a monitor expands a bid immediately beneath
its owner and can close a long liquidity inspection without searching upward;
a returning participant sees one outcome summary, then a truthful empty current
section after the simulated history handoff. Unknown metrics remain unknown.
The same simulation controls remain available to reviewers, with no real writes
or analytics events. Existing copy stays unless the authorized recommendation
explicitly removes redundant presentation; new error explanations are lab-only.

Geometry owners: header retains 24px insets; auction heading groups status and
the applicable event clock; the plan owns its disclosure position independently
of action height. Desktop cumulative facts stack 14px labels over 16px values;
narrow pairs retain 14px/20px on both sides. Progress occupies its own fact.
Outcomes use the full working width, without an empty action column. Bid detail
uses the existing disclosure presentation, not a new selected Button variant.

| Finding | Disposition for this pass |
| --- | --- |
| VR-01 | Apply status/time grouping and stable outcome position; retain launcher permission timing and still-open rebalance expiry. |
| VR-02 | Apply bounded plan grouping and a count with the bids heading; reject a prescribed 440px rail and new filler facts added merely to occupy space. |
| VR-03 | Apply responsive stacked facts and progress ownership; preserve each metric's meaning and unknown state. |
| VR-04 | Apply owned bid expansion with visible disclosure state and synchronized chart selection. |
| VR-05 | Apply one outcome group and leading outcome explanation; retain distinct progress/accuracy/deviation and the lab-only handoff, not an invented production archive action. |
| VR-06 | Rebalance identity column and add bottom collapse/focus return; reject an arbitrary 48px row-density change. |
| VR-07 | Remove the orphan unknown-description dash. The later authorized clarity pass adds scenario-backed round-purpose labels only when data is ready; they remain illustrative, not inferred production metrics. |
| VR-08 | Group deviation with execution progress as present-state context, not launch parameters. |
| VR-09 | Make wide disclosure position independent of operation height. |
| VR-10 | Associate the capped term with its warning tone; retain canonical retry controls. |
| VR-11 | Add explicit associated lab validation messages; retain current allocations and unavailable dependent target percentages. |
| VR-12 | Keep one mobile-first DOM and meaningful focus return; reject breakpoint-specific tab ordering that conflicts with narrow reading order. |
| VR-13 | Defer shared dark surface changes; no local token exception. |
| VR-14 | Add the current empty state and whole-dollar history formatting; preserve the existing focus destination on the History heading. |
| VR-15 | Retain established operation feedback; distinguish rejection/revert only through existing operation state, with product wording still gated. |
| VR-16 | Retain the accepted outlined Details popover/chevron; a chevron is not exclusive to inline expansion. |
| VR-17 | Fold the empty count into the Bids heading; do not invent a new empty-state sentence. |
| VR-18 | Not verified as a persistent defect; check loaded images during visual review rather than patch a transient capture. |

Acceptance: RED browser regressions for bid ownership, bottom collapse/focus,
validation and terminal handoff, then the same tests GREEN; focused existing
lifecycle/geometry tests, app/E2E types and scoped lint. Inspect ready/repeat/live,
outcome, warnings and editor at actual 1400/900/390/320 viewports in both themes;
inspect phone popovers and preserve state through resizing. A visible pressure
case is expiry with an indexing wait plus unavailable results. Record exact
evidence and scoped Dark/Light reconciliation at closeout. No held-out claim.

Review topology: one implementation owner, then the repository-required read-only
Dark/Light pair (medium profile), partitioned into correctness/interaction risks
and intent/visual-pattern fit. Inputs are this disposition table, Claude's original
report, the diff from `289b2af86`, and the source-bound focused evidence. Reviewers
do not edit files, operate previews or run competing browser suites. Each returns
one report under 400 words; the primary agent verifies and reconciles findings.
Both reviews are reconciled. Their shared bid-overhang finding was repaired and
verified; final owner inspection also corrected medium-width summary alignment.
The [refinement receipt](design-system-current-rebalance-workspace-evidence/index.md#independent-visual-review-refinements--preceding-candidate)
records 34 unit checks, the 74-case regression run, final 39-case alignment and
disclosure recheck, source-bound captures, types and scoped style verification.
Human visual approval and production-adoption decisions remain open.

The user-directed asset-summary follow-up first tried individual three-row grids,
then replaced them with complete logo stacks and comma-separated labels. Its
[receipt](design-system-current-rebalance-workspace-evidence/index.md#preparation-asset-summaries--latest-follow-up)
retains each revision's own source-bound checks rather than restating preceding
lifecycle runs as fresh evidence for the latest presentation.

## September 13 recent-feedback batch

Fixed point: `289b2af86e8245ced89d9058a09182799c65f825`. The inspected dirty
workspace is input; this medium, lab-only stage adds four requested refinements:
group the auction description 8px below its heading, keep weight editing beside
the basket with a blocked launch in its usual place, align live chart/bid headers
and chart/timestamp axes with a visible current point, and move the history
handoff into Lab simulation controls. Complete asset stacks, centered header,
bottom-aligned preparation and current/history ownership remain unchanged.

The existing explanatory weight sentence describes the disabled launch; reuse
existing copy except the previously recommended `Now` curve marker. The existing
preview clock defaults on (and remains pausable); its frozen illustrative time
is not a live-market feed. Motion is the passage of time, not pill animation.
No wallet, shared defaults, chart library/system, production mechanics, routes,
automatic archive policy or adjacent Discover-card work is in scope.

Acceptance evidence: rendered spacing/ownership and ticking/pausing tests RED
before edits, then focused browser coverage for preparation, hybrid draft/save,
roles, live bids, reduced motion and completion/unknown handoff at ordinary wide
and narrow viewports. App/E2E types, local lint/format, unit seams, source-bound
captures and a bounded independent review close the stage. No analytics event
is added for these local lab simulations or presentation-only changes.

The hybrid's prelaunch Basket owns Manage Weights before and after saving. The
launch action stays in the operation column, disabled by the existing prerequisite
gate and associated with the visible weight instruction. Unknown/busy/network and
viewer restrictions still apply. The redundant Weights saved field is removed.
Live chart and Bids headers share their top axis; time labels align with the plot
axis rather than the SVG paint bounds. The Now marker disappears when auction
state is unknown. The outcome has no product-facing archive control: the existing
guarded handoff is inside the collapsed Lab simulation controls.

Implementation is ready for human review. The
[receipt](design-system-current-rebalance-workspace-evidence/index.md#recent-feedback--current-candidate)
records 78 browser checks plus a final nine-case recheck, 34 unit checks, types,
scoped style checks and the reconciled independent review. No production adoption
or human visual acceptance is implied.

## September 13 clarity follow-up

Preparation titles distinguish precision,
progressive and token-removal scenarios; hybrid setup remains a prerequisite.
Unavailable data suppresses the round label. The original production enum names
are evidence for these illustrative scenario labels, not a new classification rule.
Permission timing reads Anyone can launch in; minutes are rounded upward until
the final minute, when seconds are shown. Live ending time keeps its existing precision.
The local Execution target label uses HelpTooltip to explain estimated total
progress including earlier rounds, never guaranteed completion. The production
rebalance progress component reads `relativeTarget`; the installed v5 library
normalizes its auction-size-adjusted target against initial progression.
No library math or live metrics adapter is changed. Simulation controls remain
record-owned but sit outside the white product card. The summary heading is
subordinate; all four metrics and history remain unchanged.

## Non-goals

No SDK/RPC hooks, wallet writes, calldata, new permission policy, production
router blocking, persistent storage, shared defaults, tokens or universal flow
controller. No v2 replacement, v4 transaction claim, manual bid form or copied
debug-dependent behavior. The lab cannot settle engineering uncertainties.
Chart curve is explicitly schematic; bid amounts are separate illustrative data.

## Production state transfer

Every audit ID is covered by one range below; exclusions are not silent removal.

| Audit IDs | Disposition and location | DS / proof or gate |
| --- | --- | --- |
| A-01, A-10, O-03 | Retain legacy navigation as an independent native entry; no v2 composition | DS17; actual destination |
| A-02–A-04 | Current section, per-record identity/state/deadlines; optional two-record pressure | DS01–04; isolation and clock transitions |
| A-05, B-01–B-02, P-01 | Same-view operation; proposal provenance remains a native link; record query preserves lab selection through reload/back | DS17; no nested interactive elements |
| A-06–A-09 | Retained history table and its states; current results transfer once, not duplicated | DS15–16; existing history regression |
| B-03, B-06, N-01 | Visible expiry and phase deadline outside disabled controls; explicit no-community branch | DS04; mounted phase crossing |
| B-04, M-07 | Explicit empty/not-found/loading/error preview; no default launch from absent identity | DS08,16; recovery |
| B-05 | Nonce, source and overall expiry in secondary inspection; not an auction ordinal | DS02,17; inspectability |
| C-01–C-05 | Cumulative execution percentage and compact supporting facts; auction target belongs to launch terms; pending/unknown distinct from zero | DS02–03,19; typography and partial data tests |
| D-01–D-05 | Round/estimate/assets on preparation; live replaces launch region; all assets keyboard/touch reachable | DS05,10,12; asset inspection and no live launch |
| E-01–E-10, F-01–F-07, N-02–N-05 | One local simulated operation with connection/network/role gates, receipt/reject/revert/indexing states; no timer-based re-arm | DS05,08,21; click-driven sequence and recovery |
| E-11 | No product analytics from fixture controls; adoption event mapping deferred | Engineering adoption |
| E-12, I-07, L-01–L-03, L-06, O-04 | Show effective auction size/cap; do not promote debug-only volatility, sliders or toggles into launcher controls | DS02,14; cap visible; debug tuning explicitly deferred pending production control policy |
| F-08 | Price-gap state exposes incompatible source paths as an unresolved adoption gate; no claim of proven community safety | DS08; fail-closed lab proposal, engineer review |
| G-01–G-09 | Source-identified hybrid prerequisite; inline units editor, allocation, CSV import/template, per-token max sizes, save/discard/reopen and memory-only warning | DS05,12,14–15; non-preset edit, invalid CSV, resize, failure retention |
| G-10 | Do not transfer diagnostic console logging | Source check |
| H-01–H-04, H-06–H-08 | Live schematic plus accessible bid list/details; data-wait guard, explicit simulated refresh and end-to-next auction | DS02,12,17,20; keyboard and repeat journey |
| H-05 | No manual bidding action; none exists in source | Inventory check |
| I-01–I-06, I-08–J-06 | Liquidity details/leg retries/totals; high-impact and Ondo warnings remain visible; conditional filler run/stop/error simulation with leave guard | DS05,08,12,15; simulated retries and guarded reset; router-level blocking deferred |
| K-01–K-07 | Target reached, expired complete/incomplete and unavailable outcomes; same identity, no fabricated metrics; explicit history handoff | DS02–04,08,16; completion/expiry sequence |
| L-04–L-05 | No raw diagnostics; ordinary accessible bids retained by proposed design | DS12,20; product approval before adoption |
| M-01–M-06, M-08–M-10 | Independent pending/error/metadata/price/bounds/auction-state states with known context retained and scoped retry | DS08,16; no zero fallback or stale readiness |
| N-06 | Preserve launcher-specific path; do not introduce a mode choosing unrestricted calldata | Engineering gate |
| O-01–O-02, O-05 | Snapshot v5 identities/durations; no v4 execution proof; v4 remains separate adoption gate | Source test |
| P-02–P-08 | Single responsive tree, reachable details/controls, phone editor/outcome, meaningful focus return; simulated filler guard only | DS05,09,12,14; mounted hit-tests/keyboard/resize |

## Acceptance evidence

- Revision RED test: current Auction N must own both trade plan and action; its
  surface is card-colored; cumulative progress follows it and has no next-target
  marker; the expanded list has real table columns and aligned cells. Confirm the
  old composition fails before editing the implementation.
- Revision geometry: desktop and intermediate widths use shared heading/axes and
  balanced columns; phone keeps the trade plan before the primary action; expanded
  liquidity uses the full working width with no page overflow or duplicate controls.
- Revision semantics: general inspection excludes completed-round count, duration
  and volatility; completed rounds stay in cumulative context and duration/target
  in auction terms. Remove the old invented 2% volatility display rather than attach
  a debug-only parameter to an unrelated target-help control. Production debug
  behavior and engineering review of real launch parameters remain excluded.
- Revision taste pass: ready, next-round and live first, then visitor, warnings,
  hybrid/editor, pending/indexing/recovery, no-bids, unknown data and completion;
  light/dark, 1400/900/390/320 and actual container breakpoint edges.
- Eight audited minimum states plus data errors, no-community, two-record and
  loading recovery accessible from lab controls; actual click-driven operations.
- Browser captures and direct inspection at 1400×900, middle width, 390 and
  320px, light/dark. Actual container breakpoint edges; no overflow or duplicated
  active tree; 44px controls are hit-tested, not inferred from CSS.
- Source-derived identities and native provenance; synthetic estimates, bids
  and operation states explicitly labeled. Source fingerprints bound to captures.
- Same-row typography equality, history regression, focused unit tests,
  app/E2E typecheck, scoped lint/format, documentation links/wiki/diff checks.
- Final Intent and Engineering Risk reviews; product/engineer decisions remain
  named, not silently resolved by passing fixture tests.

## Test seams

Rendered lab component for transitions, controls and data boundaries; pure local
simulation reducer for forbidden transitions; browser for geometry, routing,
focus and source-bound screenshots. No production collaborators mocked inside a
new controller. Lab fixtures cannot validate financial arguments or live sources.

## Slices

- Slice: shared auction ownership, compact context, separated cumulative evidence,
  balanced preparation and live monitoring; blocked by: none. RED ownership test.
- Slice: full-width liquidity table and constrained projection, specialist detail
  controls and general/auction inspection separation; blocked by: working ownership.
- Slice: lifecycle/role/width matrix, taste corrections, independent review and
  documentation reconciliation; blocked by: both prior slices. No commit or push.

## Unresolved decisions

Specific new state copy was requested asynchronously for approval before use.
Lab chrome may describe simulation; it is not production wording authority.
Engineering owns confirmed live-auction source, both real write paths, wrong
network behavior, calldata and units constraints, cap/volatility control policy,
hybrid persistence, filler polling/navigation guard and v4/v2 coverage.
Memory-only preparation stays explicit; no persistence introduced. Defects are
represented by safe proposed states, never advertised as fixed in production.
Final visual judgment and newly proposed copy remain human review gates.

## Candidate and verification boundary

Implemented in `src/views/internal/design-system/auctions-current/`, integrated
through the retained history review. All live behavior is local simulation.
The selection query is lab-local, including a guarded preview reset; browser
leave warns for pending operations, drafts and running filler. Production
router blocking remains unimplemented. A mocked connect deliberately chooses
the launcher persona, visible in the Viewer control; it does not infer real
wallet permissions. The UI's safe data gates are proposed adoption contracts.

The hybrid form uses React Hook Form/Zod with decimal strings and bigint-only
illustrative allocation. CSV is atomic and rejects incomplete/unknown/duplicate
records rather than copying production's silent partial-import behavior. Source
token identities and fallback logos are retained. Non-preset drafts and limits
survive failure, responsive changes and editor return, not reload. No amount
here is an SDK weight or transaction argument.

The bid curve is expressly schematic, as is production's; real-valued price
history is not invented. Bid details remain separately labelled illustrative.
Traded value is cumulative across auctions, with each simulated bid set counted
once. The completed view and its historical row share the same fixture result.
Current-list loading and unmatched routes never manufacture a usable record.
Warning pressure adds source-identified BSC NVDAon from the BUILDOUT Discover
snapshot, explicitly not a real CMC20 holding. Limited and market-closed scenes
retain session, next-open/close, capacity and upcoming-event disclosures. These
are independent illustrative inputs, not executable quotes or cap calculations.
Per-token liquidity levels/reasons and chain-qualified counterpart details use
keyboard/touch popovers; retries remain separate, reachable buttons.

Final review reconciliation restored source accuracy/NAV help and dollar impact
to the current outcome itself, not only history. The filler keeps its source
name and submitted-order count with local run/stop/error/order simulations.
Guarded browser Back confirmation applies the requested scene explicitly.
Expiry reconciles the ended live round exactly once before creating its result.
CSV file-picker import preserves the source job; the production drag/drop
gesture is not reproduced by this candidate.

New receipt/unknown/filler explanations are visibly prefixed **Lab** pending
specific copy approval, not promoted product copy. Source editor/action wording
is retained. Human visual review and engineer approval remain mandatory.

[Evidence and closeout](design-system-current-rebalance-workspace-evidence/index.md)
records the final stable source, actual checks, review reconciliation and
known limitations. Early captures/tests are diagnostics, not the final receipt.
