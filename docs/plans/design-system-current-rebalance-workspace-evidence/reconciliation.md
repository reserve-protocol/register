# Independent review reconciliation

## September 13 hierarchy revision

The final read-only Intent/Risk pair reviewed the auction-owned hierarchy,
not a fresh audit of all pre-existing dirty lab work. The owner verified every
finding before adoption; both affected-axis correction reviews found no remaining
blocker. The final source-bound current/history run passes 37/37.

| Finding | Disposition and evidence |
| --- | --- |
| 320px failed-status and Retry squeezed the ETH identity | Confirmed/fixed. Rendered ticker overflow was 46px; narrow recovery now stacks. Updated `hierarchy-320-liquidity.png` inspected by owner and Intent reviewer; resize preserves trigger focus and keyboard return. |
| Outcome repeated completed count and stretched label/value spacing | Confirmed/fixed. One cumulative count remains. RED measured 24px apparent gap; natural stacked spacing now passes and three primary outcome values align. `hierarchy-complete.png` independently inspected. |
| Expiry during indexing rendered two recovery messages/retries | Confirmed/fixed. RED found two regions and controls; CurrentResult owns recovery once, and launch stays disabled before/after data retry. Risk correction review confirms ownership and unchanged busy guard. |
| Pending operation lost Auction N identity after expiry | Owner-confirmed/fixed alongside the recovery regression. Closed/busy operation retains its heading without creating another launch or action tree. |
| Utility-sized logos actually rendered 20px | Owner-confirmed/fixed. Browser RED measured 20px; public `TokenLogo` sizing now renders 32px table identities and 24px editor/basket marks. |

Direct browser accessibility inspection preserved table/row/cell relationships at
320/900px; the new test additionally verifies focus through reflow. Human visual
acceptance and the engineering adoption boundaries remain explicit in the
[current receipt](index.md).

## Earlier workspace behavior review (retained context)

One final two-axis pair, followed only by affected-axis correction checks.
Fixed point `404bcbc414cf54eed988a9e6c95b74fa0c6e7251`; contract is the
[workspace brief](../design-system-current-rebalance-workspace.md).
Reviewers were read-only and did not operate a browser or another preview.

| Finding | Disposition and evidence |
| --- | --- |
| Guarded browser Back → Reset dismissed the dialog without applying the scene | Confirmed/fixed. Actual-hook test failed RED, then passed after explicit scene application. Browser journey tests edited hybrid → Back → confirm → ready. |
| Direct expiry skipped live-auction accounting | Confirmed/fixed. Direct expiry and end-then-expiry must produce identical reducer state; both count one round and stop filler. A subsequent advance does not double count. |
| Hybrid advertised Ready before saving weights | Confirmed/fixed. Explicit provisional `Lab: weights required` status; wallet authority remains a separate concern. |
| Liquidity/Ondo facts were incompletely represented | Confirmed/fixed. Per-token level, reason, retry, counterpart, impact and Ondo session/cap/calendar details are accessible by button/popover. Warning names identify affected assets. Synthetic NVDAon insertion is conspicuously qualified, rather than presenting XRP as an Ondo token. |
| Current results omitted source help and dollar impact | Confirmed/fixed. Accuracy and NAV source explanations retained; current and historical views consume the same result values. Known zero trade has zero illustrative impact rather than a nonzero percentage paired with $0. |
| Filler name/order count omitted | Confirmed/fixed. CowSwap Auction Filler identity, persistent submitted count and local order/run/stop/error controls retained. No SDK or actual order submission. |
| Base liquidity counterpart said WBNB | Confirmed/fixed in affected review. Counterpart now follows record chain, WETH on Base. |

Engineering Risk's affected re-review passed the actual-hook/reducer correction
tests (21/21) and found no remaining blocker in those branches. Intent confirmed
the four source omissions restored and passed the affected visual axis after
inspecting refreshed hybrid/results in both themes and narrow Ondo disclosures.
That earlier candidate passed 27/27; it did not establish acceptance of its
subsequently rejected geometry. The [current receipt](index.md) supersedes its
source digest, screenshots and verification claims.

Product wording/visual acceptance and engineering adoption remain named gates,
not review failures hidden by a successful fixture simulation. Production live
truth, calldata, permissions, wrong-network handling, filler/router behavior,
hybrid persistence and version coverage were not changed or proved here.
