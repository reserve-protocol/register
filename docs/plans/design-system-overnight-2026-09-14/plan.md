# Overnight design-system preparation — September 14

## Goal

Prepare tomorrow's chart-system work, inspect non-auction tables and isolate any
verified presentation-only fixes, and establish the remaining governance scope.
Deliver one concise morning handoff. The user authorized this work and
persistence to completion, not production migration or sensitive technical repair.

## Current state

Primary checkout is `design-system-v1` at `289b2af86e8245ced89d9058a09182799c65f825`
plus accumulated uncommitted lab work. Claude is reviewing that source tree;
leave its source, tests, fixtures and configuration untouched. The chart audit
is in the detached `register-claude-charts-audit-289b2af86` checkout under
`docs/plans/design-system-chart-readiness-audit/`. Read it as checkpoint-bound
evidence, not accepted design or permission to change source semantics.

High by multi-slice size; isolated/domain-local radius. One implementation owner
because source/fix/verification decisions are coupled. At final closeout use
the required read-only Dark/Light pair on the bounded deliverables. Root owns
reconciliation; unavailable review remains explicit and can be retried within
scope. No delegated implementation or concurrent browser suites.

## Non-goals

- No auction changes, production consumers, shared defaults/tokens, product
  copy, financial definitions, source selection, RPC/SDK/subgraph contracts,
  freshness rules, permissions, routing policy or transaction behavior changes.
- No commits, pushes, dependency upgrades/installation or new checkpoint commit.
- No approval of unattended designs, removal of chart types, automatic repair
  of audit claims, or broad repeat of existing audits.
- No source writes in the primary checkout. Isolated fixes remain unapplied
  there until Claude's review and human disposition; documentation may be added
  only to this package and the existing handoff/ledger owners.

## Acceptance evidence

- Chart inventory reconciles necessary visual families, shared styling and
  container variants, meaningful exceptions, first lab slice, inherited
  precedents, and explicit human decisions. Technical concerns have a separate
  engineer-facing disposition with evidence, uncertainty and no-change status.
- Inspect Holdings, Portfolio/withdrawals, Discover and Earn in representative
  desktop/phone/constrained and pressure states. Record actual coverage and
  concrete issues; no finding quota. Objective in-scope fixes require RED/GREEN,
  fresh affected checks and inspected before/after evidence in isolation.
- Governance source/lab state mapping identifies remaining presentation work
  and what cannot safely be called final cleanup; no speculative implementation.
- Source identity before/after proves this worker did not alter the primary
  reviewed source. Isolated diff distinguishes inherited changes from this run.
- Morning handoff links exact outputs, verification, review dispositions,
  isolated patch, useful next steps and only material unresolved decisions.

## Test seams

Existing source-bound design-system browser fixtures and feature unit tests.
Use owned alternate ports, existing offline mocks and existing dependencies;
no live wallet actions. Reuse current-source fingerprints and relevant table
checks. Documentation checks cover links, scope truth and wiki hygiene. The
Register bounded lab cadence applies, not an unrelated full-repository gate.

## Slices

- Isolate current source and establish read-only primary fingerprints; blocked
  by none. Source copy is an execution sandbox, not a new committed checkpoint.
- Reconcile chart audit into visual-system brief and separate technical
  questions; blocked by none. Verify only enough source detail to classify
  requirements and uncertainty, never silently select a new data policy.
- Inspect non-auction tables and fix demonstrated presentation defects in the
  isolated copy; blocked by isolation. Stop each candidate at sensitive semantics
  or subjective design/copy decisions and record those instead.
- Inventory governance remaining work; blocked by none. Separate list/record
  presentation, details and execution flows.
- Review, verify, document and produce morning handoff; blocked by slices above.

## Unresolved decisions

Human visual acceptance, chart-type retention, financial value/period meaning,
freshness sourcing, dense-history transport and production adoption remain open.
They do not block preparing visual contracts from explicit supplied data/state.
Strongest counterargument: another audit could create more documents without
accelerating implementation. Countermeasure: reuse existing findings, prioritize
concrete fixes and produce one first chart slice plus a small morning agenda.

## Progress

Complete as a preparation package; visual acceptance/integration remain human
decisions. Chart reconciliation, separate engineering questions, table inspection,
two verified isolated layout fixes and governance assessment are delivered in the
[morning handoff](README.md). Required Dark/Light findings are reconciled. No
overnight source changes have been integrated into the primary checkout.

Execution copy: `/private/tmp/register-design-overnight-JEwxJR/repo`, a local
shared-object clone with the actual working files overlaid and cloned existing
dependencies. Private environment files and old generated browser/build outputs
were excluded. Public reviewed source matches `primary-source-start.json`
exactly (2,437 files; digest `ef4794d91f10cd4b804475c2c0ef078d2be0bd192a6537af925729143abc80a5`).
The four Finder metadata files were copied after the initial metadata-only
mismatch; no application difference was masked. Owned browser port: 3059.
This temporary path is an execution location, not a permanent deliverable.
