# Overview mobile-axis delivery pilot

Status: complete — no implementation change required. The coordinator reconciled
the result and preserved six captures in the [result receipt](design-system-mobile-axis-pilot/README.md).
This closes the bounded investigation, not chart visual review or an implementation
benchmark. The brief below records the authorized launch contract.

App creation request: `client-new-thread:4a169ef9-90a3-4050-8c89-ddcbc6c94196`
on the local Design System V.1 project, with `gpt-5.6-sol` and `medium` explicitly
selected. This pending creation ID never resolved through the coordinator's task
list. Completion was established from the actual isolated worktree's saved output
and source comparison, not that ID. No duplicate was created; automatic task
tracking remains unproven. The result receipt records the evidence limits.

## Outcome and boundary

The user approved the proposed fresh-task approach and model on September 14.
Check that the design-system lab's Overview chart respects the established
mobile policy: no x/y axis labels on mobile, while desktop retains its useful
labels. Inspect source and rendered behavior first. Correct a demonstrated
lab presentation gap only; if it already complies, return evidence without
manufacturing an implementation or claiming an implementation benchmark passed.

Starting input: `design-system-v1` at `49f9f22ae` plus the current documentation
cleanup and this brief, copied into a separate worktree. Treat that inherited
input as read-only. Use the task's actual worktree, not the original checkout.

Start at `CLAUDE.md` and `docs/wiki/domains/design-system.md`, then follow the
normal chart/lab routes. The source-faithful chart review, not rejected generic
fixtures, is the target: `/internal/design-system/components/chart#chart-first-review`.
Current product rendering is behavior evidence; accepted V1 rules govern style.

Allowed edits: `src/views/internal/design-system/charts/`, the affected
`e2e/design-system/chart-review-lab-regressions.spec.ts`, and a concise local
result/evidence record in `docs/plans/design-system-mobile-axis-pilot/`.
Do not edit production chart renderers, shared defaults/tokens, financial
calculations, sources, history, hover returns, controls or copy. If the fix needs
one of those changes, report the boundary rather than widening the task.

## Isolation and acceptance

Use an available isolated preview port; never stop/restart/reuse the user's
3005 preview or navigate its tab. No install, commit, push, production adoption,
wallet action or changes outside this worktree. Follow focused verification,
not unrelated whole-repository suites. Existing documentation-preservation
checks are evidence for the earlier docs slice, not this implementation's gate.

Inspect an actual phone viewport and a constrained container within a desktop
viewport, plus ordinary desktop, in both themes. Preserve chart identity,
header inspection, plot, launch annotation and approved host geometry. Report
actual axis visibility and the applicable breakpoint; do not silently choose
a new responsive policy when evidence conflicts. A changed interaction or
layout needs the owning regression and direct rendered verification.

## Task-local capability profile

- Role/seam: bounded UI investigator and implementer, Sol with medium effort.
- Required abilities: follow current documentation, distinguish viewport from
  container sizing, preserve data/meaning, make local edits, verify real rendering.
- Evidence: not yet established for this model/task pairing; this user-approved
  pilot supplies bounded evidence only, with no held-out or comparative claim.
- Authority: local paths and preview above; no financial or shared-default work.
- Fallback: stop with evidence for the coordinator if required ability, tools,
  dependencies or scope are unavailable. Do not change models or relax proof.

## Return

Preserve the first completed candidate before any coordinator-requested repairs
using a patch and minimal named screenshots in the result directory; no commit.
If no fix is needed, preserve the inspection evidence instead. Report changed
files, commands/results, exact inspected states, unresolved risks, elapsed time,
available usage and repair rounds; unknown usage stays unknown. Do not store
private prompts, credentials or raw conversation logs. Keep evidence bounded.
The coordinator reviews before integration; the user judges visual acceptance.
