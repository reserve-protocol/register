diff --git a/src/views/internal/design-system/charts/next-families/portfolio-history.tsx b/src/views/internal/design-system/charts/next-families/portfolio-history.tsx
--- a/src/views/internal/design-system/charts/next-families/portfolio-history.tsx
+++ b/src/views/internal/design-system/charts/next-families/portfolio-history.tsx
@@ -2,7 +2,6 @@ import dayjs from 'dayjs'
 import {
   useEffect,
   useId,
-  useMemo,
   useRef,
   useState,
   type ComponentProps,
@@ -50,18 +49,11 @@ export function PortfolioHistory({
   const points = empty ? [] : pointsInRange(portfolioPressurePoints, range)
   const [selectedIndex, setSelectedIndex] = useState<number>()
   const [plotWidth, setPlotWidth] = useState(0)
+  const plotRef = useRef<HTMLDivElement>(null)
   const isTouchInput = useRef(false)
   const gradientId = useId().replace(/:/g, '')
-  const axisLabels = useMemo(
-    () => [
-      formatCompactUsd(0),
-      ...portfolioPressurePoints.map((point) => formatCompactUsd(point.value)),
-      formatCompactUsd(
-        Math.max(...portfolioPressurePoints.map((point) => point.value)) * 1.1
-      ),
-    ],
-    []
-  )
-  const axisWidth = useAxisWidth(axisLabels)
+  const axisValues = points.map((point) => point.value)
+  const axisMeasurementKey = `${range}:${plotWidth}:${points.length}:${Math.min(...axisValues)}:${Math.max(...axisValues)}`
+  const axisWidth = useAxisWidth(plotRef, axisMeasurementKey)
   const selected =
     selectedIndex === undefined ? points.at(-1) : points[selectedIndex]
@@ -175,6 +168,7 @@ export function PortfolioHistory({
           )}
         >
           <ResponsiveContainer
+            ref={plotRef}
             width="100%"
             height="100%"
             onResize={setPlotWidth}
