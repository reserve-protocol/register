# Accelerated Foundations documentation handoff

Authorized 2026-09-17. This packet owns a broad, reversible documentation
candidate for all nine foundations. The user explicitly prioritized having a
useful experience ready for collaborators tomorrow over waiting for serial
human acceptance between sections. The result remains local, uncommitted,
unpublished, and human-review-required.

## Outcome and authority

- User-visible result and explicit approval: replace the Foundations card
  gateway with one continuous, anchored, result-first reference. Fully resolve
  the Color presentation and apply the same documentation grammar across all
  foundations where current authority is sufficient.
- Approved human decisions, with exact sources: U14–U17 in
  [the documentation plan](design-system-documentation-experience.md#superseding-presentation-direction--2026-09-17),
  plus the 2026-09-17 acceleration note in that plan. Every section begins with
  the current system result and concise supported scope; examples use approved
  component owners or are omitted.
- Coordinator interpretations, labeled as interpretations: broad presentation
  candidates may be produced before human visual acceptance because the work is
  reversible and does not replace authority. For exploratory or provisional
  foundations, the honest primary result is the current bounded state and its
  open edges, not an invented completed system.
- Unresolved blocking choices; stop condition and owner: stop the affected
  subsection if it requires new token values, catalog meaning, normative prose,
  or a choice among unresolved designs. Keep the current source reachable and
  record the question for human review; do not block unrelated foundations.
- Preserve/change boundary and explicit non-goals: preserve catalogs, accepted
  decisions, tokens, semantic-role owners, implementation defaults, detail
  routes, evidence, Workbench/Records content, product behavior, and the
  standalone runtime. Do not delete legacy material, publish, commit, push,
  stash, reset, migrate production, or touch port 3042.

## Ownership and scoped identity

- Exact owned paths; excluded surfaces and shared defaults:
  - Own `src/views/internal/design-system/foundations-pages.tsx` and new
    documentation-owned foundation presentation modules.
  - Own new focused foundation tests and a narrowly named browser spec.
  - Own the receipt and evidence under
    `docs/plans/design-system-documentation-foundations-accelerated/`.
  - Locale files may change only for strictly necessary new documentation copy;
    reuse sourced existing language where possible.
  - Exclude `foundation-catalog.ts`, `src/app.css`, semantic-role/type/layout
    owners, shared component defaults, product routes/providers, Charts,
    Tables, Transactions, Toast, Progress, shell/runtime configuration, and
    unrelated dirty paths.
- Starting commit/ref plus authorized dirty input for owned paths and relevant
  evidence only: base `02434707c0614d2262b10560eebb05cde4319931`;
  `foundations-pages.tsx` starts at blob
  `22122471449c8e7e7dd71aad54c984ed3d109ae6`. The verified S2a shell and
  standalone entry are authorized dirty inputs but are read-only for this task.
- Returned snapshot/digest for changed owned paths and retained evidence only:
  return starting/ending blob IDs for pre-existing owned files, hashes for new
  files and retained evidence, exact verifier output, first/final renders, and
  a repair log.
- Execution location and isolation boundary; owned preview port if applicable:
  shared checkout `/Users/lill-kire/Code/register`; isolated loopback port
  3058–3065 only. Never touch port 3042.
- Actual task ID when ready and durable result path: implementation task
  `/root/foundations_accelerated`; receipt target
  `docs/plans/design-system-documentation-foundations-accelerated/README.md`.
- Previews, servers and concurrent work that must not be touched: user preview
  3042, unrelated servers, stashes, safety refs, standalone runtime files,
  shared shell/navigation files, detached Toast/Progress materials, and all
  unrelated dirty work.

## Read and preserve

- Read the repository and lab routers, workflow/stage/testing/code/UI/taste
  guidance, project rules, active V1 plan, documentation plan, Claude content
  audit, standalone receipt, this packet, every foundation catalog record, and
  the implementation/decision sources each record names.
- Target catalog entry and canonical implementation/accepted decision: all nine
  foundation entries. The catalog and accepted decision ledger remain the
  authority; presentation files only project them.
- Product/data sources, each labeled what it proves and does not prove:
  - `src/app.css`, Tailwind configuration, semantic roles, typography roles,
    layout recipes, and canonical component owners prove implemented values or
    relationships; they do not promote unresolved alternatives.
  - Existing studies, audits, comparisons, candidate directions, and product
    examples prove evidence/history only unless a higher-precedence owner says
    otherwise.
  - The earlier route audit supplies a P/S/X/W/R/D discovery map; it does not
    override catalog or accepted-decision authority.
- Existing strong qualities, copy, state/data meaning and behavior to preserve:
  stable route IDs, light/dark values, accepted role names, responsive shell,
  current detail pages, evidence reachability, localization, keyboard behavior,
  and accurate exploratory/provisional boundaries.

## Visual precedent

- Primary rendered precedent: the verified standalone documentation shell plus
  the current canonical Color role definitions, Typography role map, Spacing
  grammar, Radius roles, Motion timing rules, Iconography sizes, Accessibility
  guardrails, Elevation levels, and exploratory Layout grammar.
- Why this precedent applies to the task: these are the closest current
  authority-backed results; the task reorganizes their presentation rather than
  inventing a new visual system.
- Qualities and relationships to preserve: neutral flat documentation chrome,
  semantic tokens, square structural regions, concise hierarchy, continuous
  scrolling, stable anchors, light/dark fidelity, and progressive disclosure.
- Qualities, states or context that must not be copied: decorative foundation
  thumbnails presented as definitions, candidate-era badges, hand-built product
  UI, raw controls imitating canonical components, previous/current comparisons
  at equal prominence, review machinery before results, or stale status copy.
- Decisions with no applicable precedent that remain open: exact shadow recipes,
  general adoption of 12px/48px spacing values, completed Layout authority, and
  any new component anatomy or usage prose.

## Acceptance and proof

- Highest stable behavior seam and independent oracle: standalone Foundations
  route and direct foundation routes, checked against catalog/decision sources
  and current token/implementation owners.
- RED proof for each changed behavior, or exact limitation if RED is unavailable:
  first pin that the overview is continuous, anchored, result-first, initially
  collapses secondary detail, and does not mount the old card gateway or full
  study/evidence trees. Show focused failures before implementation.
- Representative state, affected breakpoint and interaction: all nine sections;
  Color fully expanded; one accepted, one exploratory, and one provisional
  foundation; light/dark at 320, 390, 768, and 1400px; anchor navigation,
  keyboard focus, direct reload, and technical disclosure.
- Final post-edit affected-surface run: focused units and browser matrix,
  standalone production build/artifact guard, direct routes and anchors,
  existing Color/Typography/foundation checks, application and E2E TypeScript,
  scoped lint/format/locales, rendered inspection, diff check, and wiki lint.
- Durable evidence pointer and retention owner:
  `docs/plans/design-system-documentation-foundations-accelerated/README.md`
  with first/final captures, hashes, scoped identity, repair log, and explicit
  unresolved questions; coordinator owns retention.

| Criterion                                          | Test seam                          | Rendered state                                      | Proof                        |
| -------------------------------------------------- | ---------------------------------- | --------------------------------------------------- | ---------------------------- |
| All foundations are casually scannable             | standalone aggregate route         | 320/390/768/1400, light/dark                        | focused browser + captures   |
| The actual system precedes documentation machinery | DOM order and authority map        | Color plus accepted/exploratory/provisional samples | unit/browser assertions      |
| Color is a complete primary reference              | token/role owners                  | aliases, values, purposes and boundaries            | source comparison + renders  |
| Secondary material stays secondary                 | import/DOM assertions              | closed details and linked legacy routes             | unit/browser assertions      |
| Existing authority and routes survive              | catalog and direct-route checks    | all nine routes and stable anchors                  | focused compatibility matrix |
| Standalone isolation survives                      | production artifact/provider trace | fresh Foundations and Color                         | artifact/runtime checks      |

## Pilot evaluation

- Fixed model and reasoning effort: inherited parent settings for
  `/root/foundations_accelerated`; do not change them during repairs.
- First-render artifact and source identity: retain the first complete aggregate
  Foundations render before subjective repair.
- Repair log and final-render artifact: classify every substantive repair and
  retain the final corresponding captures.
- Correct authority retrieved: score per foundation against catalog, accepted
  decision, and named implementation owner.
- Correct precedent chosen and applied: score against the sources above.
- Approved human decisions preserved: score against U14–U17 and the acceleration
  boundary.
- Substantive repair count: record exactly.
- Failure causes per repair: missing instruction, bad composition,
  implementation error, or subjective refinement.
- Focused verification caught problems before human review: score and cite.

## Return to the coordinator

Return changed paths and scoped identity, RED/GREEN proof, exact final checks,
inspected routes/states, first/final evidence, repair classification, durable
receipt, unresolved authority questions, and any engineer-review requirement.
Mark the result `implementation-verified; human visual review required`, never
accepted. Do not commit, push, publish, delete, migrate production, alter
catalog/tokens/shared defaults, or expand into Components or Patterns.
