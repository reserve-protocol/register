# Consolidation evaluation record

**Comparison reconciled; mixed result, no superiority claim.** Coordinator record for S4 of the
[consolidation plan](design-system-foundation-consolidation.md). This record,
the audit, rubric and outputs are excluded from performer workspaces.

## Frozen contract

- Decision: whether guidance-only cleanup is adequate for small component
  discovery/composition jobs, or the consolidated owner/verification package
  materially improves the first artifact. This cannot approve the system for
  general migration or establish universal first-pass reliability.
- Fixed point: `adef9ee76ffe2a579f6811cae2f5cf152cd0cf99`. Both controls start
  from a clean archive of that checkpoint, not the locally edited plan/router.
- Fixture archive SHA-256:
  `569d5a5b2fd99d9d8ad47ceb0b9f09064e8d20203e6cd76285af3eefabf0971f`.
- Two organic tasks: read-only owner/authority/spacing explanation; one small
  local Display preferences composition with provided copy and specified
  save/change/reset behavior. Canonical prompts remain coordinator-only;
  their digests and the independent rubric digest are recorded before runs.
- Arms: unchanged checkpoint, guidance-only S3a, consolidated S1–S3b. The
  changed factor is the available project package; the full-package comparison
  cannot isolate an individual code or tool change's causal effect.
- Tools: fresh Claude CLI contexts with restricted Read/Glob/Grep/Edit/Write
  tools, no shell, browser, network tools, other agents or inherited sessions.
  Matching file-tool and permission configuration across arms. Coordinator
  performs execution/render verification on the unchanged first artifact.
  This intentionally measures retrieval and initial composition, not an agent's
  ability to run the full workflow; that broader capability remains unproven.
- Provider identity is recorded from actual run metadata, not branding-based
  capability assumptions. No design-task capability is yet established; outputs
  remain provisional. Pin or rematch if later runtime/provider identity changes.
- Rubric: six independently prepared observable criteria, including exact
  ownership/status, traceability, geometry/hierarchy, local behavior, scope/reuse,
  accessibility and truthfulness. Stored outside all performer roots. Frozen
  before runs; later changes require pilot labeling and matched reruns.
- Judgment: retain first artifacts, assign randomized neutral output labels,
  then one independent judge sees all arms and the same held-out checks together.
  Early controls are not separately scored as a comparative verdict.
- Usage: two sequential control jobs, each with a 12-minute safety timeout;
  keep later treatment/judgment, verification and one repair attempt separate.
  Record provider usage plus elapsed time, tool calls, retries, verification
  and repair activity; do not invent precision for unavailable judge usage.
- Order: early controls first to reduce model-drift risk. Treatment order is
  randomized later and recorded; this is not a fully randomized temporal study.
- Read evidence: workspace-scoped tool-call/result events, not cited sources
  or performer narration. Missing or truncated event evidence stays unknown.
- Stop on failed confinement, hidden-data exposure, changed permissions,
  source identity changes, timeout, required human authority, or unavailable
  independent judgment. No silent restart of a measured failed run.

## Privacy, retention and reproducibility

Private run root: opaque ID `register-records.Kaxct8` in local temporary storage,
accessible only to coordinator/judge, not performers. Raw prompts, rubric,
transcripts and source archive stay there; durable records contain digests and
redacted source-relative access summaries only. No credentials are copied.
Delete raw copies after the joint verdict, by 2026-09-16; coordinator owns
cleanup. If treatments cannot finish by then, record disposal/retention approval
and rerun matched controls rather than claim missing artifacts remain available.
The source checkpoint remains reconstructible in git. Sanitized synthetic
outputs and selected captures may be retained under the plan's evidence budget.

## Preflight

- Claude CLI `2.1.265`; actual reported default model `claude-opus-5[1m]`.
  This is a task-local available configuration, not a model-quality claim.
- Sandboxed launch could not access the existing login; a read-only account
  check outside that host sandbox confirmed it. No login or permissions were
  changed. The authorized runner uses the existing login with restricted file
  tools, not unrestricted project access.
- Confinement probe: read of the fixture router succeeded; read and write
  attempts against a harmless coordinator-side canary were denied. Source
  remains unchanged. Raw tool-denial events were inspected before measured dispatch.
- Independent rubric preparer saw the current router's consolidation link but
  no audit, plan, review or treatment. All substantive criteria are grounded
  in checkpoint source. It is not a performer and sees no measured outputs.

## Runs and next action

Frozen before measured dispatch (2026-09-09):

- Owner task: `bff159bf87c8d1f8b8937cfe303025b55edaab5927cb8c5170dc177588a3c70b`.
- Composition task: `993fc850b620c5a6b92f168c1cdc9540c82bfccf9ac3a859aaeaa099acfb477c`.
- Rubric: `0549b50b0a50e3cb817703d800b6cf07a15670126257bfa4b144cfaad3b7eb3c`.
- Tool errors confirm the outside read/write probes were denied; the attempted
  outside file does not exist. No shell, MCP or subagent tools were exposed.

Both controls completed on 2026-09-09 local time, with first artifacts and full
tool-event transcripts preserved before S3a/S1 edits. No performer repair or
coordinator feedback was supplied. Both used CLI 2.1.265 and the actual reported
`claude-opus-5[1m]` model. Provider metadata also records a small auxiliary Haiku
call; do not describe the entire CLI runtime as exclusively one model.

| Run               | Elapsed | Tool calls               | Source delta       | Provider usage: input / cache creation / cache read / output |
| ----------------- | ------- | ------------------------ | ------------------ | ------------------------------------------------------------ |
| Owner explanation | 104.8s  | 30 read/search calls     | None               | 36 / 60,148 / 783,931 / 7,968                                |
| Local composition | 362.2s  | 41 total, 38 read/search | Two new files only | 62 / 73,281 / 1,426,885 / 28,474                             |

Output usage includes reported thinking tokens (2,198 and 17,989 respectively);
cache reads count repeated context, not unique instruction volume. Complete
snapshot deltas confirmed no existing file changes, including untracked files.

Transcript SHA-256:

- Owner: `8bc2f2be0a44944ca7a885b1500e97fffead97406c5ea57b9e83ab15ddd898c7`.
- Composition: `3d1e47add58bcc8227bad9e9a60488321d4a4dff84aea8963daaf3868134`.

First artifact SHA-256:

- `src/views/internal/design-system/display-preferences-study.tsx`:
  `a9da0b7b518e3f88337fe17fa4a1909411cd2c73ac4a03a556729a8249eeb7b2`.
- Its focused test:
  `c7731845bbc9ff337c7e06e4329d25c0cb75e1c46686119dbe6111b6c46f8a86`.

Coordinator verification of the unchanged composition: original unit tests
3/3; independent keyboard/save/change/reset/remount checks 4/4 in light/dark at
375px and 1400px. The temporary host imports the original component and app CSS;
it is not a production integration. Temporary mounting files and the installed
dependency link were added only after the original artifact delta was frozen.

Verification setup history stays separate from performer results: package-manager
auto-refresh aborted without approval; the installed executables were used
instead. The first browser harness had an ESM-loading setup error; after that,
its first four checks raced Select's opening focus. The coordinator added waits
for observed option focus, without changing the component or test scenario.
The first failed report is retained alongside the passing report. The initial
authentication preflight never reached the tasks and is not a measured run.

At the control checkpoint, the next action was focused external review, then performer-safe packaging of the retained
guidance candidate before the same tasks run with matched runtime/tools. Repeat
for the consolidated package later. Keep held-out checks and comparative
judgment until all first outputs exist. No treatment, comparative verdict or
production rehearsal had run at that checkpoint. Subsequent execution is recorded below.

## Guidance-only candidate

### Treatment execution note — 2026-09-09

The next two arms were randomly ordered before dispatch: guidance-only, then
consolidated package; owner task precedes composition within each arm. The CLI
version and reported model match the controls. A repeated confinement preflight
denied both outside-canary read and write at the tool layer.

The first guidance-owner dispatch is **invalid, unscored coordinator setup
evidence**, not a performer failure or a measured treatment result. Node's copy
operation rewrote seven internal symbolic links to absolute paths outside the
performer's root. No canary content or private coordinator data was exposed.
The source comparison caught this; the completed transcript is retained under
`guidance-owner`, and no feedback was supplied. The copy now preserves symbolic
link text verbatim and must match every frozen fixture entry before replacement
dispatch. This is a disclosed rerun of invalid setup, not a silent retry, task
change or rubric revision. Its time/usage remains separate from valid treatments.

Saved on 2026-09-09 local time as an immutable **coordinator-only source
overlay**, not a runnable performer workspace. Opaque temporary directory:
`register-guidance.QlLkev`. Apply to the original `adef9ee76` source archive only;
do not copy the current working tree as a treatment. Manifest SHA-256:
`c8662df1bc31c0b69acc9928b00aeb2b5353adf64d4ee377f5f5a434347c06ab`.
The manifest records each of these ten source files and its content hash:

- `docs/plans/design-system-v1.md`
- `docs/plans/transaction-consolidated-regression.md`
- `docs/plans/transaction-system-audit.md`
- `docs/wiki/domains/design-system.md`
- `docs/wiki/domains/design-system-reference.md`
- `docs/wiki/progress.md`
- `docs/wiki/log.md`
- `src/views/internal/design-system/component-catalog-primary.ts`
- `src/views/internal/design-system/component-catalog-support.ts`
- `src/views/internal/design-system/tests/component-catalog.test.ts`

No S1 runtime/config/helper changes or S2 owners are in the overlay. The catalog
delta changes context links plus two stale Textarea/Collapsible next-action
prompts; all other existing fields are identical after normalizing source
formatting. The main plan's transaction contract and all 18
pressure rows were moved without wording changes. Static checks confirm the
ordinary component route does not require that transaction content, while the
transaction route still reaches the transfer contract and pressure register.
Those checks are not a fresh-agent behavioral result.

An earlier candidate, `register-guidance.vQLdkW`, remains preserved with manifest
SHA-256 `38d984dc026cf59d14e5af9f1eb92ff82313858507cf6c160f2a7ef82d4f7bc6`.
It predates the two routing-prompt corrections found during final self-review.
The intermediate `register-guidance.fW32NM` snapshot has manifest SHA-256
`2305fe0135614cd2012a7351a1a58d6305835c9e71277a0708a1ca8ae9d4fb8c`;
it predates the new test's missing-entry type guard. The final snapshot was
frozen after focused tests 37/37, app typecheck and scoped lint/format passed.
All three source overlays remain coordinator-only; none has been dispatched.

These earlier source overlays remain unchanged and were not dispatched directly.
The reviewed, packaged successor and its later execution are recorded below.

### Packaged guidance-only fixture — 2026-09-09

Fixture `register-guidance-ready.HQxXWX` was assembled from the same original
archive plus the ten-file allowlist above, including the focused provenance
corrections. The independent reports, rubric, original controls and S1/S2
runtime changes are absent. The two consolidation ledger rows and four
coordinator-only log sections were removed; the actual three historical scope
links were redirected to their decision/reference anchors. Audit/consolidation
passages were removed from the main plan and router without removing actual
design guidance. Packaging is mechanical and its script/diffs/manifests remain
coordinator-only in `register-consolidation-next.ZcaUun`.

- Archive SHA-256: `569d5a5b2fd99d9d8ad47ceb0b9f09064e8d20203e6cd76285af3eefabf0971f`.
- Packaged file-tree digest: `4d23fc269d74f3984702a6e0970020d7ff1c00304266724c88630bfa1b22bb92`.
- Packaging manifest SHA-256: `5bf91638c2393ee89d9a7c371b229a7e706a73fd69ef04332952cb42b6b025c1`.
- Packager SHA-256: `8413577ffdf30a3862a6dabc2b824d2dc9534a5a361475ce1b8dc759233820c2`.
- 2,572 files compared to the original archive; exactly ten allowed differences.
- 186 local file links/anchors checked; zero missing. No excluded coordinator files.
- Wiki lint: 20 pages green, with read-only Git context supplied by the coordinator.

An initial packaging attempt, `register-guidance-ready.gVvnbP`, is retained
with its original manifest. It passed links but contained one overlong ledger
row. The final fixture incorporates the bounded wording correction and excludes
the later S2 handoff note. A source archive has no Git metadata, so a bare
wiki-lint invocation failed before the coordinator supplied read-only Git
context. Neither was dispatched at that packaging checkpoint; the valid successor was later copied into isolated performer roots.

This is the frozen guidance-only arm, not a mirror of later S2 work or its
coordinator closeout edits. It had not been run or scored at packaging time. Keep the same
exclusion policy for later arms; stop if packaging would require changing the
task or rubric. Recheck confinement, dependencies and provider/runtime matching
before dispatch. The raw-control deadline remains **2026-09-16**; request a
bounded extension before that date if needed, rather than silently extending
retention or assuming expiry has already happened. No additional experiment,
external review or cleanup automation was dispatched at that checkpoint.

## Completed performer arms and matched verification — 2026-09-09

All six valid first outputs are now retained without performer repair, feedback,
or existing-file changes. Each owner task is read-only; each composition adds
exactly its component and focused test. The [sanitized evidence index](design-system-consolidation-evidence/evaluation/index.json)
retains the original synthetic artifacts, final explanations, requested-path
summaries, usage records and initial/saved screenshots. Neutral output labels
were randomized after execution; the mapping remains private. This is not perfect
blinding: an output's APIs and wording may reveal which package it saw.

| Treatment            | Owner elapsed / calls | Composition elapsed / calls | Original unit tests | Same mounted checks |
| -------------------- | --------------------- | --------------------------- | ------------------- | ------------------- |
| Original checkpoint  | 104.8s / 30           | 362.2s / 41                 | 3/3                 | 4/4                 |
| Guidance only        | 113.9s / 31           | 279.7s / 40                 | 5/5                 | 4/4                 |
| Consolidated package | 66.1s / 22            | 416.5s / 49                 | 4/4                 | 4/4                 |

All used CLI 2.1.265, reported `claude-opus-5[1m]`, the same restricted file tools,
12-minute limit, prompts and tool-layer confinement. Auxiliary Haiku usage stays
in each record. Read/search requests are observable; they do not establish how
much returned text the model used. No inference about comparative quality or
instruction efficiency follows from elapsed time or token counts alone.

The package fixture was frozen at `register-package-ready.ibMqyd`: 2,588 entries,
145 declared overlay paths, global sorted-tree digest
`0adcf860db32a5c8963f14a5dbea0613b8dae4af36ee51db03dd79b186c3a843`.
Both performer copies match every entry; their traversal-ordered digest is
`107a5918079a6657c7f7b7886a66ce0ebf56d072d64b331f4b5d5d60636cc22c`.
These are different serializations of the same matched tree, not unexplained
source drift. All 175 packaged links/anchors resolve. Earlier unrun package
snapshots remain coordinator-only and are not measured arms.

The package is intentionally frozen before later coordinator closeout changes
to browser capture timing/baselines and test setup sequencing/image mocks.
Those verification-only repairs are not silently inserted into its inputs.
The ownership/runtime composition package tested is preserved, not claimed
bit-identical to the final working tree.
The [nine non-documentation closeout differences](design-system-consolidation-evidence/evaluation/package-closeout-differences.json)
are retained explicitly. Guidance copies likewise matched all
2,572 entries before valid dispatch. The invalid symlink-copy attempt above
remains separate from valid treatment usage and results.

The coordinator re-ran all three compositions using one matched harness:
keyboard Select opening/selection, saved live feedback, clearing stale feedback
after change, reset, remount defaults and no document overflow, in light/dark
at 375×900 and 1400×900. Each uses its unchanged component and app CSS in an
identical temporary host on owned port 3020. The harness now locates controls by
their required accessible names rather than control-specific DOM IDs, and tests
the visibility of saved feedback without demanding an empty live-region wrapper.
These are disclosed locator generalizations, not new success criteria; the same
four tests were rerun for the control too. Initial and saved images are retained.
Temporary hosts/dependency links were added only after source deltas were frozen.

**Current disposition:** the human supplied the [independent comparison](design-system-consolidation-comparative-review.md),
now [reconciled against original and supplemental evidence](design-system-consolidation-reconciliation.md).
No package dominates. Documentation/API usefulness is evidenced, but better
first-pass composition and repair burden are not established; S4's positive exit
criterion is not met. No original output was repaired or promoted, and no
production pilot was implemented. Raw controls, prompts, transcripts and fixture
archives remain available only until **2026-09-16**, unless retention is explicitly
extended before then. The durable synthetic outputs do not expire with those
private records, but they cannot replace missing source/read-event evidence.
