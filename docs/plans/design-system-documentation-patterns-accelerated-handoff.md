# Accelerated Patterns documentation handoff

Authorized 2026-09-17. This packet owns a broad, reversible Patterns wrapper
candidate after Foundations and Components release the shared documentation
files. It makes the complex-pattern destination useful for collaborators without
editing frozen family internals or promoting unresolved work. The result remains
local, uncommitted, unpublished, and human-review-required.

## Outcome and authority

- User-visible result and explicit approval: make `/patterns` one continuous,
  result-first page with stable Charts, Tables, Forms, Navigation, and
  Transactions anchors. Reuse lightweight approved owners for four patterns;
  present Transactions honestly as Exploring and Paused with no fabricated
  canonical specimen.
- Approved human decisions, with exact sources: U3, U14–U17, the accelerated
  local-candidate authorization, accepted Chart/Table/form/navigation decisions,
  and the paused transaction checkpoint referenced by
  [the documentation plan](design-system-documentation-experience.md).
- Coordinator interpretations, labeled as interpretations: Chart may reuse
  `SourceCompactChart`; Tables may use one frozen-fixture
  `CurrentRebalancesTable` result; Forms may use `PresetOrCustomField` in a
  neutral host; Navigation may show real Global and Product items together while
  explicitly preserving their separate contracts.
- Unresolved blocking choices; stop condition and owner: stop only the affected
  subsection if it requires new pattern authority, invented surrounding UI,
  financial/data/palette changes, a universal Table API, navigation behavior,
  or resumed Transaction design. Keep other patterns moving.
- Preserve/change boundary and explicit non-goals: preserve catalogs, accepted
  decisions, every legacy component route/hash/query, frozen family internals,
  fixtures, product behavior, and the standalone runtime. Defer the hashless
  Table-default correction. Do not delete, publish, commit, push, stash, reset,
  migrate production, or touch port 3042.

## Ownership and scoped identity

- Exact owned paths; excluded surfaces and shared defaults:
  - Own the smallest necessary Patterns projection changes in
    `documentation-pages.tsx`, `documentation-presentation.ts`, and `index.tsx`
    after inspecting live diffs and recording returned identity.
  - Own new documentation-only Pattern wrapper/specimen/test files and the
    necessary Patterns assertions in existing shell/standalone tests.
  - Own the receipt/evidence under
    `docs/plans/design-system-documentation-patterns-accelerated/`.
  - Locale files may change only for strictly necessary documentation copy after
    prior owners release them.
  - Strictly exclude `charts/**`, `table-family/**`,
    `auctions-current-table/**`, `auctions-browse/**`, `auctions-current/**`,
    `transaction-*`, `navigation-systems-state-sheet.tsx`,
    `navigation-review-fixtures.tsx`, `contained-form-row-review.tsx`, catalogs,
    accepted decisions, shared component defaults, product providers, and
    unrelated dirty paths.
- Starting commit/ref plus authorized dirty input for owned paths and relevant
  evidence only: base `02434707c0614d2262b10560eebb05cde4319931`; recompute and
  record live starting blobs after Foundations and Components return. The
  verified standalone entry and those candidate slices are authorized dirty
  inputs, not permission to rewrite their unrelated content.
- Returned snapshot/digest for changed owned paths and retained evidence only:
  return starting/ending blobs, new-file/evidence hashes, exact verifier output,
  first/final renders, and repair log.
- Execution location and isolation boundary; owned preview port if applicable:
  shared checkout `/Users/lill-kire/Code/register`; isolated loopback port in the
  3058–3065 range. Never touch port 3042.
- Actual task ID when ready and durable result path: implementation task
  `/root/patterns_accelerated`; receipt target
  `docs/plans/design-system-documentation-patterns-accelerated/README.md`.
- Previews, servers and concurrent work that must not be touched: user preview
  3042, unrelated servers, stashes, safety refs, active worker paths, frozen
  family internals, and unrelated dirt.

## Read and preserve

- Read the repository/lab routers, workflow/stage/testing/code/UI/taste guidance,
  project rules, active V1 and documentation plans, standalone/Foundations/
  Components receipts, this packet, relevant catalogs and accepted decisions,
  and every affected family guide before editing.
- Target catalog entry and canonical implementation/accepted decision: Chart,
  Table, contained forms, Global/Product navigation, and the paused Transaction
  checkpoint. The accepted decisions and family owners remain authority.
- Product/data sources, each labeled what it proves and does not prove:
  - Named lightweight renderers prove approved presentation within their exact
    captured or neutral-host boundaries; they do not create universal data or
    composition contracts.
  - Existing review boards, fixtures, preview documents, and transaction flows
    prove Workbench/evidence coverage only and remain outside the wrapper.
  - Catalog/adoption records prove status and implementation facts; the wrapper
    may not copy or rewrite normative authority.
- Existing strong qualities, copy, state/data meaning and behavior to preserve:
  exact Chart/Table semantics, frozen fixture provenance, distinct navigation
  contracts, repeated preset/custom relationship, paused transaction truth,
  every deep link/hash/query, and local preview reachability.

## Visual precedent

- Primary rendered precedent: `SourceCompactChart`, one approved current-table
  row, the accepted repeated preset/custom field, and real Global/Product
  navigation items, each inside the verified three-layer specimen canvas.
- Why this precedent applies to the task: these are source-faithful lightweight
  owners that communicate the approved result without mounting review boards.
- Qualities and relationships to preserve: exact component geometry, captured
  data meaning, explicit provenance, separate navigation identities, flat
  documentation hierarchy, stable anchors, and neutral specimen hosts.
- Qualities, states or context that must not be copied: full Chart families,
  Table controls, navigation page reconstructions, transaction workbenches,
  invented application shells, viewport-control conventions, or review labels
  as canonical guidance.
- Decisions with no applicable precedent that remain open: no universal Chart
  palette/data contract, Table/Row API, universal form, combined navigation
  shell, or canonical transaction controller may be inferred.

## Acceptance and proof

- Highest stable behavior seam and independent oracle: standalone Patterns page,
  accepted lightweight renderers, exact Workbench hashes, and Records owners.
- RED proof for each changed behavior, or exact limitation if RED is unavailable:
  pin all five anchors, result-first wrappers, exact alias destinations, absence
  of heavy review imports, and honest no-specimen Transactions before edits.
- Representative state, affected breakpoint and interaction: all five patterns;
  direct alias reloads; Workbench/Records links; light/dark at 320, 390, 768,
  and 1400px; keyboard focus and horizontal-overflow checks.
- Final post-edit affected-surface run: focused units/browser matrix, alias/hash/
  query compatibility, existing family smoke tests without modifying fixtures,
  standalone build/chunk and provider/network scans, application/E2E TypeScript,
  scoped lint/format/locales, rendered inspection, diff check, and wiki lint.
- Durable evidence pointer and retention owner:
  `docs/plans/design-system-documentation-patterns-accelerated/README.md` with
  first/final captures, hashes, artifact/runtime summaries, scoped identity,
  repair log, and morning-review list; coordinator owns retention.

| Criterion                                                  | Test seam                 | Rendered state                              | Proof                            |
| ---------------------------------------------------------- | ------------------------- | ------------------------------------------- | -------------------------------- |
| Five patterns are useful and scrollable                    | aggregate DOM and anchors | Charts/Tables/Forms/Navigation/Transactions | unit/browser matrix              |
| Canonical results use real lightweight owners              | direct specimen imports   | four named results                          | source/DOM/render checks         |
| Transactions remains honest                                | status/content assertions | Exploring and Paused, no specimen           | unit/render checks               |
| Heavy review machinery and frozen internals remain outside | import/chunk/diff scans   | Patterns build                              | artifact and no-diff proof       |
| Workbench and Records preserve exact access                | direct links and reloads  | hashes/query contracts                      | browser compatibility matrix     |
| Standalone isolation survives                              | production runtime        | fresh Patterns and aliases                  | provider/network/artifact checks |

## Pilot evaluation

- Fixed model and reasoning effort: inherited parent settings for
  `/root/patterns_accelerated`; do not change during repairs.
- First-render artifact and source identity: retain the first complete five-
  pattern render before subjective repair.
- Repair log and final-render artifact: classify every substantive repair and
  retain final captures.
- Correct authority retrieved: score each wrapper against its accepted decision,
  catalog record, and named renderer/checkpoint.
- Correct precedent chosen and applied: score the four specimens and the honest
  Transaction boundary.
- Approved human decisions preserved: score against U3, U14–U17, acceleration,
  frozen Chart/Table scope, and paused Transactions.
- Substantive repair count: record exactly.
- Failure causes per repair: missing instruction, bad composition,
  implementation error, or subjective refinement.
- Focused verification caught problems before human review: score and cite.

## Return to the coordinator

Return changed paths and scoped identity, RED/GREEN proof, exact final checks,
inspected routes/interactions, first/final evidence, repair classification,
durable receipt, unresolved per-pattern questions, and any engineer-review
requirement. Mark the result `implementation-verified; human visual review
required`, never accepted. Do not commit, push, publish, delete, migrate
production, alter family internals, or expand into product adoption.
