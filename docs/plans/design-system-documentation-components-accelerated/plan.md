# Accelerated Components documentation candidate

## Goal

Turn the Components destination into one continuous, anchored reference for all
45 catalog entries. Show a lightweight real canonical owner for 28 accepted
direct entries, explicit standalone-isolation slots for three accepted entries,
honest pattern slots for Card, Table, and Chart, and truthful Exploring or Not
started treatments for the remaining entries.

## Current state

The standalone documentation runtime and continuous Foundations reference are
verified local candidates. Components currently renders a lightweight row index
with no component results. The shared checkout is heavily dirty; this slice owns
only the paths named in the accelerated handoff and uses base
`02434707c0614d2262b10560eebb05cde4319931`.

## Non-goals

- Do not change catalog authority, accepted decisions, component APIs/defaults,
  product behavior, state sheets, detail routes, pattern internals, or the
  standalone runtime.
- Do not import full state sheets or heavy workbenches into the overview.
- Do not publish, commit, push, stash, reset, delete legacy material, migrate
  production, or touch port 3042.

## Acceptance evidence

- Focused unit coverage proves all 45 entries and eight family anchors render in
  catalog order with exactly 28 canonical outputs, three accepted isolation
  slots, three pattern slots, six Exploring treatments, and five Not
  started/Not planned treatments.
- Browser evidence covers light/dark at 320, 390, 768, and 1400px, direct family
  anchors, representative keyboard interactions, and no horizontal overflow.
- Standalone build, artifact/provider guard, compatibility routes, application
  and E2E TypeScript, scoped lint/format/locales, diff check, and wiki lint pass.
- First and final renders, source identities, hashes, verifier output, and repair
  classification are retained in the slice receipt.

## Test seams

The stable seams are the standalone Components route, typed catalog order and
authority, DOM anchors/treatments, direct canonical component test identifiers,
representative owner interactions, and existing component detail routes.

## Slices

- Slice: continuous 45-entry Components candidate plus focused unit/browser
  coverage and retained first render; blocked by: none.
- Slice: bounded repair, final affected-surface verification, durable receipt,
  and morning-review list; blocked by: first slice.

## Unresolved decisions

Exact canvas density and narrow overflow treatment remain reviewable
presentation choices. Any entry that needs a new variant/value, normative prose
without an owner, an API/default change, exploratory promotion, or a heavy
Workbench copy stops locally while unrelated entries continue.
