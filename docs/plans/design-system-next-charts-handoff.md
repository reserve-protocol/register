# Next chart families — isolated preparation

## Current state — September 15

The [visual revision plan](design-system-next-charts/revision-plan.md) now owns
pending work after human feedback. No fixes or new worker were launched by that
planning pass. The completed functional pass supersedes the dependency blocker below.
Integrated in the main lab at `#chart-next-families-review`, with a responsive
preview link. Six browser cases, seven fixture tests, app/e2e typechecks and
scoped lint passed. Human visual acceptance remains open. The
[integration receipt](design-system-next-charts/integration.md) owns current
status; the dispatch narrative below records historical restrictions.

## Dispatch status

Created task: `01a0a20e-2880-7b23-af6e-1e21a8318502`, local host, titled
“Prepare Yield and Portfolio chart lab”, configured Sol medium in the isolated
worktree identified by `5c5c/register`. Initial clean detached HEAD was
`2312d5434842a71118554056a95f12948b95ae74`, not the required checkpoint. The
worker correctly stopped before writes. Coordinator verified that state and
authorized switching this clean worktree to detached `49f9f22ae`, then seeding
the bounded current reference snapshot described below. No reset, commit,
branch rewrite or original-checkout write is authorized. Do not create a
duplicate. Baseline confirmed: checkpoint `49f9f22ae` plus 142 authorized paths
with matching source/destination SHA-256 hashes. Manifest fingerprint:
`d0cd87bfcd721afa3597a2421fba31c2a62b4be3d1adb72794412f34c70ba30f`.
The worker returned an isolated draft; coordinator has read its receipt but has
not reviewed or integrated the implementation. Executable and visual verification
are blocked by missing worktree dependencies: no preview, screenshots, typecheck
or browser results are claimed. APY and non-empty Portfolio data are explicitly
synthetic pressure inputs, not source-faithful captures. Snapshot paths, hashes
and the full limitations are retained in its
`docs/plans/design-system-next-charts/README.md` receipt package. Integration and
dependency setup remain separate next steps; this initial draft was not designer-review-ready.

### Authorized readiness pass

User subsequently authorized completing setup, verification and coordinator
integration for review. The same worker resumed; no duplicate task. Isolated
frozen-lockfile dependency installation is now permitted, without changing the
original checkout, package versions or sibling workspaces. Worker reports that
setup completed with lifecycle scripts disabled. Runtime/visual results remain
pending until inspected.

Coordinator acceptance requires inspected light/dark and actual mobile rendering,
focused unit/type/lint/interaction checks, visible capture versus simulation
boundaries, preserved units and original product jobs, and no silent financial
or sampling changes. Main-owned integration mounts the verified candidate below
the existing chart review at `#chart-next-families-review`; candle review remains
available at its current anchor. Existing chart defaults remain unchanged.
The new family may show clearly labeled composition studies where actual captures
are unavailable, but those do not certify source-faithful product parity.

Source-review repairs returned to the worker include: bottom controls and grid-free
family styling; no unjustified fixed header-height reservations; source-backed
domains/periods and truthful CSV units; prominent disclosure of omitted change
figures and persistent versus hover-triggered Portfolio composition; preserving
ETH-denominated Yield as an explicit exclusion of this hyUSD example. The worker
owns implementation repairs; coordinator independently verifies before integration.

The bounded snapshot contains the 32 current tracked modifications in the
coordinating checkout and its named untracked chart source/tests, family guides,
implementation handoff template and chart/workflow evidence packages. Enumerate
exact paths and compare hashes around the copy; do not copy credentials,
environment files, dependencies, Git internals, ignored or unrelated files.
Preserve these as inherited reference input and distinguish every later
implementation edit from that snapshot. Actual worktree and manifest paths
belong in the worker receipt.

## Authorized outcome

The user authorized a separate Sol medium task in its own worktree on September 15. Prepare Yield DTF historical charts and Portfolio history while candle
review continues in the coordinating task. Do not integrate automatically.

Use Overview/Home as the visual foundation. Yield price, APY, supply and staked
RSR are applications of the line/area family, not four new variants. Portfolio
needs stacked categories, a total and a legend, not a new surrounding design
language. Preserve each metric's units, time meaning, source and controls;
positive movement does not automatically mean an investment gain.

Exclude pie/allocation charts, Factsheet bars, candle interaction repairs,
production migration, financial calculations, API/SDK changes, data fetching
policy, interpolation/resampling, return reconciliation and shared defaults.
Record technical concerns separately; do not implement them as visual fixes.

## Context and isolation

Start with the repository router and required workflow, design-system router,
V1 plan, lab guide and chart family guide. The source-faithful chart reset,
mobile-preview closeout and current rendered Overview/Home code are visual
references, not permission to copy legacy rounded frames or invent wrappers.
Current source checkpoint is `49f9f22ae94d579b4c530de845e8637d299a9c7d`
plus the coordinating checkout's uncommitted chart refinements. Establish the
actual new-worktree baseline before editing; never assume a default-branch
worktree contains those changes. Obtain a bounded read-only snapshot of relevant
current reference files if absent. Do not revert or modify the original checkout.

Own new files under `src/views/internal/design-system/charts/next-families/`,
dedicated tests, local captured fixtures and
`docs/plans/design-system-next-charts/`. Own worktree-only wiring needed for an
isolated preview. Record that wiring separately for coordinator integration.
Do not edit existing candle, Overview/Home replay or shared visual owners;
report a required shared seam before changing it. Do not update Current Review,
accepted decisions or the shared progress ledger as if these candidates were
already integrated or approved. Avoid speculative universal chart APIs.

Use a dedicated preview port, preferably 3041 if free. Never acquire, restart
or stop 3005, nor operate the user's current review tab. Keep all writes and
generated output in the new worktree. No commits, pushes, installs or deployment.

## Preparation and review contract

First verify which charts are mounted/reachable today. For each retained
example, document its real product location, necessary difference from the
Overview/Home family, exact preserved units/meaning and which geometry belongs
to the host. Source-file existence alone is not proof of current use.

Retain successful plot density, legible axes, title/value hierarchy, square
structural surfaces and canonical typography. Do not invent identity/price
wrappers for chart-only examples. Show a realistic host or an explicitly labeled
plot-only specimen. Document any new categorical palette/legend proposal as
provisional; do not promote it into shared authority.

Use authentic captured inputs where available; label synthetic pressure data
and independently captured headline values honestly. Preserve calculations and
source filtering, rather than recreating them differently for the lab. No live
financial updater or wallet action belongs in the specimen.

Keep verification proportional: default plus relevant edge case, light/dark,
desktop, narrow container and actual mobile viewport; exact values/units,
legend-category relationships and pointer/touch/keyboard behavior where
supported. Wait for settled plots before screenshots. Do not claim all-green
coverage from a passing subset or silently repair deep interactions. The candle
slice originally exposed unstable touch/keyboard behavior; its failed
experimental fixes were removed. That historical gate was superseded by the
separately authorized September 15 G7 repair, whose unchanged assertions pass
focused 2/2 and full-candlestick 12/12. Production adoption remains separate.

## Return

Return a concise receipt with the actual task ID/worktree/HEAD, owned files,
reference snapshot identity, isolated preview URL, curated evidence, exact
verification, unresolved design choices and engineer-review concerns. Keep
source observations separate from diagnosed defects and implemented changes.
Coordinator reviews and integrates only the accepted delta after comparing
against the then-current chart work. If isolation or baseline is uncertain,
stop that part and report the concrete blocker rather than rebuilding shared
foundations or touching the coordinating checkout.
